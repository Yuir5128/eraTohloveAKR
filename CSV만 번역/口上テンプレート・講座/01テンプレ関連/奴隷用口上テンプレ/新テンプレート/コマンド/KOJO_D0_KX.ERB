;==================================================
;○○입上
;==================================================
;==================================================
;コマンド実行判定に関わる입上
;==================================================
;=================================================
;コマンド実行判定の操作
;Vに実行難易度、Aに実行値が入っていて、A < Vなら実行不可
;実行値はキャラによって差があるので、どの程度操作するかは実際のプレイを参考にしてください
;=================================================
@KOJO_DENIAL_PLUS_KX1
;;実行判定操作の例
;반발각인３なら実行値を０に(強制的に実行不可)
;IF MARK:3 == 3
;	PRINTFORM - 반발각인LV3({A})
;	A = 0
;ENDIF

;;반발각인２なら実行値-50(陥落済みだと結構これでも実行可能)
;IF MARK:3 == 2
;	PRINTFORM - 반발각인LV2(50)
;	A -= 50
;ENDIF

;;반발각인１以上でコマンド：キス選択の時実行値を０に
;IF MARK:3 >= 1 && SELECTCOM == 20
;	PRINTFORM - 반발각인({A})
;	A = 0
;ENDIF

;=================================================
;コマンド実行拒否입上
;=================================================
;-------------------------------------------------
;EX, 汎用コマンド実行拒否입上
;個別コマンド実行拒否입上が書かれていなければ表示される
;-------------------------------------------------
@KOJO_EXDENIAL_KX1
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;地の文呼び出し(カラー変更対応)
	CALL DENIAL_MESSAGE_COLOR
	
	PRINTFORMW 
	PRINTFORMW 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;個別コマンド実行拒否입上
;こちらが書かれていないと汎用コマンド実行拒否입上が呼ばれる
;-------------------------------------------------
;-------------------------------------------------
;3, 비패개장実行拒否입上　※サンプル
;-------------------------------------------------
@KOJO_DENIAL_KX1_3
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;地の文呼び出し(カラー変更対応)
	CALL DENIAL_MESSAGE_COLOR
	
	PRINTFORMW 
	PRINTFORMW 
ENDIF
RETURN LOCAL

