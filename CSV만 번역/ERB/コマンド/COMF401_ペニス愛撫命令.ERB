;-------------------------------------------------
;페니스애무명령
;助손命令系コマンド
;-------------------------------------------------
@COM401

PRINTL 페니스애무명령
STR:0 = 페니스애무명령
CALL KOJO_MESSAGE_COM

;스타킹びりびりフラグ
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	TFLAG:55 = 1
ENDIF


;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費체력、기력
LOSEBASE:체력 += 5
LOSEBASE:기력 += 50

SOURCE:쾌C += 80
SIF ABL:ASSI:기교
	SOURCE:쾌C += 10*ABL:ASSI:기교

SOURCE:노출 += 20
SOURCE:일탈 += 30
SOURCE:반발 += 30


;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;억압、저항持ちには、억울ソース追加
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 30
;助손の기교により쾌Cソース追加、下손すぎると통증のソース追加
IF ABL:ASSI:2 == 0
	SOURCE:통증 += 100
ELSEIF ABL:ASSI:2 == 1
	SOURCE:통증 += 50
ENDIF
;助손が소인체형で、調教対象が소인체형でなく도착적でもないと일탈のソース追加
SIF TALENT:소인체형 == 0 && TALENT:ASSI:소인체형 && TALENT:도착적
	SOURCE:일탈 += 50


;-------------------------------------------------
;汚れの処理
;-------------------------------------------------
;奴隷のＰ⇔助손の손の汚れが移動
STAIN:페니스 |= STAIN:ASSI:손
STAIN:ASSI:손 |= STAIN:페니스


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;게이경험とか加算
CALL COM_EXP(3)

;依存度ベクトル
TFLAG:33 = 1

RETURN 1

