;-------------------------------------------------
;신혼부부플레이
;シチュエーション別コマンド
;-------------------------------------------------
@COM319

PRINTL 신혼부부플레이
STR:0 = 신혼부부플레이
CALL KOJO_MESSAGE_COM

;着脱の処理　装備時はソース、経験の加算は行わない
IF TEQUIP:에이프런 == 1
	TEQUIP:에이프런 = 0
	TEQUIP:특수1 = 0
ELSE
	TEQUIP:에이프런 = 1
	TEQUIP:특수1 = 2
ENDIF


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;依存度ベクトル
TFLAG:33 = 0


RETURN 1


;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------
@EQUIP_COM319
;신혼부부플레이

;助손調教時は、에이프런を解除して関数終了
IF ASSIPLAY
	TEQUIP:에이프런 = 0
	RETURN 0
ENDIF

PRINTL ＜신혼부부플레이中＞

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費체력、기력
LOSEBASE:체력 += 0
LOSEBASE:기력 += 20

SOURCE:애정 += 1000

;연모系陥落済みだと애정のソース追加
SIF TALENT:연모 || TALENT:친애
	TIMES SOURCE:애정 , 1.50


SOURCE:순종 += 5000
SOURCE:일탈 += 500


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;애정경험
PRINTFORML %EXPNAME:23%＋１
EXP:애정경험 += 1

;依存度補正値(主人かつ연모で2、친애で4）
IF TALENT:연모 && ASSIPLAY == 0
	TFLAG:30 += 2
ELSEIF TALENT:친애 && ASSIPLAY == 0
	TFLAG:30 += 4
ENDIF


RETURN 1

