;-------------------------------------------------
;클리고문
;Ｃ系調教コマンド
;-------------------------------------------------
@COM23

PRINTL 클리고문
STR:0 = 클리고문
CALL KOJO_MESSAGE_COM


;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費체력、기력
LOSEBASE:체력 += 10
LOSEBASE:기력 += 50

SOURCE:쾌C += 200
SIF TEQUIP:클리캡
	TIMES SOURCE:쾌C , 1.50
;着衣（ズボン系）のとき
SIF BOTTOMS(TARGET)
	TIMES SOURCE:쾌C , 0.60
SIF TEQUIP:속옷（아래） || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	TIMES SOURCE:쾌C , 0.90

;삼각목마乗馬中、새드３以上か지하실にいると고통のソース追加
IF TEQUIP:삼각목마 && (ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드 || TEQUIP:지하실)
	SOURCE:통증 += 800
;지하실にいると고통のソース追加
ELSEIF TEQUIP:지하실
	SOURCE:통증 += 600
ENDIF

SIF ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드
	SOURCE:통증 += 200

SIF TEQUIP:음핵클립
	SOURCE:통증 += 400

;C민감だと고통のソース追加
SIF TALENT:C민감
	TIMES SOURCE:통증 , 1.50

SOURCE:노출 += 20
SOURCE:일탈 += 30
SOURCE:반발 += 30


;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;억압、저항持ちには、억울ソース追加
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 30

;스킨십선호だと애정のソース追加
SIF TALENT:스킨십선호
	SOURCE:애정 += 50



;-------------------------------------------------
;汚れの処理
;-------------------------------------------------
;調教者の손⇔奴隷のＶ（하의類）の汚れが移動
;道具を使う場合、汚れは移動しない
IF (TEQUIP:삼각목마 && (ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드 || TEQUIP:지하실)) || TEQUIP:클리캡 || TEQUIP:음핵클립
ELSE
	CALL COM_STAIN
ENDIF


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;게이경험とか加算
CALL COM_EXP(5)

;依存度ベクトル
TFLAG:33 = 1


RETURN 1





