;-------------------------------------------------
;지하실
;シチュエーション別コマンド
;-------------------------------------------------
@COM323

PRINTL 지하실
STR:0 = 지하실
CALL KOJO_MESSAGE_COM

;着脱の処理　終了時はソースの加算は行わない
IF TEQUIP:지하실
	TEQUIP:지하실 = 0
	RETURN 1
ELSE
	TEQUIP:지하실 = 1
ENDIF

;移動時に尿・便存在をクリア
TFLAG:66 = 0
TFLAG:57 = 0


;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;消費체력、기력
LOSEBASE:체력 += 5
LOSEBASE:기력 += 50

;낙인・猟奇系の経験がある場合のみ기력消費と공포・반발のソース
;반발は、친애・복종・예속以外のみ
IF TALENT:낙인 || TALENT:한팔결손 || TALENT:양팔결손 || TALENT:한다리결손 || TALENT:양다리결손 || TALENT:달마 || TALENT:모두발치
	SOURCE:두려움 += 200
	SIF TALENT:복종 == 0 && TALENT:친애 == 0 && TALENT:예속 == 0
		SOURCE:반발 += 200
ENDIF


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;依存度ベクトル
TFLAG:33 = 5

RETURN 1



;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------
@EQUIP_COM323
;지하실
PRINTL ＜지하실＞

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;낙인・猟奇系の経験がある場合のみ기력消費と공포・반발のソース
;반발は、친애・복종・예속以外のみ
IF TALENT:낙인 || TALENT:한팔결손 || TALENT:양팔결손 || TALENT:한다리결손 || TALENT:양다리결손 || TALENT:달마 || TALENT:모두발치
	LOSEBASE:기력 += 50
	
	SOURCE:두려움 += 100
	SIF TALENT:복종 == 0 && TALENT:친애 == 0 && TALENT:예속 == 0
		SOURCE:반발 += 100
ENDIF

RETURN 1


