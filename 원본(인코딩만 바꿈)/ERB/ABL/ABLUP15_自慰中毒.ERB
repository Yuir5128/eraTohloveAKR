;自慰中毒の可否
@ISABLUP15
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:自慰中毒 >= 10
	RETURN 0
ELSEIF ABL:自慰中毒 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	RETURN 0
ELSEIF EXP:自慰経験 == 0
	RETURN 0
ENDIF

CALL DECIDE_ABLUP15

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;自慰中毒
@ABLUP15
DRAWLINE
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:自慰中毒 >= 10
	PRINTW 既にMAXです
	RETURN 0
ELSEIF ABL:自慰中毒 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	PRINTW 既にMAXです
	RETURN 0
ENDIF

CALL DECIDE_ABLUP15

;ＬＶ２から３に上げるときは異常経験必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
IF ABL:自慰中毒 == 2 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	PRINTFORML %EXPNAME:50%あり、かつ
;ＬＶ３から４に上げるときは異常経験2以上必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
ELSEIF ABL:自慰中毒 == 3 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	PRINTFORML %EXPNAME:50%2以上、かつ
;ＬＶ４から５に上げるときは異常経験3以上必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
ELSEIF ABL:自慰中毒 == 4 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	PRINTFORML %EXPNAME:50%3以上、かつ
;ＬＶ８から９に上げるときは異常経験7以上必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
ELSEIF ABL:自慰中毒 == 8 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	PRINTFORML %EXPNAME:50%7以上、かつ
ENDIF

;露出癖が必要
PRINTFORM %ABLNAME:8%{ABL:自慰中毒+1}以上、かつ

;Ｃ感覚が必要
PRINTFORM %ABLNAME:3%{ABL:自慰中毒+1}以上、かつ

PRINTFORM [0] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:0%の珠×{B}、%PALAMNAME:9%の珠×{C}、%EXPNAME:10%{D}以上……

IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 珠不足 
	SIF I & 2
		PRINT 経験不足 
	SIF I & 4
		PRINT 能力不足 
ENDIF
PRINTL 

PRINTFORM [1] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:0%の珠×{B}、%PALAMNAME:9%の珠×{C}、%EXPNAME:11%{E}以上……

IF J == 0
	PRINT ＯＫ
ELSE
	SIF J & 1
		PRINT 珠不足 
	SIF J & 2
		PRINT 経験不足 
	SIF J & 4
		PRINT 能力不足 
ENDIF
PRINTL 

;露出癖が必要
PRINTFORM %ABLNAME:8%{ABL:自慰中毒+1}以上、かつ

;Ａ感覚が必要
PRINTFORM %ABLNAME:5%{ABL:自慰中毒+1}以上、かつ

PRINTFORM [2] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:2%の珠×{B}、%PALAMNAME:9%の珠×{C}、%EXPNAME:10%{D}以上……

IF K == 0
	PRINT ＯＫ
ELSE
	SIF K & 1
		PRINT 珠不足 
	SIF K & 2
		PRINT 経験不足 
	SIF K & 4
		PRINT 能力不足 
ENDIF
PRINTL 

PRINTFORM [3] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:2%の珠×{B}、%PALAMNAME:9%の珠×{C}、%EXPNAME:11%{E}以上……

IF L == 0
	PRINT ＯＫ
ELSE
	SIF L & 1
		PRINT 珠不足 
	SIF L & 2
		PRINT 経験不足 
	SIF L & 4
		PRINT 能力不足 
ENDIF
PRINTL 

PRINTL [100] - やめる


INPUT
IF (RESULT < 0 || RESULT > 3) && RESULT != 100
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 条件を満たしていません
	RESTART
ELSEIF J != 0 && RESULT == 1
	PRINTL 条件を満たしていません
	RESTART
ELSEIF K != 0 && RESULT == 2
	PRINTL 条件を満たしていません
	RESTART
ELSEIF L != 0 && RESULT == 3
	PRINTL 条件を満たしていません
	RESTART
ELSEIF RESULT == 100
	RETURN 0
ENDIF

ABL:自慰中毒 += 1

IF RESULT == 0 || RESULT == 1
	JUEL:欲情 -= A
	JUEL:快Ｃ -= B
	JUEL:恥情 -= C
ELSEIF RESULT == 2 || RESULT == 3
	JUEL:欲情 -= A
	JUEL:快Ａ -= B
	JUEL:恥情 -= C
ENDIF

PRINTFORMW %ABLNAME:15%のレベルが{ABL:自慰中毒}になりました


;自慰中毒LVUP判定
@DECIDE_ABLUP15
;条件別にＯＫかダメかを記録する
I = 0
J = 0
K = 0
L = 0
;能力アップ可能なら1にする
Z = 0

IF ABL:自慰中毒 == 0
	A = 3000
	B = 10000
	C = 1000
	D = 100
	E = 20
ELSEIF ABL:自慰中毒 == 1
	A = 6000
	B = 25000
	C = 3000
	D = 250
	E = 40
ELSEIF ABL:自慰中毒 == 2
	A = 12000
	B = 50000
	C = 6000
	D = 500
	E = 60
ELSEIF ABL:自慰中毒 == 3
	A = 20000
	B = 100000
	C = 15000
	D = 1000
	E = 100
ELSEIF ABL:自慰中毒 == 4
	A = 40000
	B = 300000
	C = 40000
	D = 2000
	E = 160
ELSEIF ABL:自慰中毒 == 5
	A = 45000
	B = 350000
	C = 45000
	D = 2500
	E = 200
ELSEIF ABL:自慰中毒 == 6
	A = 50000
	B = 400000
	C = 50000
	D = 3000
	E = 250
ELSEIF ABL:自慰中毒 == 7
	A = 55000
	B = 450000
	C = 55000
	D = 3000
	E = 300
ELSEIF ABL:自慰中毒 == 8
	A = 60000
	B = 500000
	C = 60000
	D = 3500
	E = 350
ELSEIF ABL:自慰中毒 == 9
	A = 70000
	B = 600000
	C = 70000
	D = 5000
	E = 500
ENDIF

;一線越えない
IF TALENT:一線越えない && (TALENT:恋慕 == 0 && TALENT:淫乱 == 0 && TALENT:服従 == 0 && TALENT:親愛 == 0 && TALENT:娼婦 == 0 && TALENT:隷属 == 0)
	TIMES A , 2.00
	TIMES B , 2.00
	TIMES C , 2.00
	TIMES D , 2.00
	TIMES E , 2.00
ENDIF
;中毒しやすい
IF TALENT:中毒しやすい
	TIMES A , 0.50
	TIMES B , 0.50
	TIMES C , 0.50
	TIMES D , 0.50
ENDIF
;スキンシップ好き
IF TALENT:スキンシップ好き
	TIMES A , 1.50
	TIMES B , 1.50
	TIMES C , 1.50
ENDIF

;ＬＶ２から３に上げるときは異常経験必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
IF ABL:自慰中毒 == 2 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	IF EXP:異常経験 == 0
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
		L |= 2
	ENDIF
;ＬＶ３から４に上げるときは異常経験2以上必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
ELSEIF ABL:自慰中毒 == 3 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	IF EXP:異常経験 < 2
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
		L |= 2
	ENDIF
;ＬＶ４から５に上げるときは異常経験3以上必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
ELSEIF ABL:自慰中毒 == 4 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	IF EXP:異常経験 < 3
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
		L |= 2
	ENDIF
;ＬＶ８から９に上げるときは異常経験7以上必要（素質：[中毒しやすい][自慰狂い]なら無視できる）
ELSEIF ABL:自慰中毒 == 8 && TALENT:中毒しやすい == 0 && TALENT:自慰狂い == 0
	IF EXP:異常経験 < 7
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
		L |= 2
	ENDIF
ENDIF

;露出癖が必要
IF ABL:露出癖 < ABL:自慰中毒 + 1
	I |= 4
	J |= 4
	K |= 4
	L |= 4
ENDIF

;Ｃ感覚が必要
IF ABL:Ｃ感覚 < ABL:自慰中毒 + 1
	I |= 4
	J |= 4
ENDIF
;A感覚が必要
IF ABL:Ａ感覚 < ABL:自慰中毒 + 1
	K |= 4
	L |= 4
ENDIF
;欲情の珠で上げる
IF JUEL:欲情 < A
	I |= 1
	J |= 1
	K |= 1
	L |= 1
ENDIF
;快Ｃの珠で上げる
IF JUEL:快Ｃ < B
	I |= 1
	J |= 1
ENDIF
;快Aの珠で上げる
IF JUEL:快Ａ < B
	K |= 1
	L |= 1	
ENDIF
;恥情の珠で上げる
IF JUEL:恥情 < C
	I |= 1
	J |= 1
	K |= 1
	L |= 1
ENDIF
;自慰経験が必要
IF EXP:自慰経験 < D
	I |= 2
	K |= 2
ENDIF
;調教自慰経験が必要
IF EXP:調教自慰経験 < E
	J |= 2
	L |= 2
ENDIF
IF I == 0
	Z = 1
ENDIF

IF J == 0
	Z = 1
ENDIF

IF K == 0 
	Z = 1

ENDIF
IF L == 0
	Z = 1
ENDIF

RETURN 1

