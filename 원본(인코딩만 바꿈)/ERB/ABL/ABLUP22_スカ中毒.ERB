;スカ中毒の可否
@ISABLUP22
;親愛、服従、隷属はMAXLV5
IF ABL:スカ中毒 >= 5
	RETURN 0
ELSEIF ABL:スカ中毒 >= 3 && TALENT:服従 == 0 && TALENT:親愛 == 0 && TALENT:隷属 == 0
	RETURN 0
ELSEIF EXP:スカ経験  == 0
	RETURN 0
ENDIF

CALL DECIDE_ABLUP22

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;スカ中毒
@ABLUP22
DRAWLINE
;親愛、服従、隷属はMAXLV5
IF ABL:スカ中毒 >= 5
	PRINTW 既にMAXです
	RETURN 0
ELSEIF ABL:スカ中毒 >= 3 && TALENT:服従 == 0 && TALENT:親愛 == 0 && TALENT:隷属 == 0
	PRINTW 既にMAXです
	RETURN 0
ENDIF

;ＬＶ１から２に上げるときは異常経験必要（素質：[中毒しやすい][恥薄い][汚臭鈍感][汚れ無視][幼児/幼児退行][幼稚]なら無視できる）
IF ABL:スカ中毒 == 1 && TALENT:恥薄い == 0 && TALENT:自慰しやすい == 0 && TALENT:汚臭鈍感 == 0 && TALENT:汚れ無視 == 0 && TALENT:中毒しやすい == 0 && TALENT:幼児／幼児退行 == 0 && TALENT:幼稚 == 0
	PRINTFORML %EXPNAME:50%あり、かつ
;ＬＶ２から３に上げるときは異常経験必要（素質：[中毒しやすい][汚れ無視][幼児/幼児退行][幼稚]なら無視できる）
ELSEIF ABL:スカ中毒 == 2 && TALENT:汚臭鈍感 == 0 && TALENT:汚れ無視 == 0 && TALENT:中毒しやすい == 0 && TALENT:幼児／幼児退行 == 0 && TALENT:幼稚 == 0
	PRINTFORML %EXPNAME:50%2以上、かつ
;ＬＶ３から４に上げるときは異常経験必要
ELSEIF ABL:スカ中毒 == 3
	PRINTFORML %EXPNAME:50%3以上、かつ
;ＬＶ４から５に上げるときは異常経験必要
ELSEIF ABL:スカ中毒 == 4
	PRINTFORML %EXPNAME:50%5以上、かつ
ENDIF

;露出癖が必要
PRINTFORM %ABLNAME:8%{ABL:スカ中毒+1}以上、かつ

;スカ経験が必要
;ＬＶ１に上げるときはスカ経験１０必要
IF ABL:スカ中毒 == 0
	PRINTFORML %EXPNAME:33%10以上、かつ
;ＬＶ１から２に上げるときはスカ経験２０必要
ELSEIF ABL:スカ中毒 == 1
	PRINTFORML %EXPNAME:33%20以上、かつ
;ＬＶ２から３に上げるときはスカ経験３０必要
ELSEIF ABL:スカ中毒 == 2
	PRINTFORML %EXPNAME:33%30以上、かつ
;ＬＶ３から４に上げるときはスカ経験５０必要
ELSEIF ABL:スカ中毒 == 3
	PRINTFORML %EXPNAME:33%50以上、かつ
;ＬＶ４から５に上げるときはスカ経験１００必要
ELSEIF ABL:スカ中毒 == 4
	PRINTFORML %EXPNAME:33%100以上、かつ
ENDIF

PRINTFORM [0] - %PALAMNAME:5%の珠×{A}、%PALAMNAME:9%の珠×{B}以上……

IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 珠不足 
	SIF I & 2
		PRINT 経験不足 
	SIF I & 4
		PRINT 能力不足 
ENDIF
PRINTL 

PRINTFORM [1] - %PALAMNAME:7%の珠×{C}、%PALAMNAME:9%の珠×{B}以上……

IF J == 0
	PRINT ＯＫ
ELSE
	SIF J & 1
		PRINT 珠不足 
	SIF J & 2
		PRINT 経験不足 
	SIF J & 4
		PRINT 能力不足 
ENDIF
PRINTL 


PRINTL [100] - やめる


INPUT
IF (RESULT < 0 || RESULT > 1) && RESULT != 100
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 条件を満たしていません
	RESTART
ELSEIF J != 0 && RESULT == 1
	PRINTL 条件を満たしていません
	RESTART
ELSEIF RESULT == 100
	RETURN 0
ENDIF

ABL:スカ中毒 += 1

IF RESULT == 0
	JUEL:恭順 -= A
	JUEL:恥情 -= B
ELSEIF RESULT == 1
	JUEL:屈服 -= C
	JUEL:恥情 -= B
ENDIF

PRINTFORMW %ABLNAME:22%のレベルが{ABL:スカ中毒}になりました

;スカ中毒LVUP判定
@DECIDE_ABLUP22
;条件別にＯＫか駄目かを記録する
I = 0
J = 0
;能力アップ可なら１にする
Z = 0

IF ABL:スカ中毒 == 0
	A = 1000
	B = 1000
	C = 3000
ELSEIF ABL:スカ中毒 == 1
	A = 2000
	B = 3000
	C = 5000
ELSEIF ABL:スカ中毒 == 2
	A = 3000
	B = 10000
	C = 20000
ELSEIF ABL:スカ中毒 == 3
	A = 5000
	B = 20000
	C = 30000
ELSEIF ABL:スカ中毒 == 4
	A = 10000
	B = 50000
	C = 100000
ENDIF

;一線越えない
IF TALENT:一線越えない && (!TALENT:恋慕 && !TALENT:淫乱 && !TALENT:服従 && !TALENT:親愛 && !TALENT:娼婦 && !TALENT:隷属)
	TIMES A , 2.00
	TIMES B , 2.00
	TIMES C , 2.00
ENDIF
;中毒しやすい
IF TALENT:中毒しやすい
	TIMES A , 0.50
	TIMES B , 0.50
	TIMES C , 0.50
ENDIF

;ＬＶ１から２に上げるときは異常経験必要（素質：[中毒しやすい][恥薄い][汚臭鈍感][汚れ無視][幼児/幼児退行][幼稚]なら無視できる）
IF ABL:スカ中毒 == 1 && !TALENT:恥薄い && !TALENT:自慰しやすい && !TALENT:汚臭鈍感 && !TALENT:汚れ無視 && !TALENT:中毒しやすい && !TALENT:幼児／幼児退行 && !TALENT:幼稚
	IF EXP:異常経験 == 0
		;異常経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ２から３に上げるときは異常経験必要（素質：[中毒しやすい][汚れ無視][幼児/幼児退行][幼稚]なら無視できる）
ELSEIF ABL:スカ中毒 == 2 && !TALENT:汚臭鈍感 && !TALENT:汚れ無視 && !TALENT:中毒しやすい && !TALENT:幼児／幼児退行 && !TALENT:幼稚
	IF EXP:異常経験 < 2
		;異常経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ３から４に上げるときは異常経験必要
ELSEIF ABL:スカ中毒 == 3
	IF EXP:異常経験 < 3
		;異常経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ４から５に上げるときは異常経験必要
ELSEIF ABL:スカ中毒 == 4
	IF EXP:異常経験 < 5
		;異常経験が不足
		I |= 2
		J |= 2
	ENDIF
ENDIF

;露出癖が必要
IF ABL:露出癖 < ABL:スカ中毒 + 1
	I |= 4
	J |= 4
ENDIF

;スカ経験が必要
;ＬＶ１に上げるときはスカ経験１０必要
IF ABL:スカ中毒 == 0
	IF EXP:スカ経験 < 10
		;スカ経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ１から２に上げるときはスカ経験２０必要
ELSEIF ABL:スカ中毒 == 1
	IF EXP:スカ経験 < 20
		;スカ経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ２から３に上げるときはスカ経験３０必要
ELSEIF ABL:スカ中毒 == 2
	IF EXP:スカ経験 < 30
		;スカ経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ３から４に上げるときはスカ経験５０必要
ELSEIF ABL:スカ中毒 == 3
	IF EXP:スカ経験 < 50
		;スカ経験が不足
		I |= 2
		J |= 2
	ENDIF
;ＬＶ４から５に上げるときはスカ経験１００必要
ELSEIF ABL:スカ中毒 == 4
	IF EXP:スカ経験 < 100
		;スカ経験が不足
		I |= 2
		J |= 2
	ENDIF
ENDIF

;恭順の珠で上げる
IF JUEL:恭順 < A
	I |= 1
ENDIF
;屈服の珠で上げる
IF JUEL:屈服 < C
	J |= 1
ENDIF
;恥辱の珠で上げる
IF JUEL:恥情 < B
	I |= 1
	J |= 1
ENDIF

SIF !I || !J
	Z = 1

RETURN 1

