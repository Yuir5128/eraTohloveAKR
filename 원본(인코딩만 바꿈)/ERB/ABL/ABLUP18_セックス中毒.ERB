;セックス中毒可否
@ISABLUP18
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:セックス中毒 >= 10
	RETURN 0
ELSEIF ABL:セックス中毒 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	RETURN 0
ELSEIF EXP:性交経験  == 0
	RETURN 0
ENDIF

CALL DECIDE_ABLUP18

IF Z == 0
	RETURN 0
ELSE
	RETURN 1
ENDIF


;セックス中毒
@ABLUP18
DRAWLINE
;恋慕、親愛、淫乱、娼婦はMAXLV10
IF ABL:セックス中毒 >= 10
	PRINTW 既にMAXです
	RETURN 0
ELSEIF ABL:セックス中毒 >= 5 && TALENT:恋慕 == 0 && TALENT:親愛 == 0 && TALENT:淫乱 == 0 && TALENT:娼婦 == 0
	PRINTW 既にMAXです
	RETURN 0
ENDIF

CALL DECIDE_ABLUP18


;ＬＶ２から３に上げるときは異常経験必要（素質：[中毒しやすい][セックス狂]なら無視できる）
IF ABL:セックス中毒 == 2 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	PRINTFORML %EXPNAME:50%あり、かつ
;ＬＶ３から４に上げるときは異常経験2以上必要（素質：[中毒しやすい][セックス狂]なら無視できる）
ELSEIF ABL:セックス中毒 == 3 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	PRINTFORML %EXPNAME:50%2以上、かつ
;ＬＶ４から５に上げるときは異常経験3以上必要（素質：[中毒しやすい][セックス狂]なら無視できる）
ELSEIF ABL:セックス中毒 == 4 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	PRINTFORML %EXPNAME:50%3以上、かつ
;ＬＶ８から９に上げるときは異常経験7以上必要（素質：[中毒しやすい][セックス狂]なら無視できる）
ELSEIF ABL:セックス中毒 == 8 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	PRINTFORML %EXPNAME:50%7以上、かつ
ENDIF

;V感覚が必要
PRINTFORML %ABLNAME:4%{ABL:セックス中毒 + 1}LV以上、かつ

PRINTFORM [0] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:1%の珠×{B}、%PALAMNAME:7%の珠×{C}、%EXPNAME:4%{D}以上……

IF I == 0
	PRINT ＯＫ
ELSE
	SIF I & 1
		PRINT 珠不足 
	SIF I & 2
		PRINT 経験不足 
	SIF I & 4
		PRINT 能力不足 
ENDIF
PRINTL 

;A感覚が必要
PRINTFORML %ABLNAME:5%{ABL:セックス中毒 + 1}LV以上、かつ

PRINTFORM [1] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:2%の珠×{B}、%PALAMNAME:7%の珠×{C}、%EXPNAME:4%{D}以上……
IF J == 0
	PRINT ＯＫ
ELSE
	SIF J & 1
		PRINT 珠不足 
	SIF J & 2
		PRINT 経験不足 
	SIF J & 4
		PRINT 能力不足 
ENDIF
PRINTL 

;C感覚が必要
PRINTFORML %ABLNAME:3%{ ABL:セックス中毒 + 1}LV以上、かつ

PRINTFORM [2] - %PALAMNAME:6%の珠×{A}、%PALAMNAME:0%の珠×{B}、%PALAMNAME:7%の珠×{C}、%EXPNAME:4%{D}以上……

IF K == 0
	PRINT ＯＫ
ELSE
	SIF K & 1
		PRINT 珠不足 
	SIF K & 2
		PRINT 経験不足 
	SIF K & 4
		PRINT 能力不足 
ENDIF
PRINTL 

PRINTL [100] - やめる


INPUT
IF (RESULT < 0 || RESULT > 2) && RESULT != 100
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL 条件を満たしていません
	RESTART
ELSEIF J != 0 && RESULT == 1
	PRINTL 条件を満たしていません
	RESTART
ELSEIF K != 0 && RESULT == 2
	PRINTL 条件を満たしていません
	RESTART
ELSEIF RESULT == 100
	RETURN 0
ENDIF

ABL:セックス中毒 += 1

IF RESULT == 0
	JUEL:欲情 -= A
	JUEL:快Ｖ -= B
	JUEL:屈服 -= C
ELSEIF RESULT == 1
	JUEL:欲情 -= A
	JUEL:快Ａ -= B
	JUEL:屈服 -= C
ELSEIF RESULT == 2
	JUEL:欲情 -= A
	JUEL:快Ｃ -= B
	JUEL:屈服 -= C
ENDIF

PRINTFORMW %ABLNAME:18%のレベルが{ABL:セックス中毒}になりました

;セックス中毒LVUP判定
@DECIDE_ABLUP18
;条件別にＯＫかダメかを記録する
I = 0
J = 0
K = 0
;能力アップ可能なら1にする
Z = 0

IF ABL:セックス中毒 == 0
	A = 2500
	B = 6000
	C = 1000
	D = 10
ELSEIF ABL:セックス中毒 == 1
	A = 5000
	B = 14000
	C = 2500
	D = 25
ELSEIF ABL:セックス中毒 == 2
	A = 9000
	B = 27000
	C = 4800
	D = 80
ELSEIF ABL:セックス中毒 == 3
	A = 17000
	B = 65000
	C = 10000
	D = 150
ELSEIF ABL:セックス中毒 == 4
	A = 30000
	B = 80000
	C = 25000
	D = 250
ELSEIF ABL:セックス中毒 == 5
	A = 35000
	B = 100000
	C = 30000
	D = 350
ELSEIF ABL:セックス中毒 == 6
	A = 40000
	B = 120000
	C = 35000
	D = 500
ELSEIF ABL:セックス中毒 == 7
	A = 45000
	B = 140000
	C = 40000
	D = 600
ELSEIF ABL:セックス中毒 == 8
	A = 50000
	B = 160000
	C = 45000
	D = 700
ELSEIF ABL:セックス中毒 == 9
	A = 60000
	B = 200000
	C = 50000
	D = 800
ENDIF

;一線越えない
IF TALENT:一線越えない && (TALENT:恋慕 == 0 && TALENT:淫乱 == 0 && TALENT:服従 == 0 && TALENT:親愛 == 0 && TALENT:娼婦 == 0 && TALENT:隷属 == 0)
	TIMES A , 2.00
	TIMES B , 2.00
	TIMES C , 2.00
	TIMES D , 2.00
ENDIF
;中毒しやすい
IF TALENT:中毒しやすい
	TIMES A , 0.50
	TIMES B , 0.50
	TIMES C , 0.50
	TIMES D , 0.50
ENDIF

;ＬＶ２から３に上げるときは異常経験必要（素質：[中毒しやすい][セックス狂]なら無視できる）
IF ABL:セックス中毒 == 2 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	IF EXP:異常経験 == 0
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
	ENDIF
;ＬＶ３から４に上げるときは異常経験2以上必要（素質：[中毒しやすい][セックス狂]なら無視できる）
ELSEIF ABL:セックス中毒 == 3 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	IF EXP:異常経験 < 2
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
	ENDIF
;ＬＶ４から５に上げるときは異常経験3以上必要（素質：[中毒しやすい][セックス狂]なら無視できる）
ELSEIF ABL:セックス中毒 == 4 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	IF EXP:異常経験 < 3
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
	ENDIF
;ＬＶ８から９に上げるときは異常経験7以上必要（素質：[中毒しやすい][セックス狂]なら無視できる）
ELSEIF ABL:セックス中毒 == 8 && TALENT:中毒しやすい == 0 && TALENT:セックス狂 == 0
	IF EXP:異常経験 < 7
		;異常経験が不足
		I |= 2
		J |= 2
		K |= 2
	ENDIF
ENDIF

;V感覚が必要
SIF ABL:Ｖ感覚 < ABL:セックス中毒 + 1
	I |= 4
;A感覚が必要
SIF ABL:Ａ感覚 < ABL:セックス中毒 + 1
	J |= 4
;C感覚が必要
SIF ABL:Ｃ感覚 < ABL:セックス中毒 + 1
	K |= 4


;欲情の珠で上げる
IF JUEL:欲情 < A
	I |= 1
	J |= 1
	K |= 1
ENDIF
;快Vの珠で上げる
SIF JUEL:快Ｖ < B
	I |= 1
;快Aの珠で上げる
SIF JUEL:快Ａ < B
	J |= 1
;快Cの珠で上げる
SIF JUEL:快Ｃ < B
	K |= 1
;屈服の珠で上げる
IF JUEL:屈服 < C
	I |= 1
	J |= 1
	K |= 1
ENDIF
;性交経験が必要
IF EXP:性交経験 < D
	I |= 2
	J |= 2
	K |= 2
ENDIF

IF I == 0 || J == 0 || K == 0
	Z = 1
ENDIF

RETURN 1

