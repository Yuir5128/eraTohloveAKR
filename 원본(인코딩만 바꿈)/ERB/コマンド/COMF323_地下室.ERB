;-------------------------------------------------
;地下室
;シチュエーション別コマンド
;-------------------------------------------------
@COM323

PRINTL 地下室
STR:0 = 地下室
CALL KOJO_MESSAGE_COM

;着脱の処理　終了時はソースの加算は行わない
IF TEQUIP:地下室
	TEQUIP:地下室 = 0
	RETURN 1
ELSE
	TEQUIP:地下室 = 1
ENDIF

;移動時に尿・便存在をクリア
TFLAG:66 = 0
TFLAG:57 = 0


;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;消費体力、気力
LOSEBASE:体力 += 5
LOSEBASE:気力 += 50

;烙印・猟奇系の経験がある場合のみ気力消費と恐怖・反発のソース
;反発は、親愛・服従・隷属以外のみ
IF TALENT:烙印 || TALENT:片腕欠損 || TALENT:両腕欠損 || TALENT:片脚欠損 || TALENT:両足欠損 || TALENT:達磨 || TALENT:全抜歯
	SOURCE:恐れ += 200
	SIF TALENT:服従 == 0 && TALENT:親愛 == 0 && TALENT:隷属 == 0
		SOURCE:反発 += 200
ENDIF


;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;依存度ベクトル
TFLAG:33 = 5

RETURN 1



;-------------------------------------------------
;装着継続の処理
;-------------------------------------------------
@EQUIP_COM323
;地下室
PRINTL ＜地下室＞

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;烙印・猟奇系の経験がある場合のみ気力消費と恐怖・反発のソース
;反発は、親愛・服従・隷属以外のみ
IF TALENT:烙印 || TALENT:片腕欠損 || TALENT:両腕欠損 || TALENT:片脚欠損 || TALENT:両足欠損 || TALENT:達磨 || TALENT:全抜歯
	LOSEBASE:気力 += 50
	
	SOURCE:恐れ += 100
	SIF TALENT:服従 == 0 && TALENT:親愛 == 0 && TALENT:隷属 == 0
		SOURCE:反発 += 100
ENDIF

RETURN 1


