;COMABLE一括管理
@GLOBAL_COMABLE(ARG)
#FUNCTION
;コマンドフィルタ
LOCALS = /{ARG}/
IF STRCOUNT(SAVESTR:0,LOCALS)
	RESULT = 0
	RETURNF 1
ENDIF

;気絶中
IF TALENT:気絶
	SELECTCASE ARG
		CASE 1, 153, 154
		CASEELSE
			RESULT = 0
			RETURNF 1
	ENDSELECT
ENDIF

