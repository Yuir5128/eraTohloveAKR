;-------------------------------------------------
;○○口上（キャラクターの名前を入れる、下にコンセプトなど一言あれば入れる）
;
;-------------------------------------------------

;-------------------------------------------------
;eratohoA及びeratohoA系列専用掛け合い口上テンプレート
;X1を奴隷のキャラ番号に置換し、Y1を調教者のキャラ番号に置換してください
;ただし、キャラ番号が一桁のキャラの場合、X1,Y1は1、X2は01のように変えてください
;EVENT_KX.ERBから口上を呼ぶため、セットで使用してください
;そしてコマンド内の適当な条件の下のPRINTFORMWの後ろに半角スペースを置いてセリフを追加してください
;口上を追加しないコマンドは無記入でも構いませんが、CFLAG:XXXの頭に『;』をつけてコメントアウトすると尚良いでしょう
;エキストラ構文など、コメントアウトされている構文を使う際には、『;』を外してコメントアウトを解除する必要があります
;最後にread meにも目を通しておくことをお勧めします
;-------------------------------------------------
;--------------------------------------------------
;EVENTTRAIN関係（X1をキャラ番号に置換）
;調教開始時のセリフ CFLAG 201～219を使用
;-------------------------------------------------
@EVENTTRAIN_PY1_KX1
;-------------------------------------------------
;調教開始時（一回のみ） CFLAG201
;-------------------------------------------------
DRAWLINE
;初調教時
IF CFLAG:201 == 0
	PRINTFORMW 
	CFLAG:201 = 1
	RETURN 0
;屈服刻印Lv1
ELSEIF CFLAG:201 < 2 && MARK:2 == 1
	PRINTFORMW 
	CFLAG:201 = 2
	RETURN 0
;屈服刻印Lv2
ELSEIF CFLAG:201 < 3 && MARK:2 == 2
	PRINTFORMW 
	CFLAG:201 = 3
	RETURN 0
;屈服刻印Lv3
ELSEIF CFLAG:201 < 4 && MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 
	CFLAG:201 = 4
	RETURN 0
;陥落
ELSEIF CFLAG:201 < 5 && (TALENT:3 || TALENT:4 || TALENT:5)
	;恋慕
	IF TALENT:3
		PRINTFORMW 
	;淫乱
	ELSEIF TALENT:4
		PRINTFORMW 
	;服従
	ELSEIF TALENT:5
		PRINTFORMW 
	ENDIF
	CFLAG:201 = 5
	RETURN 0
ENDIF

;-------------------------------------------------
;簡易助手口上（Y1を助手のキャラ番号に置換、X1をキャラ番号に変更する) CFLAG203
;掛け合いをするキャラを増やす場合は、構文をコピー＆ペーストをして、CFLAGの数を204、205と増やす
;-------------------------------------------------
;助手の有無をチェック（いない場合は二回目以降へ飛ぶ）
IF ASSI < 0 
	CALL EVENTTRAIN_SECOND_PY1_KX1
;助手
;ELSEIF NO:ASSI == Y1
	;初めて
;	IF CFLAG:203 == 0
		;陥落済み＋陥落時初口上が発生済み
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && CFLAG:201 >= 5
;			PRINTFORMW 
		;それ以外
;		ELSE
;			PRINTFORMW 
;		ENDIF
;			CFLAG:203 = 1
	;二回目以降
;	ELSEIF CFLAG:203 < 2 || FLAG:7 ==2
		;陥落済み
;		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
;			PRINTFORMW 
		;それ以外
;		ELSE
;			PRINTFORMW 
;		ENDIF
;			CFLAG:203 = 2
;	ENDIF
;口上のある助手が居ない場合は、通常の二回目以降の口上へ飛ぶ
ELSE
	CALL EVENTTRAIN_SECOND_PY1_KX1
ENDIF

;-------------------------------------------------
;調教開始時（2回目以降、X1をキャラ番号に変更する）
;-------------------------------------------------
@EVENTTRAIN_SECOND_PY1_KX1
;反発刻印Lv3
IF MARK:3 == 3 && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:202 = 4
;屈服刻印Lv0
ELSEIF MARK:2 == 0 && (CFLAG:202 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:202 = 1
;屈服刻印Lv1
ELSEIF MARK:2 == 1 && (CFLAG:202 < 2 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:202 = 2
;屈服刻印Lv2
ELSEIF MARK:2 == 2 && (CFLAG:202 < 3 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:202 = 3
;屈服刻印Lv3＋陥落素質無し
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0) && (CFLAG:202 < 4 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:202 = 4
;陥落済み
ELSEIF CFLAG:202 < 5 || FLAG:7 == 2
	;恋慕、親愛
	IF TALENT:3 || TALENT:6
		;ランダムで口上が変化する（使わない場合はすべて同じにすればよい）
		K:1 = RAND:3
		IF K:1 == 0
			PRINTFORMW 
		ELSEIF K:1 == 1
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	;淫乱、娼婦
	ELSEIF TALENT:4 || TALENT:7
		;ランダムで口上が変化する（使わない場合はすべて同じにすればよい）
		K:1 = RAND:3
		IF K:1 == 0
			PRINTFORMW 
		ELSEIF K:1 == 1
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	;服従、隷属
	ELSEIF TALENT:5 || TALENT:8
		;ランダムで口上が変化する（使わない場合はすべて同じにすればよい）
		K:1 = RAND:3
		IF K:1 == 0
			PRINTFORMW 
		ELSEIF K:1 == 1
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	ENDIF
	CFLAG:202 = 5
ENDIF
RETURN 0

;-------------------------------------------------
;EVENTEND関係（X1をキャラ番号に置換）
;調教終了時のセリフ
;-------------------------------------------------
@EVENTEND_PY1_KX1
;--------------------------------------------------
;調教終了時のセリフ
;--------------------------------------------------
DRAWLINE
;反発刻印Lv3
IF MARK:3 == 3 && (CFLAG:217 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:217 = 1
;屈服刻印Lv1以下
ELSEIF MARK:2 <= 1 && (CFLAG:219 < 1 || FLAG:7 == 2)
	PRINTFORMW 
	CFLAG:219 = 1
;屈服刻印Lv2
ELSEIF MARK:2 == 2
	PRINTFORMW 
	CFLAG:219 = 2
;屈服刻印Lv3＋陥落素質無し
ELSEIF MARK:2 == 3 && (TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0)
	PRINTFORMW 
	CFLAG:219 = 3
;陥落済み＋体力500未満
ELSEIF BASE:0 < 500 && (CFLAG:218 < 1 || FLAG:7 == 2)
	;恋慕、親愛
	IF TALENT:3 || TALENT:6
		PRINTFORMW 
	;淫乱、娼婦
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORMW 
	;服従、隷属
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:218 = 1
;陥落済み
ELSEIF CFLAG:219 < 4 || FLAG:7 == 2
	;恋慕、親愛
	IF TALENT:3 || TALENT:6
		PRINTFORMW 
	;淫乱、娼婦
	ELSEIF TALENT:4 || TALENT:7
		PRINTFORMW 
	;服従、隷属
	ELSEIF TALENT:5 || TALENT:8
		PRINTFORMW 
	ENDIF
	CFLAG:219 = 4
ENDIF
RETURN 0

;--------------------------------------------------
;@KOJO_MESSAGE_COM関係（X1をキャラ番号に置換）
;コマンド実行時に出力されます
;--------------------------------------------------
@KOJO_MESSAGE_COM__PY1_KX1
;-------------------------------------------------
;コマンド実行時のセリフ CFLAG 300～500
;-------------------------------------------------
;=================================================
;愛撫系コマンド
;=================================================
;-------------------------------------------------
;0,愛撫
;-------------------------------------------------
IF SELECTCOM == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:300 == 0
		PRINTFORMW 
		CFLAG:300 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:300 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:300 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:300 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:300 = 3
		;それ以外
		ELSEIF CFLAG:300 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:300 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;1,クンニ
;-------------------------------------------------
IF SELECTCOM == 1
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:301 == 0
		PRINTFORMW 
		CFLAG:301 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:301 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:301 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:301 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:301 = 3
		;それ以外
		ELSEIF CFLAG:301 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:301 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;2,フェラする
;-------------------------------------------------
IF SELECTCOM == 2
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:302 == 0
		PRINTFORMW 
		CFLAG:302 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:302 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:302 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:302 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:302 = 3
		;それ以外
		ELSEIF CFLAG:302 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:302 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;3,指挿入れ
;-------------------------------------------------
IF SELECTCOM == 3
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:303 == 0
		PRINTFORMW 
		CFLAG:303 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:303 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:303 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:303 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:303 = 3
		;それ以外
		ELSEIF CFLAG:303 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:303 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;4,アナル舐め
;-------------------------------------------------
IF SELECTCOM == 4
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:304 == 0
		PRINTFORMW 
		CFLAG:304 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:304 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:304 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:304 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:304 = 3
		;それ以外
		ELSEIF CFLAG:304 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:304 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;5,アナル愛撫
;-------------------------------------------------
IF SELECTCOM == 5
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:305 == 0
		PRINTFORMW 
		CFLAG:305 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:305 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:305 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:305 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:305 = 3
		;それ以外
		ELSEIF CFLAG:305 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:305 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;6,胸揉み
;-------------------------------------------------
IF SELECTCOM == 6
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:306 == 0
		PRINTFORMW 
		CFLAG:306 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:306 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:306 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:306 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:306 = 3
		;それ以外
		ELSEIF CFLAG:306 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:306 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;7,乳首責め
;-------------------------------------------------
IF SELECTCOM == 7
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:307 == 0
		PRINTFORMW 
		CFLAG:307 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:307 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:307 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:307 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:307 = 3
		;それ以外
		ELSEIF CFLAG:307 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:307 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;8,胸愛撫
;-------------------------------------------------
IF SELECTCOM == 8
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:308 == 0
		PRINTFORMW 
		CFLAG:308 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:308 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:308 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:308 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:308 = 3
		;それ以外
		ELSEIF CFLAG:308 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:308 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;9,秘貝開帳
;-------------------------------------------------
IF SELECTCOM == 9
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:309 == 0
		PRINTFORMW 
		CFLAG:309 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:309 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:309 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:309 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:309 = 3
		;それ以外
		ELSEIF CFLAG:309 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:309 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;10,胸自慰
;-------------------------------------------------
IF SELECTCOM == 10
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:310 == 0
		PRINTFORMW 
		CFLAG:310 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:310 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:310 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:310 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:310 = 3
		;それ以外
		ELSEIF CFLAG:310 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:310 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;11,自慰
;-------------------------------------------------
IF SELECTCOM == 11
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:311 == 0
		PRINTFORMW 
		CFLAG:311 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:311 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:311 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:311 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:311 = 3
		;それ以外
		ELSEIF CFLAG:311 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:311 = 2
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;12,くるん愛撫 CFLAG:420 
;-------------------------------------------------
IF SELECTCOM == 12
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:420 == 0
		PRINTFORMW 
		CFLAG:420 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:420 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:420 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:420 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:420 = 3
		;それ以外
		ELSEIF CFLAG:420 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:420 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;13,手淫する CFLAG:446 
;-------------------------------------------------
IF SELECTCOM == 13
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:446 == 0
		PRINTFORMW 
		CFLAG:446 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:446 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:446 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:446 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:446 = 3
		;それ以外
		ELSEIF CFLAG:446 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:446 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;コミュニケーション系コマンド
;=================================================
;-------------------------------------------------
;20,キスする
;-------------------------------------------------
IF SELECTCOM == 20
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:312 == 0
		PRINTFORMW 
		CFLAG:312 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:312 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:312 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:312 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:312 = 3
		;それ以外
		ELSEIF CFLAG:312 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:312 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;21,何もしない
;-------------------------------------------------
IF SELECTCOM == 21
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:313 == 0
		PRINTFORMW 
		CFLAG:313 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:313 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:313 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:313 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:313 = 3
		;それ以外
		ELSEIF CFLAG:313 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:313 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;22,会話　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 22
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:314 == 0
		PRINTFORMW 
		CFLAG:314 = 1
	;二回目以降
	ELSEIF CFLAG:314 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:314 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;23,プレゼント　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 23
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:315 == 0
		PRINTFORMW 
		CFLAG:315 = 1
	;二回目以降
	ELSEIF CFLAG:315 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:315 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;24,スキンシップ　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 24
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:316 == 0
		PRINTFORMW 
		CFLAG:316 = 1
	;二回目以降
	ELSEIF CFLAG:316 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:316 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;25,まったりする　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 25
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:317 == 0
		PRINTFORMW 
		CFLAG:317 = 1
	;二回目以降
	ELSEIF CFLAG:317 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:317 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;26,家事　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 26
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:318 == 0
		PRINTFORMW 
		CFLAG:318 = 1
	;二回目以降
	ELSEIF CFLAG:318 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:318 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;27,歌の練習　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 27
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:319 == 0
		PRINTFORMW 
		CFLAG:319 = 1
	;二回目以降
	ELSEIF CFLAG:319 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:319 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;28,罵倒
;-------------------------------------------------
IF SELECTCOM == 28
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:320 == 0
		PRINTFORMW 
		CFLAG:320 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:320 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:320 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:320 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:320 = 3
		;それ以外
		ELSEIF CFLAG:320 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:320 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;29,ビデオ鑑賞　陥落済みのみ
;-------------------------------------------------
IF SELECTCOM == 29
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:321 == 0
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:321 = 1
	;二回目以降
	ELSEIF CFLAG:321 < 2 || FLAG:7 == 2
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:321 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;30,暴力を振るう CFLAG:473
;-------------------------------------------------
IF SELECTCOM == 30
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:473 == 0
		PRINTFORMW 
		CFLAG:473 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:473 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:473 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:473 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:473 = 3
		;それ以外
		ELSEIF CFLAG:473 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:473 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;道具使用コマンド
;=================================================
;40,ローター
;-------------------------------------------------
IF SELECTCOM == 40
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:322 == 0
		PRINTFORMW 
		CFLAG:322 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:322 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:322 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:322 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:322 = 3
		;それ以外
		ELSEIF CFLAG:322 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:322 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;41,Ｅマッサージャ
;-------------------------------------------------
IF SELECTCOM == 41
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:323 == 0
		PRINTFORMW 
		CFLAG:323 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:323 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:323 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:323 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:323 = 3
		;それ以外
		ELSEIF CFLAG:323 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:323 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;42,クリキャップ
;-------------------------------------------------
IF SELECTCOM == 42 && TEQUIP:11 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:324 == 0
		PRINTFORMW 
		CFLAG:324 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:324 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:324 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:324 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:324 = 3
		;それ以外
		ELSEIF CFLAG:324 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:324 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 42 && TEQUIP:11 == 1
	IF CFLAG:324 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;43,オナホール
;-------------------------------------------------
IF SELECTCOM == 43 && TEQUIP:12 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:325 == 0
		PRINTFORMW 
		CFLAG:325 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:325 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:325 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:325 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:325 = 3
		;それ以外
		ELSEIF CFLAG:325 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:325 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 43 && TEQUIP:12 == 1
	IF CFLAG:325 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;44,バイブ
;-------------------------------------------------
IF SELECTCOM == 44 && TEQUIP:13 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:326 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:326 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:326 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:326 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:326 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:326 = 3
		;それ以外
		ELSEIF CFLAG:326 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:326 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 44 && TEQUIP:13 == 1
	IF CFLAG:326 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;45,アナルバイブ
;-------------------------------------------------
IF SELECTCOM == 45 && TEQUIP:14 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:327 == 0
		PRINTFORMW 
		CFLAG:327 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:327 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:327 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:327 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:327 = 3
		;それ以外
		ELSEIF CFLAG:327 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:327 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 45 && TEQUIP:14 == 1
	IF CFLAG:327 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;46,アナルビーズ
;-------------------------------------------------
IF SELECTCOM == 46 && TEQUIP:15 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:328 == 0
		PRINTFORMW 
		CFLAG:328 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:328 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:328 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:328 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:328 = 3
		;それ以外
		ELSEIF CFLAG:328 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:328 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 46 && TEQUIP:15 == 1
	IF CFLAG:328 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;47,二プルキャップ
;-------------------------------------------------
IF SELECTCOM == 47 && TEQUIP:16 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:329 == 0
		PRINTFORMW 
		CFLAG:329 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:329 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:329 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:329 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:329 = 3
		;それ以外
		ELSEIF CFLAG:329 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:329 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 47 && TEQUIP:16 == 1
	IF CFLAG:329 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;48,乳首ローター
;-------------------------------------------------
IF SELECTCOM == 48
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:330 == 0
		PRINTFORMW 
		CFLAG:330 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:330 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:330 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:330 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:330 = 3
		;それ以外
		ELSEIF CFLAG:330 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:330 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;49,搾乳機
;-------------------------------------------------
IF SELECTCOM == 49 && TEQUIP:17 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:331 == 0
		PRINTFORMW 
		CFLAG:331 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:331 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:331 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:331 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:331 = 3
		;それ以外
		ELSEIF CFLAG:331 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:331 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 49 && TEQUIP:17 == 1
	IF CFLAG:331 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;50,カテーテル　CFLAG:447
;-------------------------------------------------
IF SELECTCOM == 50 && TEQUIP:38 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:447 == 0
		PRINTFORMW 
		CFLAG:447 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:447 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:447 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:447 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:447 = 3
		;それ以外
		ELSEIF CFLAG:447 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:447 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 50 && TEQUIP:38 == 1
	IF CFLAG:447 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;=================================================
;セックス系コマンド
;=================================================
;-------------------------------------------------
;60,正常位
;-------------------------------------------------
IF SELECTCOM == 60
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:332 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:332 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:332 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:332 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:332 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:332 = 3
		;それ以外
		ELSEIF CFLAG:332 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:332 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;61,後背位
;-------------------------------------------------
IF SELECTCOM == 61
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:333 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:333 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:333 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:333 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:333 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:333 = 3
		;それ以外
		ELSEIF CFLAG:333 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:333 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;62,正常位アナル
;-------------------------------------------------
IF SELECTCOM == 62
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:334 == 0
		PRINTFORMW 
		CFLAG:334 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:334 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:334 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:334 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:334 = 3
		;それ以外
		ELSEIF CFLAG:334 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:334 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;63,後背位アナル
;-------------------------------------------------
IF SELECTCOM == 63
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:335 == 0
		PRINTFORMW 
		CFLAG:335 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:335 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:335 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:335 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:335 = 3
		;それ以外
		ELSEIF CFLAG:335 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:335 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;64,騎乗位する
;-------------------------------------------------
;IF SELECTCOM == 64
;	CALL TRAIN_MESSAGE
;	;初めて
;	IF CFLAG:336 == 0
;		PRINTFORMW 
;		CFLAG:336 = 1
;	;二回目以降
;	ELSE
;		;陥落済み
;		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:336 < 4 || FLAG:7 == 2)
;			;恋慕、親愛
;			IF TALENT:3 || TALENT:6
;				PRINTFORMW 
;			;淫乱、娼婦
;			ELSEIF TALENT:4 || TALENT:7
;				PRINTFORMW 
;			;服従、隷属
;			ELSEIF TALENT:5 || TALENT:8
;				PRINTFORMW 
;			ENDIF
;			CFLAG:336 = 4
;		;屈服刻印Lv3
;		ELSEIF MARK:2 == 3 && (CFLAG:336 < 3 || FLAG:7 == 2)
;			PRINTFORMW 
;			CFLAG:336 = 3
;		;それ以外
;		ELSEIF CFLAG:336 < 2 || FLAG:7 == 2
;			PRINTFORMW 
;			CFLAG:336 = 2
;		ENDIF
;	ENDIF
;ENDIF

;-------------------------------------------------
;65,乳ファック
;-------------------------------------------------
IF SELECTCOM == 65
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:337 == 0
		PRINTFORMW 
		CFLAG:337 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:337 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:337 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:337 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:337 = 3
		;それ以外
		ELSEIF CFLAG:337 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:337 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;66,騎乗位
;-------------------------------------------------
IF SELECTCOM == 66
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:338 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:338 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:338 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:338 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:338 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:338 = 3
		;それ以外
		ELSEIF CFLAG:338 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:338 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;67,騎乗位アナル
;-------------------------------------------------
IF SELECTCOM == 67
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:339 == 0
		PRINTFORMW 
		CFLAG:339 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:339 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:339 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:339 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:339 = 3
		;それ以外
		ELSEIF CFLAG:339 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:339 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;68,対面座位
;-------------------------------------------------
IF SELECTCOM == 68
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:340 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:340 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:340 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:340 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:340 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:340 = 3
		;それ以外
		ELSEIF CFLAG:340 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:340 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;69,背面座位
;-------------------------------------------------
IF SELECTCOM == 69
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:341 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:341 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:341 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:341 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:341 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:341 = 3
		;それ以外
		ELSEIF CFLAG:341 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:341 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;70,対面座位アナル
;-------------------------------------------------
IF SELECTCOM == 70
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:342 == 0
		PRINTFORMW 
		CFLAG:342 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:342 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:342 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:342 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:342 = 3
		;それ以外
		ELSEIF CFLAG:342 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:342 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;71,背面座位アナル
;-------------------------------------------------
IF SELECTCOM == 71
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:343 == 0
		PRINTFORMW 
		CFLAG:343 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:343 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:343 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:343 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:343 = 3
		;それ以外
		ELSEIF CFLAG:343 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:343 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;72,下着ずらし挿入 CFLAG:421 
;-------------------------------------------------
IF SELECTCOM == 72
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:421 == 0
		PRINTFORMW 
		CFLAG:421 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:421 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:421 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:421 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:421 = 3
		;それ以外
		ELSEIF CFLAG:421 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:421 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;73,下着ずらし挿入アナル CFLAG:422 
;-------------------------------------------------
IF SELECTCOM == 73
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:422 == 0
		PRINTFORMW 
		CFLAG:422 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:422 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:422 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:422 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:422 = 3
		;それ以外
		ELSEIF CFLAG:422 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:422 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;74,対面立位 CFLAG:439 
;-------------------------------------------------
IF SELECTCOM == 74
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:439 == 0
		PRINTFORMW 
		CFLAG:439 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:439 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:439 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:439 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:439 = 3
		;それ以外
		ELSEIF CFLAG:439 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:439 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;75,対面立位アナル CFLAG:440 
;-------------------------------------------------
IF SELECTCOM == 75
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:440 == 0
		PRINTFORMW 
		CFLAG:440 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:440 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:440 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:440 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:440 = 3
		;それ以外
		ELSEIF CFLAG:440 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:440 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;76,背面立位 CFLAG:441 
;-------------------------------------------------
IF SELECTCOM == 76
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:441 == 0
		PRINTFORMW 
		CFLAG:441 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:441 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:441 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:441 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:441 = 3
		;それ以外
		ELSEIF CFLAG:441 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:441 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;77,背面立位アナル CFLAG:442 
;-------------------------------------------------
IF SELECTCOM == 77
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:442 == 0
		PRINTFORMW 
		CFLAG:442 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:442 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:442 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:442 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:442 = 3
		;それ以外
		ELSEIF CFLAG:442 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:442 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;奉仕系コマンド
;=================================================
;-------------------------------------------------
;80,手淫
;-------------------------------------------------
IF SELECTCOM == 80
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:344 == 0
		PRINTFORMW 
		CFLAG:344 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:344 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:344 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:344 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:344 = 3
		;それ以外
		ELSEIF CFLAG:344 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:344 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;81,フェラチオ
;-------------------------------------------------
IF SELECTCOM == 81
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:345 == 0
		PRINTFORMW 
		CFLAG:345 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:345 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:345 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:345 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:345 = 3
		;それ以外
		ELSEIF CFLAG:345 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:345 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;82,パイズリ
;-------------------------------------------------
IF SELECTCOM == 82
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:346 == 0
		PRINTFORMW 
		CFLAG:346 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:346 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:346 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:346 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:346 = 3
		;それ以外
		ELSEIF CFLAG:346 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:346 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;83,素股
;-------------------------------------------------
IF SELECTCOM == 83
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:347 == 0
		PRINTFORMW 
		CFLAG:347 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:347 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:347 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:347 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:347 = 3
		;それ以外
		ELSEIF CFLAG:347 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:347 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;84,泡踊り
;-------------------------------------------------
IF SELECTCOM == 84
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:348 == 0
		PRINTFORMW 
		CFLAG:348 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:348 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:348 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:348 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:348 = 3
		;それ以外
		ELSEIF CFLAG:348 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:348 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;85,足扱き
;-------------------------------------------------
IF SELECTCOM == 85
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:349 == 0
		PRINTFORMW 
		CFLAG:349 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:349 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:349 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:349 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:349 = 3
		;それ以外
		ELSEIF CFLAG:349 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:349 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;86,足舐め強制
;-------------------------------------------------
IF SELECTCOM == 86
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:350 == 0
		PRINTFORMW 
		CFLAG:350 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:350 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:350 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:350 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:350 = 3
		;それ以外
		ELSEIF CFLAG:350 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:350 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;87,縦パイズリ
;-------------------------------------------------
IF SELECTCOM == 87
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:351 == 0
		PRINTFORMW 
		CFLAG:351 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:351 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:351 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:351 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:351 = 3
		;それ以外
		ELSEIF CFLAG:351 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:351 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;88,パイズリフェラ
;-------------------------------------------------
IF SELECTCOM == 88
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:352 == 0
		PRINTFORMW 
		CFLAG:352 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:352 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:352 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:352 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:352 = 3
		;それ以外
		ELSEIF CFLAG:352 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:352 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;89,母乳飲み　陥落済みのみ
;-------------------------------------------------
IF SELECTCOM == 89
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:353 == 0
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:353 = 1
	;二回目以降
	ELSEIF CFLAG:353 < 2 || FLAG:7 == 2
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:353 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;90,髪扱き
;-------------------------------------------------
IF SELECTCOM == 90
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:471 == 0
		PRINTFORMW 
		CFLAG:471 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:471 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:471 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:471 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:471 = 3
		;それ以外
		ELSEIF CFLAG:471 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:471 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;91,指チュパ
;-------------------------------------------------
IF SELECTCOM == 91
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:472 == 0
		PRINTFORMW 
		CFLAG:472 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:472 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:472 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:472 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:472 = 3
		;それ以外
		ELSEIF CFLAG:472 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:472 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;92,腋扱き CFLAG:474
;-------------------------------------------------
IF SELECTCOM == 92
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:474 == 0
		PRINTFORMW 
		CFLAG:474 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:474 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:474 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:474 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:474 = 3
		;それ以外
		ELSEIF CFLAG:474 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:474 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;SM系コマンド
;=================================================
;-------------------------------------------------
;100,スパンキング
;-------------------------------------------------
IF SELECTCOM == 100
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:354 == 0
		PRINTFORMW 
		CFLAG:354 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:354 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:354 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:354 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:354 = 3
		;それ以外
		ELSEIF CFLAG:354 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:354 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;101,乳スパンキング
;-------------------------------------------------
IF SELECTCOM == 101
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:355 == 0
		PRINTFORMW 
		CFLAG:355 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:355 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:355 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:355 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:355 = 3
		;それ以外
		ELSEIF CFLAG:355 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:355 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;102,鞭
;-------------------------------------------------
IF SELECTCOM == 102
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:356 == 0
		PRINTFORMW 
		CFLAG:356 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:356 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:356 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:356 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:356 = 3
		;それ以外
		ELSEIF CFLAG:356 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:356 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;103,蝋燭
;-------------------------------------------------
IF SELECTCOM == 103
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:357 == 0
		PRINTFORMW 
		CFLAG:357 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:357 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:357 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:357 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:357 = 3
		;それ以外
		ELSEIF CFLAG:357 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:357 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;104,針
;-------------------------------------------------
IF SELECTCOM == 104
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:358 == 0
		PRINTFORMW 
		CFLAG:358 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:358 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:358 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:358 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:358 = 3
		;それ以外
		ELSEIF CFLAG:358 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:358 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;105,アイマスク
;-------------------------------------------------
IF SELECTCOM == 105 && TEQUIP:18 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:359 == 0
		PRINTFORMW 
		CFLAG:359 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:359 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:359 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:359 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:359 = 3
		;それ以外
		ELSEIF CFLAG:359 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:359 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 105 && TEQUIP:18 == 1
	IF CFLAG:359 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;106,縄
;-------------------------------------------------
IF SELECTCOM == 106 && TEQUIP:19 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:360 == 0
		PRINTFORMW 
		CFLAG:360 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:360 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:360 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:360 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:360 = 3
		;それ以外
		ELSEIF CFLAG:360 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:360 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 106 && TEQUIP:19 == 1
	IF CFLAG:360 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;107,ボールギャグ
;-------------------------------------------------
IF SELECTCOM == 107 && TEQUIP:20 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:361 == 0
		PRINTFORMW 
		CFLAG:361 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:361 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:361 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:361 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:361 = 3
		;それ以外
		ELSEIF CFLAG:361 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:361 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 107 && TEQUIP:20 == 1
	IF CFLAG:361 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;108,拷問　CFLAG:419　
;-------------------------------------------------
IF SELECTCOM == 108
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:419 == 0
		PRINTFORMW 
		CFLAG:419 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:419 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:419 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:419 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:419 = 3
		;それ以外
		ELSEIF CFLAG:419 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:419 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;110,家具にする　CFLAG:438　
;-------------------------------------------------
IF SELECTCOM == 110
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:438 == 0
		PRINTFORMW 
		CFLAG:438 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:438 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:438 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:438 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:438 = 3
		;それ以外
		ELSEIF CFLAG:438 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:438 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;111,剃毛　CFLAG:475
;-------------------------------------------------
IF SELECTCOM == 111
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:475 == 0
		PRINTFORMW 
		CFLAG:475 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:475 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:475 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:475 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:475 = 3
		;それ以外
		ELSEIF CFLAG:475 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:475 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;112,首絞め　CFLAG:476　
;-------------------------------------------------
IF SELECTCOM == 112
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:476 == 0
		PRINTFORMW 
		CFLAG:476 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:476 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:476 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:476 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:476 = 3
		;それ以外
		ELSEIF CFLAG:476 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:476 = 2
		ENDIF
	ENDIF
ENDIF


;=================================================
;助手によるコマンド、及びレズプレイ
;=================================================
;-------------------------------------------------
;120,クンニ強制
;-------------------------------------------------
IF SELECTCOM == 120
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:362 == 0
		PRINTFORMW 
		CFLAG:362 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:362 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:362 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:362 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:362 = 3
		;それ以外
		ELSEIF CFLAG:362 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:362 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;121,貝あわせ
;-------------------------------------------------
IF SELECTCOM == 121
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:363 == 0
		PRINTFORMW 
		CFLAG:363 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:363 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:363 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:363 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:363 = 3
		;それ以外
		ELSEIF CFLAG:363 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:363 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;122,助手を犯させる
;-------------------------------------------------
IF SELECTCOM == 122
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:364 == 0
		PRINTFORMW 
		CFLAG:364 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:364 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:364 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:364 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:364 = 3
		;それ以外
		ELSEIF CFLAG:364 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:364 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;123,足コキする
;-------------------------------------------------
IF SELECTCOM == 123
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:365 == 0
		PRINTFORMW 
		CFLAG:365 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:365 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:365 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:365 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:365 = 3
		;それ以外
		ELSEIF CFLAG:365 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:365 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;124,ダブルフェラ
;-------------------------------------------------
IF SELECTCOM == 124
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:366 == 0
		PRINTFORMW 
		CFLAG:366 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:366 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:366 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:366 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:366 = 3
		;それ以外
		ELSEIF CFLAG:366 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:366 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;125,ダブルパイズリ
;-------------------------------------------------
IF SELECTCOM == 125
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:367 == 0
		PRINTFORMW 
		CFLAG:367 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:367 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:367 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:367 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:367 = 3
		;それ以外
		ELSEIF CFLAG:367 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:367 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;126,乳の揉み合い
;-------------------------------------------------
IF SELECTCOM == 126
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:368 == 0
		PRINTFORMW 
		CFLAG:368 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:368 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:368 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:368 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:368 = 3
		;それ以外
		ELSEIF CFLAG:368 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:368 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;127,兜合わせ	CFLAG:434
;-------------------------------------------------
IF SELECTCOM == 127
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:434 == 0
		PRINTFORMW 
		CFLAG:434 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:434 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:434 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:434 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:434 = 3
		;それ以外
		ELSEIF CFLAG:434 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:434 = 2
		ENDIF
	ENDIF
ENDIF


;=================================================
;ハードな調教コマンド
;=================================================
;-------------------------------------------------
;140,イラマチオ
;-------------------------------------------------
IF SELECTCOM == 140
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:369 == 0
		PRINTFORMW 
		CFLAG:369 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:369 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:369 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:369 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:369 = 3
		;それ以外
		ELSEIF CFLAG:369 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:369 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;141,フィストファック　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 141
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:370 == 0
		PRINTFORMW 
		CFLAG:370 = 1
	;二回目以降
	ELSEIF CFLAG:370 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:370 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;142,アナルフィスト　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 142
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:371 == 0
		PRINTFORMW 
		CFLAG:371 = 1
	;二回目以降
	ELSEIF CFLAG:371 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:371 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;143,両穴フィスト　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 143
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:372 == 0
		PRINTFORMW 
		CFLAG:372 = 1
	;二回目以降
	ELSEIF CFLAG:372 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:372 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;144,放尿
;-------------------------------------------------
IF SELECTCOM == 144
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:373 == 0
		PRINTFORMW 
		CFLAG:373 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:373 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:373 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:373 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:373 = 3
		;それ以外
		ELSEIF CFLAG:373 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:373 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;145,アナル奉仕強制
;-------------------------------------------------
IF SELECTCOM == 145
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:374 == 0
		PRINTFORMW 
		CFLAG:374 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:374 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:374 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:374 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:374 = 3
		;それ以外
		ELSEIF CFLAG:374 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:374 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;146,浣腸器＋プラグ
;-------------------------------------------------
IF SELECTCOM == 146 && TEQUIP:21 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:375 == 0
		PRINTFORMW 
		CFLAG:375 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:375 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:375 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:375 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:375 = 3
		;それ以外
		ELSEIF CFLAG:375 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:375 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 146 && TEQUIP:21 == 1
	IF CFLAG:375 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;147,空気浣腸
;-------------------------------------------------
IF SELECTCOM == 147
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:376 == 0
		PRINTFORMW 
		CFLAG:376 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:376 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:376 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:376 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:376 = 3
		;それ以外
		ELSEIF CFLAG:376 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:376 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;148,拡張バルーン
;-------------------------------------------------
IF SELECTCOM == 148 && TEQUIP:22 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:377 == 0
		PRINTFORMW 
		CFLAG:377 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:377 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:377 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:377 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:377 = 3
		;それ以外
		ELSEIF CFLAG:377 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:377 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 148 && TEQUIP:22 == 1
	IF CFLAG:377 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;149,アナル電極　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 149 && TEQUIP:23 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:378 == 0
		PRINTFORMW 
		CFLAG:378 = 1
	;二回目以降
	ELSEIF CFLAG:378 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:378 = 2
	ENDIF
ELSEIF SELECTCOM == 149 && TEQUIP:23 == 1
	IF CFLAG:378 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;150,乳房電極　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 150 && TEQUIP:24 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:379 == 0
		PRINTFORMW 
		CFLAG:379 = 1
	;二回目以降
	ELSEIF CFLAG:379 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:379 = 2
	ENDIF
ELSEIF SELECTCOM == 150 && TEQUIP:24 == 1
	IF CFLAG:379 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;151,鰻風呂　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 151
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:380 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:380 = 1
	;二回目以降
	ELSEIF CFLAG:380 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:380 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;152,蟲風呂　服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 152
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:381 == 0
		PRINTFORMW 
		CFLAG:381 = 1
	;二回目以降
	ELSEIF CFLAG:381 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:381 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;153,尿道バルーン	CFLAG:435
;-------------------------------------------------
IF SELECTCOM == 153
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:435 == 0
		PRINTFORMW 
		CFLAG:435 = 1
	;二回目以降
	ELSEIF CFLAG:435 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:435 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;154,肉便器	CFLAG:436 服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 154
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:436 == 0
		PRINTFORMW 
		CFLAG:436 = 1
	;二回目以降
	ELSEIF CFLAG:436 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:436 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;155,獣姦	CFLAG:437 服従、隷属のみ
;-------------------------------------------------
IF SELECTCOM == 155
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:437 == 0
		PRINTFORMW 
		CFLAG:437 = 1
	;二回目以降
	ELSEIF CFLAG:437 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:437 = 2
	ENDIF
ENDIF

;-------------------------------------------------
;156,烙印　CFLAG:448
;R = 0なにもしない、R = 1傷痕、R = 2ピアス、R = 3刺青、 R = 4焼き印
;-------------------------------------------------
IF SELECTCOM == 156
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:448 == 0
		PRINTFORMW 
		CFLAG:448 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:448 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:448 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:448 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:448 = 3
		;それ以外
		ELSEIF CFLAG:448 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:448 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;157,飲尿　CFLAG:477
;-------------------------------------------------
IF SELECTCOM == 157
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:477 == 0
		PRINTFORMW 
		CFLAG:477 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:477 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:477 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:477 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:477 = 3
		;それ以外
		ELSEIF CFLAG:477 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:477 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;触手系コマンド
;=================================================
;-------------------------------------------------
;160,触手召喚
;-------------------------------------------------
IF SELECTCOM == 160 && TEQUIP:25 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:382 == 0
		PRINTFORMW 
		CFLAG:382 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:382 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:382 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:382 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:382 = 3
		;それ以外
		ELSEIF CFLAG:382 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:382 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 160 && TEQUIP:25 == 1
	IF CFLAG:382 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;161,触手挿入
;-------------------------------------------------
IF SELECTCOM == 161
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:383 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:383 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:383 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:383 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:383 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:383 = 3
		;それ以外
		ELSEIF CFLAG:383 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:383 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;162,アナル触手
;-------------------------------------------------
IF SELECTCOM == 162
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:384 == 0
		PRINTFORMW 
		CFLAG:384 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:384 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:384 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:384 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:384 = 3
		;それ以外
		ELSEIF CFLAG:384 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:384 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;163,触手クリ責め
;-------------------------------------------------
IF SELECTCOM == 163
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:385 == 0
		PRINTFORMW 
		CFLAG:385 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:385 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:385 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:385 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:385 = 3
		;それ以外
		ELSEIF CFLAG:385 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:385 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;164,触手乳首責め
;-------------------------------------------------
IF SELECTCOM == 164
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:386 == 0
		PRINTFORMW 
		CFLAG:386 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:386 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:386 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:386 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:386 = 3
		;それ以外
		ELSEIF CFLAG:386 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:386 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;165,触手搾乳
;-------------------------------------------------
IF SELECTCOM == 165
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:387 == 0
		PRINTFORMW 
		CFLAG:387 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:387 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:387 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:387 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:387 = 3
		;それ以外
		ELSEIF CFLAG:387 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:387 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;166,触手緊縛
;-------------------------------------------------
IF SELECTCOM == 166
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:388 == 0
		PRINTFORMW 
		CFLAG:388 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:388 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:388 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:388 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:388 = 3
		;それ以外
		ELSEIF CFLAG:388 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:388 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;167,触手浣腸
;-------------------------------------------------
IF SELECTCOM == 167
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:389 == 0
		PRINTFORMW 
		CFLAG:389 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:389 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:389 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:389 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:389 = 3
		;それ以外
		ELSEIF CFLAG:389 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:389 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;168,触手口辱
;-------------------------------------------------
IF SELECTCOM == 168 && TEQUIP:36 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:390 == 0
		PRINTFORMW 
		CFLAG:390 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:390 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:390 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:390 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:390 = 3
		;それ以外
		ELSEIF CFLAG:390 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:390 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 168 && TEQUIP:36 == 1
	IF CFLAG:390 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;169,触手ペニス責め
;-------------------------------------------------
IF SELECTCOM == 169
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:391 == 0
		PRINTFORMW 
		CFLAG:391 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:391 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:391 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:391 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:391 = 3
		;それ以外
		ELSEIF CFLAG:391 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:391 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;170,触手尿道挿入 CFLAG:449
;-------------------------------------------------
IF SELECTCOM == 170 && TEQUIP:38 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:449 == 0
		PRINTFORMW 
		CFLAG:449 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:449 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:449 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:449 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:449 = 3
		;それ以外
		ELSEIF CFLAG:449 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:449 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 170 && TEQUIP:38 == 1
	IF CFLAG:449 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;=================================================
;特殊コマンド
;=================================================
;-------------------------------------------------
;180,ローション,
;-------------------------------------------------
IF SELECTCOM == 180
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:392 == 0
		PRINTFORMW 
		CFLAG:392 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:392 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:392 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:392 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:392 = 3
		;それ以外
		ELSEIF CFLAG:392 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:392 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;181,媚薬,
;-------------------------------------------------
IF SELECTCOM == 181
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:393 == 0
		PRINTFORMW 
		CFLAG:393 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:393 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:393 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:393 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:393 = 3
		;それ以外
		ELSEIF CFLAG:393 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:393 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;182,利尿剤,
;-------------------------------------------------
IF SELECTCOM == 182
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:394 == 0
		PRINTFORMW 
		CFLAG:394 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:394 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:394 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:394 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:394 = 3
		;それ以外
		ELSEIF CFLAG:394 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:394 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;183,ビデオカメラ,
;-------------------------------------------------
IF SELECTCOM == 183 && TEQUIP:28 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:395 == 0
		PRINTFORMW 
		CFLAG:395 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:395 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:395 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:395 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:395 = 3
		;それ以外
		ELSEIF CFLAG:395 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:395 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 183 && TEQUIP:28 == 1
	IF CFLAG:395 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;184,野外プレイ,
;-------------------------------------------------
IF SELECTCOM == 184 && TEQUIP:29 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:396 == 0
		PRINTFORMW 
		CFLAG:396 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:396 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:396 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:396 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:396 = 3
		;それ以外
		ELSEIF CFLAG:396 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:396 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 184 && TEQUIP:29 == 1
	IF CFLAG:396 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;185,助手を犯す,
;-------------------------------------------------
IF SELECTCOM == 185
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:397 == 0
		PRINTFORMW 
		CFLAG:397 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:397 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:397 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:397 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:397 = 3
		;それ以外
		ELSEIF CFLAG:397 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:397 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;186,羞恥プレイ,
;-------------------------------------------------
IF SELECTCOM == 186 && TEQUIP:30 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:398 == 0
		PRINTFORMW 
		CFLAG:398 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:398 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:398 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:398 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:398 = 3
		;それ以外
		ELSEIF CFLAG:398 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:398 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 186 && TEQUIP:30 == 1
	IF CFLAG:398 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;187,お風呂場プレイ,
;-------------------------------------------------
IF SELECTCOM == 187 && TEQUIP:31 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:399 == 0
		PRINTFORMW 
		CFLAG:399 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:399 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:399 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:399 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:399 = 3
		;それ以外
		ELSEIF CFLAG:399 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:399 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 187 && TEQUIP:31 == 1
	IF CFLAG:399 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;188,シャワー,
;-------------------------------------------------
IF SELECTCOM == 188 && TEQUIP:32 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:400 == 0
		PRINTFORMW 
		CFLAG:400 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:400 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:400 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:400 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:400 = 3
		;それ以外
		ELSEIF CFLAG:400 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:400 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 188 && TEQUIP:32 == 1
	IF CFLAG:400 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;189,新妻プレイ,　恋慕、親愛のみ
;-------------------------------------------------
IF SELECTCOM == 189 && TEQUIP:33 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:401 == 0
		PRINTFORMW 
		CFLAG:401 = 1
	;二回目以降
	ELSEIF CFLAG:401 < 2 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:401 = 2
	ENDIF
ELSEIF SELECTCOM == 189 && TEQUIP:33 == 1
	IF CFLAG:401 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;190,写真撮影,
;-------------------------------------------------
IF SELECTCOM == 190
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:402 == 0
		PRINTFORMW 
		CFLAG:402 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:402 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:402 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:402 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:402 = 3
		;それ以外
		ELSEIF CFLAG:402 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:402 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;191,強精神薬,
;-------------------------------------------------
IF SELECTCOM == 191 
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:403 == 0
		PRINTFORMW 
		CFLAG:403 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:403 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:403 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:403 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:403 = 3
		;それ以外
		ELSEIF CFLAG:403 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:403 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;192,コンドーム精飲（奴隷）
;-------------------------------------------------
IF SELECTCOM == 192
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:404 == 0
		PRINTFORMW 
		CFLAG:404 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:404 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:404 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:404 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:404 = 3
		;それ以外
		ELSEIF CFLAG:404 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:404 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;193,コンドーム精飲（助手）
;-------------------------------------------------
IF SELECTCOM == 193
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:405 == 0
		PRINTFORMW 
		CFLAG:405 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:405 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:405 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:405 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:405 = 3
		;それ以外
		ELSEIF CFLAG:405 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:405 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;194,ザーメンキス
;-------------------------------------------------
IF SELECTCOM == 194
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:406 == 0
		PRINTFORMW 
		CFLAG:406 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:406 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:406 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:406 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:406 = 3
		;それ以外
		ELSEIF CFLAG:406 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:406 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;195,デートに行く CFLAG:423 
;-------------------------------------------------
IF SELECTCOM == 195
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:423 == 0
		PRINTFORMW 
		CFLAG:423 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:423 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:423 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:423 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:423 = 3
		;それ以外
		ELSEIF CFLAG:423 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:423 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;196,剥いてやる CFLAG:482
;-------------------------------------------------
IF SELECTCOM == 196
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:482 == 0
		PRINTFORMW 
		CFLAG:482 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:482 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:482 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:482 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:482 = 3
		;それ以外
		ELSEIF CFLAG:482 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:482 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;197,バター犬　CFLAG:483
;-------------------------------------------------
IF SELECTCOM == 197
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:483 == 0
		PRINTFORMW 
		CFLAG:483 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:483 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:483 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:483 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:483 = 3
		;それ以外
		ELSEIF CFLAG:483 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:483 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;200番台,逆調教系コマンド
;=================================================
;-------------------------------------------------
;200,自慰みせつけ CFLAG:424 
;-------------------------------------------------
IF SELECTCOM == 200
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:424 == 0
		PRINTFORMW 
		CFLAG:424 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:424 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:424 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:424 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:424 = 3
		;それ以外
		ELSEIF CFLAG:424 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:424 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;201,胸を揉ませる CFLAG:425 
;-------------------------------------------------
IF SELECTCOM == 201
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:425 == 0
		PRINTFORMW 
		CFLAG:425 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:425 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:425 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:425 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:425 = 3
		;それ以外
		ELSEIF CFLAG:425 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:425 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;202,愛撫させる CFLAG:426 
;-------------------------------------------------
IF SELECTCOM == 202
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:426 == 0
		PRINTFORMW 
		CFLAG:426 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:426 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:426 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:426 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:426 = 3
		;それ以外
		ELSEIF CFLAG:426 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:426 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;203,罵倒させる CFLAG:427 
;-------------------------------------------------
IF SELECTCOM == 203
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:427 == 0
		PRINTFORMW 
		CFLAG:427 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:427 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:427 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:427 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:427 = 3
		;それ以外
		ELSEIF CFLAG:427 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:427 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;204,プレゼントさせる CFLAG:428 
;-------------------------------------------------
IF SELECTCOM == 204
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:428 == 0
		PRINTFORMW 
		CFLAG:428 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:428 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:428 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:428 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:428 = 3
		;それ以外
		ELSEIF CFLAG:428 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:428 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;205,正常位させる CFLAG:429 
;-------------------------------------------------
IF SELECTCOM == 205
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:429 == 0
		PRINTFORMW 
		CFLAG:429 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:429 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:429 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:429 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:429 = 3
		;それ以外
		ELSEIF CFLAG:429 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:429 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;206,後背位させる CFLAG:430 
;-------------------------------------------------
IF SELECTCOM == 206
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:430 == 0
		PRINTFORMW 
		CFLAG:430 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:430 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:430 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:430 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:430 = 3
		;それ以外
		ELSEIF CFLAG:430 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:430 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;207,スパンキングさせる CFLAG:431 
;-------------------------------------------------
IF SELECTCOM == 207
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:431 == 0
		PRINTFORMW 
		CFLAG:431 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:431 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:431 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:431 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:431 = 3
		;それ以外
		ELSEIF CFLAG:431 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:431 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;208,鞭打たせる CFLAG:432 
;-------------------------------------------------
IF SELECTCOM == 208
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:432 == 0
		PRINTFORMW 
		CFLAG:432 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:432 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:432 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:432 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:432 = 3
		;それ以外
		ELSEIF CFLAG:432 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:432 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;209,イマラチオする CFLAG:433 
;-------------------------------------------------
IF SELECTCOM == 209
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:433 == 0
		PRINTFORMW 
		CFLAG:433 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:433 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:433 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:433 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:433 = 3
		;それ以外
		ELSEIF CFLAG:433 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:433 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;210, 足を舐めるCFLAG:450 
;-------------------------------------------------
IF SELECTCOM == 210
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:450 == 0
		PRINTFORMW 
		CFLAG:450 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:450 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:450 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:450 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:450 = 3
		;それ以外
		ELSEIF CFLAG:450 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:450 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;211,騎乗位する CFLAG:451 
;-------------------------------------------------
IF SELECTCOM == 211
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:451 == 0
		PRINTFORMW 
		CFLAG:451 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:451 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:451 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:451 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:451 = 3
		;それ以外
		ELSEIF CFLAG:451 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:451 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;212,騎乗位アナルする CFLAG:452 
;-------------------------------------------------
IF SELECTCOM == 212
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:452 == 0
		PRINTFORMW 
		CFLAG:452 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:452 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:452 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:452 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:452 = 3
		;それ以外
		ELSEIF CFLAG:452 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:452 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;213,正常位アナルさせる CFLAG:453 
;-------------------------------------------------
IF SELECTCOM == 213
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:453 == 0
		PRINTFORMW 
		CFLAG:453 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:453 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:453 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:453 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:453 = 3
		;それ以外
		ELSEIF CFLAG:453 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:453 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;214,後背位アナルさせる CFLAG:454 
;-------------------------------------------------
IF SELECTCOM == 214
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:454 == 0
		PRINTFORMW 
		CFLAG:454 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:454 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:454 = 4
		;従順Lv3以上
		ELSEIF ABL:0 >= 3 && (CFLAG:454 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:454 = 3
		;それ以外
		ELSEIF CFLAG:454 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:454 = 2
		ENDIF
	ENDIF
ENDIF

;=================================================
;220番台,助手命令コマンド
;=================================================
;-------------------------------------------------
;220,クリトリス愛撫命令 CFLAG:455
;-------------------------------------------------
IF SELECTCOM == 220 && TEQUIP:49 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:455 == 0
		PRINTFORMW 
		CFLAG:455 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:455 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:455 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:455 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:455 = 3
		;それ以外
		ELSEIF CFLAG:455 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:455 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 220 && TEQUIP:49 == 1
	IF CFLAG:455 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;221,ペニス愛撫命令 CFLAG:456
;-------------------------------------------------
IF SELECTCOM == 221 && TEQUIP:49 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:456 == 0
		PRINTFORMW 
		CFLAG:456 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:456 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:456 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:456 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:456 = 3
		;それ以外
		ELSEIF CFLAG:456 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:456 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 221 && TEQUIP:49 == 1
	IF CFLAG:456 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;222,胸愛撫命令 CFLAG:457
;-------------------------------------------------
IF SELECTCOM == 222 && TEQUIP:44 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:457 == 0
		PRINTFORMW 
		CFLAG:457 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:457 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:457 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:457 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:457 = 3
		;それ以外
		ELSEIF CFLAG:457 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:457 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 222 && TEQUIP:44 == 1
	IF CFLAG:457 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;223,指挿入れ命令 CFLAG:458
;-------------------------------------------------
IF SELECTCOM == 223 && TEQUIP:45 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:458 == 0
		PRINTFORMW 
		CFLAG:458 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:458 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:458 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:458 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:458 = 3
		;それ以外
		ELSEIF CFLAG:458 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:458 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 223 && TEQUIP:45 == 1
	IF CFLAG:458 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;224,アナル愛撫命令 CFLAG:459
;-------------------------------------------------
IF SELECTCOM == 224 && TEQUIP:46 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:459 == 0
		PRINTFORMW 
		CFLAG:459 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:459 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:459 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:459 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:459 = 3
		;それ以外
		ELSEIF CFLAG:459 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:459 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 224 && TEQUIP:46 == 1
	IF CFLAG:459 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;225,挿入命令 CFLAG:460
;-------------------------------------------------
IF SELECTCOM == 225 && TEQUIP:47 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:460 == 0
		PRINTFORMW 
		CFLAG:460 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:460 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:460 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:460 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:460 = 3
		;それ以外
		ELSEIF CFLAG:460 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:460 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 225 && TEQUIP:47 == 1
	IF CFLAG:460 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF
;-------------------------------------------------
;226,アナル挿入命令 CFLAG:461
;-------------------------------------------------
IF SELECTCOM == 226 && TEQUIP:48 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:461 == 0
		PRINTFORMW 
		CFLAG:461 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:461 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:461 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:461 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:461 = 3
		;それ以外
		ELSEIF CFLAG:461 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:461 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 226 && TEQUIP:48 == 1
	IF CFLAG:461 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;=================================================
;300番台,拡張触手コマンド
;=================================================
;-------------------------------------------------
;300,触手乳腺責め CFLAG:462
;-------------------------------------------------
IF SELECTCOM == 300
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:462 == 0
		PRINTFORMW 
		CFLAG:462 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:462 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:462 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:462 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:462 = 3
		;それ以外
		ELSEIF CFLAG:462 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:462 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;301,触手Ｇスポ責め CFLAG:463
;-------------------------------------------------
IF SELECTCOM == 301
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:463 == 0
		PRINTFORMW 
		CFLAG:463 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:463 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:463 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:463 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:463 = 3
		;それ以外
		ELSEIF CFLAG:463 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:463 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;302,触手子宮責め CFLAG:464
;-------------------------------------------------
IF SELECTCOM == 302
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:464 == 0
		PRINTFORMW 
		CFLAG:464 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:464 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:464 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:464 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:464 = 3
		;それ以外
		ELSEIF CFLAG:464 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:464 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;303,触手前立腺責め CFLAG:465
;-------------------------------------------------
IF SELECTCOM == 303
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:465 == 0
		PRINTFORMW 
		CFLAG:465 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:465 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:465 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:465 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:465 = 3
		;それ以外
		ELSEIF CFLAG:465 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:465 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;304,触手ヴァギナ拡張 CFLAG:466
;-------------------------------------------------
IF SELECTCOM == 304
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:466 == 0
		PRINTFORMW 
		CFLAG:466 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:466 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:466 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:466 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:466 = 3
		;それ以外
		ELSEIF CFLAG:466 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:466 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;305,触手アナル拡張 CFLAG:467
;-------------------------------------------------
IF SELECTCOM == 305
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:467 == 0
		PRINTFORMW 
		CFLAG:467 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:467 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:467 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:467 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:467 = 3
		;それ以外
		ELSEIF CFLAG:467 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:467 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;306,触手両穴拡張 CFLAG:468
;-------------------------------------------------
IF SELECTCOM == 306
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:468 == 0
		PRINTFORMW 
		CFLAG:468 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:468 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:468 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:468 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:468 = 3
		;それ以外
		ELSEIF CFLAG:468 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:468 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;307,触手産卵 CFLAG:469
;-------------------------------------------------
IF SELECTCOM == 307 && TEQUIP:41 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:469 == 0
		PRINTFORMW 
		CFLAG:469 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:469 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:469 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:469 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:469 = 3
		;それ以外
		ELSEIF CFLAG:469 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:469 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 307 && TEQUIP:41 == 1
	IF CFLAG:469 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;308,触手アナル産卵 CFLAG:470
;-------------------------------------------------
IF SELECTCOM == 308 && TEQUIP:42 == 0
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:470 == 0
		PRINTFORMW 
		CFLAG:470 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:470 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:470 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:470 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:470 = 3
		;それ以外
		ELSEIF CFLAG:470 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:470 = 2
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 308 && TEQUIP:42 == 1
	IF CFLAG:470 == 1 || FLAG:7 == 2
		CALL TRAIN_MESSAGE
		PRINTFORMW 
	ENDIF
ENDIF

;=================================================
;500番台,派生コマンド
;=================================================
;-------------------------------------------------
;500,３Ｐ
;-------------------------------------------------
IF SELECTCOM == 500
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:407 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:407 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:407 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:407 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:407 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:407 = 3
		;それ以外
		ELSEIF CFLAG:407 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:407 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;501,シックスナイン
;-------------------------------------------------
IF SELECTCOM == 501
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:408 == 0
		PRINTFORMW 
		CFLAG:408 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:408 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:408 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:408 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:408 = 3
		;それ以外
		ELSEIF CFLAG:408 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:408 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;502,岩清水
;-------------------------------------------------
IF SELECTCOM == 502
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:409 == 0
		PRINTFORMW 
		CFLAG:409 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:409 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:409 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:409 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:409 = 3
		;それ以外
		ELSEIF CFLAG:409 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:409 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;503,Gスポット刺激
;-------------------------------------------------
IF SELECTCOM == 503
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:410 == 0
		PRINTFORMW 
		CFLAG:410 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:410 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:410 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:410 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:410 = 3
		;それ以外
		ELSEIF CFLAG:410 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:410 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;504,乱れ牡丹
;-------------------------------------------------
IF SELECTCOM == 504
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:411 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:411 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:411 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:411 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:411 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:411 = 3
		;それ以外
		ELSEIF CFLAG:411 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:411 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;505,騎乗乳房自慰
;-------------------------------------------------
IF SELECTCOM == 505
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:412 == 0
		;処女
		IF TALENT:0
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
		CFLAG:412 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:412 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:412 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:412 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:412 = 3
		;それ以外
		ELSEIF CFLAG:412 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:412 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;506,挿入Ｇスポ責め
;-------------------------------------------------
IF SELECTCOM == 506
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:413 == 0
		PRINTFORMW 
		CFLAG:413 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:413 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:413 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:413 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:413 = 3
		;それ以外
		ELSEIF CFLAG:413 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:413 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;507,挿入子宮口責め
;-------------------------------------------------
IF SELECTCOM == 507
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:414 == 0
		PRINTFORMW 
		CFLAG:414 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:414 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:414 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:414 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:414 = 3
		;それ以外
		ELSEIF CFLAG:414 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:414 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;508,６９パイズリ
;-------------------------------------------------
IF SELECTCOM == 508
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:415 == 0
		PRINTFORMW 
		CFLAG:415 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:415 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:415 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:415 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:415 = 3
		;それ以外
		ELSEIF CFLAG:415 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:415 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;509,搾乳
;-------------------------------------------------
IF SELECTCOM == 509
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:416 == 0
		PRINTFORMW 
		CFLAG:416 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:416 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:416 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:416 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:416 = 3
		;それ以外
		ELSEIF CFLAG:416 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:416 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;510,ダブル素股
;-------------------------------------------------
IF SELECTCOM == 510
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:417 == 0
		PRINTFORMW 
		CFLAG:417 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:417 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:417 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:417 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:417 = 3
		;それ以外
		ELSEIF CFLAG:417 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:417 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;511,二本フェラ
;-------------------------------------------------
IF SELECTCOM == 511
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:418 == 0
		PRINTFORMW 
		CFLAG:418 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:418 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:418 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:418 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:418 = 3
		;それ以外
		ELSEIF CFLAG:418 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:418 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;512,二輪刺し CFLAG:443
;-------------------------------------------------
IF SELECTCOM == 512
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:443 == 0
		PRINTFORMW 
		CFLAG:443 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:443 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:443 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:443 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:443 = 3
		;それ以外
		ELSEIF CFLAG:443 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:443 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;513,二輪刺しアナル CFLAG:444
;-------------------------------------------------
IF SELECTCOM == 513
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:444 == 0
		PRINTFORMW 
		CFLAG:444 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:444 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:444 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:444 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:444 = 3
		;それ以外
		ELSEIF CFLAG:444 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:444 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;514,駅弁 CFLAG:445
;-------------------------------------------------
IF SELECTCOM == 514
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:445 == 0
		PRINTFORMW 
		CFLAG:445 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:445 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:445 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:445 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:445 = 3
		;それ以外
		ELSEIF CFLAG:445 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:445 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;515,スパンキングセックス　CFLAG:478
;-------------------------------------------------
IF SELECTCOM == 515
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:478 == 0
		PRINTFORMW 
		CFLAG:478 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:478 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:478 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:478 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:478 = 3
		;それ以外
		ELSEIF CFLAG:478 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:478 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;516,スパンキングアナル　CFLAG:479
;-------------------------------------------------
IF SELECTCOM == 516
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:479 == 0
		PRINTFORMW 
		CFLAG:479 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:479 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:479 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:479 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:479 = 3
		;それ以外
		ELSEIF CFLAG:479 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:479 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;517,首絞めセックス　CFLAG:480
;-------------------------------------------------
IF SELECTCOM == 517
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:480 == 0
		PRINTFORMW 
		CFLAG:480 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:480 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:480 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:480 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:480 = 3
		;それ以外
		ELSEIF CFLAG:480 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:480 = 2
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;518,首絞めアナル　CFLAG:481
;-------------------------------------------------
IF SELECTCOM == 518
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:481 == 0
		PRINTFORMW 
		CFLAG:481 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:481 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:481 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:481 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:481 = 3
		;それ以外
		ELSEIF CFLAG:481 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:481 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;590,前立腺刺激　CFLAG:484
;-------------------------------------------------
IF SELECTCOM == 590
	CALL TRAIN_MESSAGE
	;初めて
	IF CFLAG:484 == 0
		PRINTFORMW 
		CFLAG:484 = 1
	;二回目以降
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:484 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:484 = 4
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:484 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:484 = 3
		;それ以外
		ELSEIF CFLAG:484 < 2 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:484 = 2
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG関係（X1をキャラ番号に置換）
;パラメータ変化などをトリガーにした口上　CFLAG 221～260を使用
;-------------------------------------------------
;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_A（X1をキャラ番号に置換）
;調教者の射精をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_PY1_KX1
;-------------------------------------------------
;調教者の射精時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;射精箇所変更時のテキスト　有効時はここで関数終了
;-------------------------------------------------
IF CFLAG:106
	CALL PALAM_MESSAGE_A
	;髪
	IF CFLAG:106 == 1
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:221 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:221 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:221 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:221 = 2
		;それ以外
		ELSEIF CFLAG:221 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:221 = 1
		ENDIF
	;顔
	ELSEIF CFLAG:106 == 2
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:222 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:222 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:222 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:222 = 2
		;それ以外
		ELSEIF CFLAG:222 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:222 = 1
		ENDIF
	;胸
	ELSEIF CFLAG:106 == 3
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:223 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:223 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:223 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:223 = 2
		;それ以外
		ELSEIF CFLAG:223 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:223 = 1
		ENDIF
	;腹
	ELSEIF CFLAG:106 == 4
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:224 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:224 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:224 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:224 = 2
		;それ以外
		ELSEIF CFLAG:224 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:224 = 1
		ENDIF
	;腋
	ELSEIF CFLAG:106 == 5
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:225 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:225 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:225 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:225 = 2
		;それ以外
		ELSEIF CFLAG:225 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:225 = 1
		ENDIF
	;太腿
	ELSEIF CFLAG:106 == 6
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:226 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:226 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:226 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:226 = 2
		;それ以外
		ELSEIF CFLAG:226 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:226 = 1
		ENDIF
	;割れ目
	ELSEIF CFLAG:106 == 7
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:227 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:227 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:227 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:227 = 2
		;それ以外
		ELSEIF CFLAG:227 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:227 = 1
		ENDIF
	;尻
	ELSEIF CFLAG:106 == 8
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:228 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:228 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:228 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:228 = 2
		;それ以外
		ELSEIF CFLAG:228 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:228 = 1
		ENDIF
	;足 CFLAG:254
	ELSEIF CFLAG:106 == 10
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:254 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:254 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:254 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:254 = 2
		;それ以外
		ELSEIF CFLAG:254 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:254 = 1
		ENDIF
	;ゴム
	ELSEIF CFLAG:106 == 9
		;セックスで射精かつ、アナルセックス以外を実行時
		IF TFLAG:2 > 0 && SELECTCOM != 62 && SELECTCOM != 63 && SELECTCOM != 67 && SELECTCOM != 70 && SELECTCOM != 71
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:229 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:229 = 3
			;屈服刻印Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:229 < 2 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:229 = 2
			;それ以外
			ELSEIF CFLAG:229 < 1 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:229 = 1
			ENDIF
		ELSE
			;陥落済み
			IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:230 < 3 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:230 = 3
			;屈服刻印Lv3
			ELSEIF MARK:2 == 3 && (CFLAG:230 < 2 || FLAG:7 == 2)
				PRINTFORMW 
				CFLAG:230 = 2
			;それ以外
			ELSEIF CFLAG:230 < 1 || FLAG:7 == 2
				PRINTFORMW 
				CFLAG:230 = 1
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;手、口以外で射精
;対応コマンドはキス、素股、足コキ、アナル強制奉仕、ダブル素股
;-------------------------------------------------
IF TFLAG:9 == 1
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:231 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:231 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:231 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:231 = 2
	;それ以外
	ELSEIF CFLAG:231 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:231 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;助手を犯すで射精
;-------------------------------------------------
IF TFLAG:7 > 0
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:232 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:232 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:232 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:232 = 2
	;それ以外
	ELSEIF CFLAG:232 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:232 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;口で射精
;-------------------------------------------------
IF TFLAG:0 > 0
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:233 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:233 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:233 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:233 = 2
	;それ以外
	ELSEIF CFLAG:233 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:233 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;手で射精
;-------------------------------------------------
IF TFLAG:1 > 0
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:234 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:234 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:234 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:234 = 2
	;それ以外
	ELSEIF CFLAG:234 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:234 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;胸で射精
;-------------------------------------------------
IF TFLAG:34 > 0
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:235 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:235 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:235 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:235 = 2
	;それ以外
	ELSEIF CFLAG:235 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:235 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;足で射精
;-------------------------------------------------
IF TFLAG:16 > 0
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:236 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:236 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:236 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:236 = 2
	;それ以外
	ELSEIF CFLAG:236 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:236 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;セックスで射精
;-------------------------------------------------
IF TFLAG:2 > 0
	CALL PALAM_MESSAGE_A
	;アナルセックス系
	IF SELECTCOM == 62 || SELECTCOM == 63 || SELECTCOM == 67 || SELECTCOM == 70 || SELECTCOM == 71 || SELECTCOM == 73 || SELECTCOM == 75 || SELECTCOM == 77
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:237 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:237 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:237 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:237 = 2
		;それ以外
		ELSEIF CFLAG:237 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:237 = 1
		ENDIF
	;セックス系
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:238 < 3 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:238 = 3
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3 && (CFLAG:238 < 2 || FLAG:7 == 2)
			PRINTFORMW 
			CFLAG:238 = 2
		;それ以外
		ELSEIF CFLAG:238 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:238 = 1
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;逆セックスで射精させた
;-------------------------------------------------
IF (SELECTCOM == 64 || SELECTCOM == 205 || SELECTCOM == 206 || SELECTCOM == 211 || SELECTCOM == 212 || SELECTCOM == 213 || SELECTCOM == 214) && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:239 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:239 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:239 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:239 = 2
	;それ以外
	ELSEIF CFLAG:239 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:239 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教者の空射精 CFLAG:255
;-------------------------------------------------
;調教者空射精
IF TFLAG:164
	CALL PALAM_MESSAGE_A
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:255 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:255 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:255 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:255 = 2
	;それ以外
	ELSEIF CFLAG:255 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:255 = 1
	ENDIF
	TFLAG:164 = 0
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_E（X1をキャラ番号に置換）
;触手の射精をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_PY1_KX1
;-------------------------------------------------
;触手の射精時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;触手が射精
;-------------------------------------------------
IF TFLAG:15 >0
	CALL PALAM_MESSAGE_E
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:240 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:240 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:240 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:240 = 2
	;それ以外
	ELSEIF CFLAG:240 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:240 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_B関係（X1をキャラ番号に置換）
;奴隷の絶頂をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_B_PY1_KX1
;-------------------------------------------------
;奴隷の絶頂時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;初めてC絶頂
;-------------------------------------------------
IF NOWEX:0 > 0 && CFLAG:241 == 0
	PRINTFORMW 
	CFLAG:241 = 1
ENDIF

;-------------------------------------------------
;初めてV絶頂
;-------------------------------------------------
IF NOWEX:1 > 0 && CFLAG:242 == 0
	PRINTFORMW 
	CFLAG:242 = 1
ENDIF

;-------------------------------------------------
;初めてA絶頂
;-------------------------------------------------
IF NOWEX:2 > 0 && CFLAG:243 == 0
	PRINTFORMW 
	CFLAG:243 = 1
ENDIF

;-------------------------------------------------
;初めてB絶頂
;-------------------------------------------------
IF NOWEX:3 > 0 && CFLAG:244 == 0
	PRINTFORMW 
	CFLAG:244 = 1
ENDIF

;-------------------------------------------------
;絶頂
;-------------------------------------------------
IF TFLAG:29 > 0
	CALL PALAM_MESSAGE_B
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:245 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:245 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:245 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:245 = 2
	;それ以外
	ELSEIF CFLAG:245 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:245 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;射精
;-------------------------------------------------
;射精している
IF TFLAG:10 > 0
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:246 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:246 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:246 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:246 = 2
	;それ以外
	ELSEIF CFLAG:246 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:246 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;空射精　※リボンパッチによる追加
;-------------------------------------------------
;空射精している
IF NOWEX:40
	CALL PALAM_MESSAGE_A2
	;子供サイズ
	IF TALENT:114
		PRINTFORMW 
	ELSE
		PRINTFORMW
	ENDIF
ENDIF
;-------------------------------------------------
;射精止め解除で射精　※リボンパッチによる追加
;-------------------------------------------------
;射精止め解除で射精
IF SELECTCOM == 109 && TEQUIP:37 == 0 && TFLAG:10 > 0
	CALL PALAM_MESSAGE_A
ENDIF

;-------------------------------------------------
;噴乳
;-------------------------------------------------
;噴乳している
IF TFLAG:11 > 0
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:247 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:247 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:247 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:247 = 2
	;それ以外
	ELSEIF CFLAG:247 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:247 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;放尿
;-------------------------------------------------
;放尿している
IF TFLAG:29 > 0 && (TEQUIP:27 || TALENT:57)
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:248 < 3 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:248 = 3
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3 && (CFLAG:248 < 2 || FLAG:7 == 2)
		PRINTFORMW 
		CFLAG:248 = 2
	;それ以外
	ELSEIF CFLAG:248 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:248 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_C関係（X1をキャラ番号に置換）
;調教後の変化をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_C_PY1_KX1
;-------------------------------------------------
;調教後のテキスト
;-------------------------------------------------
;-------------------------------------------------
;処女喪失(処女のみ)
;-------------------------------------------------
IF TFLAG:3 > 0
	CALL PALAM_MESSAGE_C
	;恋慕か親愛かつ主人のセックスによる処女喪失
	IF TFLAG:20
		PRINTFORMW 
	;それ以外の時に処女喪失
	ELSE
		;陥落済み
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
			PRINTFORMW 
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;童貞喪失
;-------------------------------------------------
IF TFLAG:160 == 1
	CALL PALAM_MESSAGE_C
	;恋慕か親愛かつ主人のセックスによる童貞喪失
	IF ASSIPLAY == 0 && (TALENT:3 || TALENT:6)
		PRINTFORMW 
		PRINTFORMW 
	;それ以外の時に童貞喪失
	ELSE
		;陥落済み
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
			PRINTFORMW 
			PRINTFORMW 
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3
			PRINTFORMW 
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
			PRINTFORMW 
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;バックバージン喪失
;-------------------------------------------------
IF TFLAG:161 == 1
	CALL PALAM_MESSAGE_C
	;恋慕か親愛かつ主人のセックスによるバックバージン喪失
	IF ASSIPLAY == 0 && (TALENT:3 || TALENT:6)
		PRINTFORMW 
		PRINTFORMW 
	;それ以外の時にバックバージン喪失
	ELSE
		;陥落済み
		IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
			PRINTFORMW 
			PRINTFORMW 
		;屈服刻印Lv3
		ELSEIF MARK:2 == 3
			PRINTFORMW 
			PRINTFORMW 
		;それ以外
		ELSE
			PRINTFORMW 
			PRINTFORMW 
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;@KOJO_MESSAGE_PALAMCNG_D関係（X1をキャラ番号に置換）
;パラメータ変化をトリガーにした口上
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_D_PY1_KX1
;-------------------------------------------------
;パラメータ変化時のテキスト
;-------------------------------------------------
;-------------------------------------------------
;初めて潤滑がLV3超えた
;-------------------------------------------------
P = PALAM:4 + UP:4
IF P > PALAMLV:3 && CFLAG:249 == 0
	CALL PALAM_MESSAGE_D
	;ローションを使った場合
	IF SELECTCOM == 180
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:249 = 1
ENDIF

;-------------------------------------------------
;初めて欲情がLV2超えた
;-------------------------------------------------
P = PALAM:6 + UP:6
IF P > PALAMLV:2 && CFLAG:250 == 0
	CALL PALAM_MESSAGE_D
	;媚薬を使った場合
	IF SELECTCOM == 181
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:250 = 1
ENDIF

;-------------------------------------------------
;初めて屈服がLV3超えた
;-------------------------------------------------
P = PALAM:7 + UP:7
IF P > PALAMLV:3 && CFLAG:251 == 0
	CALL PALAM_MESSAGE_D
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:251 = 1
ENDIF

;-------------------------------------------------
;初めて恭順がLV3超えた
;-------------------------------------------------
P = PALAM:5 + UP:5
IF P > PALAMLV:3 && CFLAG:252 == 0
	CALL PALAM_MESSAGE_D
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:252 = 1
ENDIF

;-------------------------------------------------
;初めて恐怖がLV3超えた
;-------------------------------------------------
P = PALAM:11 + UP:11
IF P > PALAMLV:3 && CFLAG:253 == 0
	CALL PALAM_MESSAGE_D
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:253 = 1
ENDIF


;-------------------------------------------------
;@KOJO_MESSAGE_MARKCNG関係（X1をキャラ番号に置換）
;刻印変動をトリガーにした口上
;刻印変動後に口上を発動します
;-------------------------------------------------
@KOJO_MESSAGE_MARKCNG_PY1_KX1
;ボールギャグ着用時には地の文を表示して口上をスキップする
IF TEQUIP:20 &&  SELECTCOM != 107
	CALL MARK_MESSAGE
	RETURN 0
ENDIF
SIF TFLAG:22 != 3 && TFLAG:23 != 3 && TFLAG:24 != 13 && TFLAG:24 != 23 && TFLAG:24 != 33 && TFLAG:21 != 3
	CALL MARK_MESSAGE
;-------------------------------------------------
;苦痛刻印Lv3
;-------------------------------------------------
IF TFLAG:22 == 3
	CALL MARK_MESSAGE_A
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;快楽刻印Lv3
;-------------------------------------------------
IF TFLAG:23 == 3
	CALL MARK_MESSAGE_P
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;屈服刻印Lv3
;-------------------------------------------------
IF TFLAG:24 == 13 || TFLAG:24 == 23 || TFLAG:24 == 33
	CALL MARK_MESSAGE_S
	;快楽による屈服
	IF TFLAG:24 == 13
		PRINTFORMW 
	;恐怖と苦痛による屈服
	ELSEIF TFLAG:24 == 23
		PRINTFORMW 
	;屈服と恭順による屈服
	ELSEIF TFLAG:24 == 33
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;反発刻印Lv3
;-------------------------------------------------
IF TFLAG:21 == 3
	CALL MARK_MESSAGE_R
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;イベント口上集（X1、X2をキャラ番号に置換する） CFLAG 261～299を使用
;-------------------------------------------------
@KOJO_MESSAGE_EVENT_PY1_KX1
;-------------------------------------------------
;朝フェラ
;-------------------------------------------------
IF TFLAG:13 == 1
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:261 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;朝パイズリ
;-------------------------------------------------
IF TFLAG:13 == 2
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:262 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:262 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;朝騎乗位
;-------------------------------------------------
IF TFLAG:13 == 3
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:263 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:263 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教後レズプレイ
;-------------------------------------------------
IF TFLAG:13 == 4
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:264 == 0
		PRINTFORMW 
		CFLAG:264 = 1
	;それ以外
	ELSEIF CFLAG:264 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:264 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教後セックス
;-------------------------------------------------
IF TFLAG:13 == 5
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:265 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:265 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;強精神薬の反動
;-------------------------------------------------
IF TFLAG:13 == 6
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:266 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:266 = 2
	ELSEIF CFLAG:266 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:266 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教後搾乳
;-------------------------------------------------
IF TFLAG:13 == 7
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:267 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:267 = 2
	ELSEIF CFLAG:267 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:267 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教後自慰
;-------------------------------------------------
IF TFLAG:13 == 8
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:268 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:268 = 2
	ELSEIF CFLAG:268 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:268 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教後夜這い
;-------------------------------------------------
IF TFLAG:13 == 9
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:269 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:269 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;黄色のキャンディ使用時
;-------------------------------------------------
IF TFLAG:13 == 10
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:270 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:270 = 2
	ELSEIF CFLAG:270 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:270 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;妊娠発覚時
;-------------------------------------------------
IF TFLAG:13 == 11
	SIF CFLAG:271 >= 1 && FLAG:7 != 2
		RETURN 0
	CALL EVENT_MESSAGE
	;妊娠対象をTARGETに格納し、父親は奴隷はV、助手はWとする
	;崩壊してしまった場合
	IF TALENT:9 == 1
		PRINTFORMW 
	;父親が主人で母親が恋慕か親愛持ち
	ELSEIF (TALENT:3 || TALENT:6) && CFLAG:102 == 1
		PRINTFORMW 
	;父親が奴隷か助手で母親が淫乱か娼婦持ち
	ELSEIF (TALENT:4 || TALENT:7) && (CFLAG:102 == 2 || CFLAG:102 == 3)
		;父親(助手）との相性が100より大きい場合
		IF CFLAG:102 == 2 && RELATION:W > 100
			PRINTFORMW 
	 	;父親(奴隷）との相性が100より大きい場合
		ELSEIF CFLAG:102 == 3 && RELATION:V > 100
			PRINTFORMW 
		ELSE
			PRINTFORMW 
		ENDIF
	;服従か隷属持ち
	ELSEIF (TALENT:5 || TALENT:8)
		PRINTFORMW 
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:271 = 1
ENDIF

;-------------------------------------------------
;出産時
;-------------------------------------------------
IF TFLAG:13 == 12
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:272 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:272 = 2
	ELSEIF CFLAG:272 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:272 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;育児室
;-------------------------------------------------
IF TFLAG:13 == 13
	CALL EVENT_MESSAGE
	;出産前
	IF TALENT:153
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 2 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:273 = 2
		ELSEIF CFLAG:273 < 1 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:273 = 1
		ENDIF
	;出産後
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:273 < 4 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:273 = 4
		ELSEIF CFLAG:273 < 3 || FLAG:7 == 2
			PRINTFORMW 
			CFLAG:273 = 3
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;親離れ時
;-------------------------------------------------
IF TFLAG:13 == 14
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:274 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:274 = 2
	ELSEIF CFLAG:274 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:274 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;定食屋台時
;-------------------------------------------------
IF TFLAG:13 == 15
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:275 == 0
		PRINTFORMW 
		CFLAG:275 = 1
	;それ以外
	ELSEIF CFLAG:275 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:275 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;娼館でバイト時
;-------------------------------------------------
IF TFLAG:13 == 16
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:276 == 0
		PRINTFORMW 
		CFLAG:276 = 1
	;それ以外
	ELSEIF CFLAG:276 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:276 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;レンタル時
;-------------------------------------------------
IF TFLAG:13 == 17
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:277 == 0
		PRINTFORMW 
		CFLAG:277 = 1
	;それ以外
	ELSEIF CFLAG:277 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:277 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;売却時
;-------------------------------------------------
IF TFLAG:13 == 18
	CALL EVENT_MESSAGE
	;陥落済み
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;恋慕、親愛+反発刻印Lv3未満
		IF (TALENT:3 || TALENT:6) && MARK:3 < 3
			PRINTFORMW 
		;淫乱、娼婦+反発刻印Lv3未満
		ELSEIF (TALENT:4 || TALENT:7) && MARK:3 < 3
			PRINTFORMW 
		;服従、隷属+反発刻印Lv3未満
		ELSEIF (TALENT:5 || TALENT:8) && MARK:3 < 3
			PRINTFORMW 
		ENDIF
	;反発刻印Lv3
	ELSEIF MARK:3 == 3
		PRINTFORMW 
	;それ以外
	ELSE
		PRINTFORMW 
	ENDIF
ENDIF

;-------------------------------------------------
;ビデオ売却時
;-------------------------------------------------
IF TFLAG:13 == 19
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:278 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:278 = 2
	ELSEIF CFLAG:278 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:278 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;コンサート時
;-------------------------------------------------
IF TFLAG:13 == 20
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:279 == 0
		PRINTFORMW 
		CFLAG:279 = 1
	;それ以外
	ELSEIF CFLAG:279 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:279 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;乱交ライブ時
;-------------------------------------------------
IF TFLAG:13 == 21
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:280 == 0
		PRINTFORMW 
		CFLAG:280 = 1
	;それ以外
	ELSEIF CFLAG:280 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:280 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;公衆便所プレイ時
;-------------------------------------------------
IF TFLAG:13 == 22
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:281 == 0
		PRINTFORMW 
		CFLAG:281 = 1
	;それ以外
	ELSEIF CFLAG:281 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:281 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;朝料理イベント時 CFLAG:282 
;-------------------------------------------------
IF TFLAG:13 == 23
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:282 == 0
		PRINTFORMW 
		CFLAG:282 = 1
	;それ以外
	ELSEIF CFLAG:282 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:282 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;調教後夜這い受け CFLAG:284
;-------------------------------------------------
IF TFLAG:13 == 27
	CALL EVENT_MESSAGE
	;貞操対帯あり
	IF TEQUIP:2 == 5
		PRINTFORML 
	ELSE
		;陥落済み
		IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:284 < 1 || FLAG:7 == 2)
			;恋慕、親愛
			IF TALENT:3 || TALENT:6
				PRINTFORMW 
			;淫乱、娼婦
			ELSEIF TALENT:4 || TALENT:7
				PRINTFORMW 
			;服従、隷属
			ELSEIF TALENT:5 || TALENT:8
				PRINTFORMW 
			ENDIF
			CFLAG:284 = 1
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;朝足コキ CFLAG:285 
;-------------------------------------------------
IF TFLAG:13 == 30
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:285 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:285 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;モーニングキス CFLAG:286 
;-------------------------------------------------
IF TFLAG:13 == 31
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:286 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:286 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;朝風呂 CFLAG:287 
;-------------------------------------------------
IF TFLAG:13 == 32
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:287 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:287 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;朝３Ｐ CFLAG:288 
;-------------------------------------------------
IF TFLAG:13 == 33
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:288 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:288 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;朝飲尿 CFLAG:289
;-------------------------------------------------
IF TFLAG:13 == 34
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:289 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:289 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;夜酒宴 CFLAG:290　恋慕陥落のみ 
;-------------------------------------------------
IF TFLAG:13 == 35
	CALL EVENT_MESSAGE
	;初めて
	IF CFLAG:290 == 0
		PRINTFORMW 
		CFLAG:290 = 1
	;それ以外
	ELSEIF CFLAG:290 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:290 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜添い寝 CFLAG:291　恋慕以外は体力５００以下のときだけ
;-------------------------------------------------
IF TFLAG:13 == 36
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:291 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:291 = 2
	ELSEIF CFLAG:291 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:291 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜Ｍプレイ CFLAG:292
;地の文はK:5==1スパンキング =2鞭打ち =3足舐め =4主人Ｓで逆襲Ｍプレイ
;-------------------------------------------------
IF TFLAG:13 == 37
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:292 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:292 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜Ｓプレイ CFLAG:293
;地の文はK:5==1スパンキング =2鞭打ち =3足舐め =4主人Ｍでご奉仕Ｓプレイ
;-------------------------------------------------
IF TFLAG:13 == 38
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:293 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:293 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜レズプレイ CFLAG:294　突っこまずにいちゃいちゃ
;-------------------------------------------------
IF TFLAG:13 == 39
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:294 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:294 = 2
	ELSEIF CFLAG:294 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:294 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜覗き見 CFLAG:295　恋慕・服従陥落しているとき、他の奴隷の夜這いを覗き見
;-------------------------------------------------
IF TFLAG:13 == 40
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:295 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:295 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜睡眠中 CFLAG:296　
;-------------------------------------------------
IF TFLAG:13 == 41
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:296 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:296 = 2
	ELSEIF CFLAG:296 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:296 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜NTRプレイ CFLAG:297　恋慕・畏怖対象が主人→対象→誰かのときのみ
;-------------------------------------------------
IF TFLAG:13 == 42
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:297 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:297 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜３Ｐ CFLAG:298 
;-------------------------------------------------
IF TFLAG:13 == 43
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:298 < 1 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:298 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;夜自慰 CFLAG:299　
;-------------------------------------------------
IF TFLAG:13 == 44
	CALL EVENT_MESSAGE
	;陥落済み
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:299 < 2 || FLAG:7 == 2)
		;恋慕、親愛
		IF TALENT:3 || TALENT:6
			PRINTFORMW 
		;淫乱、娼婦
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;服従、隷属
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
		CFLAG:299 = 2
	ELSEIF CFLAG:299 < 1 || FLAG:7 == 2
		PRINTFORMW 
		CFLAG:299 = 1
	ENDIF
ENDIF


;-------------------------------------------------
;衣装変更時 CFLAG:283（を適当に適用） 
;T:A = Y　　　　着脱する衣装を限定する時
;A = X　　　　　着脱部位全体を示す時
;TEQUIP:A = Y　 今何を着ているか（着てない場合は0）
;を組み合わせます。分かりにくくてスマン。
;脱がす、着せるに関わらず、特定の組み合わせの衣装になったときの記述例に裸靴下とセーラー入れておきます。
;水兵服＋スカートのように、最後に「Y != 0」をつければ着せたときだけ表示される組み合わせ口上になります。
;コメントアウトしてあるので、ご使用の際は解除を行って下さい。（下から2番目のENDIFも解除するのを忘れずに）
;-------------------------------------------------
IF TFLAG:13 == 25
	CALL EVENT_MESSAGE
	;靴下or足袋だけにしたとき。
;	IF TEQUIP:1 && TEQUIP:0 == 0 && TEQUIP:2 == 0 && TEQUIP:3 == 0 && TEQUIP:4 == 0 && TEQUIP:5 == 0 && TEQUIP:6 == 0
;		PRINTFORMW 
;	;水兵服＋スカート
;	ELSEIF TEQUIP:4 == 1 && TEQUIP:5 == 2 && Y != 0
;		PRINTFORMW 
;	ELSE
		;脱がしたときの口上
		IF  Y == 0 && CFLAG:283 == 0 || FLAG:7 == 2
			;陥落素質無し
			IF TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0 && TALENT:9 == 0
				;貞操帯を外す
				IF A == 2 && T:2 == 5
					PRINTFORMW 
				;貞操帯以外の下着を外す
				ELSEIF A == 2
					PRINTFORMW 
				ENDIF
			ENDIF
			CFLAG:283 = 1
		;着せた時の口上
		ELSEIF CFLAG:283 < 2 || FLAG:7 == 2
			;陥落素質無し
			IF TALENT:3 == 0 && TALENT:4 == 0 && TALENT:5 == 0 && TALENT:6 == 0 && TALENT:7 == 0 && TALENT:8 == 0 && TALENT:9 == 0
				;女装（オトコキャラとちびえりざの場合）
				IF (TALENT:140 || C == 40) && (T:3 == 1 || T:3 == 2 || T:2 == 1 || T:2 == 6 || T:4 == 1 || T:6 == 2 || T:6 == 3 || T:6 == 4)
					PRINTFORMW 
				ENDIF
				;貞操帯施錠
				IF T:2 == 5
					PRINTFORML 
				ENDIF
			ENDIF
			CFLAG:283 = 2
		ENDIF
;	ENDIF
ENDIF


;-------------------------------------------------
;朝料理イベント時 CFLAG:284 を、使用できるように確保してみる
;書きかえたい場所の「CALL EVENT_MESSAGE」を消してごっそり変えてもいいかも
;表示される基本の地の文はEVENT_M.ERBを参照にして下さい
;-------------------------------------------------
IF TFLAG:13 == 26
	;最初から弁当を食べる時
	IF K == 1
		CALL EVENT_MESSAGE
		PRINTFORM 
	;売るのをやめて食べる時
	ELSEIF K == 2
		CALL EVENT_MESSAGE
		PRINTFORM 
	;主人が弁当を作るとき
	ELSEIF K == 3
		CALL EVENT_MESSAGE
		PRINTFORM 
	;食後の運動(性的な意味で)直前
	ELSEIF K == 4
		CALL EVENT_MESSAGE
		PRINTFORM 
	;毒を盛られた
	ELSEIF K == 5
		CALL EVENT_MESSAGE
		PRINTFORM 
	;毒を見抜いたので弱みを握るイベント（相性のいい子に食べさせるver）
	ELSEIF K == 6
		CALL EVENT_MESSAGE
		PRINTFORM 
	;毒を見抜いたので弱みを握るイベント（問い詰めるver）
	ELSEIF K == 7
		CALL EVENT_MESSAGE
		PRINTFORM 
	;毒殺された
	ELSEIF K == 8
		CALL EVENT_MESSAGE
		PRINTFORM 
	ENDIF
ENDIF

;-------------------------------------------------
;@@SET_SLAVE_AFTERWARDS関係(X1、Y1をキャラ番号に置換する)
;-------------------------------------------------
@SET_SLAVE_AFTERWARDS_KY1
PRINTFORML 
;-------------------------------------------------
;@KOJO_MESSAGE_ENDING関係(X1、X2をキャラ番号に置換する)
;-------------------------------------------------
;-------------------------------------------------
;条件設定
;-------------------------------------------------
@EVENTTURNEND_PY1_KX1
#LATER
SIF TARGET < 0
	RETURN 0
SIF NO:TARGET != X1
	RETURN 0
;特殊ハーレムエンド用条件判定構文サンプル
;XX内に条件とするキャラのキャラ番号を入れると、好きな組み合わせのハーレムエンドを設定できる
;ハーレムエンドの条件を満たしたとき、ここの特殊ハーレムエンドの条件も満たしている場合、ハーレムエンドから派生する
;A = 0
;B = 0
;REPEAT CHARANUM
;	IF NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX || NO:COUNT == XX
;		SIF TALENT:COUNT:6 || TALENT:COUNT:7 || TALENT:COUNT:8
;			A += 1
;		SIF TALENT:COUNT:3 || TALENT:COUNT:4 || TALENT:COUNT:5 || TALENT:COUNT:6 || TALENT:COUNT:7 || TALENT:COUNT:8
;			B += 1
;	ENDIF
;REND
;SIF A && A+B >= XXX
;	CFLAG:21 = 2
;単体エンド用分岐フラグ、単体エンドのある上位陥落素質のみコメントアウトを解除する
;SIF TALENT:6
;	CFLAG:21 = 1
;SIF TALENT:7
;	CFLAG:21 = 1
;SIF TALENT:8
;	CFLAG:21 = 1
;-------------------------------------------------
;エンド本文
;-------------------------------------------------
@KOJO_MESSAGE_ENDING_PY1_KX1
;恋慕単体エンド
;IF TALENT:6 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.1X2A (○○エンド)
;淫乱単体エンド
;ELSEIF TALENT:7 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.1X2B (○○エンド)
;服従単体エンド
;ELSEIF TALENT:8 && CFLAG:21 == 1
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.1X2C (○○エンド)
;ENDIF

;特殊ハーレムエンド
;IF CFLAG:21 == 2
	;DRAWLINE
	;PRINTFORMW …………
	;PRINTFORMW ………
	;PRINTFORMW ……
	;PRINTFORMW 

	;PRINTFORMW 
	;PRINTFORMW 　　　　　　　　―― Ending No.2X2 (○○エンド)
;ENDIF

;CALL GAME_CONTINUE
;RETURN 0

