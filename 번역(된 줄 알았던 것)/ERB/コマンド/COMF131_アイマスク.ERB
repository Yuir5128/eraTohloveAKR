;-------------------------------------------------
;아이마스크
;SM-구속계 커맨드
;-------------------------------------------------
@COM131

PRINTL 아이마스크
STR:0 = 아이마스크
CALL KOJO_MESSAGE_COM


;착탈의 처리
IF TEQUIP:아이마스크 == 0 || TEQUIP:특수 3!= 2
	TEQUIP:아이마스크 = 1
	TEQUIP:특수 3 = 2
ELSE
	TEQUIP:아이마스크 = 0
	TEQUIP:특수 3 = 0
ENDIF


;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 0
LOSEBASE:기력 += 20

SOURCE:두려움 += 600
;지하실에 있으면두려움의 소스 추가
SIF TEQUIP:지하실
	TIMES SOURCE:두려움, 1.50

SOURCE:노출 += 100
SOURCE:일탈 += 300
SOURCE:반발 += 600

;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;속박경험가 높으면 소비 감소
IF EXP:속박경험 < EXPLV:3
	TIMES LOSEBASE:기력, 1.00
ELSEIF EXP:속박경험 < EXPLV:4
	TIMES LOSEBASE:기력, 0.90
ELSE
	TIMES LOSEBASE:기력, 0.70
ENDIF


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
EXP:속박경험 += 2
PRINTL 속박경험＋2

;게이경험라든지 가산
CALL COM_EXP(1)

;의존도 벡터
TFLAG:33 = 3

;의존도 보정치(복종로 1, 예속로 2)
IF TALENT:복종 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:예속 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF


RETURN 1



;-------------------------------------------------
;장착 계속의 처리
;-------------------------------------------------
@EQUIP_COM131
PRINTL ＜아이마스크장착중＞

;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
LOSEBASE:체력 += 0
LOSEBASE:기력 += 30

SOURCE:두려움 += 150
SOURCE:노출 += 50
SOURCE:일탈 += 150
SOURCE:반발 += 300

;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;속박경험가 높으면 소비 감소
A = 40
IF EXP:속박경험 < EXPLV:3
	TIMES A , 1.00
ELSEIF EXP:속박경험 < EXPLV:4
	TIMES A , 0.90
ELSE
	TIMES A , 0.70
ENDIF
LOSEBASE:기력 += A

;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
EXP:속박경험 += 1
PRINTL 속박경험＋1

RETURN 1


