;COMABLE 일괄관리
@GLOBAL_COMABLE(ARG)
#FUNCTION
;커맨드 필터
LOCALS = /{ARG}/
IF STRCOUNT(SAVESTR:0, LOCALS)
	RESULT = 0
	RETURNF 1
ENDIF

;기절중
IF TALENT:기절
	SELECTCASE ARG
		CASE 1, 153, 154
		CASEELSE
			RESULT = 0
			RETURNF 1
	ENDSELECT
ENDIF

