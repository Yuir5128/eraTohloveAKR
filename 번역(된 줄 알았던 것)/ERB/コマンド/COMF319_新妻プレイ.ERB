;-------------------------------------------------
;신혼부부플레이
;시추에이션별 커맨드
;-------------------------------------------------
@COM319

PRINTL 신혼부부플레이
STR:0 = 신혼부부플레이
CALL KOJO_MESSAGE_COM

;착탈의 처리 장비시는 소스, 경험의 가산은 실시하지 않는다
IF TEQUIP:에이프런 == 1
	TEQUIP:에이프런 = 0
	TEQUIP:특수1 = 0
ELSE
	TEQUIP:에이프런 = 1
	TEQUIP:특수1 = 2
ENDIF


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;의존도 벡터
TFLAG:33 = 0


RETURN 1


;-------------------------------------------------
;장착 계속의 처리
;-------------------------------------------------
@EQUIP_COM319
;신혼부부플레이

;조수조교시는, 에이프런를 해제해 함수 종료
IF ASSIPLAY
	TEQUIP:에이프런 = 0
	RETURN 0
ENDIF

PRINTL ＜신혼부부플레이중＞

;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 0
LOSEBASE:기력 += 20

SOURCE:애정 += 1000

;연모계 함락이 끝난 상태라면애정의 소스 추가
SIF TALENT:연모 || TALENT:친애
	TIMES SOURCE:애정, 1.50


SOURCE:순종 += 5000
SOURCE:일탈 += 500


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;애정경험
PRINTFORML %EXPNAME:23%＋1
EXP:애정경험 += 1

;의존도 보정치(주인 또한연모로 2, 친애로 4)
IF TALENT:연모 && ASSIPLAY == 0
	TFLAG:30 += 2
ELSEIF TALENT:친애 && ASSIPLAY == 0
	TFLAG:30 += 4
ENDIF


RETURN 1

