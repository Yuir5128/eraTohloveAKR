;-------------------------------------------------
;애널자극
;A계 조교 커맨드
;-------------------------------------------------
@COM50

;-------------------------------------------------
;특수 파생 처리
;-------------------------------------------------
;변의가 높으면식분한다에 파생
IF BASE:변의 >= 90
	CALL COM_ABLE286
	SIF RESULT == 1
		JUMP COM286
ENDIF

PRINTL 애널자극
STR:0 = 애널자극
CALL KOJO_MESSAGE_COM

;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 5
LOSEBASE:기력 += 30

SOURCE:쾌A += 30

;조교자의 TALENT:혀놀림를 본다
IF TALENT:PLAYER:혀놀림
	TIMES SOURCE:쾌A , 1.50
ELSEIF TALENT:PLAYER:고양이혀
	TIMES SOURCE:쾌A , 0.75
ENDIF

;착의 때
SIF (TEQUIP:속옷（아래） && TEQUIP:속옷（아래） != 5) || TEQUIP:특수 2 == 5 || TEQUIP:특수 2 == 6
	TIMES SOURCE:쾌A , 0.70
SIF EXP:A경험 >= 100 && PALAM:욕정 >= PALAMLV:3
	TIMES SOURCE:쾌A , 1.20

SOURCE:액체 += 50
SOURCE:일탈 += 40
SOURCE:반발 += 30


;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;억압, 저항소유에는, 억울소스 추가
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 50

;스킨십선호라면애정의 소스 추가
SIF TALENT:스킨십선호
	SOURCE:애정 += 50


;-------------------------------------------------
;더러움의 처리
;-------------------------------------------------
CALL COM_STAIN

;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
EXP:A경험 += 1
PRINTL A경험＋1

IF TALENT:PLAYER:고양이혀
	EXP:PLAYER:가학쾌락경험 += 1
	PRINTFORML %CALLNAME:PLAYER%의가학쾌락경험＋1
ENDIF

;게이경험라든지 가산
CALL COM_EXP(3)

;의존도 벡터
TFLAG:33 = 3


RETURN 1


