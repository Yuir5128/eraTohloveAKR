;-------------------------------------------------
;미약
;약품계 커맨드
;-------------------------------------------------
@COM301

IF TEQUIP:촉수
	PRINTL 촉수미약
	STR:0 = 촉수미약
ELSE
	PRINTL 미약
	STR:0 = 미약
ENDIF

CALL KOJO_MESSAGE_COM


;사용한 아이템 줄인다
SIF TEQUIP:촉수 == 0
	ITEM:미약 -= 1

TEQUIP:미약 = 1


;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
;조교자가조제지식를 가지고 있으면 소비를 억제 당한다
IF TALENT:PLAYER:조제지식
	LOSEBASE:체력 += 0
	LOSEBASE:기력 += 10
ELSE
	LOSEBASE:체력 += 10
	LOSEBASE:기력 += 50
ENDIF

;미약효과로욕정의 업이 2배가 되므로 실제로는+5000
UP:욕정 = 2500
SOURCE:일탈 += 1000


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;게이경험라든지 가산
CALL COM_EXP(1)

;의존도 벡터
TFLAG:33 = 0

RETURN 1

