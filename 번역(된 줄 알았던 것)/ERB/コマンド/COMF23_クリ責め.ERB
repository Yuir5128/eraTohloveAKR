;-------------------------------------------------
;클리고문
;C계 조교 커맨드
;-------------------------------------------------
@COM23

PRINTL 클리고문
STR:0 = 클리고문
CALL KOJO_MESSAGE_COM


;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 10
LOSEBASE:기력 += 50

SOURCE:쾌C += 200
SIF TEQUIP:클리캡
	TIMES SOURCE:쾌C , 1.50
;착의(바지계) 때
SIF BOTTOMS(TARGET)
	TIMES SOURCE:쾌C , 0.60
SIF TEQUIP:속옷（아래） || TEQUIP:특수 2 == 5 || TEQUIP:특수 2 == 6
	TIMES SOURCE:쾌C , 0.90

;삼각목마승마중, 새드 3 이상이나지하실에 있으면고통의 소스 추가
IF TEQUIP:삼각목마 && (ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드 || TEQUIP:지하실)
	SOURCE:통증 += 800
;지하실에 있으면고통의 소스 추가
ELSEIF TEQUIP:지하실
	SOURCE:통증 += 600
ENDIF

SIF ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드
	SOURCE:통증 += 200

SIF TEQUIP:음핵클립
	SOURCE:통증 += 400

;C민감라면고통의 소스 추가
SIF TALENT:C민감
	TIMES SOURCE:통증, 1.50

SOURCE:노출 += 20
SOURCE:일탈 += 30
SOURCE:반발 += 30


;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;억압, 저항소유에는, 억울소스 추가
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 30

;스킨십선호라면애정의 소스 추가
SIF TALENT:스킨십선호
	SOURCE:애정 += 50



;-------------------------------------------------
;더러움의 처리
;-------------------------------------------------
;조교자의손⇔노예의 V(하의류)의 더러움이 이동
;도구를 사용하는 경우, 더러움은 이동하지 않는다
IF (TEQUIP:삼각목마 && (ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드 || TEQUIP:지하실)) || TEQUIP:클리캡 || TEQUIP:음핵클립
ELSE
	CALL COM_STAIN
ENDIF


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;게이경험라든지 가산
CALL COM_EXP(5)

;의존도 벡터
TFLAG:33 = 1


RETURN 1





