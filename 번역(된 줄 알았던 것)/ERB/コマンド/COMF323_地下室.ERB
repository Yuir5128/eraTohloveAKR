;-------------------------------------------------
;지하실
;시추에이션별 커맨드
;-------------------------------------------------
@COM323

PRINTL 지하실
STR:0 = 지하실
CALL KOJO_MESSAGE_COM

;착탈의 처리 종료시는 소스의 가산은 실시하지 않는다
IF TEQUIP:지하실
	TEQUIP:지하실 = 0
	RETURN 1
ELSE
	TEQUIP:지하실 = 1
ENDIF

;이동시에 오줌·변존재를 클리어
TFLAG:66 = 0
TFLAG:57 = 0


;-------------------------------------------------
;특수소스 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 5
LOSEBASE:기력 += 50

;낙인·엽기계의 경험이 있는 경우만기력소비와공포·반발의 소스
;반발는, 친애·복종·예속 이외만
IF TALENT:낙인 || TALENT:한팔결손 || TALENT:양팔결손 || TALENT:한다리결손 || TALENT:양다리결손 || TALENT:달마 || TALENT:모두발치
	SOURCE:두려움 += 200
	SIF TALENT:복종 == 0 && TALENT:친애 == 0 && TALENT:예속 == 0
		SOURCE:반발 += 200
ENDIF


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;의존도 벡터
TFLAG:33 = 5

RETURN 1



;-------------------------------------------------
;장착 계속의 처리
;-------------------------------------------------
@EQUIP_COM323
;지하실
PRINTL ＜지하실＞

;-------------------------------------------------
;특수소스 처리
;-------------------------------------------------
;낙인·엽기계의 경험이 있는 경우만기력소비와공포·반발의 소스
;반발는, 친애·복종·예속 이외만
IF TALENT:낙인 || TALENT:한팔결손 || TALENT:양팔결손 || TALENT:한다리결손 || TALENT:양다리결손 || TALENT:달마 || TALENT:모두발치
	LOSEBASE:기력 += 50
	
	SOURCE:두려움 += 100
	SIF TALENT:복종 == 0 && TALENT:친애 == 0 && TALENT:예속 == 0
		SOURCE:반발 += 100
ENDIF

RETURN 1


