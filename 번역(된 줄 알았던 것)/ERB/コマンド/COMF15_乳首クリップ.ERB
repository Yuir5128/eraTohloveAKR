;-------------------------------------------------
;유두클립
;B계 조교 커맨드
;-------------------------------------------------
@COM15

PRINTL 유두클립
STR:0 = 유두클립
CALL KOJO_MESSAGE_COM


;착탈의 처리
IF TEQUIP:유두클립 == 0
	TEQUIP:유두클립 = 1
ELSE
	TEQUIP:유두클립 = 0
ENDIF


;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 10
LOSEBASE:기력 += 50

SOURCE:쾌B += 350

SOURCE:통증 += 200
;지하실에 있으면고통의 소스 추가
SIF TEQUIP:지하실
	TIMES SOURCE:통증, 2.00
;B민감 함몰유두라면고통의 소스 추가
SIF TALENT:B민감 || TALENT:함몰유두
	TIMES SOURCE:통증, 1.50

SOURCE:노출 += 30
SOURCE:굴복 += 100
SOURCE:일탈 += 100
SOURCE:반발 += 100


;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;억압, 저항소유에는, 억울소스 추가
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 100



;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;게이경험라든지 가산
CALL COM_EXP(1)

;의존도 벡터
TFLAG:33 = 1

;의존도 보정치(복종로 1, 예속로 2)
IF TALENT:복종 && ASSIPLAY == 0
	TFLAG:30 += 1
ELSEIF TALENT:예속 && ASSIPLAY == 0
	TFLAG:30 += 2
ENDIF


RETURN 1


;-------------------------------------------------
;장착 계속의 처리
;-------------------------------------------------
@EQUIP_COM15
;유두클립장비중
PRINTL ＜유두클립장비중＞

;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
LOSEBASE:체력 += 5
LOSEBASE:기력 += 20

SOURCE:쾌B += 100

IF TEQUIP:지하실
	SOURCE:통증 += 450
ELSE
	SOURCE:통증 += 200
ENDIF

;B민감 함몰유두라면고통의 소스 추가
SIF TALENT:B민감 || TALENT:함몰유두
	TIMES SOURCE:통증, 1.50

SOURCE:노출 += 15
SOURCE:굴복 += 50
SOURCE:일탈 += 50
SOURCE:반발 += 50

;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;억압, 저항소유에는, 억울소스 추가
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 50

RETURN 1



