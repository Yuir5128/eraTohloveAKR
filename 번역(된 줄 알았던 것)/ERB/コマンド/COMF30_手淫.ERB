;-------------------------------------------------
;수음한다
;P계 조교 커맨드
;-------------------------------------------------
@COM30

PRINTL 수음한다
STR:0 = 수음한다
CALL KOJO_MESSAGE_COM


;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비체력, 기력
LOSEBASE:체력 += 5
LOSEBASE:기력 += 30

SOURCE:쾌C += 80
SIF TEQUIP:전동오나홀
	TIMES SOURCE:쾌C , 1.50

;착의 때
SIF TEQUIP:속옷（아래） || TEQUIP:특수 2 == 5 || TEQUIP:특수 2 == 6
	TIMES SOURCE:쾌C , 0.90
SIF BOTTOMS(TARGET)
	TIMES SOURCE:쾌C , 0.70

SOURCE:두려움 += 20
SOURCE:욕정 += 30
SOURCE:노출 += 20
SOURCE:일탈 += 15
SOURCE:반발 += 15

;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;억압, 저항소유에는, 억울소스 추가
SIF TALENT:억압 || TALENT:저항
	SOURCE:울굴 += 20

;스킨십선호라면애정의 소스 추가
SIF TALENT:스킨십선호
	SOURCE:애정 += 50

;-------------------------------------------------
;더러움의 처리
;-------------------------------------------------
;조교자의손⇔노예의 C(속옷, 바지계 의류)의 더러움이 이동
;도구를 사용하는 경우는 더러움은 이동하지 않는다
IF TEQUIP:전동오나홀
ELSE
	CALL COM_STAIN
ENDIF

;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;게이경험라든지 가산
CALL COM_EXP(1)

;의존도 벡터
TFLAG:33 = 1


RETURN 1



