;==================================================
;00입상
;==================================================
;==================================================
;조교자,노예,촉수의사정입상
;==================================================
;=================================================
;조교자의사정
;=================================================
;-------------------------------------------------
;A_1,사정처 선택으로머리카락를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_1
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난 상태+(정액중독 3L 혹은봉사정신 5L) 혹은 망집
	IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET))  || (CFLAG:5005 & 4)
		PRINTFORML 「인가,머리카락에는………!」
		PRINTFORMW %CALLNAME%는입에서는 그렇게 말하고 있지만,머리카락를 더럽혀졌다고 하는 사실에 흥분을 숨기지 못할 모습이다
	;그 이외
	ELSE
		PRINTFORMW %CALLNAME%는머리카락 타 떨어져 내리는 백탁에 절구[絶句] 하고 있다
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_2,사정처 선택으로 얼굴을 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_2
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
	IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET)) || (CFLAG:5005 & 4)
		PRINTFORML 「읏,얼굴 질척질척에……」
		PRINTFORMW %CALLNAME%는 숨을 거칠게 하면서,얼굴에 걸린 정액에 닿고 있다
	;그 이외
	ELSE
		PRINTFORMW %CALLNAME%는 얼굴에 걸린 정액을 필사적으로 닦고 있다
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_3,사정처 선택으로가슴를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_3
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
	IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET)) || (CFLAG:5005 & 4)
		;상의,전신상의,속옷（위）를 착용
		IF TOPS(TARGET) || TEQUIP:상의 || TEQUIP:속옷（위）
			PRINTFORML 「옷 따위에 내는 것이 아니다………」
			PRINTFORMW %CALLNAME%는입에서는 그렇게 말하고 있지만,흥분에 얼굴을 느슨하게하고 있다
		;윗도리라고 없다
		ELSE
			PRINTFORML 「응…,핫……」
			PRINTFORMW %CALLNAME%는가슴의 정액에,황홀의 표정을 하고 있다
		ENDIF
	;그 이외
	ELSE
		;상의,전신상의,속옷（위）를 착용
		IF TOPS(TARGET) || TEQUIP:상의 || TEQUIP:속옷（위）
			PRINTFORMW %CALLNAME%는 옷이 들러붙는 감각에 노골적인불쾌감을 나타내고 있다
		;윗도리라고 없다
		ELSE
			PRINTFORMW %CALLNAME%는 얼굴을 찡그리고 있다
		ENDIF
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_4,사정처 선택으로 배를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_4
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_5,사정처 선택으로 겨드랑이를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_5
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;겨드랑이잡라면 돌아간다
	SIF SELECTCOM == 82
		RETURN LOCAL
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
	SIF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET)) || (CFLAG:5005 & 4)
		PRINTFORML 「낫!왜 그런 장소에……?」
	
	PRINTFORMW %CALLNAME%는 곤혹한 눈으로 이쪽을 보고 있다
	
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_6,사정처 선택으로 허벅지를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_6
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_7,사정처 선택으로 균열을 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_7
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
	IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET)) || (CFLAG:5005 & 4)
		IF RAND:2 == 0
			PRINTFORML 「하……… 냄새만으로,어째서\@ (CFLAG:5005 & 1) ? 사#사\@는 이런……」
		ELSE
			PRINTFORML 「이런 장소는,에……」
		ENDIF
		PRINTFORMW %CALLNAME%는 가랑이의 미지근한 감촉에 신체를 근질근질 시키고 있다
	;함락이 끝난 상태
	ELSEIF KANRAKU(TARGET)
		PRINTFORMW %CALLNAME%는 발해진 정액에 얼굴을 붉히고 있다
	;그 이외
	ELSE
		PRINTFORMW %CALLNAME%는 발해진 정액에 모멸을 담은 시선을 보내고 있다
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_8,사정처 선택으로 엉덩이를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_8
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
		IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET)) || (CFLAG:5005 & 4)
			IF RAND:2 == 0
				PRINTFORML 「우…,어째서\@ (CFLAG:5005 & 1) ? 사#사\@는,이렇게………」
			ELSE
				PRINTFORML 「이런,장소는…… 에…,」
			ENDIF
			PRINTFORMW %CALLNAME%는 둔부의 미끈미끈 한 감촉에 신체를 근질근질 시키고 있다
		;그 이외
		ELSE
			PRINTFORMW 둔부의 감촉에%CALLNAME%는 얼굴을 굳어지게 하고 있다
		ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_9,사정처 선택으로 고무를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_9
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;섹스/애널섹스로사정
	IF SEXPLAY(TARGET) == 1 || SEXPLAY(TARGET) == 2
		;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
		IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET) && KANRAKU(TARGET) < 4) || (CFLAG:5005 & 4)
			;망집
			IF (CFLAG:5005 &4)
				PRINTFORML 「아,도… 해……(안)중… 는,우… 그렇지만…… 정액,많이,… 읏우」
			ELSE
				PRINTFORML 「하…,아우… 아,핫…… 도,진흙의…,정액…,풀응… 아,읏」
			ENDIF
			PRINTFORMW %CALLNAME%는 충분히 채워진콘돔를 앞에 넋을 잃고 있다
		;함락이 끝난 상태
		ELSEIF KANRAKU(TARGET)
			PRINTFORMW 얼굴을 붉게 한%CALLNAME%는 백탁으로 충분히 채워진콘돔로부터 눈을 돌리고 있다
		;그 이외
		ELSE
			PRINTFORML %CALLNAME%는 혐오감으로 가득 되면서도,안에 직접 나오지 않았던 것에 안도하고 있는 모습이다
		ENDIF
	;그 이외를 실행시
	ELSE
		;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
		IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET) && KANRAKU(TARGET) < 4) || (CFLAG:5005 & 4)
			PRINTFORML 「앗,하… 아,…… 아,풀응,정액…… 많이……」
			PRINTFORMW %CALLNAME%는 백탁으로 충분히 채워진콘돔를 앞에 넋을 잃고 있다
		;함락이 끝난 상태
		ELSEIF KANRAKU(TARGET)
			PRINTFORMW 얼굴을 붉게 한%CALLNAME%는 충분히 채워진콘돔로부터 눈을 돌리고 있다
		;그 이외
		ELSE
			PRINTFORMW %CALLNAME%는 토할 것 같은 얼굴을 하고 있다
		ENDIF
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_10,사정처 선택으로발를 선택
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_10
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_11,키스,스마타,풋잡,애널강제 봉사,더블스마타로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_11
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_12,조수를범한다로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_12
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_13,입를 사용한 봉사로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_13
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
	IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET)) || (CFLAG:5005 & 4)
		IF RAND:2 == 0
			PRINTFORML 「읏… 응무…,응읏…… 아,맛있어……」
		ELSE
			PRINTFORML 「읏,우우…… 응구………,읏,후……」
		ENDIF
		PRINTFORMW %CALLNAME%는 발해진 정액을 맛보도록(듯이) 저작[咀嚼] 해 삼키고 있다
	;함락이 끝난 상태
	ELSEIF KANRAKU(TARGET)
		IF RAND:2 == 0
			PRINTFORML 「…… 맛이 없구나」
		ELSE
			PRINTFORML 「…… 읏,푸핫」
		ENDIF
		PRINTFORMW %CALLNAME%는 발해진 정액을 어떻게든 삼키고 있다
	;그 이외
	ELSE
		PRINTFORMW 발해진 정액에%CALLNAME%파리않다 와,기침하고 있다
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_14,손를 사용한 봉사로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_14
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;함락이 끝난+정액중독 3L 혹은봉사정신 5L 혹은 망집
	IF ((ABL:봉사정신 >= 5 || ABL:정액중독 >= 3) && KANRAKU(TARGET) < 4) || (CFLAG:5005 & 4)
		PRINTFORML 「완전히………,」
		PRINTFORMW %CALLNAME%는손에 붙은 정액을,그대로 혀로 빨아냈다
		;더러운 처리 생략
	;함락이 끝난 상태
	ELSEIF KANRAKU(TARGET)
		PRINTFORMW %CALLNAME%는손에 붙은 정액을 봐,미간을 대고 있다
	;그 이외
	ELSE
		PRINTFORMW %CALLNAME%는손에 붙은 정액을 마루에 문지르지 않는 꾸물거리고 있다
	ENDIF
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_15,가슴를 사용한 봉사로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_15
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_16,발를 사용한 봉사로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_16
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;반발
	IF (CFLAG:5005 & 32)
		PRINTFORML 「……….」
		PRINTFORMW %CALLNAME%는 혐오감에 얼굴을 비뚤어지게 하고 있다
		RETURN LOCAL
	ENDIF

	;새드
	IF (CFLAG:5005 & 512)
		SELECTCASE RAND:5
			CASE 4
				PRINTFORML 「\@ TFLAG:16 == 2? 이렇게#이런 것으로\@내 부끄럽지 않은 것인지?」
				PRINTFORML 「…… 아아,부끄럽지 않기 때문에 내고 있었구나」
				PRINTFORMW %CALLNAME%는 업신여긴 눈으로 이쪽을 보고 있다
			CASE 3
				PRINTFORML 「이봐요」
				PRINTFORML %CALLNAME%는 정액으로\@ TFLAG:16 == 2? 흠뻑더러워진#더러워진\@발를 이쪽에 내밀고 있다
				PRINTFORMW 아무래도 빨라고 하고 있는 것 같다
			CASE 2
				PRINTFORML 「누가 내도 좋다고 말했어?」
				PRINTFORML 「…… 조금,예의 범절이 필요하다」
				PRINTFORMW %CALLNAME%는사정 한지 얼마 안된 음경을,한층 더 훑어 냈다
			CASE 1
				PRINTFORML 「그렇게 기분 좋았던 것일까?」
				PRINTFORMW %CALLNAME%는사정 한지 얼마 안된 음경을발로 희롱하면서,이쪽에 물어 보고 있다
			CASE 0
				PRINTFORML 「이런 것이 좋다고는……」
				PRINTFORMW %CALLNAME%는 한숨을 쉬면서,이쪽을 보고 있다
		ENDSELECT
		RETURN LOCAL
	ENDIF
	
	;외
	SELECTCASE RAND:3
		CASE 2
			PRINTFORML 「이,이런 것으로 낸다고는……」
		CASE 1
			PRINTFORML 「우와……」
		CASE 0
			;마조
			IF (CFLAG:5005 & 256)
				PRINTFORML 「아니 뭐,기분은 알지만……」
			ELSE
				PRINTFORML 「아니 뭐,취미는 사람 각자이니까……」
			ENDIF
	ENDSELECT
	;망집
	IF (CFLAG:5005 & 4)
		PRINTFORMW %CALLNAME%는 조금얼굴을 붉히고 있다
	ELSE
		PRINTFORMW %CALLNAME%는 약간 당긴 눈으로 이쪽을 보고 있다
	ENDIF
	
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_17,애널섹스로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_17
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;KOJO_CVC_K174.ERB 참조
	CALL SEX_EVENT_174_PALAMCNG_A_K174_17_18
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_18,섹스로사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_18
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	;KOJO_CVC_K174.ERB 참조
	CALL SEX_EVENT_174_PALAMCNG_A_K174_17_18
	
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_19,역섹스로 조교 대상이 동시에사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_19
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_20,역섹스로 조교자만이사정
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_20
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_21,조교자의 하늘사정
;조교자가[아이사이즈]의 경우
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_21
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;A_22,조교자 이외의사정
;조수를범한다나촉수에범하게시킨다등의 경우
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_A_K174_22
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_A_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL

;=================================================
;촉수의사정
;=================================================
;-------------------------------------------------
@KOJO_MESSAGE_PALAMCNG_E_K174_1
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL PALAM_MESSAGE_E_COLOR
	
	PRINTFORML 
	PRINTFORML 
ENDIF
RETURN LOCAL


