
;==================================================
;이벤트대사집(1,01을 캐릭터 번호에 치환한다) CFLAG 261~299를 사용
;==================================================
@KOJO_MESSAGE_EVENT_K49
;--------------------------------------------------
;특정조수,주인시에 특수조수대사을 호출한다(Y1를 특정조수,Z를 주인의 캐릭터 번호에 치환)
;--------------------------------------------------
;IF NO:MASTER == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_KZ
;	RETURN 0
;ENDIF
;IF ASSI >= 0 && ASSIPLAY
;	SIF NO:ASSI == Y1
;	CALL KOJO_MESSAGE_EVENT_PY1_KZ
;	RETURN 0
;ENDIF


;==================================================
;아침 이벤트
;==================================================
;-------------------------------------------------
;아침 페라 CFLAG:261
;-------------------------------------------------
IF TFLAG:13 == 1
	CALL EVENT_MESSAGE
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:261 < 1 || FLAG:7 == 2)
		;연모,친애
		IF TALENT:3 || TALENT:6
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				IF RAND:2 == 0
					PRINTFORMW 「으음,%UNICODE(0x2661)% 는,대장. 일발 뽑아 시원해져 오늘도 노력하자구」
				ELSE
					PRINTFORMW 「응응,,…… 는,후후,타비치는 것은 누 실일까 실인?」
				ENDIF
			ELSE
				PRINTFORMW 
			ENDIF
		;음란,창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종,예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ENDIF
	CFLAG:261 = 1
ENDIF

;-------------------------------------------------
;아침파이즈리 CFLAG:262
;-------------------------------------------------
IF TFLAG:13 == 2
	CALL EVENT_MESSAGE
	CFLAG:262 = 1
ENDIF

;-------------------------------------------------
;아침기승위 CFLAG:263
;-------------------------------------------------
IF TFLAG:13 == 3
	CALL EVENT_MESSAGE
	CFLAG:263 = 1
ENDIF

;-------------------------------------------------
;아침 요리 이벤트시 CFLAG:282
;연애계 함락만
;-------------------------------------------------
IF TFLAG:13 == 23
	CALL EVENT_MESSAGE
	IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
		PRINTFORMW 「안녕씨,대장. 아침 식사라면 가능해 있어. 자,앉았다 앉았다」
	ELSE
		PRINTFORMW 「오우,안녕이다,%CALLNAME:PLAYER%. 아침 식사는 먹겠지? 되어져 있어」
	ENDIF
	CFLAG:282 = 1
ENDIF

;-------------------------------------------------
;아침풋잡 CFLAG:285 
;-------------------------------------------------
IF TFLAG:13 == 30
	CALL EVENT_MESSAGE
	CFLAG:285 = 1
ENDIF

;-------------------------------------------------
;모닝 키스 CFLAG:286
;-------------------------------------------------
IF TFLAG:13 == 31
	CALL EVENT_MESSAGE
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:286 < 1 || FLAG:7 == 2)
		;연모,친애
		IF TALENT:3 || TALENT:6
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				IF RAND:2 == 0
					PRINTFORMW 「…… 안녕,도미 실마리」
					PRINTFORMW %CALLNAME%는 드물게 응석부린 모습으로 동글동글 %CALLNAME:MASTER%의가슴에 머리를 비벼대고 있다…
				ELSE
					PRINTFORMW 「대장,헤헤에,오늘도 함께다……」
					PRINTFORMW %CALLNAME%는 말랑 웃고 있다……
				ENDIF
			ELSE
				PRINTFORMW 
			ENDIF
		;음란,창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종,예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	ENDIF
	CFLAG:286 = 1
ENDIF

;-------------------------------------------------
;아침 목욕 CFLAG:287 
;-------------------------------------------------
IF TFLAG:13 == 32
	CALL EVENT_MESSAGE
	CFLAG:287 = 1
ENDIF

;-------------------------------------------------
;아침 3P CFLAG:288
;-------------------------------------------------
IF TFLAG:13 == 33
	CALL EVENT_MESSAGE
	CFLAG:288 = 1
ENDIF

;-------------------------------------------------
;조음뇨 CFLAG:289
;-------------------------------------------------
IF TFLAG:13 == 34
	CALL EVENT_MESSAGE
	CFLAG:289 = 1
ENDIF



;==================================================
;밤이벤트
;==================================================
;-------------------------------------------------
;착유→밤이벤트로 변경 CFLAG:267
;-------------------------------------------------
IF TFLAG:13 == 7
	CALL EVENT_MESSAGE
	CFLAG:267 = 1
ENDIF

;-------------------------------------------------
;요바이→밤이벤트로 변경(노예수) CFLAG:269
;-------------------------------------------------
IF TFLAG:13 == 9
	CALL EVENT_MESSAGE
	CFLAG:269 = 1
ENDIF

;-------------------------------------------------
;요바이 주인수 CFLAG:284
;-------------------------------------------------
IF TFLAG:13 == 27
	CALL EVENT_MESSAGE
	CFLAG:284 = 1
ENDIF

;-------------------------------------------------
;밤주연 CFLAG:290 연모함락만
;-------------------------------------------------
IF TFLAG:13 == 35
	CALL EVENT_MESSAGE
	IF (NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)) && (CFLAG:290 < 1 || FLAG:7 == 2)
		PRINTFORMW 「하하하,대장과 마주 보고 앉음좋은 것이다」
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:290 = 1
ENDIF

;-------------------------------------------------
;밤곁잠 CFLAG:291 연모 이외는체력 500 이하 때 만
;-------------------------------------------------
IF TFLAG:13 == 36
	CALL EVENT_MESSAGE
	IF (NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)) && (CFLAG:290 < 1 || FLAG:7 == 2)
		PRINTFORMW 「대장의 심장 뛰는 소리가 들리는구나…… 자주(잘) 잘 수 있을 것 같다」
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:291 = 1
ENDIF

;-------------------------------------------------
;밤M플레이 CFLAG:292
;-------------------------------------------------
IF TFLAG:13 == 37
	CALL EVENT_MESSAGE
	CFLAG:292 = 1
ENDIF

;-------------------------------------------------
;밤S플레이 CFLAG:293
;지문은 K:5==1스팽킹 =2채찍치는 것 =3발 빨고 =4 주인 M로 봉사 S플레이
;-------------------------------------------------
IF TFLAG:13 == 38
	CALL EVENT_MESSAGE
	CFLAG:293 = 1
ENDIF

;-------------------------------------------------
;밤레즈비언 플레이 CFLAG:294돌입하지 않고 러브러브
;-------------------------------------------------
IF TFLAG:13 == 39
	CALL EVENT_MESSAGE
	CFLAG:294 = 1
ENDIF

;-------------------------------------------------
;밤엿보기 CFLAG:295 연모·복종함락 하고 있을 때,다른 노예의 요바이를 엿보기
;-------------------------------------------------
IF TFLAG:13 == 40
	CALL EVENT_MESSAGE
	PRINTFORMW 「나도 대장의 곳 갈까……」
	CFLAG:295 = 1
ENDIF

;-------------------------------------------------
;밤수면중 CFLAG:296　
;함락 하고 있으면,연모승손에 따라 잠,음란승손에 봉사와자위,복종발도로 잔다
;함락 하고 있지 않으면 돌아간다
;-------------------------------------------------
IF TFLAG:13 == 41
	CALL EVENT_MESSAGE
	;함락이 끝난 상태
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:296 < 2 || FLAG:7 == 2)
		;연모,친애
		IF (TALENT:3 || TALENT:6)
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				PRINTFORMW 「대장,도미 실마리. 쭉 옆에 두고 있어 주어라」
			ELSE
				PRINTFORMW 
			ENDIF
		;음란,창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종,예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
			CFLAG:296 = 2
	ELSEIF CFLAG:296 < 1 || FLAG:7 == 2
		IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
			PRINTFORMW 「…… 대장. 나,」
			PRINTFORMW %CALLNAME%는 %CALLNAME:MASTER%에 접한 손가락끝을 가만히 응시하고 있다……
		ELSE
			PRINTFORMW 
		ENDIF
	ENDIF
	CFLAG:296 = 1
ENDIF

;-------------------------------------------------
;밤NTR 플레이 CFLAG:297 연모·공포 대상이 주인→대상→누군가 때만
;-------------------------------------------------
IF TFLAG:13 == 42
	CALL EVENT_MESSAGE
	CFLAG:297 = 1
ENDIF

;-------------------------------------------------
;밤 3P CFLAG:298 
;-------------------------------------------------
IF TFLAG:13 == 43
	CALL EVENT_MESSAGE
	CFLAG:298 = 1
ENDIF

;-------------------------------------------------
;밤자위 CFLAG:299
;-------------------------------------------------
IF TFLAG:13 == 44
	CALL EVENT_MESSAGE
	;함락이 끝난 상태
	IF (TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8) && (CFLAG:299 < 1 || FLAG:7 == 2)
		;연모,친애
		IF TALENT:3 || TALENT:6
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				IF RAND:2 == 0
					PRINTFORMW 「응…… 도미 서점,안타까워……」
				ELSE
					PRINTFORMW 「도미 실마리,대장…… 앗」
				ENDIF
			ELSE
				PRINTFORMW 「아,앙……%CALLNAME:PLAYER%……」
			ENDIF
		;음란,창부
		ELSEIF TALENT:4 || TALENT:7
			PRINTFORMW 
		;복종,예속
		ELSEIF TALENT:5 || TALENT:8
			PRINTFORMW 
		ENDIF
	CFLAG:299 = 1
	ENDIF
ENDIF



;==================================================
;조교후이벤트
;==================================================
;-------------------------------------------------
;조교후레즈비언 플레이 CFLAG:264
;-------------------------------------------------
IF TFLAG:13 == 4
	CALL EVENT_MESSAGE
	CFLAG:264 = 1
ENDIF

;-------------------------------------------------
;조교후섹스 수·공공용 CFLAG:265
;-------------------------------------------------
IF TFLAG:13 == 5
	CALL EVENT_MESSAGE
	PRINTFORMW 「나쁘다,아직부족해 있는이다. 이제(벌써) 조금 교제해 주어라」
	PRINTFORMW %CALLNAME%는 낼름 혀로 %CALLNAME:PLAYER%의 코끝을 빨면 그대로 덥쳐 갔다
	CFLAG:265 = 1
ENDIF

;-------------------------------------------------
;조교후자위 CFLAG:268
;-------------------------------------------------
IF TFLAG:13 == 8
	CALL EVENT_MESSAGE
	PRINTFORMW 「아,아%UNICODE(0x2661)% ,손,멈추지 않는……%UNICODE(0x2661)%」
	CFLAG:268 = 1
ENDIF



;==================================================
;조교중 이벤트
;==================================================
;-------------------------------------------------
;강정신약의 반동 CFLAG:266
;-------------------------------------------------
IF TFLAG:13 == 6
	CALL EVENT_MESSAGE
	CFLAG:266 = 1
ENDIF

;-------------------------------------------------
;찍은 비디오를 매각하는 CFLAG:278
;-------------------------------------------------
IF TFLAG:13 == 19
	CALL EVENT_MESSAGE
	CFLAG:278 = 1
ENDIF

;-------------------------------------------------
;찍은 비디오를손바탕으로 남기는 CFLAG:500
;-------------------------------------------------
IF TFLAG:13 == 24
	CALL EVENT_MESSAGE
	CFLAG:500 = 1
ENDIF



;==================================================
;노역
;==================================================
;-------------------------------------------------
;정식포장마차시 CFLAG:275
;-------------------------------------------------
IF TFLAG:13 == 15 
	IF CFLAG:275 == 0
		IF ABL:요리기능 >= 4
			PRINTFORMW 「반처인가…… 좋다,맡기고 녹고 대장」
			PRINTFORMW 팔을 흔들 수 있는 것이 기쁜 것 같고,%CALLNAME%는 만면의 웃는 얼굴로 수긍했다.
		ELSE
			PRINTFORMW 「아─,나쁘지만 대장. 나요리는 좀 더 나오는거야」
			PRINTFORMW 미간을 내려 마음 속 미안한 듯이 말하는%CALLNAME%에,급사와 접객으로 노력해 주라고 말하면 파앗 표정이 밝아졌다.
			PRINTFORMW 전혀 얇은가슴를 두드려,알았다고 수긍한다.
			PRINTFORMW 「오우,그렇다면 맡겨 두어라. 대장에게 좋은 곳 보이지 않으면」
		ENDIF
	ELSE
		PRINTFORMW 「좋아,그러면 일인분 노력해 온다고 할까」
	ENDIF
	CALL EVENT_MESSAGE
		IF RAND:2 == 0
			PRINTFORML 
			PRINTFORMW %CALLNAME%는발음도 난폭하고 기분이 안좋은 것 같게 백 야드로 돌아왔다.
			PRINTFORMW 「나참,나에게 접하는 것은 대장만이라고 말하고 있는데,저 녀석들 (듣)묻지 않는다」
			PRINTFORMW 「대장,이번 교육네나 개가 있으면,소매치기 말해 버려도 좋은가?」
		ELSE
			PRINTFORML 
			PRINTFORMW 「대장,수고 하셨습니다. 오늘도 잘 일했군」
		ENDIF
	CFLAG:275 = 1
ENDIF

;-------------------------------------------------
;도검뢰무시 CFLAG:279
;-------------------------------------------------
IF TFLAG:13 == 20
	IF CFLAG:279 == 0
		PRINTFORMW 「노래? 나고상한 일은…… 그렇게 말하는 것이 아니고,부르거나 춤추거나?」
		PRINTFORMW 「흠,몸 움직이는 일이라면 어떻게든 될까. 그런데,어떻게 하면 된다,대장?」
	ELSE
		PRINTFORMW 「좋아,갔다오군 대장. 나치노 무대 보고 있어 주어라!」
	ENDIF
	CALL EVENT_MESSAGE
	IF F == 0
		PRINTFORMW 「미안,대장. 나로 한 일이……」
	ELSE
		PRINTFORMW 「봐 주었는지? 대장의 응원이 나치노 제일의 활력이다!」
	ENDIF
	CFLAG:279 = 1
ENDIF

;-------------------------------------------------
;창관봉공시(나갈 때) CFLAG:276
;-------------------------------------------------
IF TFLAG:13 == 16
	CALL EVENT_MESSAGE
	CFLAG:276 = 1
ENDIF

;-------------------------------------------------
;창관봉공시(돌아왔을 때)
;-------------------------------------------------
IF TFLAG:13 == 69
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;구경거리 오두막시 CFLAG:280
;-------------------------------------------------
IF TFLAG:13 == 21
	IF CFLAG:280 == 0
		PRINTFORMW 「대장이 해라고 말한다면 부디도 응이……」
	ELSE
		PRINTFORMW 「관객이 대장 뿐이라면 얼마든지 서비스 살았지만 말야」
	ENDIF
	CALL EVENT_MESSAGE
	IF (CFLAG:166 && TALENT:처녀) || CFLAG:167 || CFLAG:168
		PRINTFORMW 
	;스트립 쇼
	ELSEIF B == 1
		;함락이 끝난 상태
		;연모,친애
		IF TALENT:3 || TALENT:6
			PRINTFORMW 「나치노나─다운 곳 봐의 사,호색가들. 뭐,보여 아 하기 때문에 겨우 차분히 배례해 가면 좋은 거야」
			PRINTFORMW 「하지만,나에게 손가락의 한 개라도 접하려고 하고 자빠져 보는거야. 팔 마다 떨어질 각오 해 둬?」
			PRINTFORMW 뒤숭숭한대사에 왠지 끓는 손님을 슬쩍 봐,%CALLNAME%는 남은 명주에손를 걸었다……
		ELSE
			PRINTFORMW 
		ENDIF
	;그 이외
	ELSE
		PRINTFORMW 
	ENDIF
	CFLAG:280 = 1
ENDIF

;-------------------------------------------------
;렌탈시(나갈 때)
;-------------------------------------------------
IF TFLAG:13 == 68
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;렌탈시(돌아왔을 때) CFLAG:277
;-------------------------------------------------
IF TFLAG:13 == 17
	CALL EVENT_MESSAGE
	CFLAG:277 = 1
ENDIF

;-------------------------------------------------
;공중 변소 플레이시 CFLAG:281
;-------------------------------------------------
IF TFLAG:13 == 22
	CALL EVENT_MESSAGE
	CFLAG:281 = 1
ENDIF

;-------------------------------------------------
;노예에게 요리를 만들게 한다
;-------------------------------------------------
IF TFLAG:13 == 26
	CALL EVENT_MESSAGE
	;최초부터 도시락을 먹을 때
	IF K == 1 || K == 2
	;주인이 도시락을 만들 때
	ELSEIF K == 3
	;밥후의 운동(성적인 의미로) 직전
	ELSEIF K == 4
	;독을 담아졌다
	ELSEIF K == 5
	;독을 간파했으므로 약점을 잡는 이벤트(궁합이 좋은 아이에게 먹이는 ver)
	ELSEIF K == 6
	;독을 간파했으므로 약점을 잡는 이벤트(캐묻는 ver)
	ELSEIF K == 7
	;독살되었다
	ELSEIF K == 8
	ENDIF
ENDIF

;-------------------------------------------------
;노예를 매각한다
;생각난 것으로 써 본 것입니다만.
;「팔려 줄래?」라면 「싫다」 「팔지 않으면 안 되게 되었다」라면 「그런 원인 어떻게든 해 준다」
;그렇지만 「판다」라면 「그런가」라고 말해 버리는,제약 기구씨는 그런 느낌이라고 생각하고 있습니다.
;써 놓여져 있을 수 있는입니다만 불쌍해 팔지 말아 주세요.
;-------------------------------------------------
IF TFLAG:13 == 18
	CALL EVENT_MESSAGE
	;함락이 끝난 상태
	IF TALENT:3 || TALENT:4 || TALENT:5 || TALENT:6 || TALENT:7 || TALENT:8
		;연모,친애+반발각인Lv3 미만
		IF (TALENT:3 || TALENT:6) && MARK:3 < 3
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				PRINTFORMW 「에? 나를 팔아……? 거짓말일 것이다,대장」
				PRINTFORMW 정해진 일이니까 곧바로 준비를 하라고 고하면,안색을 없애%CALLNAME%는 숙였다.
				PRINTFORMW 「아아,아니,그랬구나. 대장. 내가 착각 하고 있었다. 원래 대장 그렇게 말하는 심산이었던 것이구나」
				PRINTFORMW 「대장은 나에게 무엇을 해도 되고,나는 대장에게 무엇을 되어도 상관없다. 제약 기구통요시미츠가 허락하는,누구에게도 무엇도 말하게 하지 않고…… 말하지 않아」
				PRINTFORMW %CALLNAME%의 소리가 떨려,숙인 악처로부터 탁탁 물방울이 떨어진다.
				PRINTFORMW 「하지만,대장」
				PRINTFORMW ,라고가슴원으로손를 잡아,%CALLNAME%는 얼굴을 들었다.
				PRINTFORMW 「고금소가,있어 응이다. 맵고 슬퍼서,차라리 이대로 부서져 버릴 수 있으면 좋은데」
				PRINTFORMW 코를 붉게 해 얼굴을 왜곡해 너덜너덜눈물을 떨어뜨리는%CALLNAME%의 모습은 겉모습보다 어리게 보였다. 하지만,소리를 지르는 일을 하지 않는 우는 방법은,아이라고는 생각되지 않는다.
				PRINTFORMW 어느 쪽에도 보여 어느 쪽도 아닌,변함없는 이상함이다.
				PRINTFORMW 「나는,나는!나,쭉,녹슬고 접혀 썩을 때까지,대장의,대장만의%CALLNAME%로 있고 싶었던……!」
				PRINTFORMW 
				PRINTFORMW 그런가,라고 %CALLNAME:PLAYER%가 말하면 아주 수긍한%CALLNAME%는,최후에 「언제까지나 건강하게」라고 말해 %CALLNAME:PLAYER%의 원으로부터 떠나 갔다……
				PRINTFORMW 
			ENDIF
		ENDIF
	;그 이외
	ELSE
		PRINTFORMW 
	ENDIF

ENDIF



;==================================================
;아이템 구입시
;노란환약는 아래쪽에 개별범위 있어. 여기에서는 쓰지 않는다
;==================================================
IF TFLAG:13 == 28
	CALL EVENT_MESSAGE
	SELECTCASE BOUGHT
		;수
		CASE 55
		;공
		CASE 56
		;선인당고
		CASE 60
		;약의재료(후타)
		CASE 61
		;약의재료(소멸)
		CASE 62
		;푸른환약
		CASE 63
		;붉은환약
		CASE 64
		;풍유환
		CASE 65
		;축유환
		CASE 66
		;프롤락틴억제제
		CASE 67
		;3환계항우울제
		CASE 68
		;성전환약
		CASE 70
		;천사의기적
		CASE 71
		;악마의저주
		CASE 72
		;풍근환
		CASE 73
		;축근환
		CASE 74
		;포근환
		CASE 75
		;기억소거제
		CASE 80
		;되감기약
		CASE 81
		;배란유발제
		CASE 82
		;고성능피임구
		CASE 83
		;긴급피임약
		CASE 85
		;부적,부적극
		CASE 96,97
	ENDSELECT
ENDIF



;==================================================
;의상 변경시 CFLAG:283(를 적당하게 적용) 
;==================================================
;----------------------------------------------
;덮어 씌울 때의대사
;----------------------------------------------
IF TFLAG:13 == 25
	CALL EVENT_MESSAGE
	;일단 텐프레.
	;전신상의의 백의를 입힌다
	;IF T:6 == 6
	;	PRINTFORMW 
	;남자인데 여자의 모습을 시킨다
	;ELSEIF
	IF (TALENT:남자 || TALENT:반음반양) && (T:2 == 1 || T:2 == 4 || T:3 == 1 || T:3 == 2 || T:3 == 4 || T:3 == 5 || T:4 == 11 || (T:5 >= 1 && T:5 <= 6) || T:5 == 9 || T:5 == 12)
		;연모,친애
		IF TALENT:3 || TALENT:6
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				PRINTFORMW 「오우,이번은 이 녀석인가. 이것 입어 어떻게 해 주었으면 한다? 소망 대로다,대장」
			ELSE
				PRINTFORMW 「오우,이번은 이 녀석인가」
			ENDIF
		ELSE
			PRINTFORMW 「뭐,나도 이 면상이고. 그런데,어때 어울리겠지?」
		ENDIF
	ENDIF
ENDIF
;----------------------------------------------
;벗겼을 때의대사
;----------------------------------------------
IF TFLAG:13 == 76
	CALL EVENT_MESSAGE
	;속옷（아래）를 벗게 한다
	IF A == 2
		;연모,친애
		IF TALENT:3 || TALENT:6
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				PRINTFORMW 
				PRINTFORMW 「헤헤,기다리고 있을 수 없다는 얼굴이구나 대장. 그런 눈으로 볼 수 있으면,오슬오슬 하네요」
			ELSE
				PRINTFORMW 
			ENDIF
		ELSE
			PRINTFORMW 
			PRINTFORMW 「이것으로 좋은가?」
		ENDIF
	;하카마를 벗게 한다
	;ELSEIF A == 4 && T:4 == 18
	ENDIF
ENDIF



;==================================================
;소질 취득시
;==================================================
;-------------------------------------------------
;연모를 취득했다
;-------------------------------------------------
IF TFLAG:13 == 46
	IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
		PRINTFORMW 「이봐 대장,가슴가 괴롭다. …… 적당,두괄등 없으면 안 되는가」
	ELSE
		PRINTFORMW 
	ENDIF
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;음란를 취득했다
;-------------------------------------------------
IF TFLAG:13 == 47
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;복종를 취득했다
;-------------------------------------------------
IF TFLAG:13 == 48
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;친애를 취득했다
;-------------------------------------------------
IF TFLAG:13 == 49
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;창부를 취득했다
;-------------------------------------------------
IF TFLAG:13 == 50
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;예속를 취득
;-------------------------------------------------
IF TFLAG:13 == 51
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;붕괴
;-------------------------------------------------
IF TFLAG:13 == 52
	CALL EVENT_MESSAGE
	;상사상애로 상대가 매각되었다
	IF TFLAG:53 == 1
	;임신가 원인으로붕괴 했다
	ELSEIF TFLAG:53 == 2
	;격렬한 조교에 의해붕괴 했다
	ELSEIF TFLAG:53 == 3
	;약에 의해붕괴 했다
	ELSEIF TFLAG:53 == 4
	ENDIF
ENDIF

;-------------------------------------------
;부적효과 발휘시(도검 부활)
;-------------------------------------------
IF TFLAG:13 == 72
	CALL EVENT_MESSAGE
	;상사상애로 상대가 매각되었다
	IF TFLAG:53 == 1
	;임신가 원인으로붕괴 했다
	ELSEIF TFLAG:53 == 2
	;격렬한 조교에 의해붕괴 했다
	ELSEIF TFLAG:53 == 3
	;약에 의해붕괴 했다
	ELSEIF TFLAG:53 == 4
	ENDIF
ENDIF

;-------------------------------------------------
;상사상애취득
;-------------------------------------------------
IF TFLAG:13 == 53
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;섹스중독취득
;-------------------------------------------------
IF TFLAG:13 == 55
	PRINTFORMW 
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;자위광취득
;-------------------------------------------------
IF TFLAG:13 == 56
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;음란구멍취득
;-------------------------------------------------
IF TFLAG:13 == 57
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;항문광취득
;-------------------------------------------------
IF TFLAG:13 == 58
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;음란가슴취득
;-------------------------------------------------
IF TFLAG:13 == 59
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;즉각함락취득
;-------------------------------------------------
IF TFLAG:13 == 60
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;새드취득
;-------------------------------------------------
IF TFLAG:13 == 61
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;마조취득
;-------------------------------------------------
IF TFLAG:13 == 62
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;도S취득
;-------------------------------------------------
IF TFLAG:13 == 63
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;도M취득
;-------------------------------------------------
IF TFLAG:13 == 64
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;실금버릇취득
;-------------------------------------------------
IF TFLAG:13 == 65
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;유아 퇴행 했다
;-------------------------------------------------
IF TFLAG:13 == 66
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;정통시
;-------------------------------------------------
IF TFLAG:13 == 70
	CALL EVENT_MESSAGE
ENDIF

;-------------------------------------------------
;방뇨 혹은 배변 했을 때 CFLAG:248
;-------------------------------------------------
IF TFLAG:13 == 71
	CALL EVENT_MESSAGE
	CFLAG:248 = 1
ENDIF

;-------------------------------------------------
;미약의 효과가 끊어졌을 때 CFLAG:256
;-------------------------------------------------
IF TFLAG:13 == 73
	CALL EVENT_MESSAGE
		PRINTFORMW %CALLNAME%는 당신을 껴안는 것처럼 팔을 돌려,미간을 대었고 장독면에서 신음했다.
		PRINTFORMW 「이만큼 힘든 약,나에뿐 효과 나온다 라고 이상할 것이다……!」
	CFLAG:256 = 1
ENDIF



;==================================================
;임신·출산·육아 관련
;==================================================
;-------------------------------------------------
;황색의 환약 사용시 CFLAG:270
;강제 유산
;-------------------------------------------------
IF TFLAG:13 == 10
	CALL EVENT_MESSAGE
	CFLAG:270 = 1
ENDIF

;-------------------------------------------------
;임신발각시 CFLAG:271
;-------------------------------------------------
IF TFLAG:13 == 11
	CALL EVENT_MESSAGE
	;처음
	IF CFLAG:271 == 0
		;부친이 주인으로 모친이연모나친애소유
		IF (TALENT:3 || TALENT:6) && CFLAG:101 % 100 == 2
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				PRINTFORML 「…… 내가 임신했다야아?」
				PRINTFORML 「그토록 자식 따돌려지면 그것은 그렇게 될 것이다……」
				PRINTFORMW 쓴웃음 짓고 있는%CALLNAME%에,계획대로웃으면 눈을 끔뻑깜박이게 했다.
				PRINTFORML 와손를 턱에 맞히고 잠깐 입다문후,%CALLNAME%입는 구구법,이라고 그야말로 즐거운 듯이 웃으면서 %CALLNAME:MASTER%를 올려본다.
				PRINTFORML 「무엇이다 대장,그렇게 암잉키 싶었던 것일까?」
				PRINTFORML 그렇다,라고 하면%CALLNAME%는 좋은 기분인 채 %CALLNAME:MASTER%에 다가서 그손를 취했다.
				PRINTFORML %CALLNAME%는 자신의손에 %CALLNAME:MASTER%의손를 거듭해 그 얇은 배를 어루만지면 절절히 중얼거렸다.
				PRINTFORMW 「여기에,대장과 나치노아이가 있지 않는가. 이상하지만,기쁘고 좋은 기분이다」
			ELSE
				PRINTFORMW 
			ENDIF
		ELSE
			PRINTFORMW 
		ENDIF
	CFLAG:271 = 1
	;그 이외
	ELSEIF CFLAG:271 < 1 || FLAG:7 == 2
		;부친이 주인으로 모친이연모나친애소유
		IF (TALENT:3 || TALENT:6) && CFLAG:101 % 100 == 2
			IF NO:MASTER == 0 || (ASSI > 0 && NO:ASSI == 0)
				PRINTFORML 「…… 내가임신 해도?」
				PRINTFORML 확실히 낳아 키워 주고라고 말하면%CALLNAME%는 숙여 이마의 근처를 집게 손가락으로 끊임없이 긁고 있다.
				PRINTFORMW 「…… 응. 대장이 그렇게 말한다면…… 맡겨 두어라」
				PRINTFORMW 숙여도 들여다 보는%CALLNAME%의 목덜미나 귀가 새빨갛게 물들고 있는 것을 봐 %CALLNAME:MASTER%는 빙그레 하고 있다…
			ELSE
				PRINTFORMW 
			ENDIF
		ELSE
			PRINTFORMW 
		ENDIF
	ENDIF
	CFLAG:271 = 1
ENDIF

;-------------------------------------------------
;출산시 CFLAG:272
;-------------------------------------------------
IF TFLAG:13 == 12
	CALL EVENT_MESSAGE
	CFLAG:272 = 1
ENDIF

;-------------------------------------------------
;육아실 CFLAG:273
;-------------------------------------------------
IF TFLAG:13 == 13
	CALL EVENT_MESSAGE
	CFLAG:273 = 1
ENDIF

;-------------------------------------------------
;자립시 CFLAG:274
;-------------------------------------------------
IF TFLAG:13 == 14
	CALL EVENT_MESSAGE
	CFLAG:274 = 1
ENDIF

;-------------------------------------------------
;아이도 조교하는 것을 선택한 CFLAG:501
;-------------------------------------------------
IF TFLAG:13 == 45
	CALL EVENT_MESSAGE
	CFLAG:501 = 1
ENDIF


