;==================================================
;00입상
;==================================================
;=================================================
;개별 일상 이벤트
;·「일상을 본다」로 00이 메인 캐릭터로 선택되었을 경우에 불린다
;·캐릭터가연모함락＞음란·복종함락＞미함락＞공범자의 순서로 불리기 쉬워진다
;·RETURN 0으로 범용 일상 이벤트 호출해,RETURN 1하면 범용 일상 이벤트는 표시되지 않게 된다
;·공범자 변경 기능 OFF가 되어 있으므로,연모/친애/상사상애/복종/예속는 주인에 대해서의 함락시 이외 없어진다
;=================================================
@DAILY_LIFE_MASSAGE_KX1
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;개별 일상 이벤트예
	
	;확률로 범용 일상 이벤트도 호출한다
	;3 분의 2의 확률로 범용 일상 이벤트의 표시
	;SIF RAND:3 > 0
	;	RETURN 0
	
	;이벤트의 발생 조건
	;주인과[상사상애]때만 발생한다
	;SIF !TALENT:상사상애
	;	RETURN 0
	
	;이벤트 본문
	PRINTFORMW 
	PRINTFORMW 
	
	RETURN LOCAL
ENDIF


