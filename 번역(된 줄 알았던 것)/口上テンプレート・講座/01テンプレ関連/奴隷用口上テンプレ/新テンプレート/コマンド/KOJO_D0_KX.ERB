;==================================================
;00대사
;==================================================
;==================================================
;커맨드 실행 판정에 관련되는대사
;==================================================
;=================================================
;커맨드 실행 판정의 조작
;V에 실행 난이도,A에 실행치가 들어가 있어,A < V라면 실행 불가
;실행치는 캐릭터에 의해 차이가 있으므로,어느 정도 조작할까는 실제의 플레이를 참고로 해 주세요
;=================================================
@KOJO_DENIAL_PLUS_KX1
;;실행 판정 조작의 예
;반발각인 3라면 실행치를 0에(강제적으로 실행 불가)
;IF MARK:3 == 3
;	PRINTFORM - 반발각인LV3({A})
;	A = 0
;ENDIF

;;반발각인 2라면 실행치-50(함락이 끝난 상태라면 상당히 이것이라도 실행 가능)
;IF MARK:3 == 2
;	PRINTFORM - 반발각인LV2(50)
;	A -= 50
;ENDIF

;;반발각인 1 이상으로 커맨드:키스 선택때 실행치를 0에
;IF MARK:3 >= 1 && SELECTCOM == 20
;	PRINTFORM - 반발각인({A})
;	A = 0
;ENDIF

;=================================================
;커맨드 실행 거부대사
;=================================================
;-------------------------------------------------
;EX,범용 커맨드 실행 거부대사
;개별 커맨드 실행 거부대사이 쓰여지지 않으면 표시된다
;-------------------------------------------------
@KOJO_EXDENIAL_KX1
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL DENIAL_MESSAGE_COLOR
	
	PRINTFORMW 
	PRINTFORMW 
ENDIF
RETURN LOCAL

;-------------------------------------------------
;개별 커맨드 실행 거부대사
;이쪽이 쓰여지지 않았다고 범용 커맨드 실행 거부대사이 불린다
;-------------------------------------------------
;-------------------------------------------------
;3,비패개장실행 거부대사 ※샘플
;-------------------------------------------------
@KOJO_DENIAL_KX1_3
;-------------------------------------------------
;기입 체크(=0,비표시,1,표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;지문 호출(칼라 변경 대응)
	CALL DENIAL_MESSAGE_COLOR
	
	PRINTFORMW 
	PRINTFORMW 
ENDIF
RETURN LOCAL

