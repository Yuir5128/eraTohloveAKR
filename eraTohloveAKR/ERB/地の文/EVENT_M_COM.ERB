
;조교시 텍스트

;===========================================================
;애무계
;===========================================================
;-------------------------------------------------
;0,애무
;-------------------------------------------------
@MESSAGE_COM0
PRINTFORM %CALLNAME:PLAYER%는

;착의 플레이
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3 && !TEQUIP:정조대
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	;스커트
	ELSE
		PRINT 스커트를 걷어 올리면,
	ENDIF
ENDIF
IF TEQUIP:속옷（아래） || TEQUIP:속옷（위） || BOTTOMS(TARGET) >= 4 || TEQUIP:상의 || TOPS(TARGET)
	PRINT 옷 위로부터
ENDIF

PRINTFORM %CALLNAME%의
;임신
IF TALENT:임신
	IF (CFLAG:110 - 30) >= DAY
		PRINT 배의 부푼 곳이 눈에 띄어 왔다
	ELSE
		PRINT 둥글게 배의 부풀었다
	ENDIF
;작은체구
ELSEIF TALENT:작은체구
	PRINT 날씬한
;소인체형
ELSEIF TALENT:소인체형
	PRINT 아이 같아 보였다
;큰체구
ELSEIF TALENT:큰체구
	PRINT 씩씩하다
ENDIF
PRINTFORM 체를\@ TALENT:PLAYER:한팔결손? 편손로 # 손로\@열심히애무 했다…


;-------------------------------------------------
;1,키스한다
;-------------------------------------------------
@MESSAGE_COM1
IF TALENT:기절
	PRINTFORML %CALLNAME:MASTER%의 키스로%CALLNAME%는 의식을 되찾은 것 같다…
	RETURN 0
ENDIF

;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%에 가까워져,입술을 거듭해 왔다…
;통상 모드
ELSE
	;조수가 함락 대상
	IF ASSI > -1 && MASTER_SEARCH2(TARGET,ASSI)
		;주인 조교중
		IF MASTER == PLAYER
			;주인의 함락 대상이 조수나 노예
			IF MASTER_SEARCH2(MASTER,ASSI) || MASTER_SEARCH2(MASTER,TARGET)
				PRINTFORML %CALLNAME:MASTER%는 %CALLNAME:ASSI%에 과시하듯이해%CALLNAME%의 입술을 탐냈다…
			ELSE
				PRINTFORML %CALLNAME:ASSI%의 눈을 신경쓰는%CALLNAME%의 입술을 빼앗아,욕망대로 탐냈다…
			ENDIF
		;조수조교중
		ELSE
			PRINTFORML %CALLNAME%는\@ KANRAKU(TARGET) == 4? 사랑스러울 것 같다 # 기쁠 것 같다\@에 %CALLNAME:PLAYER%에 다가서,
			PRINTFORML 적극적으로 혀를 서로 걸고…
			;주인의 함락 대상이 조수나 노예
			SIF MASTER_SEARCH2(MASTER,ASSI) || MASTER_SEARCH2(MASTER,TARGET)
				PRINTFORML 그 모습은 옆으로부터 보고 있어질투심을 느낄 정도다…
		ENDIF
	;조수조교중
	ELSEIF ASSI > -1 && ASSI == PLAYER
		PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 얼굴에 가까워져,입술에 입술을 거듭했다…
	ELSE
		IF KANRAKU(TARGET) > 0 && KANRAKU(TARGET) < 4
			PRINTFORML %CALLNAME:PLAYER%가%CALLNAME%의 얼굴에 입술을 가까이 하면,
			PRINTFORM %CALLNAME%는\@ BIND_CHECK_B(5) ? 신체를 갖다대고 # 목에손를 돌려 껴안아\@
			SELECTCASE KANRAKU(TARGET)
				CASE 1
					PRINTL 입술에 입술을 거듭했다…
				CASE 2
					PRINTL 정열적으로 혀를 걸어 왔다…
				CASEELSE
					PRINTL 키스에 응했다…
			ENDSELECT
		ELSE
			PRINTFORM %CALLNAME%의입강을 구석구석까지 빨고 회
			;순종LV3 이상＋게이경험 2000 이상
			IF ABL:순종 >= 3 && EXP:게이경험 >= 2000
				PRINTFORM 해,혀를 서로 걸면서 타액을 교환했다…
			;순종LV3 이상
			ELSEIF ABL:순종 >= 3
				IF TALENT:반항적 || TALENT:겁쟁이 || TALENT:스킨십불편 || TALENT:부끄럼쟁이 || TALENT:정조관념 || MARK:반발각인
					PRINTFORM 와%CALLNAME%도 흠칫흠칫 혀를 걸어 왔다…
				ELSE
					PRINTFORM 와%CALLNAME%도 적극적으로 혀를 걸어 왔다…
				ENDIF
			ELSE
				PRINTFORM 했다…
			ENDIF
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;2,아무것도안한다
;-------------------------------------------------
@MESSAGE_COM2
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 아무것도 하지 않고,%CALLNAME:PLAYER%를 응시하고 있다…
ELSE
	PRINTFORML %CALLNAME:MASTER%는,잠깐%CALLNAME%를 방치했다…
ENDIF

;-------------------------------------------------
;3,비패개장
;-------------------------------------------------
@MESSAGE_COM3

;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는
	
	;착의 때
	IF TEQUIP:속옷（아래） || TEQUIP:하의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINTL 슈트의 지퍼를 열어,
		;바지
		ELSEIF BOTTOMS(TARGET) >= 4
			SIF TEQUIP:속옷（아래）
				PRINT 속옷마다
			PRINTFORML 바지를 내려,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINTL 스커트를 걷어 올려,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINTL 의 옷자락을 걷어 올려,
		;속옷만
		ELSEIF TEQUIP:속옷（아래）
			PRINTL 속옷을 내려,
		ENDIF
	ENDIF
	PRINTFORM 자신의
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열에
	ELSE
		PRINT 쿠레바스에
	ENDIF
	PRINTFORML 손가락을 주어,좌우에 펼쳐 과시해 왔다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME%에,자신의 비부[秘部]를 넓혀 보이듯이 명령했다
	
	PRINTFORM %CALLNAME%는
	;노출벽
	IF ABL:노출벽 == 0
		IF (TALENT:수치심없음 || TALENT:감정부족 || TALENT:무관심) && !TALENT:부끄럼쟁이
			PRINTFORML 귀찮은 것 같게 이쪽을 노려보았지만,
		ELSE
			PRINTFORML 눈물을 머금어 이쪽을 노려보았지만,
		ENDIF
		PRINTFORM 재차 명령되면,마지못해라고 하는 모습으로
	ELSEIF ABL:노출벽 == 1
		SIF !TALENT:수치심없음
			PRINTFORM 얼굴을 새빨갛게 하면서도
	ELSEIF ABL:노출벽 >= 3
		PRINTFORM 희희낙락 한 표정으로
	ENDIF
	
	;착의 때
	IF TEQUIP:속옷（아래） || TEQUIP:하의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINTL 슈트의 지퍼를 열어,
		;바지
		ELSEIF BOTTOMS(TARGET) >= 4
			SIF TEQUIP:속옷（아래）
				PRINT 속옷마다
			PRINTFORML 바지를 내려,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINTL 스커트를 걷어 올려,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINTL 의 옷자락을 걷어 올려,
		;속옷만
		ELSEIF TEQUIP:속옷（아래）
			PRINTL 속옷을 내려,
		ENDIF
	ENDIF
	
	;노출벽LV5 이상＋음란or창부
	IF ABL:노출벽 >= 5 && (TALENT:음란 || TALENT:창부)
		PRINTFORML 양손의 손가락을 음순에 걸어,질구 마다 %CALLNAME:PLAYER%에 과시하듯이 크게 펼쳤다
		IF TFLAG:38  > 0
			PRINTFORML 꿈틀거리는질내로부터는,조금 전사정 한 정액이,걸죽 야무지지 못하게 흘러넘쳐 왔다…
		ELSE
			PRINTFORML 부드러운 것 같은질내는,충분히 점액으로 젖어,이쪽을 권하는 것처럼,흠칫흠칫 꿈틀거리고 있다…
		ENDIF
	ELSEIF ABL:노출벽 >= 5
		PRINTFORML 양손의 손가락을 음순에 걸어,질구 마다 %CALLNAME:PLAYER%에 과시하듯이 크게 펼쳤다
		IF TFLAG:38 > 0
			PRINTFORML 크게 펼칠 수 있었던 질구는,조금 전사정 한 정액을 흘려 보내고 있다…
		ELSE
			PRINTFORML %CALLNAME%의손가질내부까지 펼쳐 보이면,질벽이 호흡하듯이 빠끔빠끔움직여,
			PRINTFORML 다 퍼진 질구는,애액을 끝 없게 흘러넘치게 하고 있다…
		ENDIF
	ELSEIF ABL:노출벽 >= 3
		PRINTFORML 양손의 손가락을 사용해 음순 뿐만이 아니라,질구도 동시에 펼쳤다
		IF TFLAG:38 > 0
			PRINTFORM %CALLNAME%의손가 질구를 한층 더 좌우에 펼치면,
			PRINTFORML 펼쳐진들이입는,조금 전사정 한 정액을 흘려 보내고 있다…
		ELSE
			PRINTFORML 질내를 볼 수 있는 일에 느끼고 있는지,%CALLNAME%의 뺨은 은은하게 붉게 물들고 있다
		ENDIF
	ELSEIF ABL:노출벽 == 2
		PRINTFORML 집게 손가락과 중지로 싸인을 만들듯이,%CALLNAME:PLAYER%에 자신의 비밀스런 장소를 노출했다
		IF TFLAG:38 > 0
			PRINTFORML 드러내진 질구로부터는,조금 전사정 한 정액이,보글보글 흘러넘쳐 온다…
		ELSE
			PRINTFORML 퍼진 여음[女陰]으로부터 이쪽을 들여다 보는 질구는,거두어야 할 물건을 기다려 바라는것 같이,애액에 젖어,추잡하게 꿈틀거리고 있다…
		ENDIF
	ELSEIF ABL:노출벽 == 1
		IF TALENT:수치심없음
			PRINTFORML 성기를 분명히 펼쳐 보였다
		ELSE
			PRINTFORML 흠칫흠칫,성기를 분명히 펼쳐 보였다
		ENDIF
	ELSE
		PRINTFORM 자신의
		;작은체구
		IF TALENT:작은체구
			PRINT 어린 균열에
		ELSE
			PRINT 쿠레바스에
		ENDIF
		PRINTFORML 손가락을 주어,좌우에 펼쳐 보였다
	ENDIF
ENDIF


;-------------------------------------------------
;4,자위
;-------------------------------------------------
@MESSAGE_COM4

;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓히면,
		ELSE
			PRINT 를 찢으면,
		ENDIF
	ENDIF
;통상 모드
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀 주어
		ELSE
			PRINT 를 찢어 주어
		ENDIF
	ENDIF
	
	PRINTFORM 자위를 명령하면,%CALLNAME%는
	;순종LV3 미만
	IF ABL:순종 < 3
		;프라이드높음
		SIF TALENT:프라이드높음
			PRINT 프라이드의 높이를 보이면서
		;반항적
		IF TALENT:반항적
			PRINT 반항적인 태도로 노려봐 왔지만,재차의 명령에 마지못해
		;부끄럼쟁이
		ELSEIF TALENT:부끄럼쟁이
			PRINT 부끄러움에 귀까지 새빨갛게 되면서,
		;꿋꿋함
		ELSEIF TALENT:꿋꿋함
			PRINT 굴욕에 입술을 악물어,
		;겁쟁이,점잖다
		ELSEIF TALENT:겁쟁이 || TALENT:얌전함
			PRINT 떨리는 손가락끝으로
		;무관심,감정부족
		ELSEIF TALENT:무관심 || TALENT:감정부족
			PRINT 무표정한 얼굴로 묵묵히
		ELSE
			PRINT 마지못해
		ENDIF
	;순종LV5 미만
	ELSEIF ABL:순종 < 5
		PRINT 조교의 성과인가,얌전하게 명령에 따라,
	ENDIF
ENDIF

;비디오촬영중
SIF TEQUIP:비디오카메라
	PRINT 비디오카메라의 앞에서
;노출벽LV3 이상
SIF ABL:노출벽 >= 3
	PRINT 과시하듯이해

;착의 때
IF TEQUIP:속옷（아래） || TEQUIP:하의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열어,
	;바지
	ELSEIF BOTTOMS(TARGET) >= 4
		SIF TEQUIP:속옷（아래）
			PRINT 속옷마다
		PRINTFORM 바지를 내려,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINTL 스커트를 걷어 올려,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINTL 의 옷자락을 걷어 올려,
	;속옷만
	ELSEIF TEQUIP:속옷（아래）
		PRINT 속옷을 내려,
	ENDIF
ENDIF

PRINTFORML 

;바이브
IF TEQUIP:바이브
	PRINTFORM 바기나에 넣어진바이브를 스스로 삽입질 하면서
;로터
ELSEIF TEQUIP:로터삽입
	PRINTFORM 로터의 끈을 느슨하게 이끌면서
ELSEIF PENIS(TARGET)
	;아이사이즈
	IF TALENT:아이사이즈
		;소인체형가 아니다
		SIF TALENT:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:말자지
		;소인체형
		IF TALENT:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:포경
		PRINT 가죽 관
	PRINTFORM 페니스를 스스로의손로 쓸어올려
ELSE
	PRINTFORM 비밀스런 장소를 스스로의 손가락으로 만지작거려
ENDIF

IF SELECTCOM == 4 || SELECTCOM == 5 || SELECTCOM == 6 || SELECTCOM == 7
	PRINTFORM 쾌감을 맛보고 있다…
ELSE
	PRINTFORM 자위를 시작했다…
ENDIF

;-------------------------------------------------
;5,가슴자위
;-------------------------------------------------
@MESSAGE_COM5
PRINTFORM %CALLNAME%는

;비디오카메라촬영중＋노출벽LV3 이상
IF TEQUIP:비디오카메라 && ABL:노출벽 >= 3
	PRINT 카메라에 과시하듯이해
;비디오카메라촬영중
ELSEIF TEQUIP:비디오카메라
	PRINT 카메라의 앞에서
;노출벽LV3 이상
ELSEIF ABL:노출벽 >= 3
	PRINT 과시하듯이해
ENDIF

;착의 때
IF TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET)
	IF TEQUIP:상의 || TEQUIP:전신상의
		PRINTFORM 윗도리를 걷어 올리고
	ELSEIF TEQUIP:속옷（위） >= 4
		A = 3
		CALL CLOTHES_NAME(TARGET)
		PRINT 를 걷어 올리고
	ELSE
		PRINT 속옷을 비켜 놓아
	ENDIF
ENDIF

SIF TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET) || TEQUIP:비디오카메라 || ABL:노출벽 >= 3
	PRINTFORML ,

IF TEQUIP:니플캡
	PRINTFORMW 니플캡를 이끌어
ELSEIF TEQUIP:유두클립
	PRINTFORMW 유두클립를 이끌어
ELSEIF TEQUIP:착유기
	PRINTFORMW 착유기를 동요시켜,
ELSE
	;빈유
	SIF TALENT:빈유
		PRINT 그 소극적인
	;거유
	SIF TALENT:거유
		PRINT 그 큰
	;폭유
	SIF TALENT:폭유
		PRINT 그 거대한
	;모유체질
	SIF TALENT:모유체질
		PRINT 모유가 방울져 떨어진다
	PRINTFORM 가슴를 스스로의 손가락으로 만지작거리면서
ENDIF

IF SELECTCOM == 4 || SELECTCOM == 5 || SELECTCOM == 6 || SELECTCOM == 7
	PRINTFORM 쾌감을 맛보고 있다…
ELSE
	PRINTFORM 가슴자위를 시작했다…
ENDIF


;-------------------------------------------------
;6,자위
;-------------------------------------------------
@MESSAGE_COM6

;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓히면,
		ELSE
			PRINT 를 찢으면,
		ENDIF
	ENDIF
;통상 모드
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀 주어
		ELSE
			PRINT 를 찢어 주어
		ENDIF
	ENDIF
	
	PRINTFORM 자위를 명령하면,%CALLNAME%는
	;순종LV3 미만
	IF ABL:순종 < 3
		;프라이드높음
		SIF TALENT:프라이드높음
			PRINT 프라이드의 높이를 보이면서
		;반항적
		IF TALENT:반항적
			PRINT 반항적인 태도로 노려봐 왔지만,재차의 명령에 마지못해
		;부끄럼쟁이
		ELSEIF TALENT:부끄럼쟁이
			PRINT 부끄러움에 귀까지 새빨갛게 되면서,
		;꿋꿋함
		ELSEIF TALENT:꿋꿋함
			PRINT 굴욕에 입술을 악물어,
		;겁쟁이,점잖다
		ELSEIF TALENT:겁쟁이 || TALENT:얌전함
			PRINT 떨리는 손가락끝으로
		;무관심,감정부족
		ELSEIF TALENT:무관심 || TALENT:감정부족
			PRINT 무표정한 얼굴로 묵묵히
		ELSE
			PRINT 마지못해
		ENDIF
	;순종LV5 미만
	ELSEIF ABL:순종 < 5
		PRINT 조교의 성과인가,얌전하게 명령에 따라,
	ENDIF
ENDIF

;비디오촬영중
SIF TEQUIP:비디오카메라
	PRINT 비디오카메라의 앞에서
;노출벽LV3 이상
SIF ABL:노출벽 >= 3
	PRINT 과시하듯이해

;착의 때
IF TEQUIP:속옷（아래） || TEQUIP:하의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열어,
	;바지
	ELSEIF BOTTOMS(TARGET) >= 4
		SIF TEQUIP:속옷（아래）
			PRINT 속옷마다
		PRINTFORM 바지를 내려,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINTL 스커트를 걷어 올려,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINTL 의 옷자락을 걷어 올려,
	;정조대
	ELSEIF TEQUIP:정조대
		PRINTFORM 정조대의 틈새로부터,
	;속옷만
	ELSEIF TEQUIP:속옷（아래）
		PRINT 속옷을 내려,
	ENDIF
ENDIF
PRINTFORML 

IF TEQUIP:애널바이브
	PRINTFORM 아누스로부터 얼굴을 내밀고 있는바이브를 만지작거리고 있다…
ELSEIF TEQUIP:애널비즈
	PRINTFORM 애널비즈를 뽑아 내거나 밀어넣거나 하고 즐기기시작했다…
ELSEIF TEQUIP:로터A삽입
	PRINTFORM 아누스로터의 끈을 잡아당겨 쾌감을 맛보고 있다…
ELSEIF TEQUIP:확장벌룬
	PRINTFORM 확장벌룬를 동요시켜 쾌감을 맛보고 있다…
ELSEIF ABL:A감각 > 2
	PRINTFORM 부드러운 엉덩이의 구멍을 만지작거리고 있다…
ELSEIF ABL:A감각 > 1
	PRINTFORM 엉덩이의 구멍이 좋은 곳을 찾듯이 손가락을 움직이고 있다…
ELSE
	PRINTFORM 슬슬 엉덩이의 구멍에 손가락을 넣었다…
ENDIF


;-------------------------------------------------
;7,항문자위
;-------------------------------------------------
@MESSAGE_COM7

;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓히면,
		ELSE
			PRINT 를 찢으면,
		ENDIF
	ENDIF
;통상 모드
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀 주어
		ELSE
			PRINT 를 찢어 주어
		ENDIF
	ENDIF
	
	PRINTFORM 자위를 명령하면,%CALLNAME%는
	;순종LV3 미만
	IF ABL:순종 < 3
		;프라이드높음
		SIF TALENT:프라이드높음
			PRINT 프라이드의 높이를 보이면서
		;반항적
		IF TALENT:반항적
			PRINT 반항적인 태도로 노려봐 왔지만,재차의 명령에 마지못해
		;부끄럼쟁이
		ELSEIF TALENT:부끄럼쟁이
			PRINT 부끄러움에 귀까지 새빨갛게 되면서,
		;꿋꿋함
		ELSEIF TALENT:꿋꿋함
			PRINT 굴욕에 입술을 악물어,
		;겁쟁이,점잖다
		ELSEIF TALENT:겁쟁이 || TALENT:얌전함
			PRINT 떨리는 손가락끝으로
		;무관심,감정부족
		ELSEIF TALENT:무관심 || TALENT:감정부족
			PRINT 무표정한 얼굴로 묵묵히
		ELSE
			PRINT 마지못해
		ENDIF
	;순종LV5 미만
	ELSEIF ABL:순종 < 5
		PRINT 조교의 성과인가,얌전하게 명령에 따라,
	ENDIF
ENDIF

;비디오촬영중
SIF TEQUIP:비디오카메라
	PRINT 비디오카메라의 앞에서
;노출벽LV3 이상
SIF ABL:노출벽 >= 3
	PRINT 과시하듯이해

;착의 때
IF TEQUIP:속옷（아래） || TEQUIP:하의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열어,
	;바지
	ELSEIF BOTTOMS(TARGET) >= 4
		SIF TEQUIP:속옷（아래）
			PRINT 속옷마다
		PRINTFORM 바지를 내려,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINTL 스커트를 걷어 올려,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINTL 의 옷자락을 걷어 올려,
	;속옷만
	ELSEIF TEQUIP:속옷（아래）
		PRINT 속옷을 내려,
	ENDIF
ENDIF

PRINTFORML 

;양쪽 모두에 완구
IF (TEQUIP:바이브 || TEQUIP:로터삽입) && (TEQUIP:애널바이브 || TEQUIP:애널비즈 || TEQUIP:로터A삽입 || TEQUIP:확장벌룬)
;바이브＋애널바이브
	PRINTFORM 물었다
	IF TEQUIP:바이브 && TEQUIP:애널바이브
		PRINTFORM 2개의바이브를 스스로 삽입질 하면서
	ELSE
		PRINTFORM 2개의 완구를 스스로 빼고 꽂기 하면서
	ENDIF
ELSE
	;V인 만큼 완구
	IF TEQUIP:바이브 || TEQUIP:로터삽입
		;바이브
		IF TEQUIP:바이브
			PRINTFORM 바기나에 넣어진바이브를 스스로 삽입질 하면서
		;로터
		ELSEIF TEQUIP:로터삽입
			PRINTFORM 로터의 끈을 느슨하게 이끌면서
		ENDIF
	;A인 만큼 완구
	ELSEIF TEQUIP:애널바이브 || TEQUIP:애널비즈 || TEQUIP:로터A삽입 || TEQUIP:확장벌룬
		;애널바이브
		IF TEQUIP:애널바이브
			PRINTFORM 돌진해지고 있는애널바이브를 스스로 삽입질 하면서
		;애널비즈
		ELSEIF TEQUIP:애널비즈
			PRINTFORM 돌진해지고 있는애널비즈를 스스로 삽입질 하면서
		;로터
		ELSEIF TEQUIP:로터A삽입
			PRINTFORM 애널로터의 끈을 느슨하게 이끌면서
		ELSEIF TEQUIP:확장벌룬
			PRINTFORM 확장벌룬를 동요시키면서
		ENDIF
	;완구 없음
	ELSE
		PRINTFORM 전후의 구멍을 스스로의 손가락으로 만지작거려
	ENDIF
ENDIF

IF SELECTCOM == 4 || SELECTCOM == 5 || SELECTCOM == 6 || SELECTCOM == 7
	PRINTFORM ,쾌감을 맛보고 있다…
ELSE
	PRINTFORM ,항문자위를 시작했다…
ENDIF

;-------------------------------------------------
;8,애널개방
;-------------------------------------------------
@MESSAGE_COM8
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는
	
	;착의 때
	IF TEQUIP:속옷（아래） || TEQUIP:하의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINTL 슈트의 지퍼를 열어,
		;바지
		ELSEIF BOTTOMS(TARGET) >= 4
			SIF TEQUIP:속옷（아래）
				PRINT 속옷마다
			PRINTFORML 바지를 내려,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINTL 스커트를 걷어 올려,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINTL 의 옷자락을 걷어 올려,
		;정조대
		ELSEIF TEQUIP:정조대
			PRINTL 정조대의 틈새로부터,
		;속옷만
		ELSEIF TEQUIP:속옷（아래）
			PRINTL 속옷을 내려,
		ENDIF
	ENDIF
	PRINTFORM 자신의 항문에 양손를 주어,좌우에 펼쳐 과시해 왔다…
	
;통상 모드
ELSE
	PRINTFORML %CALLNAME%에,자신의 항문을 벌려 보이듯이 명령했다
	
	PRINTFORM %CALLNAME%는
	;노출벽
	IF ABL:노출벽 == 0
		IF (TALENT:수치심없음 || TALENT:감정부족 || TALENT:무관심) && !TALENT:부끄럼쟁이
			PRINTFORML 귀찮은 것 같게 이쪽을 노려보았지만,
		ELSE
			PRINTFORML 눈물을 머금어 이쪽을 노려보았지만,
		ENDIF
		PRINTFORM 재차 명령되면,마지못해라고 하는 모습으로
	ELSEIF ABL:노출벽 == 1
		SIF !TALENT:수치심없음
			PRINTFORM 얼굴을 새빨갛게 하면서도
	ELSEIF ABL:노출벽 >= 3
		PRINTFORM 희희낙락 한 표정으로
	ENDIF
	
	;착의 때
	IF TEQUIP:속옷（아래） || TEQUIP:하의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINTL 슈트의 지퍼를 열어,
		;바지
		ELSEIF BOTTOMS(TARGET) >= 4
			SIF TEQUIP:속옷（아래）
				PRINT 속옷마다
			PRINTFORML 바지를 내려,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINTL 스커트를 걷어 올려,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINTL 의 옷자락을 걷어 올려,
		;정조대
		ELSEIF TEQUIP:정조대
			PRINTFORM 정조대의 틈새로부터,
		;속옷만
		ELSEIF TEQUIP:속옷（아래）
			PRINTL 속옷을 내려,
		ENDIF
	ENDIF
	
	;노출벽LV5 이상＋음란or창부
	IF ABL:노출벽 >= 5 && (TALENT:음란 || TALENT:창부)
		PRINTFORML 양손로 자신의 항문을 %CALLNAME:PLAYER%에 과시하듯이 크게 펼쳤다
		IF STAIN:장내 & 4
			PRINTFORML 추잡하게 꿈틀거리는장내로부터는,야무지지 못하게 정액이 흘려 보내지고 있다…
		ELSE
			PRINTFORML 부드러운 것 같은장내는,충분히 점액으로 젖어,이쪽을 권하는 것처럼,흠칫흠칫 꿈틀거리고 있다…
		ENDIF
	ELSEIF ABL:노출벽 >= 5
		PRINTFORML 양손로 자신의 항문을 %CALLNAME:PLAYER%에 과시하듯이 크게 펼쳤다
		IF STAIN:장내 & 4
			PRINTFORML 추잡하게 꿈틀거리는장내로부터는,야무지지 못하게 정액이 흘러넘치고 있다…
		ELSE
			PRINTFORML 흠칫흠칫 꿈틀거리는 구멍으로부터는,점액을 끝 없게 흘러넘치게 하고 있다…
		ENDIF
	ELSEIF ABL:노출벽 >= 3
		PRINTFORML 양손로,%CALLNAME:PLAYER%에 자신의 항문을 펼쳐 보였다
		IF STAIN:장내 & 4
			PRINTFORML 눌러 펼칠 수 있었던 항문으로부터는,정액이 걸죽 흘러 떨어졌다…
		ELSE
			PRINTFORML 중을 볼 수 있는 일에 느끼고 있는지,%CALLNAME%의 뺨은 은은하게 붉게 물들고 있다…
		ENDIF
	ELSEIF ABL:노출벽 == 2
		PRINTFORML 양팔로,%CALLNAME:PLAYER%에 자신의 항문을 펼쳐 보였다
			IF STAIN:장내 & 4
				PRINTFORML 드러내진장내로부터는,정액이 흘러넘치고 있다…
			ELSE
				PRINTFORML 퍼진 구멍은 거두어야 할 물건을 기다려 바라는것 같이,점액에 젖어,추잡하게 꿈틀거리고 있다…
			ENDIF
	ELSEIF ABL:노출벽 == 1
		IF TALENT:수치심없음
			PRINTFORML 항문을 분명히 펼쳐 보였다
		ELSE
			PRINTFORML 주저하면서도 흠칫흠칫,항문을 분명히 펼쳐 보였다
		ENDIF
	ELSE
		PRINTFORM 자신의 항문에 양손를 주어,좌우에 펼쳐 보였다
	ENDIF
ENDIF

;===========================================================
;B계
;===========================================================
;-------------------------------------------------
;10,가슴애무
;-------------------------------------------------
@MESSAGE_COM10
;착의 플레이
IF TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET)
	IF TOPS(TARGET)
		A = 6
	ELSEIF TEQUIP:상의
		A = 5
	ELSEIF TEQUIP:속옷（위）
		A = 3
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
ENDIF

PRINTFORM %CALLNAME%의

;절벽
IF TALENT:절벽
	PRINT 거의 무에 동일하다
;작은체구＋빈유
ELSEIF TALENT:작은체구 && TALENT:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:폭유
	PRINT 손에 남을 만큼 큰
ENDIF

;착의 플레이
IF TALENT:PLAYER:골드핑거 && (TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET))
	PRINTFORM 가슴를 비비듯이애무 했다…
;욕망의 PALAMLV3 이상＋
ELSEIF PALAM:욕정 >= PALAMLV:3 && TEQUIP:착유기 == 0 && TEQUIP:니플캡 == 0
	PRINTFORM 가슴의 첨단의,완전히 딱딱해진 돌기를 집어올렸다…
;작은체구
ELSEIF TALENT:절벽
	PRINTFORM 가슴를 상냥하게 어루만지듯이 접했다…
ELSE
	PRINTFORM 가슴를 비비듯이애무 했다…
ENDIF


;-------------------------------------------------
;11,가슴주무르기
;-------------------------------------------------
@MESSAGE_COM11
;착의 플레이
IF TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET)
	IF TOPS(TARGET)
		A = 6
	ELSEIF TEQUIP:상의
		A = 5
	ELSEIF TEQUIP:속옷（위）
		A = 3
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
ENDIF

PRINTFORM %CALLNAME%의

;욕정의 PALAMLV3 이상
SIF PALAM:욕정 > PALAMLV:3
	PRINT 기대에 떨린다
;작은체구＋빈유
IF TALENT:작은체구 && TALENT:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:폭유
	PRINT 손에 남을 만큼 큰
ENDIF

;고문중
IF TEQUIP:지하실
	PRINTFORM 가슴를,잡듯이 강하게 주물렀다…
;모유체질
ELSEIF TALENT:모유체질
	;욕정의 PALAMLV3 이상＋연모or친애
	IF PALAM:욕정 > PALAMLV:3 && (TALENT:연모 || TALENT:친애)
		PRINTFORM 가슴를 강하게 짜 주면,
		PRINTFORM %CALLNAME%는 교성을 줘
		;빈유
		SIF TALENT:빈유
			PRINT 그 소극적인
		;거유
		SIF TALENT:거유
			PRINT 그 큰
		;폭유
		SIF TALENT:폭유
			PRINT 그 거대한
		PRINTFORML 모유가 방울져 떨어지는가슴를 강조하듯이 내밀어,
		PRINTFORM 보다 강한 자극을 요구했다…
	;욕정의 PALAMLV3 이상
	ELSEIF PALAM:욕정 > PALAMLV:3
		PRINTFORM 가슴를 강하게 짰다…
	ELSE
		PRINTFORM 가슴를 짰다…
	ENDIF
ELSE
	;욕정의 PALAMLV3 이상＋연모or친애
	IF PALAM:욕정 > PALAMLV:3 && (TALENT:연모 || TALENT:친애)
		PRINTFORM 가슴를 강하게 주물러 주면,
		PRINTFORM %CALLNAME%는 교성을 줘
		;빈유
		SIF TALENT:빈유
			PRINT 그 소극적인
		;거유
		SIF TALENT:거유
			PRINT 그 큰
		;폭유
		SIF TALENT:폭유
			PRINT 그 거대한
		PRINTFORML 가슴를 강조하듯이 내밀어,
		PRINTFORM 보다 강한애무를 요구했다…
	;욕정의 PALAMLV3 이상
	ELSEIF PALAM:욕정 > PALAMLV:3
		PRINTFORM 가슴를 강하게 주물렀다…
	ELSE
		PRINTFORM 가슴를 주물렀다…
	ENDIF
ENDIF


;-------------------------------------------------
;12,유두로터
;-------------------------------------------------
@MESSAGE_COM12
PRINTFORM %CALLNAME:PLAYER%는

;착의 플레이
IF TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET)
	IF TOPS(TARGET)
		A = 6
	ELSEIF TEQUIP:상의
		A = 5
	ELSEIF TEQUIP:속옷（위）
		A = 3
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
ENDIF

PRINTFORM %CALLNAME%의
;모유체질
SIF TALENT:모유체질
	PRINT 모유가 방울져 떨어진다
;욕정의 PLAMLV3 이상
IF PALAM:욕정 >= PALAMLV:3
	PRINT 기대에 딱딱하게 날카로워졌다
;욕정의 PLAMLV3 이하 또한함몰유두
ELSEIF TALENT:함몰유두
	PRINT 함몰했다
ENDIF
PRINTFORM 유두에 떨리는로터를 꽉 눌렀다…


;-------------------------------------------------
;13,니플캡
;-------------------------------------------------
@MESSAGE_COM13
PRINTFORM %CALLNAME:PLAYER%는
IF TEQUIP:니플캡
	PRINTFORM %CALLNAME%의 유두로부터니플캡를 떼어냈다
ELSE
	PRINTFORM %CALLNAME%의 유두에니플캡를 끌어당겼다
ENDIF


;-------------------------------------------------
;14,유두고문
;-------------------------------------------------
@MESSAGE_COM14
;착의 플레이
IF TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET)
	IF TOPS(TARGET)
		A = 6
	ELSEIF TEQUIP:상의
		A = 5
	ELSEIF TEQUIP:속옷（위）
		A = 3
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
ENDIF

PRINTFORM %CALLNAME%의

;절벽
IF TALENT:절벽
	PRINT 거의 무에 동일하다
;작은체구＋빈유
ELSEIF TALENT:작은체구 && TALENT:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:폭유
	PRINT 손에 남을 만큼 큰
ENDIF

;욕정의 PALAMLV3 이상
IF PALAM:욕정 >= PALAMLV:3
	PRINT 가슴의 첨단의,
	PRINT 완전히 딱딱해진 돌기
;욕정의 PALAMLV3 이하 또한함몰유두
ELSEIF TALENT:함몰유두
	PRINT 가슴의 함몰한 돌기
ELSE
	PRINT 가슴의 첨단의,
	PRINT 아직 부드러운 돌기
ENDIF

SIF TEQUIP:니플캡 || TEQUIP:유두클립
	PRINTFORM 에 매달렸다

IF TEQUIP:니플캡
	PRINT 니플캡
ELSEIF TEQUIP:유두클립
	PRINT 유두클립
ENDIF
PRINTFORM 를

IF TEQUIP:니플캡 || TEQUIP:유두클립
	;고문중
	IF TEQUIP:지하실
		PRINTFORM 마음껏 이끌었다…
	ELSE
		PRINTFORM 집거나 이끌거나 해 자극했다…
	ENDIF
;고문중
ELSEIF TEQUIP:지하실
	PRINTFORM 손톱을 세워 짜부러뜨렸다…
;착의 플레이
ELSEIF TALENT:PLAYER:골드핑거 && (TEQUIP:속옷（위） || TEQUIP:상의 || TOPS(TARGET))
	PRINTFORM 쓸어올렸다…
ELSE
	PRINTFORM 집어올렸다…
ENDIF


;-------------------------------------------------
;15,유두클립
;-------------------------------------------------
@MESSAGE_COM15
PRINTFORM %CALLNAME:PLAYER%는
PRINTFORM %CALLNAME%의

IF TEQUIP:유두클립
	PRINTFORM 유두로부터 클립을 제외해 주었다
ELSE
	;욕정의 PALAMLV3 이상
	IF PALAM:욕정 >= PALAMLV:3
		PRINT 완전히 딱딱해진 돌기에
	ELSE
		PRINT 아직 부드러운 돌기에
	ENDIF
	;고문중
	SIF TEQUIP:지하실
		PRINT ,톱니모양이 붙은 강력한
	PRINTFORM 클립을 물게 했다
ENDIF


;-------------------------------------------------
;16,유방전극
;-------------------------------------------------
@MESSAGE_COM16
PRINTFORM %CALLNAME%의
IF TEQUIP:착유기
	PRINT 착유기에 전극를 연결해,
ELSEIF TEQUIP:유두클립
	PRINT 유두클립에 전극를 연결해,
ENDIF
PRINTFORM 스윗치를 넣었다…


;-------------------------------------------------
;17,착유기
;-------------------------------------------------
@MESSAGE_COM17
PRINTFORM %CALLNAME:PLAYER%는
;해제시
IF TEQUIP:착유기
	PRINTFORM %CALLNAME%로부터착유기를 떼어냈다
ELSE
	PRINTFORM %CALLNAME%에착유기를 달았다
ENDIF


;-------------------------------------------------
;18,모유마시기
;-------------------------------------------------
@MESSAGE_COM18
PRINTFORM %CALLNAME%의

;빈유＋작은체구
IF TALENT:작은체구 && TALENT:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:폭유
	PRINT 손에 남을 만큼 큰
ENDIF

;연모나친애＋욕정의 PALAMLV3 이상
IF (TALENT:연모 || TALENT:친애) && PALAM:욕정 >= PALAMLV:3
	PRINTFORML 가슴를 좁히듯이하면서
	PRINTFORM 그 첨단을 강하게 빨아 올리면,
	PRINTFORM %CALLNAME%는 교성을 주면서도
	;빈유
	SIF TALENT:빈유
		PRINT 그 소극적인
	;거유
	SIF TALENT:거유
		PRINT 그 큰
	;폭유
	SIF TALENT:폭유
		PRINT 그 거대한
	PRINTFORM 가슴
	;거유나폭유
	SIF TALENT:거유 || TALENT:폭유
		PRINT 의 골짜기
	PRINTFORM 에 %CALLNAME:PLAYER%의 머리를 대어,
	PRINTFORM 쾌감으로 힘이 들어가 버리는지 강하게 껴안고 압박했다…
;음란나창부＋욕정의 PALAMLV3 이상
ELSEIF (TALENT:음란 || TALENT:창부) && PALAM:욕정 >= PALAMLV:3
	PRINTFORM 가슴를 좁히듯이하면서 그 첨단을 강하게 빨아 올리면,
	PRINTFORM %CALLNAME%는 교성을 줘
	;빈유
	SIF TALENT:빈유
		PRINT 그 소극적인
	;거유
	SIF TALENT:거유
		PRINT 그 큰
	;폭유
	SIF TALENT:폭유
		PRINT 그 거대한
	PRINTFORM 가슴를 강조하듯이 내밀어,
	PRINTFORM %CALLNAME:PLAYER%의 얼굴에 강압했다…
;연모나친애
ELSEIF (TALENT:연모 || TALENT:친애)
	PRINTFORM 가슴를 주무르면서 그 첨단을 빨아 올리면,
	PRINTFORM %CALLNAME%는 상냥한 미소를 띄우고
	;빈유
	SIF TALENT:빈유
		PRINT 그 소극적인
	;거유
	SIF TALENT:거유
		PRINT 그 큰
	;폭유
	SIF TALENT:폭유
		PRINT 그 거대한
			PRINTFORM 가슴
	;거유나폭유
	SIF TALENT:거유 || TALENT:폭유
		PRINT 의 골짜기
	PRINTFORM 에 %CALLNAME:PLAYER%의 머리를 대어,
	PRINTFORM 상냥하게 껴안았다…
ELSE
	PRINTFORM 가슴의 첨단을 들이마시고 모유를 마셨다…
ENDIF



;===========================================================
;C계
;===========================================================
;-------------------------------------------------
;20,로터
;-------------------------------------------------
@MESSAGE_COM20
PRINTFORM %CALLNAME:PLAYER%는

;착의(벗기지 않는다) 때
IF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
	IF TEQUIP:하의
		A = 4
	ELSEIF TEQUIP:속옷（아래）
		A = 2
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
;착의(비켜 놓는다) 때
ELSEIF TEQUIP:속옷（아래） || TEQUIP:하의
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF

	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 위로부터
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의

;페니스가 있다
IF PENIS(TARGET)
	PRINTFORM 페니스에 떨리는로터를 꽉 눌렀다…
ELSE
	;욕정의 PALAMLV3 이상
	SIF PALAM:욕정 >= PALAMLV:3
		PRINT 새빨갛게 부어 올랐다
	PRINTFORM 비부[秘部]에 떨리는로터를 꽉 눌렀다…
ENDIF


;-------------------------------------------------
;21,진동마사지기
;-------------------------------------------------
@MESSAGE_COM21
PRINTFORM %CALLNAME:PLAYER%는

;착의(벗기지 않는다) 때
IF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
	IF TEQUIP:하의
		A = 4
	ELSEIF TEQUIP:속옷（아래）
		A = 2
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
;착의(비켜 놓는다) 때
ELSEIF TEQUIP:속옷（아래） || TEQUIP:하의
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF

	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 위로부터
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의

;페니스가 있다
IF PENIS(TARGET)
	PRINTFORM 페니스에진동마사지기를 강압했다…
ELSE
	;욕정의 PALAMLV3 이상
	SIF PALAM:욕정 >= PALAMLV:3
		PRINT 새빨갛게 부어 올랐다
	PRINTFORM 비부[秘部]에진동마사지기를 강압했다…
ENDIF



;-------------------------------------------------
;22,클리캡
;-------------------------------------------------
@MESSAGE_COM22
PRINTFORM %CALLNAME:PLAYER%는
;해제시
IF TEQUIP:클리캡
	PRINTFORM %CALLNAME%로부터 전동클리캡를 제외했다
ELSE

	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	PRINTFORM 전동클리캡를 달았다
ENDIF


;-------------------------------------------------
;23,클리고문
;-------------------------------------------------
@MESSAGE_COM23

;삼각목마승마중,새드 3 이상이나지하실에 있다고 꾸짖는 방법이 바뀐다
IF TEQUIP:삼각목마 && (ABL:PLAYER:새드끼 >= 3 || TALENT:PLAYER:새드 || TEQUIP:지하실)
	PRINTFORM %CALLNAME%를 실은삼각목마를,흔들흔들 전후에 흔들어 주었다…
ELSE
	PRINTFORM %CALLNAME:PLAYER%는
	
	;착의(벗기지 않는다) 때
	IF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
		IF TEQUIP:하의
			A = 4
		ELSEIF TEQUIP:속옷（아래）
			A = 2
		ENDIF
		CALL CLOTHES_NAME(TARGET)
		PRINT 위로부터,
	;착의(비켜 놓는다) 때
	ELSEIF TEQUIP:속옷（아래） || TEQUIP:하의
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
		
		;스타킹계
		IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
			SIF TEQUIP:특수2 == 5
				PRINT 스타킹
			SIF TEQUIP:특수2 == 6
				PRINT 타이츠
			PRINT 위로부터
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의
	
	;욕정의 PALAMLV3 이상
	SIF PALAM:욕정 >= PALAMLV:3
		PRINT 새빨갛게 부어 올랐다
	
	PRINTFORM 음핵을,
	;고문중
	IF TEQUIP:지하실
		PRINTFORM 손톱을 세워 세게 긁어 돌렸다
	ELSE
		PRINTFORM 굴리듯이해 자극했다…
	ENDIF
ENDIF

;-------------------------------------------------
;24,음핵클립
;-------------------------------------------------
@MESSAGE_COM24
PRINTFORM %CALLNAME:PLAYER%는

IF TEQUIP:음핵클립
	PRINTFORM %CALLNAME%의
	PRINTFORM 음핵으로부터 클립을 제외해 주었다
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	PRINTFORM %CALLNAME%의
	;욕정의 PALAMLV3 이상
	SIF PALAM:욕정 >= PALAMLV:3
		PRINT 새빨갛게 부어 올랐다
	PRINTFORM 음핵에,
	;고문중
	SIF TEQUIP:지하실
		PRINTFORM 톱니모양이 붙은 강력한
	PRINTFORM 클립을 물게 했다
ENDIF


;-------------------------------------------------
;25,음핵전극
;-------------------------------------------------
@MESSAGE_COM25
PRINTFORM %CALLNAME:PLAYER%는

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의음핵클립에 전극를 연결해,스윗치를 넣었다…




;===========================================================
;P계
;===========================================================
;-------------------------------------------------
;30,수음한다
;-------------------------------------------------
@MESSAGE_COM30
PRINTFORM %CALLNAME:PLAYER%는

;착의(벗기지 않는다) 때
IF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
	IF TEQUIP:하의
		A = 4
	ELSEIF TEQUIP:속옷（아래）
		A = 2
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
;착의(비켜 놓는다) 때
ELSEIF TEQUIP:속옷（아래） || TEQUIP:하의
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF

	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 위로부터
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의

;오나홀 장착중
IF TEQUIP:전동오나홀
	PRINTFORM 오나홀에 휩싸여진페니스를 힘들게 잡아,상하에 쓸어올렸다…
;착의(벗기지 않는다) 때
ELSEIF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
	PRINTFORM 페니스를 주물렀다…
ELSE
	;욕정의 PALAMLV3 이상
	SIF PALAM:욕정 >= PALAMLV:3
		PRINT 딱딱하게 우뚝 솟았다
	IF TALENT:아이사이즈
		PRINT 어리다
	ELSEIF TALENT:말자지
		PRINT 주저해 할 것 같을 정도큰
	ENDIF
	SIF TALENT:포경
		PRINT 포경
	PRINTFORM 페니스에 손가락을 걸어 쓸어올렸다…
ENDIF


;-------------------------------------------------
;31,펠라한다
;-------------------------------------------------
@MESSAGE_COM31
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

;욕정의 PALAMLV3 이상
SIF PALAM:욕정 >= PALAMLV:3
	PRINT 딱딱하게 우뚝 솟았다
IF TALENT:아이사이즈
	PRINT 어리다
ELSEIF TALENT:말자지
	PRINT 주저해 할 것 같을 정도큰
ENDIF
SIF TALENT:포경
	PRINT 포경
PRINTFORM 페니스를입에 물어 핥아댔다…


;-------------------------------------------------
;32,전동오나홀
;-------------------------------------------------
@MESSAGE_COM32
PRINTFORM %CALLNAME:PLAYER%는
;해제시
IF TEQUIP:전동오나홀
	PRINTFORM %CALLNAME%로부터 오나홀을 제외했다…
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의
	;아이사이즈
	IF TALENT:아이사이즈
		;소인체형가 아니다
		SIF TALENT:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:말자지
		;소인체형
		IF TALENT:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:포경
		PRINT 포경
	PRINT 페니스를 오나홀에
	IF TALENT:말자지
		PRINT 밀어넣었다
	ELSE
		PRINT 삽입했다
	ENDIF
ENDIF


;-------------------------------------------------
;33,귀두고문
;-------------------------------------------------
@MESSAGE_COM33

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의
;욕정의 PALAMLV3 이상
SIF PALAM:욕정 >= PALAMLV:3
	PRINT 딱딱하게 우뚝 솟았다
IF TALENT:아이사이즈
	PRINT 어리다
ELSEIF TALENT:말자지
	PRINT 주저해 할 것 같을 정도큰
ENDIF
SIF TALENT:포경
	PRINT 포경
PRINTFORM 페니스의 귀두를손의 히등으로 쓸어올렸다…


;-------------------------------------------------
;34,구슬애무
;-------------------------------------------------
@MESSAGE_COM34
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의 구슬을 보들보들 주물렀다…


;-------------------------------------------------
;35,구슬고문
;-------------------------------------------------
@MESSAGE_COM35
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의페니스의 봉투를
IF TEQUIP:삼각목마
	PRINTFORM 목마의 키에 빠득빠득 강압했다…
ELSE
	PRINTFORM 빠득빠득 강하게 비벼넣었다…
ENDIF

;-------------------------------------------------
;36,벗겨낸다
;-------------------------------------------------
@MESSAGE_COM36
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의페니스를 캐스트 오프 했다…



;===========================================================
;V계
;===========================================================
;-------------------------------------------------
;40,커닐링구스
;-------------------------------------------------
@MESSAGE_COM40
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

IF TEQUIP:속옷（아래）
	A = 2
	CALL CLOTHES_NAME(TARGET)
	;스타킹계
	SIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		PRINT 와
ENDIF
;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
ENDIF
IF TEQUIP:속옷（아래） || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	PRINTFORM 위로부터%CALLNAME%의 비순[秘唇]에 물고 늘어져,핥아댔다…
ELSE
	PRINTFORM %CALLNAME%의 비순[秘唇]을 정중하게 빨고 풀어
	
	;V경험 100 이상＋욕망의 PALAMLV3 이상
	IF EXP:V경험 >= 100 && PALAM:욕정 >= PALAMLV:3
		PRINTFORM ,
		;처녀
		IF TALENT:처녀
			PRINT 남을 모른다
		;윤활LV4 이상
		ELSEIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖었다
		ENDIF

		;작은체구의 유무로 분기
		IF TALENT:작은체구
			PRINT 어린 균열
		ELSE
			PRINT 비밀의 틈[秘裂]
		ENDIF
		PRINTFORM 에 혀끝을 찔러 넣어 휘저어나
	ENDIF
	PRINTL …
ENDIF


;-------------------------------------------------
;41,손가락삽입
;-------------------------------------------------
@MESSAGE_COM41
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	;완구 사용할 때는 짜증나기 때문에 읽지 않는다
	IF TEQUIP:바이브 || TEQUIP:로터삽입
	ELSE
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의

;처녀
IF TALENT:처녀
	PRINT 남을 모른다
;윤활LV4 이상
ELSEIF PALAM:윤활 >= PALAMLV:4
	PRINT 흠뻑 젖었다
ENDIF

;작은체구의 유무로 분기
IF TALENT:작은체구
	PRINT 어린 균열
ELSE
	PRINT 비밀의 틈[秘裂]
ENDIF

;바이브의 유무로 분기
IF TEQUIP:바이브
	PRINTFORM 중(안)에서,요염하게 꾸불꾸불하는바이브를 전후에 빼고 꽂기 했다
ELSEIF TEQUIP:로터삽입
	PRINTFORM 로부터 처진,로터의 끈을 느슨하게 이끌어 주었다
ELSE
	PRINTFORM 에 %CALLNAME:PLAYER%의 손가락을 천천히 삽입했다…
ENDIF


;-------------------------------------------------
;42,로터삽입
;-------------------------------------------------
@MESSAGE_COM42
PRINTFORM %CALLNAME:PLAYER%는

;해제시
IF TEQUIP:로터삽입
	PRINTFORM %CALLNAME%로부터 떨리는로터를 천천히 뽑아 냈다…
;장착시
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF

	PRINTFORM %CALLNAME%의

	;윤활LV5 이상으로 한층 더 추가
	SIF PALAM:윤활 >= PALAMLV:5
		PRINT 충분히
	;윤활LV3 이상으로 한층 더 추가
	SIF PALAM:윤활 >= PALAMLV:3
		PRINT 점액을 기렸다

	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브
		PRINT 완구를 문 채로의

	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열에
	ELSE
		PRINT 비밀의 틈[秘裂]에
	ENDIF
	PRINTFORML 떨리는로터를 밀어넣었다…
ENDIF


;-------------------------------------------------
;43,바이브
;-------------------------------------------------
@MESSAGE_COM43
PRINTFORM %CALLNAME:PLAYER%는

;해제시
IF TEQUIP:바이브
	PRINTFORM %CALLNAME%로부터 꿈틀거리는바이브를 천천히 뽑아 냈다…
;장착시
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	PRINTFORM %CALLNAME%의

	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	PRINTFORM 성기에
	;고문중
	SIF TEQUIP:지하실
		PRINT 흉악한 형태의
	PRINTFORM 바이브를 꽂아넣었다
ENDIF


;-------------------------------------------------
;44,바기나전극
;-------------------------------------------------
@MESSAGE_COM44
PRINTFORM %CALLNAME:PLAYER%는

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 더욱 펴넓혀,
	ELSE
		PRINT 에 손톱을 세워 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의바이브에 전극를 연결해,스윗치를 넣었다…




;===========================================================
;A계
;===========================================================
;-------------------------------------------------
;50,애널자극
;-------------------------------------------------
@MESSAGE_COM50
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

IF TEQUIP:속옷（아래） && !TEQUIP:정조대
	A = 2
	CALL CLOTHES_NAME(TARGET)
	;스타킹계
	SIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		PRINT 와
ENDIF
;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
ENDIF
IF (TEQUIP:속옷（아래） && !TEQUIP:정조대) || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	PRINTFORM 위로부터%CALLNAME%의애널를 혀끝에서 핥아댔다…
ELSE
	PRINTFORM %CALLNAME%의
	;A경험 10 이하
	SIF EXP:A경험 <= 10
		PRINT 딱딱하게 닫혔다
	PRINTFORM 아누스를 정중하게 빨고 풀어
	;A경험 100 이상＋욕망의 PALAMLV3 이상
	SIF EXP:A경험 >= 100 && PALAM:욕정 >= PALAMLV:3
		PRINTFORM ,혀끝을 찔러 넣어 구멍안까지 휘저어나
	PRINTL …
ENDIF


;-------------------------------------------------
;51,애널애무
;-------------------------------------------------
@MESSAGE_COM51
PRINTFORM %CALLNAME:PLAYER%는

;도구를 사용하는 경우는 표시하지 않는다
IF TEQUIP:애널바이브 || TEQUIP:애널비즈 || TEQUIP:확장벌룬 || TEQUIP:로터A삽입
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

;애널바이브
IF TEQUIP:애널바이브
	PRINTFORM 조금씩 떨리는애널바이브를 전후에 빼고 꽂기 했다…
;애널비즈
ELSEIF TEQUIP:애널비즈
	PRINTFORM 비즈의 끈을 잡아당겨 가볍게 출납해 주었다…
;로터A삽입
ELSEIF TEQUIP:로터A삽입
	PRINTFORM 애널로부터 처진로터의 끈을,느슨하게 이끌어 주었다
;확장벌룬
ELSEIF TEQUIP:확장벌룬
	PRINTFORM 애널를 한계까지 넓히고 있는 벌룬을 전후에 움직여 주었다…
ELSE
	PRINTFORM %CALLNAME%의애널를 농
	;A경험 100 이상＋욕망의 PALAMLV3 이상
	SIF PALAM:욕정 >= PALAMLV:3 && ABL:A감각 >= 3
		PRINT 라고,손가락끝으로 직장까지 휘저어
	PRINTL …
ENDIF


;-------------------------------------------------
;52,로터A삽입
;-------------------------------------------------
@MESSAGE_COM52
PRINTFORM %CALLNAME:PLAYER%는

;해제시
IF TEQUIP:로터A삽입
	PRINTFORM %CALLNAME%의 아누스로부터 떨리는로터를 뽑았다…
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF

	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORML %CALLNAME%의후 구멍에 떨리는로터를 밀어넣었다…
ENDIF


;-------------------------------------------------
;53,애널비즈
;-------------------------------------------------
@MESSAGE_COM53
PRINTFORM %CALLNAME:PLAYER%는
IF TEQUIP:애널비즈
	PRINTFORM %CALLNAME%의 아누스로부터애널비즈를 기세 좋게 뽑아 냈다…
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF

	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	PRINTFORM %CALLNAME%의후 구멍에애널비즈를 하나씩 삼키게 해 갔다…
ENDIF


;-------------------------------------------------
;54,애널바이브
;-------------------------------------------------
@MESSAGE_COM54
PRINTFORM %CALLNAME:PLAYER%는

;해제시
IF TEQUIP:애널바이브
	PRINTFORM %CALLNAME%의 아누스로부터애널바이브를 뽑았다…
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의후 구멍에
	
	;고문중
	IF TEQUIP:지하실
		PRINT 흉악한 형태의
	ELSEIF TALENT:동물귀
		IF TALENT:동물귀 == 11
			PRINT 매끈매끈한 고양이
		ELSEIF TALENT:동물귀 == 12
			PRINT 주렁주렁의 개
		ELSEIF TALENT:동물귀 == 13
			PRINT 둥실둥실 한 토끼
		ELSE
			PRINT ,그
			SIF MALE(TARGET) == 0
				PRINT 녀
			PRINT 의 귀에 잘 어울리는 짐승
		ENDIF
		PRINT 의 꼬리를 본뜬
	ENDIF
	
	PRINTFORML 애널바이브를 꽂아넣었다…
ENDIF


;-------------------------------------------------
;55,확장벌룬
;-------------------------------------------------
@MESSAGE_COM55
;해제시
IF TEQUIP:확장벌룬
	PRINTFORM %CALLNAME%가 펼쳐진 후 구멍으로부터 벌룬을 뽑아 주었다…
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF

	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 더욱 펴넓혀,
		ELSE
			PRINT 에 손톱을 세워 찢어,
		ENDIF
	ENDIF
	PRINTFORM %CALLNAME%의후 구멍에 에어벌룬을 삽입해,공기를 보냈다…
ENDIF


;-------------------------------------------------
;56,애널전극
;-------------------------------------------------
@MESSAGE_COM56
;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 더욱 펴넓혀,
	ELSE
		PRINT 에 손톱을 세워 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의 애널바이브에 전극를 연결해,스윗치를 넣었다…



;===========================================================
;U계
;===========================================================
;-------------------------------------------------
;60,카테터
;-------------------------------------------------
@MESSAGE_COM60
PRINTFORM %CALLNAME:PLAYER%는
IF TEQUIP:카테터 == 1
	PRINTFORM %CALLNAME%의 요도로부터
	IF TEQUIP:촉수
		PRINT 촉수
	ELSE
		PRINT 카테터
	ENDIF
	PRINT 를 뽑아 냈다…
ELSE
	IF TEQUIP:촉수
		PRINTFORML 극세의촉수가%CALLNAME%의 요도안에 기어들었다
	ELSE
		;착의 때
		;노출 가능한 옷이 있으면,우선노출 시킨다
		IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
			PRINTFORM %CALLNAME%의
			;본테이지
			IF BOTTOMS(TARGET) == 3
				PRINT 슈트의 지퍼를 열면,
			;스커트
			ELSEIF BOTTOMS(TARGET) == 1
				PRINT 스커트를 걷어 올리면,
			;그 이외의전신상의
			ELSEIF BOTTOMS(TARGET) == 2
				A = 4
				CALL CLOTHES_NAME(TARGET)
				PRINT 의 옷자락을 걷어 올리면,
			ENDIF
		ENDIF
		
		PRINTFORM %CALLNAME%의 요도에
		;고문중
		IF TEQUIP:지하실
			PRINTFORML 통증를 주는 오돌오돌 부착의카테터를 삽입했다
		ELSE
			PRINTFORML 요도 개발용의카테터를 삽입했다
		ENDIF
	ENDIF
ENDIF

;-------------------------------
;장착시 메세지
@EQUIP_MESSAGE_COM60
	RETURN 0

;-------------------------------------------------
;61,요도벌룬
;-------------------------------------------------
@MESSAGE_COM61
IF TEQUIP:요도벌룬
	PRINTFORML %CALLNAME%가 펼쳐진 요도로부터 벌룬카테터를 뽑아 주었다…
	IF EXP:U확장경험 < EXPLV:1
		PRINTFORML %CALLNAME%의 요도는 천천히 닫아 갔다
	ELSEIF EXP:U확장경험 < EXPLV:2
		PRINTFORML %CALLNAME%의 요도는 실룩거리면서 느슨느슨 닫아 갔다
	ELSEIF EXP:U확장경험 < EXPLV:3
		PRINTFORML %CALLNAME%의 요도는 뻐끔 입를 열고 있다
	ELSEIF EXP:U확장경험 < EXPLV:4
		PRINTFORML %CALLNAME%의 요도는 펼쳐진 채로 좀처럼 돌아오지 않는 것 같다
	ELSE
		PRINTFORML %CALLNAME%의 요도로부터 그 중이 뻔히 보임이 되어 있다
	ENDIF
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		PRINTFORM %CALLNAME%의
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	PRINTFORML %CALLNAME%의 요도에 벌룬카테터를 삽입해,공기를 보냈다…
ENDIF


;-------------------------------------------------
;62,전동카테터
;-------------------------------------------------
@MESSAGE_COM62

PRINTFORM %CALLNAME%의카테터에 전극를 연결해,스윗치를 넣었다…



;===========================================================
;봉사시킨다
;===========================================================
;-------------------------------------------------
;70,수음
;-------------------------------------------------
@MESSAGE_COM70
PRINTFORM %CALLNAME%는
;연모나친애
SIF TALENT:연모 || TALENT:친애
	PRINTFORM 사랑하는 것 같은 표정으로,
PRINTFORM %CALLNAME:PLAYER%의
;오나홀 장착중
IF TEQUIP:PLAYER:전동오나홀
	PRINTFORM 오나홀에 휩싸여진페니스를 힘들게 잡아,상하에 쓸어올려 왔다…
ELSE
	;아이사이즈
	IF TALENT:PLAYER:아이사이즈
		;소인체형가 아니다
		SIF TALENT:PLAYER:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:PLAYER:말자지
		;소인체형
		IF TALENT:PLAYER:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:PLAYER:포경
		PRINT 포경
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML 페니스에 손가락을 걸어 쓸어올려 왔다…
	;통상 모드
	ELSE
		PRINTFORML 페니스에 손가락을 걸어 쓸어올리고 있다…
	ENDIF
ENDIF


;-------------------------------------------------
;71,펠라치오
;-------------------------------------------------
@MESSAGE_COM71
PRINTFORM %CALLNAME%는
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 사랑하는 것 같은 표정으로,
;음란나창부
SIF (TALENT:음란 || TALENT:창부)
	PRINT 스스로
PRINTFORM %CALLNAME:PLAYER%의
;아이사이즈
IF TALENT:PLAYER:아이사이즈
	;소인체형가 아니다
	SIF TALENT:PLAYER:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:PLAYER:말자지
	;소인체형
	IF TALENT:PLAYER:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
SIF TALENT:PLAYER:포경
	PRINT 포경
;역조교모드
IF TEQUIP:역조교
	PRINTFORML 페니스에 혀를 기게 해 핥아대 왔다…
;통상 모드
ELSE
	PRINTFORML 페니스에 혀를 기게 해 핥아대고 있다…
ENDIF


;-------------------------------------------------
;72,이라마치오
;-------------------------------------------------
@MESSAGE_COM72
PRINTFORM %CALLNAME:PLAYER%는
;아이사이즈
IF TALENT:PLAYER:아이사이즈
	;소인체형가 아니다
	SIF TALENT:PLAYER:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:PLAYER:말자지
	;소인체형
	IF TALENT:PLAYER:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
SIF TALENT:PLAYER:포경
	PRINT 포경
PRINTFORM 페니스를 난폭하게%CALLNAME%의
;소인체형
SIF TALENT:소인체형
	PRINT 작은
;역조교모드
IF TEQUIP:역조교
	PRINTFORM 목까지 물어 왔다…
;통상 모드
ELSE
	PRINTFORM 목까지 찔러 넣었다…
ENDIF


;-------------------------------------------------
;73,파이즈리
;-------------------------------------------------
@MESSAGE_COM73
PRINTFORM %CALLNAME%는
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 넋을 잃은 표정을 하면서,
;빈유
IF TALENT:빈유 || TALENT:절벽
	PRINT 얇은가슴로 열심히페니스를
;거유
ELSEIF TALENT:거유
	PRINT 풍만한가슴로페니스를 감싸
;폭유
ELSEIF TALENT:폭유
	PRINT 너무 풍만할 정도의가슴로페니스를 숨기듯이해
ELSE
	PRINT 가슴로페니스를 끼워
ENDIF
;역조교모드
IF TEQUIP:역조교
	PRINTFORM 쓸어올려 왔다…
;통상 모드
ELSE
	PRINTFORM 쓸어올리고 있다…
ENDIF


;-------------------------------------------------
;74,세로파이즈리
;-------------------------------------------------
@MESSAGE_COM74
PRINTFORM %CALLNAME%는
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 넋을 잃은 표정을 하면서,
PRINTFORML 풍만한가슴를 대어,%CALLNAME:PLAYER%를 올려보고 있다
PRINTFORML 그 골짜기에,천천히를페니스를 찔러넣어 허리를 사용했다…


;-------------------------------------------------
;75,파이즈리펠라
;-------------------------------------------------
@MESSAGE_COM75
PRINTFORM %CALLNAME%는
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 넋을 잃은 표정을 하면서,
;빈유
IF TALENT:빈유 || TALENT:절벽
	PRINT 얇은가슴 뿐만이 아니라 혀도 사용해 열심히페니스전체를 자극해
;거유
ELSEIF TALENT:거유
	PRINT 페니스를 풍만한가슴로 감싸,남은 귀두를 빨아 자극해
;폭유
ELSEIF TALENT:폭유
	PRINT 너무 풍만할 정도의가슴로페니스를 숨겨 버려,때때로 혀를 넣어,열심히페니스를 빨아
ELSE
	PRINT 가슴로페니스를 끼워,남은 부분을 혀로,자극해
ENDIF
;역조교모드
IF TEQUIP:역조교
	PRINTL 왔다…
;통상 모드
ELSE
	PRINTL 있다…
ENDIF


;-------------------------------------------------
;76,스마타
;-------------------------------------------------
@MESSAGE_COM76

;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 망타이츠
		IF TFLAG:55
			PRINTL 의 깨어진 부분을 더욱 펴넓히면
		ELSE
			PRINTL 에 손톱을 세워 찢으면
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME:PLAYER%의
	;아이사이즈
	IF TALENT:PLAYER:아이사이즈
		;소인체형가 아니다
		SIF TALENT:PLAYER:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:PLAYER:말자지
		;소인체형
		IF TALENT:PLAYER:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:PLAYER:포경
		PRINT 포경
	PRINTFORM 페니스를 가랑이에 끼워,
	
	;음란나창부＋욕정의 PALAMLV5 이상
	IF (TALENT:음란 || TALENT:창부) && PALAM:욕정 >= PALAMLV:5
		PRINTFORM 쾌락에 빠지고 자른 표정으로 %CALLNAME:PLAYER%를 응시하면서
	;연모나친애＋욕정의 PALAMLV5 이상
	ELSEIF (TALENT:연모 || TALENT:친애) && PALAM:욕정 >= PALAMLV:5
		PRINTFORM 당장 녹을 것 같은 표정을 띄우면서
	;욕정의 PALAMLV4 이상
	ELSEIF PALAM:욕정 >= PALAMLV:4
		PRINTFORM 입의 구석으로부터 군침을 흘리면서
	;연모나친애
	ELSEIF (TALENT:연모 || TALENT:친애)
		PRINTFORM 도취해 버린 눈동자로 %CALLNAME:PLAYER%를 응시하면서
	;음란나창부
	ELSEIF (TALENT:음란 || TALENT:창부)
		PRINTFORM 음탕한 눈초리로 도발하면서
	;수치심없음
	ELSEIF TALENT:수치심없음
		PRINTFORM 대담하게 허리를 움직이면서
	;감정부족
	ELSEIF TALENT:감정부족
		PRINTFORM 희미하게 상기 한 얼굴로
	ELSE
		PRINTFORM 부끄러운 듯이
	ENDIF
	;작은체구
	IF TALENT:작은체구
		PRINT 가녀린 넓적다리를
	ELSE
		PRINT 소중한 곳을
	ENDIF
	PRINTFORM 페니스에 문질러 왔다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME%가 닫은 넓적다리의 사이에페니스를 끼워 넣어,
	
	SIF VAGINA(TARGET)
		PRINT 비순[秘唇]을 쓸어올리듯이해
	
	PRINTFORM 페니스를 다뤘다…
ENDIF


;-------------------------------------------------
;77,손가락츄파
;-------------------------------------------------
@MESSAGE_COM77
PRINTFORM %CALLNAME%는
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 사랑하는 것 같은 표정으로,
;음란나창부
SIF (TALENT:음란 || TALENT:창부)
	PRINT 스스로
;역조교모드
IF TEQUIP:역조교
	PRINTFORMW %CALLNAME:PLAYER%의 손가락을 물어 핥아대 왔다…
;통상 모드
ELSE
	PRINTFORMW %CALLNAME:PLAYER%의 손가락을 물어 핥아대고 있다…
ENDIF

;-------------------------------------------------
;80,풋잡
;-------------------------------------------------
@MESSAGE_COM80
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의

;아이사이즈
IF TALENT:PLAYER:아이사이즈
	;소인체형가 아니다
	SIF TALENT:PLAYER:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:PLAYER:말자지
	;소인체형
	IF TALENT:PLAYER:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
PRINT 페니스를

;신발를 신고 있을 때
IF TEQUIP:신발 > 0
	;새드가 아닌＋남자
	IF ABL:새드끼 < 3 && PENIS(TARGET)
		PRINT 자신이 아픈 것 같은 얼굴을 하면서
	ENDIF
	A = 9
	CALL CLOTHES_NAME(TARGET)
	PRINT 로 짓밟듯이
	;새드
	IF ABL:새드끼 >= 5
		PRINT 희희낙락 하고 있던인 체하며
	ELSE
		PRINT 슬슬 쓸어올려
	ENDIF
ELSE
	IF TEQUIP:특수2
		A = 1
		CALL CLOTHES_NAME(TARGET)
		PRINT 에 휩싸여졌다
	ENDIF
	PRINTFORM 발뒤에서
	;새드
	SIF ABL:새드끼 >= 5
		PRINT 강요하면서
	;연모나친애
	SIF (TALENT:연모 || TALENT:친애)
		PRINTFORM 열심히
	PRINTFORM 쓸어올려
ENDIF

;역조교모드
IF TEQUIP:역조교
	PRINTL 왔다…
;통상 모드
ELSE
	PRINTL 있다…
ENDIF



;-------------------------------------------------
;81,헤어잡
;-------------------------------------------------
@MESSAGE_COM81
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에 가까워져,
IF RONGE(TARGET)
	PRINTL 머리카락의털를페니스에 얽히게 할 수 있어 다뤘다…
ELSE
	PRINTL 머리카락의털에페니스를 문질렀다…
ENDIF


;-------------------------------------------------
;82,겨드랑이잡
;-------------------------------------------------
@MESSAGE_COM82
PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 겨드랑이를 잡게 하면페니스를 삽입해,잡아당기기 시작했다…


;-------------------------------------------------
;83,엉덩이 코키
;-------------------------------------------------
@MESSAGE_COM83
PRINTFORM %CALLNAME:PLAYER%는
PRINTFORM 네발로 엎드림이 된%CALLNAME%의

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

IF TEQUIP:속옷（아래） && !TEQUIP:정조대
	A = 2
	CALL CLOTHES_NAME(TARGET)
	;스타킹계
	SIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		PRINT 와
ENDIF
;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
ENDIF

IF (TEQUIP:속옷（아래） && !TEQUIP:정조대) || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	PRINTFORM 위로부터,그 엉덩이에페니스를 문질렀다…
ELSE
	PRINTFORM 엉덩이의 균열에페니스를 문질렀다…
ENDIF


;-------------------------------------------------
;84,니플퍽
;-------------------------------------------------
@MESSAGE_COM84
PRINTFORM %CALLNAME%의
;빈유
IF TALENT:빈유
	PRINT 얇은가슴로,희미한 부푼 곳에 칠하듯이해
;절벽
ELSEIF TALENT:절벽
	PRINT 대부분 평평한가슴로,켜 돌리듯이해
;거유
ELSEIF TALENT:거유
	PRINT 풍만한가슴로,파묻히게 한 찌르듯이해
;폭유
ELSEIF TALENT:폭유
	PRINT 너무 풍부한가슴로,들어가게 할 수 있는 묻듯이해
ELSE
	PRINT 가슴로
ENDIF
PRINTFORM 억지로페니스를 다뤘다…


;-------------------------------------------------
;85,발핥기강제
;-------------------------------------------------
@MESSAGE_COM85
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의발에 혀를 기게 해 왔다…
;통상 모드
ELSE
	;야외플레이중
	IF TEQUIP:야외플레이
		;주
		IF TIME == 0
			PRINT 햇볕이 눈부신 맑은 하늘아래에서,
		;야
		ELSE
			PRINT 달빛이 상냥한 심야,
		ENDIF
		;겁쟁이,프라이드높음,부끄럼쟁이의 언젠가
		IF TALENT:겁쟁이 || TALENT:프라이드높음 || TALENT:부끄럼쟁이
			PRINTL 이 치태를 누군가에게 보여지지 않는 것인지와 움찔움찔 하면서
		ELSE
			PRINTL 풍으로 어루만질 수 있어 맨살을 조금 진동시켜
		ENDIF
	;미약유효
	ELSEIF TEQUIP:미약
		PRINT 어딘가 정신나간 것처럼 시선을 공중에 감돌게 하면서
	ENDIF
	PRINTFORM %CALLNAME%는 내밀어진 %CALLNAME:PLAYER%의발에 혀를 기게 하고 있다…
ENDIF


;-------------------------------------------------
;86,구슬비비기강제
;-------------------------------------------------
@MESSAGE_COM86
PRINTFORM %CALLNAME%는
;연모나친애
SIF TALENT:연모 || TALENT:친애
	PRINT 사랑하는 것 같은 표정으로,
PRINTFORM %CALLNAME:PLAYER%의
;역조교모드
IF TEQUIP:역조교
	PRINTL 페니스의 봉투를 비비어애무 해 왔다…
;통상 모드
ELSE
	PRINTL 페니스의 봉투를 비비고 있다…
ENDIF

;-------------------------------------------------
;87,구슬핥기강제
;-------------------------------------------------
@MESSAGE_COM87
PRINTFORM %CALLNAME%는
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 사랑하는 것 같은 표정으로,
PRINTFORM %CALLNAME:PLAYER%의
;역조교모드
IF TEQUIP:역조교
	PRINTL 페니스의 봉투를입에 포함해 핥아대 왔다…
;통상 모드
ELSE
	PRINTL 페니스의 봉투를입에 포함해 핥아대고 있다…
ENDIF


;===========================================================
;섹스계
;===========================================================
;-------------------------------------------------
;100,정상위
;-------------------------------------------------
@MESSAGE_COM100
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 100
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의질내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의질내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;그 이외
	ELSE
		PRINTFORM 질내로부터 앞지르는 일 없이%CALLNAME%를 위로 향해 재워,다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브 || TEQUIP:로터삽입
		PRINT 완구를 문 채로의
	;처녀
	SIF TALENT:처녀
		PRINT 남을 모른다
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열을
	ELSE
		PRINT 비밀의 틈[秘裂]을
	ENDIF
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브
		PRINT 무리하게 펼치면,
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 로 관철했다…
ENDIF

;-------------------------------------------------
;101,후배위
;-------------------------------------------------
@MESSAGE_COM101
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 101
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의질내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의질내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;전회의 커맨드가 「정상위」
	ELSEIF PREVCOM == 100
		PRINTFORM 질내로부터 앞지르는 일 없이%CALLNAME%의후로 돌아,다시 허리를 움직이기 시작했다…
	ELSE
		PRINTFORM 삽입한 채로%CALLNAME%를 재워 네발로 엎드리게 해,다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM 후로부터%CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브 || TEQUIP:로터삽입
		PRINT 완구를 문 채로의
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열에
	ELSE
		PRINT 비밀의 틈[秘裂]에
	ENDIF
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브
		PRINT 쑤셔 넣듯이해,
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 를 찔러 넣었다…
ENDIF

;-------------------------------------------------
;102,대면좌위
;-------------------------------------------------
@MESSAGE_COM102
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 102
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의질내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의질내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;전회 커맨드가정상위,기승위,대면입위의
	ELSEIF PREVCOM == 100 || PREVCOM == 106 || PREVCOM == 104
		PRINTFORM 질내로부터 앞지르는 일 없이%CALLNAME%를
		;전회의 조교가정상위
		IF PREVCOM == 100
			PRINT 안아 올려,
		;전회의 조교가대면입위
		ELSEIF PREVCOM == 104
			PRINT 껴안아 주저앉아,
		ELSE
			PRINT 꽉 껴안아,
		ENDIF
		PRINTFORM 다시 허리를 움직이기 시작했다…
	;전회 커맨드가후배위,배면좌위,배면입위
	ELSE
		PRINTFORM 삽입한 채로 신체를 이쪽에 향이나 키
		;전회의 조교가후배위
		IF PREVCOM == 101
			PRINT 라고 안아 올리면,
		;전회의 조교가배면입위
		ELSEIF PREVCOM == 105
			PRINT 라고 주저앉아,
		ELSE
			PRINT 와 꽉 껴안아,
		ENDIF
		PRINTFORM 다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 안아 올려,
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브 || TEQUIP:로터삽입
		PRINT 완구를 문 채로의
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열을
	ELSE
		PRINT 비밀의 틈[秘裂]을
	ENDIF
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 로 관철했다…
ENDIF

;-------------------------------------------------
;103,배면좌위
;-------------------------------------------------
@MESSAGE_COM103
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 100
		PRINTFORM %CALLNAME%를 등 뒤로부터 껴안아,다시%CALLNAME%의 신체를 흔들기 시작했다…
	;전회 커맨드가후배위나기승위
	ELSEIF PREVCOM == 101 || PREVCOM == 106
		PRINTFORM 질내로부터페니스를 앞지르는 일 없이%CALLNAME%를 등으로부터 껴안아,신체를 흔들기 시작했다…
	;전회 커맨드가대면입위나배면입위
	ELSEIF PREVCOM == 104 || PREVCOM == 105
		PRINTFORML 질내로부터페니스를 앞지르는 일 없이 주저앉아,
		IF PREVCOM == 104
			PRINTFORM %CALLNAME%의 신체를 이쪽에 향하게 해 꽉 껴안아,격렬하게 허리를 흔들기 시작했다…
		ELSE
			PRINTFORM %CALLNAME%의 신체를 등으로부터 껴안아,신체를 흔들기 시작했다…
		ENDIF
	;전회 커맨드가정상위나대면좌위
	ELSE
		PRINTFORM 삽입한 채로 반대를 향하게 한%CALLNAME%의 신체를
		;전회의 조교가정상위
		IF PREVCOM == 100
			PRINT 안아 올려,
		ELSE
			PRINT 꽉 껴안아,
		ENDIF
		PRINTFORM 격렬하게 허리를 흔들기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓히면서
		ELSE
			PRINT 를 찢고 나서
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 안아 올려,
	
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브 || TEQUIP:로터삽입
		PRINT 완구를 문 채로의
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열
	ELSE
		PRINT 비밀의 틈[秘裂]
	ENDIF
	
	PRINTFORM 를 기다리고 있던 것으로 천천히 뽑았다…
ENDIF

;-------------------------------------------------
;104,대면입위
;-------------------------------------------------
@MESSAGE_COM104
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 104
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의 질내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의질내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;전회 커맨드가정상위,기승위,대면좌위의 경우
	ELSEIF PREVCOM == 100 || PREVCOM == 106 || PREVCOM == 102
		PRINTFORM 삽입한 채로%CALLNAME%의 신체를 서게 해
		PRINTFORM 다시 허리를 움직이기 시작했다…
	;전회 커맨드가후배위,배면좌위,배면입위의 경우
	ELSE
		PRINTFORM 삽입한 채로 신체를 이쪽에 향하여
		SIF PREVCOM == 101 || PREVCOM == 106
			PRINT 서게 해
		PRINTFORM 다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 안아 올려,
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브 || TEQUIP:로터삽입
		PRINT 완구를 문 채로의
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열을
	ELSE
		PRINT 비밀의 틈[秘裂]을
	ENDIF
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 로 관철했다…
ENDIF

;-------------------------------------------------
;105,배면입위
;-------------------------------------------------
@MESSAGE_COM105
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 105
		PRINTFORM %CALLNAME%를 등 뒤로부터 껴안아,다시 허리를 움직이기 시작했다…
	;전회 커맨드가후배위나기승위나배면좌위
	ELSEIF PREVCOM == 101 || PREVCOM == 106 || PREVCOM == 103
		PRINTFORM 삽입한 채로%CALLNAME%의 신체를 서게 해 신체를 흔들기 시작했다…
	;전회 커맨드가정상위나대면좌위나대면입위
	ELSE
		PRINTFORM 삽입한 채로 반대를 향하게 한%CALLNAME%의 신체를
		;전회의 조교가정상위나대면좌위
		IF PREVCOM == 100 || PREVCOM == 102
			PRINTFORM ,서게 해
		ENDIF
		PRINTFORM 격렬하게 허리를 흔들기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 서게 해
	
	;바이브 넣고 있을 뿐
	SIF TEQUIP:바이브 || TEQUIP:로터삽입
		PRINT 완구를 문 채로의
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖었다
	;작은체구
	IF TALENT:작은체구
		PRINT 어린 균열
	ELSE
		PRINT 비밀의 틈[秘裂]
	ENDIF
	
	PRINTFORM 를 기다리고 있던 것으로 천천히 뽑았다…
ENDIF

;-------------------------------------------------
;106,기승위
;-------------------------------------------------
@MESSAGE_COM106
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 V섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 100 && PREVCOM <= 106
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 106
		PRINTFORM %CALLNAME%는 삽입된 채로의페니스를 단단히 조여,다시 허리를 움직이기 시작했다…
	;역조교모드
	ELSEIF TEQUIP:역조교
		PRINTFORM %CALLNAME%는 삽입된 채로 %CALLNAME:PLAYER%에 걸쳐,허리를 움직여 왔다…
	;전회 커맨드가정상위나후배위
	ELSEIF PREVCOM == 100 || PREVCOM == 101
		PRINTFORM 삽입한 채로%CALLNAME%를 안아 올려,아래로부터 허리를 밀어올렸다…
	;전회 커맨드가대면좌위나배면좌위나대면입위나배면입위
	ELSE
		PRINTFORM %CALLNAME:PLAYER%는 삽입한 채로 누워,아래로부터 허리를 밀어올렸다…
	ENDIF
ELSE
	;역조교모드가 아니다
	IF !TEQUIP:역조교
		
		;착의 때
		SIF (BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3) || (TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6)
			PRINTFORM %CALLNAME%의
		
		;노출 가능한 옷이 있으면,우선노출 시킨다
		IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
			PRINTFORM %CALLNAME%의
			;본테이지
			IF BOTTOMS(TARGET) == 3
				PRINT 슈트의 지퍼를 열면,
			;스커트
			ELSEIF BOTTOMS(TARGET) == 1
				PRINT 스커트를 걷어 올리면,
			;그 이외의전신상의
			ELSEIF BOTTOMS(TARGET) == 2
				A = 4
				CALL CLOTHES_NAME(TARGET)
				PRINT 의 옷자락을 걷어 올리면,
			ENDIF
		ENDIF
		
		;스타킹계
		IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
			SIF TEQUIP:특수2 == 5
				PRINT 스타킹
			SIF TEQUIP:특수2 == 6
				PRINT 타이츠
			IF TFLAG:55
				PRINTL 의 깨어진 부분을 더욱 펴넓혔다
			ELSE
				PRINTL 에 손톱을 세워 찢어 주었다
			ENDIF
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의 위에 걸쳐,
	
	;바이브 넣고 있을 뿐
	IF TEQUIP:바이브
		PRINT 바이브가 파묻힌 구멍에서
	;처녀＋작은체구
	ELSEIF TALENT:작은체구 && TALENT:처녀
		PRINT 남을 모르는 어린 균열로
	;처녀
	ELSEIF TALENT:처녀
		PRINT 남을 모르는 비밀의 틈[秘裂]으로
	ENDIF
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	
	PRINTFORM 를 삼켜 갔다…
ENDIF



;===========================================================
;애널섹스계
;===========================================================
;-------------------------------------------------
;110,정상위애널
;-------------------------------------------------
@MESSAGE_COM110
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 110
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의장내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의장내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;그 이외
	ELSE
		PRINTFORM 장내로부터 앞지르는 일 없이%CALLNAME%를 위로 향해 재워,다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 가 메워진 채로의
	ENDIF
	
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINT 아누스를
	;애널바이브애널비즈
	SIF TEQUIP:애널바이브 || TEQUIP:애널비즈
		PRINT 억지로 펼치면,
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 로 관철했다…
ENDIF

;-------------------------------------------------
;111,후배위애널
;-------------------------------------------------
@MESSAGE_COM111
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 111
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의장내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의장내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;전회의 커맨드가정상위애널
	ELSEIF PREVCOM == 110
		PRINTFORM 장내로부터 앞지르는 일 없이%CALLNAME%의후로 돌아,다시 허리를 움직이기 시작했다…
	;전회의 커맨드가대면좌위애널,배면좌위애널,기승위애널나대면입위애널나배면입위애널
	ELSE
		PRINTFORM 삽입한 채로%CALLNAME%를 재워 네발로 엎드리게 해,다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM 후로부터%CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 를 문 채로의
	ENDIF
	
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINT 아누스에
	;애널바이브애널비즈
	SIF TEQUIP:애널바이브 || TEQUIP:애널비즈
		PRINT 쑤셔 넣듯이해,
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 를 찔러 넣었다…
ENDIF

;-------------------------------------------------
;112,대면좌위애널
;-------------------------------------------------
@MESSAGE_COM112
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 112
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의장내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의장내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;전회 커맨드가정상위애널,기승위애널
	ELSEIF PREVCOM == 110 || PREVCOM == 116
		PRINTFORM 장내로부터
		;조교자가남자나반음반양나후타나리나쉬메일이다
		IF PENIS(PLAYER)
			PRINT 페니스를
		ELSE
			PRINT 딜도를
		ENDIF
			PRINTFORM 앞지르는 일 없이%CALLNAME%의 신체를 꽉 껴안아,
		PRINTFORM 다시 허리를 움직이기 시작했다…
	;전회 커맨드가후배위애널,배면좌위애널
	ELSEIF PREVCOM == 111 || PREVCOM == 113
		PRINTFORM 삽입한 채로 신체를 이쪽에 향이나 키
		;전회의 조교가후배위애널
		IF PREVCOM == 111
			PRINT 라고 안아 올리면,
		ELSE
			PRINT 와 꽉 껴안아,
		ENDIF
		PRINTFORM 다시 허리를 움직이기 시작했다…
	;전회 커맨드가대면입위애널,배면입위애널
	ELSE
		PRINTFORM 삽입한 채로
		SIF PREVCOM == 115
			PRINTFORM %CALLNAME%의 몸을 이쪽에 향하게 해
		PRINTFORM 주저앉아,꽉 껴안아,
		PRINTFORM 다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓히면서
		ELSE
			PRINT 를 찢고 나서
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 안아 올려,
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 를 문 채로의
	ENDIF
	
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINT 아누스에
	;애널바이브애널비즈
	SIF TEQUIP:애널바이브 || TEQUIP:애널비즈
		PRINT 쑤셔 넣듯이해,
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 를 찔러 넣었다…
ENDIF

;-------------------------------------------------
;113,배면좌위애널
;-------------------------------------------------
@MESSAGE_COM113
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 113
		PRINTFORM %CALLNAME%를 등 뒤로부터 껴안아,다시%CALLNAME%의 신체를 흔들기 시작했다…
	;전회 커맨드가후배위애널나기승위애널
	ELSEIF PREVCOM == 111 || PREVCOM == 116
		PRINTFORM 장내로부터
		;조교자가남자나반음반양나후타나리나쉬메일이다
		IF PENIS(PLAYER)
			SIF TALENT:PLAYER:포경
				PRINT 포경
			PRINT 페니스를
		ELSE
			PRINT 딜도를
		ENDIF
		PRINTFORM 앞지르는 일 없이%CALLNAME%의 신체를 등으로부터 껴안아,신체를 흔들기 시작했다…
	;전회 커맨드가정상위애널나대면좌위애널
	ELSEIF PREVCOM == 110 || PREVCOM == 112
		PRINTFORM 삽입한 채로 반대를 향하게 한%CALLNAME%의 신체를
		;전회의 조교가정상위애널
		IF PREVCOM == 110
			PRINT 안아 올려,
		ELSE
			PRINT 꽉 껴안아,
		ENDIF
		PRINTFORM 격렬하게 허리를 흔들기 시작했다…
	;전회 커맨드가대면입위애널,배면입위애널
	ELSE
		PRINTFORM 삽입한 채로
		SIF PREVCOM == 114
			PRINTFORM %CALLNAME%의 몸을 반대로 향하게 해
		PRINTFORM 주저앉아,꽉 껴안아,격렬하게 허리를 흔들기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓히면서
		ELSE
			PRINT 를 찢고 나서
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 안아 올려,
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINTFORM 가 메워진 채로의
	ENDIF
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINTFORM 아누스
	
	PRINTFORM 를 기다리고 있던 것으로 천천히 뽑았다…
ENDIF

;-------------------------------------------------
;114,대면입위애널
;-------------------------------------------------
@MESSAGE_COM114
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 114
		;조교자에게 진짜의페니스가 있다
		IF PENIS(PLAYER)
			PRINTFORM %CALLNAME%의장내의 감촉을 맛보면서,다시 허리를 움직이기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME%의장내로부터 뽑지 않고,다시 허리를 움직이기 시작했다…
		ENDIF
	;전회 커맨드가정상위애널,기승위애널,대면좌위애널
	ELSEIF PREVCOM == 110 || PREVCOM == 116 || PREVCOM == 112
		PRINTFORM 장내로부터
		;조교자가남자나반음반양나후타나리나쉬메일이다
		IF PENIS(PLAYER)
			SIF TALENT:PLAYER:포경
				PRINT 포경
			PRINT 페니스를
		ELSE
			PRINT 딜도를
		ENDIF
		PRINTFORM 앞지르는 일 없이%CALLNAME%의 신체를 서게 해
		PRINTFORM 다시 허리를 움직이기 시작했다…
	;전회 커맨드가후배위애널,배면좌위애널,배면입위애널
	ELSE
		PRINTFORM 삽입한 채로 신체를 이쪽에 향이나 키
		;전회의 조교가후배위애널,배면좌위애널
		SIF PREVCOM == 111 || PREVCOM == 113
			PRINT 라고 서게 해
		PRINTFORM 다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 서게 해
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 를 문 채로의
	ENDIF
	
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINT 아누스에
	;애널바이브애널비즈
	SIF TEQUIP:애널바이브 || TEQUIP:애널비즈
		PRINT 쑤셔 넣듯이해,
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 를 찔러 넣었다…
ENDIF


;-------------------------------------------------
;115,배면입위애널
;-------------------------------------------------
@MESSAGE_COM115
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 115
		PRINTFORM %CALLNAME%를 등 뒤로부터 껴안아,다시%CALLNAME%의 신체를 흔들기 시작했다…
	;전회 커맨드가후배위애널나기승위애널나배면좌위애널
	ELSEIF PREVCOM == 111 || PREVCOM == 116 || PREVCOM == 113
		PRINTFORM 삽입한 채로%CALLNAME%의 신체를 서게 해 신체를 흔들기 시작했다…
	;전회 커맨드가정상위애널나대면좌위애널나대면입위애널
	ELSE
		PRINTFORM 삽입한 채로 반대를 향하게 한%CALLNAME%의 신체를
		;전회의 조교가정상위애널나대면좌위애널
		IF PREVCOM == 110 || PREVCOM == 112
			PRINT 서게 해
		ENDIF
		PRINTFORM 격렬하게 허리를 흔들기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME%의
	
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%의 신체를 서게 한 채로,
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 를 문 채로의
	ENDIF
	
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINT 아누스에
	;애널바이브애널비즈
	SIF TEQUIP:애널바이브 || TEQUIP:애널비즈
		PRINT 쑤셔 넣듯이해,
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	PRINT 를 찔러 넣었다…
ENDIF


;-------------------------------------------------
;116,기승위애널
;-------------------------------------------------
@MESSAGE_COM116
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드도 A섹스계
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 110 && PREVCOM <= 116
	;전회의 커맨드가 이번과 같음
	IF PREVCOM == 116
		PRINTFORM %CALLNAME%는 삽입된 채로의 것을 단단히 조여,다시 허리를 움직이기 시작했다…
	;역조교모드
	ELSEIF TEQUIP:역조교
		PRINTFORM %CALLNAME%는 삽입된 채로 %CALLNAME:PLAYER%에 걸쳐,허리를 움직여 왔다…
	;전회 커맨드가정상위애널나후배위애널
	ELSEIF PREVCOM == 110 || PREVCOM == 111
		PRINTFORM 장내로부터 앞지르는 일 없이%CALLNAME%를 안아 올려,아래로부터 허리를 밀어올렸다…
	;전회 커맨드가대면좌위애널나배면좌위애널나대면입위애널나배면입위애널
	ELSE
		PRINTFORM %CALLNAME:PLAYER%는 삽입한 채로 누워,아래로부터 허리를 밀어올렸다…
	ENDIF
ELSE
	;역조교모드가 아니다
	IF !TEQUIP:역조교
		PRINTFORM %CALLNAME%의
		
		;착의 때
		;노출 가능한 옷이 있으면,우선노출 시킨다
		IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
			;본테이지
			IF BOTTOMS(TARGET) == 3
				PRINT 슈트의 지퍼를 열면,
			;스커트
			ELSEIF BOTTOMS(TARGET) == 1
				PRINT 스커트를 걷어 올리면,
			;그 이외의전신상의
			ELSEIF BOTTOMS(TARGET) == 2
				A = 4
				CALL CLOTHES_NAME(TARGET)
				PRINT 의 옷자락을 걷어 올리면,
			ENDIF
		ENDIF
		
		;스타킹계
		IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
			SIF TEQUIP:특수2 == 5
				PRINT 스타킹
			SIF TEQUIP:특수2 == 6
				PRINT 타이츠
			IF TFLAG:55
				PRINTL 의 깨어진 부분을 더욱 펴넓혔다
			ELSE
				PRINTL 에 손톱을 세워 찢어 주었다
			ENDIF
		ENDIF
	ENDIF
	
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의 위에 걸치면,
	
	;조교자가남자나반음반양나후타나리나쉬메일이다
	IF PENIS(PLAYER)
		;아이사이즈
		IF TALENT:PLAYER:아이사이즈
			;소인체형가 아니다
			SIF TALENT:PLAYER:소인체형 == 0
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:PLAYER:말자지
			;소인체형
			IF TALENT:PLAYER:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:PLAYER:포경
			PRINT 포경
		PRINT 페니스를
	ELSE
		PRINT 유사페니스를
	ENDIF
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINTFORM 가 메워진 채로의
	ELSE
		PRINTFORM 그
		;윤활LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 되었다
	ENDIF
	PRINTFORM 아누스로 삼켜 갔다…
ENDIF


;===========================================================
;SM·하드계
;===========================================================
;-------------------------------------------------
;130,밧줄
;-------------------------------------------------
@MESSAGE_COM130
IF !TEQUIP:밧줄
	PRINTFORM %CALLNAME%를밧줄로 묶어 사지의 자유를 빼앗았다…
;해제시
ELSE
	PRINTFORMW %CALLNAME%로부터밧줄를 풀었다
ENDIF


;-------------------------------------------------
;131,아이마스크
;-------------------------------------------------
@MESSAGE_COM131
;해제시
IF TEQUIP:아이마스크
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%로부터아이마스크를 제외해 주었다
ELSE
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에아이마스크를 대게 했다
ENDIF


;-------------------------------------------------
;132,볼개그
;-------------------------------------------------
@MESSAGE_COM132
;해제시
IF TEQUIP:볼개그
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%로부터볼개그를 취해 주었다
ELSE
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에볼개그를 끼우게 했다
ENDIF


;-------------------------------------------------
;133,정조대
;-------------------------------------------------
@MESSAGE_COM133
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

IF TEQUIP:정조대
	PRINTFORM %CALLNAME%의 하반신을 경고하는정조대의 열쇠를 제외해,벗게 해 주었다…
ELSE
	PRINTFORM %CALLNAME%에정조대를 입게 해 제대로 자물쇠를 채웠다…
ENDIF


;-------------------------------------------------
;134,사정관리
;-------------------------------------------------
@MESSAGE_COM134
PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

IF TEQUIP:사정관리
	PRINTFORM %CALLNAME%를 경고하는리본를 풀어 주었다…
ELSE
	PRINTFORM %CALLNAME%의페니스의 근원에,
	;고문중
	IF TEQUIP:지하실
		PRINTFORML 사정관리용의리본를 삐걱삐걱 묶었다
	ELSE
		PRINTFORML 프릴이 붙은 사랑스러운리본를 힘들게 묶었다
	ENDIF
	PRINT 혈류가 모여 단단히 조일 수 있어 귀두가 검붉게 팽창하고 있다…
ENDIF


;-------------------------------------------------
;140,스팽킹
;-------------------------------------------------
@MESSAGE_COM140

PRINTFORM %CALLNAME:PLAYER%는

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;바지,속옷(정조대는 포함하지 않는다),스타킹계는 위로부터
IF BOTTOMS(TARGET) >= 4
	A = 4
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터
ELSEIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	A = 1
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터
ELSEIF TEQUIP:속옷（아래） && !TEQUIP:정조대
	A = 2
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터
ENDIF

;고문중
IF TEQUIP:지하실
	PRINTFORM %CALLNAME%의 엉덩이를스팽킹용의 패들로 고정시켜두었다…
ELSE
	PRINTFORM %CALLNAME%의 엉덩이를 평손로 강하게 두드렸다…
ENDIF


;-------------------------------------------------
;141,젖스팽킹
;-------------------------------------------------
@MESSAGE_COM141
PRINTFORM %CALLNAME:PLAYER%는

;착의 플레이
IF TEQUIP:상의 || TOPS(TARGET)
	IF TOPS(TARGET)
		A = 6
	ELSEIF TEQUIP:상의
		A = 5
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터
ENDIF

PRINTFORM %CALLNAME%의

IF TALENT:거유
	PRINT 풍만한 유방을
ELSEIF TALENT:폭유
	PRINT 너무 풍부한 유방을
ELSEIF TALENT:빈유
	PRINT 가까스로 부푼 곳의 확인 할 수 있는가슴를
ELSEIF TALENT:절벽
	PRINT 부푼 곳이 거의 전무인 평평한가슴를
ELSE
	PRINT 가슴를
ENDIF

;고문중
IF TEQUIP:지하실
	PRINTL 스팽킹용의 패들로 고정시켜두었다…
ELSE
	PRINTL 평손로 강하게 두드렸다…
ENDIF

;유두클립 붙어 있을 때,랜덤으로 튀어난다(치는 것손의기교가 필요)
IF TEQUIP:유두클립 && ABL:PLAYER:기교
	R = 50/ABL:PLAYER:기교
	IF !RAND:R
		PRINTW 하면,그 박자에 유두에 문 클립이,힘차게 튀어날았다…
		TEQUIP:유두클립 = 0
		SOURCE:쾌B += 800
	ENDIF
	R = 0
ENDIF

;-------------------------------------------------
;142,채찍
;-------------------------------------------------
@MESSAGE_COM142
PRINTFORM %CALLNAME:PLAYER%는채찍를 치켜들어,

;성기
IF TFLAG:56 == 1
	;착의 플레이
	;바지,타이츠,속옷은 위로부터
	IF BOTTOMS(TARGET) >= 4
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 위로부터
	ELSEIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		A = 1
		CALL CLOTHES_NAME(TARGET)
		PRINT 위로부터
	ELSEIF TEQUIP:속옷（아래） && !TEQUIP:정조대
		A = 2
		CALL CLOTHES_NAME(TARGET)
		PRINT 위로부터
	ENDIF
	
	PRINTFORML %CALLNAME%의 성기를 노려 고정시켜두었다
	
	;클립 붙어 있을 때,랜덤으로 튀어난다(치는 것손의기교가 필요)
	IF TEQUIP:음핵클립 && ABL:PLAYER:기교
		R = 50/ABL:PLAYER:기교
		IF !RAND:R
			PRINTL 그채찍는 정확하게 음핵을 붙잡아,물고 있던 클립을 튕겨날렸다…
			TEQUIP:음핵클립 = 0
			SOURCE:쾌C += 800
		ENDIF
		R = 0
	ENDIF
ELSEIF TFLAG:56 == 2
	;착의 플레이
	IF TEQUIP:상의 || TEQUIP:전신상의
		IF TEQUIP:전신상의
			A = 6
		ELSEIF TEQUIP:상의
			A = 5
		ENDIF
		CALL CLOTHES_NAME(TARGET)
		PRINT 위로부터
	ENDIF
	
	PRINTFORML %CALLNAME%의 유두를 노려 고정시켜두었다
	
	;클립 붙어 있을 때,랜덤으로 튀어난다(치는 것손의기교가 필요)
	IF TEQUIP:유두클립 && ABL:PLAYER:기교
		R = 50/ABL:PLAYER:기교
		IF !RAND:R
			PRINTL 그채찍는 정확하게 유두를 붙잡아,물고 있던 클립을 튕겨날렸다…
			TEQUIP:유두클립 = 0
			SOURCE:쾌B += 800
		ENDIF
		R = 0
	ENDIF
ELSE
	;착의 플레이
	SIF TEQUIP:전신상의 || TEQUIP:상의 || TEQUIP:하의
		PRINT 옷 위로부터
	
	PRINTFORML %CALLNAME%의 몸을 몇번이나 고정시켜두었다
	
	;전회 커맨드가촛불라고,촛농이 난다
	SIF PREVCOM == 144
		PRINTL 채찍가 피부를 칠 때에,몸에 부착해 굳어지고 있던 촛농이 기분 좋게 튀어 흩어졌다…
ENDIF


;-------------------------------------------------
;143,바늘
;-------------------------------------------------
@MESSAGE_COM143
PRINTFORM %CALLNAME:PLAYER%는
;조
IF TFLAG:56 == 3 && TEQUIP:지하실
	PRINTFORM %CALLNAME%의손를 취해,그 손가락과 손톱의 사이에 굵은바늘을 쑤셔 넣었다…
;성기
ELSEIF TFLAG:56 == 1
	PRINTFORM %CALLNAME%의 성기에
	;조교자가바느질잘함나새드나지하실
	IF TALENT:PLAYER:바느질잘함 || TALENT:PLAYER:새드 || TEQUIP:지하실
		PRINT 가차 없이바늘을 찔렀다…
	ELSE
		PRINT 바늘을 꽂았다…
	ENDIF
;유두
ELSEIF TFLAG:56 == 2
	SIF TEQUIP:유두클립
		PRINTFORM 클립에 끼여,납작 무너졌다
	PRINTFORM %CALLNAME%의 유두에
	;조교자가바느질잘함나새드나지하실
	IF TALENT:PLAYER:바느질잘함 || TALENT:PLAYER:새드 || TEQUIP:지하실
		PRINT 가차 없이바늘을 찔렀다…
	ELSE
		PRINT 바늘을 꽂았다…
	ENDIF
ELSE
	;팔이 없으면발에
	IF TALENT:양팔결손
		PRINTFORMW %CALLNAME%의발의,피부의 얇은 내퇴[內腿]를 노려바늘을 찔렀다…
	;발가 없으면 팔에
	ELSEIF TALENT:양다리결손
		PRINTFORMW %CALLNAME%의 팔을 잡아,바늘을 꽂았다…
	;양쪽 모두 있으면 랜덤으로 팔
	ELSEIF RAND:2 == 0
		PRINTFORMW %CALLNAME%의 팔을 잡아,바늘을 꽂았다…
	;양쪽 모두 있으면 랜덤으로발
	ELSE
		PRINTFORMW %CALLNAME%의발의,피부의 얇은 내퇴[內腿]를 노려바늘을 찔렀다…
	ENDIF
ENDIF



;-------------------------------------------------
;144,촛불
;-------------------------------------------------
@MESSAGE_COM144
	;밧줄등으로 긴박중
	SIF TEQUIP:밧줄
		PRINTFORM 속박되어 움직일 수 없다
	;성기
	IF TFLAG:56 == 1
		PRINTFORM %CALLNAME%의발를 열게 해
		;페니스가 있으면 우선
		IF PENIS(TARGET)
			IF TEQUIP:전동오나홀
				PRINT 음구에 휩싸여진페니스의 주위에
			ELSE
				IF BASE:사정 >= 8000
					PRINT 딱딱 발기했다
				ELSEIF BASE:사정 >= 2000
					PRINT 느슨하게 일어섰다
				ELSE
					PRINT 쇠약해졌다
				ENDIF
				PRINTFORM 페니스를 중심으로
			ENDIF
		ELSE
			IF TEQUIP:클리캡
				SIF TEQUIP:바이브
					PRINT 바이브를 물었다
				IF TALENT:작은체구 || TALENT:소인체형
					PRINT 어린 균열에
				ELSE
					PRINT 비부[秘部]에
				ENDIF
			ELSE
				SIF PALAM:욕정 >= PALAMLV:3
					PRINT 새빨갛게 충혈했다
				PRINTFORM 클리토리스 목표로 해
			ENDIF
		ENDIF
	;유두
	ELSEIF TFLAG:56 == 2
		PRINTFORM %CALLNAME%의
		SIF ABL:B감각 >= 3
			PRINT 민감한
		PRINTFORM 유두에,
	;설
	ELSEIF TFLAG:56 == 3
		PRINTFORM %CALLNAME%에 혀를 내밀게 해
	;상반신,하반신
	ELSE
		IF RAND:3 == 0
			PRINTFORM %CALLNAME%의 엉덩이나 등에,남김없이
		ELSEIF RAND:2 == 0
			PRINTFORM %CALLNAME%의 옆구리나 넓적다리등,피부의 얇은 부분을 중심으로
		ELSE
			PRINTFORM %CALLNAME%의 몸에,
		ENDIF
	ENDIF
	SIF TEQUIP:지하실
		PRINT 화상 입을 것 같을 정도
	PRINTFORMW 열기납을 늘어뜨렸다…
	

;-------------------------------------------------
;145,삼각목마
;-------------------------------------------------
@MESSAGE_COM145
IF TEQUIP:삼각목마
	PRINTFORML 고통에 신음하는%CALLNAME%를,삼각목마 위로부터 구제해 주었다…
ELSE
	;밧줄등으로 긴박중
	IF TEQUIP:밧줄
		PRINTFORM 속박되어 움직일 수 없는%CALLNAME%를,
	ELSE
		PRINTFORM %CALLNAME%를밧줄로 단단히 묶어,
	ENDIF
	SIF TEQUIP:지하실
		PRINTFORM 예리한 키의
	PRINTFORML 목마에 걸치게 했다…
ENDIF

;-------------------------------
;장착시 메세지
@EQUIP_MESSAGE_COM145
SIF FLAG:6 == 0
	RETURN 0

PRINTL 
PRINTFORML %CALLNAME%는,목마에 걸친 채로 꾸짖어지고 있다.
PRINTL 
IF ABL:마조끼 <= 2
	PRINTFORML 비부[秘部]에 먹혀드는 예리한 키의 격통에,소리를 높이는 것도 괴로운 듯하다…
ELSEIF ABL:마조끼 <= 4
	PRINTFORML 이마에 비지땀을 띄워,사타구니에 먹혀드는 키의 격통에 고민의 소리를 높이고 있다…
ELSE
	PRINTFORML 격통에 고민의 소리를 높이면서도,어딘가 황홀로 한 표정을 띄우고 있다…
ENDIF
WAIT

;-------------------------------------------------
;150,가구로한다
;내용이 애매한 것으로 랜덤 표시에. 맞지 않는 사람은 지문 호출 컷 해 주세요.
;-------------------------------------------------
@MESSAGE_COM150
IF RAND:4 == 0 && ITEM:저온초
	IF RAND:3 == 0 && EXP:V경험 >= 10 && !TALENT:처녀 && !TALENT:재생처녀
		PRINTFORM 굴곡위로 한%CALLNAME%의바기나에촛불를 꽂아넣어,촛대로서 보내게 했다…
	ELSEIF RAND:2 == 0 && EXP:A경험 >= 10
		PRINTFORM 굴곡위로 한%CALLNAME%의 아누스에촛불를 꽂아넣어,촛대로서 보내게 했다…
	ELSE
		;손도발도 사용할 수 없으면 위에 둔다
		IF TALENT:달마
			PRINTFORM 눕힌 %CALLNAME%의 위에촛불를 세워,촛대로서 보내게 했다…
		;손를 사용할 수 없으면발에
		ELSEIF TALENT:양팔결손
			PRINTFORM %CALLNAME%의발에촛불를 사이에 두게 해 촛대로서 보내게 했다…
		;손를 사용할 수 있으면손에
		ELSE
			PRINTFORM %CALLNAME%의손에촛불를 갖게해 촛대로서 보내게 했다…
		ENDIF
	ENDIF
	ITEM:저온초 -= 1
ELSEIF RAND:3 == 0
	IF EXP:V경험 && VAGINA(TARGET)
		PRINTFORM 굴곡위로 한%CALLNAME%의바기나에 꽃을 꽂아,화병으로 감상했다…
	ELSEIF EXP:A경험
		PRINTFORM 굴곡위로 한%CALLNAME%의애널에 꽃을 꽂아,화병으로 감상했다…
	ELSEIF EXP:U경험 && PENIS(TARGET)
		PRINTFORM 앉게 한%CALLNAME%의페니스의 요도에 꽃을 꽂아 넣어,화병으로 감상했다…
	ELSE
		PRINTFORM 앉게 한%CALLNAME%의 비밀스런 장소에 꽃을 꽂아,화병으로 감상했다…
	ENDIF
ELSEIF RAND:2 == 0
	PRINTFORM 네발로 엎드리게 한%CALLNAME%를 테이블에 본지 얼마 안 되는,위에 티 세트를 두어 차를 즐겼다…
ELSE
	PRINTFORM 네발로 엎드리게 한%CALLNAME%의 위에 걸터앉아,의자로서 보내게 했다…
ENDIF
PRINTL

;-------------------------------------------------
;151,고문 ;이건 왜 내용이 없지?
;-------------------------------------------------
@MESSAGE_COM151

;-------------------------------------------------
;152,매도한다
;-------------------------------------------------
@MESSAGE_COM152
PRINTFORM %CALLNAME:PLAYER%는,생각나는 한 갖은 험담을%CALLNAME%에 퍼부었다…

;-------------------------------------------------
;153,발로 밟는다
;-------------------------------------------------
@MESSAGE_COM153
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%를 차 넘어뜨려,가차 없이 짓밟았다…

IF TALENT:기절
	PRINTFORML 
	PRINTFORML %CALLNAME:MASTER%의 공격으로%CALLNAME%는 의식을 되찾은 것 같다…
ENDIF


;-------------------------------------------------
;154,배빵
;-------------------------------------------------
@MESSAGE_COM154
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%를 질질 끌어 넘어뜨려,그 배를 후려갈겼다…

IF TALENT:기절
	PRINTFORML 
	PRINTFORML %CALLNAME:MASTER%의 공격으로%CALLNAME%는 의식을 되찾은 것 같다…
;랜덤으로 구토
ELSEIF RAND:5 == 0
	PRINTFORML 
	PRINTFORML %CALLNAME%는 그 충격에 계속 참지 못하고,위안의 물건을 모두 되돌려 버린 것 같다…
	;구토시의 소스 변동 따위
	CALL NAUSEA_CHECK2
ENDIF

;-------------------------------------------------
;155,목조르기
;-------------------------------------------------
@MESSAGE_COM155
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의
;소인체형
IF TALENT:소인체형
	PRINT 부드럽고 가냘프다
;작은체구
ELSEIF TALENT:작은체구
	PRINT 호리호리 했다
;큰체구
ELSEIF TALENT:큰체구
	PRINT 단단하게 씩씩하다
ENDIF
PRINTFORM 목을 조였다…


;-------------------------------------------------
;156,낙인
;-------------------------------------------------
@MESSAGE_COM156
;노예의 몸을 검시
IF TALENT:낙인
	PRINTFORMW %CALLNAME%의 몸에는,벌써 %CSTR:1%의 표가 깊게 새겨지고 있다…
	PRINTFORML 
ENDIF

PRINTFORML 자,그럼,어떻게 하지…
PRINTL [0] -   그만둔다
IF TALENT:낙인 && NO:MASTER != S
	PRINTL [10] -   모두 자신의 표에 다시 붙인다
ELSE
	SIF (TALENT:낙인 & 1) == 0
		PRINTL [1] -   칼로 새긴다
	SIF (TALENT:낙인 & 2) == 0
		PRINTL [2] -   문신을 새긴다
	SIF (TALENT:낙인 & 4) == 0
		PRINTL [3] -   인두로 지진다
	SIF (TALENT:낙인 & 8) == 0
		PRINTL [4] -   피어스를 끼운다
ENDIF
$INPUT_LOOP1
INPUT
IF RESULT == 0
	PRINTFORML 오늘은 그만두자…
	TFLAG:56 = 0
;덧쓰기
ELSEIF RESULT == 20 && TALENT:낙인 && NO:MASTER != S
	PRINTFORML %CSTR:1%가 남긴낙인을 ,모두 %CALLNAME:PLAYER%의 물건에 덧쓰기하기로 했다
	
	;지금의 주인이연모·공포 대상
	IF TALENT:연모 || TALENT:복종 || TALENT:친애 || TALENT:예속
		PRINTFORML %CALLNAME%는 일찍이 맛본통증를 생각해 냈는지 일순간 기가 죽었지만,
		PRINTFORML %CSTR:1%의낙인을 지울 수 있다면과 협력적이다…
	;연모·공포 대상으로 밀린낙인이다
	ELSEIF CFLAG:41 == S
		PRINTFORML %CALLNAME%는,%CSTR:1%의 표를 지우고 싶지 않으면 울어 간원 했지만,
		PRINTFORML 가르쳐 둔 약이 효능 내면 원망하는 듯한 시선을 보내는 만큼 되었다…
	ELSE
		PRINTFORML %CALLNAME%는 이것도 저것도 단념한 것처럼 되는 대로로 있다…
	ENDIF
	
	;상흔
	SIF TALENT:낙인 & 1
		TFLAG:56 |= 1
	;문신
	SIF TALENT:낙인 & 2
		TFLAG:56 |= 2
	;소인
	SIF TALENT:낙인 & 4
		TFLAG:56 |= 4
	;피어스
	SIF TALENT:낙인 & 8
		TFLAG:56 |= 8
;상처를 붙인다
ELSEIF RESULT == 1 && (TALENT:낙인 & 1) == 0
	PRINTFORM 나이프를 꺼내%CALLNAME%에 향하면
	IF TALENT:연모 || TALENT:복종 || TALENT:친애 || TALENT:예속
		PRINTFORML 무서워해 날뛰기 시작했지만,
		PRINTFORML 소유물의 증거라고 들으니 얌전하게 %CALLNAME:MASTER%의 칼날을 받아들였다…
	;연모·공포 대상이 따로 있을 때저항＋
	ELSEIF CFLAG:40
		PRINTFORML 격렬하게 날뛰기 시작했지만,
		PRINTFORML 무리하게 억누르고 몇차례 베어 붙이는 동안에 저항은 그쳐 갔다…
	ELSE
		PRINTFORML 무서워해 날뛰기 시작했지만,
		IF PENIS(TARGET)
			PRINT 국부에
		ELSE
			PRINT 목덜미에
		ENDIF
		PRINTFORML 칼날을 맞혀 위협하면 저항을 단념해,점잖고 %CALLNAME:MASTER%의 칼날에 참고 있다…
	ENDIF
	TFLAG:56 |= 1
;문신을 넣는다
ELSEIF RESULT == 2 && (TALENT:낙인 & 4) == 0
	IF TALENT:연모 || TALENT:복종 || TALENT:친애 || TALENT:예속
		PRINTFORML %CALLNAME%는,%CALLNAME:MASTER%의 이름을 넣어 받을 수 있으니까 라며,
		PRINTFORML 바늘을 찔리는통증에 참고 있다
	ELSE
		PRINTFORML 가르쳐 둔 약에저항을 그만둔%CALLNAME%를 재워,%CALLNAME:MASTER%의 이름을 새겨 넣었다
	ENDIF
	TFLAG:56 |= 2
;소인을 누른다
ELSEIF RESULT == 3 && (TALENT:낙인 & 8) == 0
	PRINTFORM 공포에 얼굴을 비뚤어지게 하고 그만두어 주듯이 간원 하는 %CALLNAME%
	IF TALENT:연모 || TALENT:복종 || TALENT:친애 || TALENT:예속
		PRINTL 를 소유물의 증거이니까와 설득해,
	ELSE
		PRINTL 를 무리하게 억눌러,
	ENDIF
	PRINTFORML 충분히 가열한 인두를 몸에 강압했다
	TFLAG:56 |= 4
;피어스를 연다
ELSEIF RESULT == 4 && (TALENT:낙인 & 2) == 0
	IF TALENT:연모 || TALENT:복종 || TALENT:친애 || TALENT:예속
		PRINTFORML %CALLNAME%는,%CALLNAME:MASTER%의 표가 된다면과 몸에 구멍을 뚫을 수 있는통증에 참고 있다
	ELSE
		PRINTFORML 가르쳐 둔 약에저항을 그만둔%CALLNAME%를 재워,몸에 피어스를 열어 주었다
	ENDIF
	TFLAG:56 |= 8
ELSE
	GOTO INPUT_LOOP1
ENDIF

;-------------------------------------------------
;157,물고문
;-------------------------------------------------
@MESSAGE_COM157
PRINTFORM %CALLNAME%의 머리를 움켜잡고,
IF TEQUIP:목욕탕
	PRINTL 머리부터 욕조에 가라앉혀 주었다…
ELSE
	PRINTL 물을 받아놓은 대야에 가라앉혀 주었다…
ENDIF

;-------------------------------------------------
;158,간지럼고문
;-------------------------------------------------
@MESSAGE_COM158
SIF TEQUIP:밧줄
	PRINT 속박되어 움직일 수 없는
PRINTFORM %CALLNAME%를 억눌러,그 체내를 마구 간질였다…

;-------------------------------------------------
;160,피스트퍽
;-------------------------------------------------
@MESSAGE_COM160
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열어,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

;윤활LV4 이상
SIF PALAM:윤활 >= PALAMLV:4
	PRINT 흠뻑 젖었다
;작은체구
IF TALENT:작은체구
	PRINT 어린 균열에
ELSE
	PRINT 비밀의 틈[秘裂]에
ENDIF

;소인체형
SIF TALENT:PLAYER:소인체형
	PRINT 작은
PRINTFORM 주먹을 찔러 넣었다…

;-------------------------------------------------
;161,애널피스트
;-------------------------------------------------
@MESSAGE_COM161
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

;윤활LV4 이상
SIF PALAM:윤활 >= PALAMLV:4
	PRINT
PRINT 아누스에

;소인체형
SIF TALENT:PLAYER:소인체형
	PRINT 작은
PRINTFORM 주먹을 찔러 넣었다…


;-------------------------------------------------
;162,양구멍피스트
;-------------------------------------------------
@MESSAGE_COM162
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

;윤활LV4 이상
SIF PALAM:윤활 >= PALAMLV:4
	PRINT 흠뻑 젖었다
;작은체구
IF TALENT:작은체구
	PRINT 어린 균열과
ELSE
	PRINT 비밀의 틈[秘裂]과
ENDIF
PRINT 아누스에

;소인체형
SIF TALENT:PLAYER:소인체형
	PRINT 작은
PRINTFORM 양의주먹을 찔러 넣었다…

;-------------------------------------------------
;163,본체간
;-------------------------------------------------
@MESSAGE_COM163
PRINTFORM %CALLNAME:MASTER%는%CALLNAME%의
	IF CFLAG:도종 % 10 == 9
		PRINT 검
	ELSEIF CFLAG:도종 % 10 == 8
		PRINT 나기나타
	ELSEIF CFLAG:도종 % 10 == 7
		PRINT 창
	ELSE
		PRINT 칼
	ENDIF
PRINTFORM 본체를 잡으면%CALLNAME%의 뇌에 직접,
;에로한 이미지
IF TFLAG:56 == 1
	PRINTFORML 감미로운 이미지를 흘려 넣었다
	PRINTFORML %CALLNAME%는 공허한 눈동자로 허공을 응시하면서,단정치 못한 소리를 지르고 있다…
;무서운 이미지
ELSEIF TFLAG:56 == 2
	PRINTFORML 무서운 이미지를 흘려 넣었다
	PRINTFORML %CALLNAME%는 공허한 눈으로 허공을 응시하면서,덜컹덜컹신체를 진동시키고 있다…
;행복한 이미지
ELSEIF TFLAG:56 == 3
	PRINTFORML 환희로 가득 찬 이미지를 흘려 넣었다
	PRINTFORML %CALLNAME%는 공허한 눈으로 허공을 응시하면서,기분 나쁜 웃음소리를 주고 있다…
ENDIF

;===========================================================
;스카계
;===========================================================
;-------------------------------------------------
;180,관장
;-------------------------------------------------
@MESSAGE_COM180
PRINTFORM %CALLNAME%의

;착의 때
;노출 가능한 옷이 있으면,우선노출 시킨다
IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
ENDIF

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORML 후 구멍에관장액을 흘려 넣었다…

;마조끼
IF ABL:마조끼 == 0
	PRINTFORM %CALLNAME%는 고민의 표정을 띄우고 있다
ELSEIF ABL:마조끼 == 1
	PRINTFORM %CALLNAME%는 비지땀을 띄워,배설감에 견디고 있다
ELSEIF ABL:마조끼 == 2
	PRINTFORM %CALLNAME%는 괴로운 듯한 표정을 하면서도,얼굴을 홍조 시키고 머뭇머뭇엉덩이를 흔들고 있다
ELSEIF ABL:마조끼 == 3
	PRINTFORM %CALLNAME%는,배설감에 시달리면서도,때때로 황홀로 한 표정을 띄우고 있다
ELSEIF ABL:마조끼 == 4
	PRINTFORM %CALLNAME%는,넋을 잃은 표정을 띄워,배설감과 복통을 즐기고 있다
ELSEIF ABL:마조끼 >= 5
	PRINTFORML %CALLNAME%는,배설감을 마음껏 맛보고 있다
	PRINTFORM %CALLNAME%의 표정은 쾌감에 야무지지 못하게 느슨해져,그입
	SIF VAGINA(TARGET)
		PRINT 와 질구
	PRINTFORM 로부터는 뚝뚝 군침을 방울져 떨어지게 하고 있다
ENDIF


;-------------------------------------------------
;181,육변기
;-------------------------------------------------
@MESSAGE_COM181
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회 커맨드가 A섹스
IF TFLAG:60 && (PREVCOM >= 110 &&  PREVCOM == 116) && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME%에 삽입한 채로,
ELSE
	;착의 때
	;노출 가능한 옷이 있으면,우선노출 시킨다
	IF BOTTOMS(TARGET) >= 1 && BOTTOMS(TARGET) <= 3
		;본테이지
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트의 지퍼를 열면,
		;스커트
		ELSEIF BOTTOMS(TARGET) == 1
			PRINT 스커트를 걷어 올리면,
		;그 이외의전신상의
		ELSEIF BOTTOMS(TARGET) == 2
			A = 4
			CALL CLOTHES_NAME(TARGET)
			PRINT 의 옷자락을 걷어 올리면,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 찢어,
		ENDIF
	ENDIF
	
	;애널바이브애널비즈
	IF TEQUIP:애널바이브 || TEQUIP:애널비즈
		IF TEQUIP:애널바이브 && TALENT:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:애널바이브 && TALENT:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 를 문 채로의
	ENDIF
	;윤활LV4 이상
	SIF PALAM:윤활 >= PALAMLV:4
		PRINT 점액 투성이가 되었다
	PRINTFORM 아누스에페니스를 찔러 넣어,
ENDIF
PRINTFORML 장내에 방뇨했다…

PRINTFORM 대량의 뜨거운액체가 따라지는 감각에,
;마조끼
IF ABL:마조끼 == 0
	PRINTFORML %CALLNAME%는 고민의 표정을 띄우고 있다
ELSEIF ABL:마조끼 == 1
	PRINTFORML %CALLNAME%는 비지땀을 띄우고 있다
ELSEIF ABL:마조끼 == 2
	PRINTFORML %CALLNAME%는 괴로운 듯한 표정을 하면서도,얼굴을 홍조 시키고 머뭇머뭇엉덩이를 흔들고 있다
ELSEIF ABL:마조끼 == 3
	PRINTFORML %CALLNAME%는,배설감에 시달리면서도,때때로 황홀로 한 표정을 띄우고 있다
ELSEIF ABL:마조끼 == 4
	PRINTFORML %CALLNAME%는,넋을 잃은 표정을 띄워,배설감과 복통을 즐기고 있다
ELSEIF ABL:마조끼 >= 5
	PRINTFORML %CALLNAME%는,배설감을 마음껏 맛보고 있다
ENDIF


;-------------------------------------------------
;182,방뇨
;-------------------------------------------------
@MESSAGE_COM182
;벌써카테터가 들어가있는 경우는,무리하게 내게 할 수가 있다
IF TEQUIP:카테터
	;촉수플레이중
	IF TEQUIP:촉수
		PRINTFORM 촉수에 명령하면,%CALLNAME%의 요도에 꽂아진극세촉수로부터 모여 있던 오줌이 분출했다…
	ELSE
		PRINTFORM %CALLNAME%의 요도에 꽂아진카테터를 안쪽에 밀어넣으면,모여 있던 오줌이 분출했다…
	ENDIF
ELSE
	;역조교모드가 아니다
	IF !TEQUIP:역조교
		SIF !TALENT:PLAYER:소인체형 && TALENT:소인체형 && !TEQUIP:야외플레이 && TEQUIP:머리장식!= 2
			PRINTFORM %CALLNAME%를 작은아이와 같이 양발를 안아
		PRINTFORM %CALLNAME:PLAYER%가
		;하의류를 입고 있다
		IF (TEQUIP:속옷（아래） && !TEQUIP:정조대) || BOTTOMS(TARGET) >= 3 || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
			PRINT 착의인 채 흘림을 하는 듯
		;야외에서,견이 붙이고 있다
		ELSEIF TEQUIP:야외플레이 && TEQUIP:머리장식 == 2 && !TEQUIP:밧줄
			PRINT 개와 같이 편발를 올려 방뇨하는 듯
		ENDIF
		PRINTFORM 명하면
		
	ENDIF

	IF TEQUIP:촉수
		PRINT 촉수에 얽어매졌다
	ELSEIF TEQUIP:밧줄
		PRINT 단단히 묶어졌다
	ENDIF
	PRINTFORM %CALLNAME%는,
	
	IF ABL:노출벽 >= 6 || (ABL:노출벽 >= 4 && TALENT:수치심없음)
		PRINT 어딘가 도취한 것 같은 표정으로
	;수치플레이중
	ELSEIF TEQUIP:수치플레이 && ABL:노출벽 >= 4
		PRINT 거울에 비치는 자신의 모습에 브르르 몸을 진동시키면,
	ELSEIF ABL:노출벽 >= 4
		PRINT 수치와 쾌락의 뒤섞인 복잡한 표정으로,
	ELSEIF TALENT:수치심없음
		PRINT 타인에게 보여지는 것 따위 신경쓰지 않는 것 같아,
	ELSEIF TALENT:부끄럼쟁이
		PRINT 수치와 굴욕감에 눈물을 머금으면서
	ELSEIF (TALENT:프라이드높음 || TALENT:반항적 || TALENT:건방짐) && !TEQUIP:역조교
		PRINT 분한 듯이 표정을 비뚤어지게 하면서
	ELSE
		PRINT 수치에 뺨을 물들이면서
	ENDIF
	
	SIF TEQUIP:밧줄
		PRINT 동작을 할 수 없는 채
	;하의류를 입고 있다
	IF (TEQUIP:속옷（아래） && !TEQUIP:정조대) || BOTTOMS(TARGET) >= 3 || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		IF BOTTOMS(TARGET) == 3
			PRINT 슈트
		ELSEIF BOTTOMS(TARGET) >= 4
			PRINT 바지
		ELSEIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
			A = 1
			CALL CLOTHES_NAME(TARGET)
		ELSEIF TEQUIP:속옷（아래） && !TEQUIP:정조대
			A = 2
			CALL CLOTHES_NAME(TARGET)
		ENDIF
		PRINTFORML 안에 흘림을 시작했다…
	;야외에서,견이 붙이고 있다
	ELSEIF TEQUIP:야외플레이 && TEQUIP:머리장식 == 2 && !TEQUIP:밧줄
		PRINTL 편발를 올려 방뇨하기 시작했다…
	;촉수플레이중,혹은 아기 포즈
	ELSEIF TEQUIP:촉수 || (!TALENT:PLAYER:소인체형 && TALENT:소인체형)
		PRINTL 오줌을 공중에 흩뿌렸다…
	;긴박중
	ELSEIF TEQUIP:밧줄
		PRINTL 흘림을 시작했다…
	;그 이외
	ELSE
		;야외플레이중
		SIF TEQUIP:야외플레이
			PRINT 풀숲 중(안)에서
		;여자 아이는 주저앉는다
		SIF !PENIS(TARGET) && VAGINA(TARGET)
			PRINT 주저앉아
		PRINTL 방뇨하기 시작했다…
	ENDIF
ENDIF


;-------------------------------------------------
;183,배변
;-------------------------------------------------
@MESSAGE_COM183

;역조교모드가 아니다
IF !TEQUIP:역조교
	;아기 포즈
	SIF !TALENT:PLAYER:소인체형 && TALENT:소인체형
		PRINTFORM %CALLNAME%를 작은아이와 같이 양발를 안아
	
	PRINTFORM %CALLNAME:PLAYER%가
	
	;하의류를 입고 있다
	SIF (TEQUIP:속옷（아래） && !TEQUIP:정조대) || BOTTOMS(TARGET) >= 3 || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		PRINT 착의인 채 탈분을 하는 듯
	PRINTFORM 명하면
ENDIF

IF TEQUIP:촉수
	PRINT 촉수에 얽어매졌다
ELSEIF TEQUIP:밧줄
	PRINT 단단히 묶어졌다
ENDIF
PRINTFORM %CALLNAME%는,

IF ABL:노출벽 >= 6 || (ABL:노출벽 >= 4 && TALENT:수치심없음)
	PRINT 어딘가 도취한 것 같은 표정으로
;수치플레이중
ELSEIF TEQUIP:수치플레이 && ABL:노출벽 >= 4
	PRINT 거울에 비치는 자신의 모습에 브르르 몸을 진동시키면,
ELSEIF ABL:노출벽 >= 4
	PRINT 수치와 쾌락의 뒤섞인 복잡한 표정으로,
ELSEIF TALENT:부끄럼쟁이
	PRINT 수치와 굴욕감에 눈물을 머금으면서
ELSEIF (TALENT:프라이드높음 || TALENT:반항적 || TALENT:건방짐) && !TEQUIP:역조교
	PRINT 분한 듯이 표정을 비뚤어지게 하면서
ELSE
	PRINT 수치에 뺨을 물들이면서
ENDIF
SIF TEQUIP:밧줄
	PRINT 동작을 할 수 없는 채

;하의류를 입고 있다
IF (TEQUIP:속옷（아래） && !TEQUIP:정조대) || BOTTOMS(TARGET) >= 3 || TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트
	ELSEIF BOTTOMS(TARGET) >= 4
		PRINT 바지
	ELSEIF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		A = 1
		CALL CLOTHES_NAME(TARGET)
	ELSEIF TEQUIP:속옷（아래） && !TEQUIP:정조대
		A = 2
		CALL CLOTHES_NAME(TARGET)
	ENDIF
	PRINTL 안에 흘림을 시작했다…
;촉수플레이중,혹은 아기 포즈
ELSEIF TEQUIP:촉수 || (!TALENT:PLAYER:소인체형 && TALENT:소인체형)
	PRINTL 배변하기 시작했다…
;긴박중
ELSEIF TEQUIP:밧줄
	PRINTL 흘림을 시작했다…
;그 이외
ELSE
	;야외플레이중
	IF TEQUIP:야외플레이
		PRINT 풀숲 중(안)에서
	ELSE
		PRINT 그 자리에
	ENDIF
	PRINTL 주저앉아 배변하기 시작했다…
ENDIF

;-------------------------------------------------
;184,음뇨시킨다
;-------------------------------------------------
@MESSAGE_COM184

;그 자리에 오줌이 있다
IF TFLAG:66
	;역조교모드
	IF TEQUIP:역조교
		IF ABL:스캇중독 >= 2 || ABL:마조끼 >= 4 || ABL:순종 >= 5
			PRINTFORML %CALLNAME%는 마루의 오줌에 얼굴을 접근하면,
		ELSE
			PRINTFORML %CALLNAME%는 마루의 오줌에 얼굴을 접근해,얼굴을 찡그리고 있다
		ENDIF
	ELSE
		IF ABL:스캇중독 >= 2 || ABL:마조끼 >= 4 || ABL:순종 >= 5
			PRINTFORML 마루의 오줌을 빨아 예쁘게 하듯이 명령하면,
		ELSE
			PRINTFORML %CALLNAME%의 머리를 잡아,오줌이 모인 마루에 강압했다
		ENDIF
	ENDIF
	
	;스캇중독 3/(마조끼 4+스캇중독 2)로부터 흘리지 않게 된다
	IF ABL:스캇중독 >= 3 || (ABL:마조끼 >= 4 && ABL:스캇중독 >= 2)
		IF ABL:스캇중독 >= 5
			PRINTFORM %CALLNAME%는 꿀꺽꿀꺽목을 울려,
		ELSEIF ABL:마조끼 >= 5
			PRINTFORM %CALLNAME%는 흥분에 몸을 비틀게 해
		ELSE
			PRINTFORM %CALLNAME%는 어딘가 도취한 것 같은 표정으로,
		ENDIF
		SIF ABL:스캇중독 >= 5 || ABL:마조끼 >= 5
			PRINTFORM 한 방울도 남기는 일 없이
		PRINTFORM 오줌을 정중하게 빨아냈다…
	;스캇중독가 없고,음뇨경험도 낮다고 토해낸다
	ELSEIF !ABL:스캇중독 && EXP:음뇨경험 < 10
		;역조교모드
		IF TEQUIP:역조교
			PRINTFORM 마시려고 한 것 같지만,결국입는 붙이지 않을 수 없었던 것 같다…
		ELSE
			PRINTFORM %CALLNAME%는입를 단단하게 닫아,강압된 오줌의 악취에 참고 있다…
		ENDIF
	;그 이외는 어떻게든 마신다
	ELSE
		IF ABL:스캇중독 || ABL:마조끼 >= 4
			PRINTFORM %CALLNAME%는 가끔 목이 막혀 버리면서
		ELSE
			PRINTFORM %CALLNAME%는 몹시 놀라면서
		ENDIF
		
		IF ABL:스캇중독 >= 2
			PRINTFORM 도,오줌을 모두 다 마신 것 같다…
		ELSEIF ABL:마조끼 >= 4 || ABL:봉사정신 >= 5
			PRINTFORM 도,어떻게든 모두 빨아내려고 분투하고 있다…
		ELSE
			PRINTFORM ,흠칫흠칫 혀를 내밀고 있다……
		ENDIF
	ENDIF
ELSE
	IF ABL:스캇중독 >= 4
		PRINTFORML 기쁜듯이입를 연%CALLNAME%에,오줌을 먹여 주었다
	ELSEIF (ABL:스캇중독 && ABL:마조끼 >= 3) || TEQUIP:역조교
		PRINTFORML 흠칫흠칫입를 연%CALLNAME%에,오줌을 먹여 주었다
	ELSEIF ABL:순종 >= 5 || ABL:봉사정신 >= 5 || ABL:스캇중독 >= 3
		PRINTFORML 얌전하게입를 연%CALLNAME%에,오줌을 먹여 주었다
	ELSEIF ABL:마조끼 >= 4
		PRINTFORML %CALLNAME%의입를 열게 해 오줌을 먹여 주었다
	ELSE
		PRINTFORML 싫어하는%CALLNAME%의 얼굴을 억눌러,오줌을 먹여 주었다
	ENDIF
	
	;스캇중독 3/(마조끼 4+스캇중독 2)로부터 흘리지 않게 된다
	IF ABL:스캇중독 >= 3 || (ABL:마조끼 >= 4 && ABL:스캇중독 >= 2)
		IF ABL:스캇중독 >= 5
			PRINTFORM %CALLNAME%는 꿀꺽꿀꺽목을 울려,
		ELSEIF ABL:마조끼 >= 5
			PRINTFORM %CALLNAME%는 흥분에 몸을 비틀게 해
		ELSE
			PRINTFORM %CALLNAME%는 어딘가 도취한 것 같은 표정으로,
		ENDIF
		SIF ABL:스캇중독 >= 5 || ABL:마조끼 >= 5
			PRINTFORM 한 방울도 흘리는 일 없이
		PRINTFORM %CALLNAME:PLAYER%의 오줌을 다 마셨다…
	;스캇중독가 없고,음뇨경험도 낮다고 토해낸다
	ELSEIF !ABL:스캇중독 && EXP:음뇨경험 < 10
		PRINTFORM %CALLNAME%는 참지 못하고 숨이 막혀,전신을 철벅철벅 적셔 버렸다…
	;그 이외는 어떻게든 마신다
	ELSE
		IF ABL:스캇중독 || ABL:마조끼 >= 4
			PRINTFORM %CALLNAME%는 가끔 목이 막혀 버리면서
		ELSE
			PRINTFORM %CALLNAME%는 몹시 놀라면서
		ENDIF
		
		IF ABL:스캇중독 >= 2
			PRINTFORM 도,오줌을 모두 다 마신 것 같다…
		ELSEIF ABL:마조끼 >= 4 || ABL:봉사정신 >= 5
			PRINTFORM 도,어떻게든 주어진 것을 다 마시자라면 분투하고 있다…
		ELSE
			PRINTFORM ,질식하지 않게,어떻게든 목을 울리고 있다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;185,식분시킨다 ;위도 그렇고 이딴 커맨드는 왜 있어;;
;-------------------------------------------------
@MESSAGE_COM185

;역조교모드
IF TEQUIP:역조교
	IF ABL:스캇중독 >= 2 || ABL:마조끼 >= 4 || ABL:순종 >= 5
		PRINTFORML %CALLNAME%는 마루의 편리하게 얼굴을 접근하면,
	ELSE
		PRINTFORML %CALLNAME%는 마루의 편리하게 얼굴을 접근해,얼굴을 찡그리고 있다
	ENDIF
ELSE
	IF ABL:스캇중독 >= 2 || ABL:마조끼 >= 4 || ABL:순종 >= 5
		PRINTFORML 마루의 물건을 먹어 예쁘게 하듯이 명령하면,
	ELSE
		PRINTFORML %CALLNAME%의 머리를 잡아,마루의 편리하게 강압했다
	ENDIF
ENDIF

PRINTFORM %CALLNAME%는
IF ABL:스캇중독 >= 4
	SIF ABL:스캇중독 >= 5
		PRINTFORM 배설물의 악취와 쓴 맛에 황홀로 하면서,
	PRINT 스스로 낸 변을 모두 먹
	IF TEQUIP:야외플레이
		PRINTL 었다…
	ELSE
		PRINTL 고,마루의 더러움까지 정중하게 빨아냈다…
	ENDIF
ELSEIF ABL:스캇중독 == 3
	IF ABL:마조끼 >= 5
		PRINTFORM 어딘가 도취한 것 같은 표정으로 거기에
	ELSE
		PRINTFORM 심한 냄새에 숨이 막히면서,필사적으로
	ENDIF
	PRINTFORM 혀를 기게 하고 있다…
ELSEIF ABL:스캇중독 >= 2 || ABL:마조끼 >= 4 || ABL:순종 >= 5
	PRINTFORM 구역질를 견디고 그것을입에 포함했지만,삼키지 못하고 고민의 표정을 띄우고 있다…
ELSE
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORM 무엇을 하려고 했는지,곧에 구와 얼굴을 올려 버렸다…
	ELSE
		PRINTFORM 비명을 올리면,심한 악취와 혐오감에 계속 참지 못하고 위안의 물건을 되돌려 버렸다…
		;구토시의 소스 변동 따위
		CALL NAUSEA_CHECK2
	ENDIF
ENDIF


;===========================================================
;커뮤니케이션계
;===========================================================
;-------------------------------------------------
;200,회화
;-------------------------------------------------
@MESSAGE_COM200
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%과회화를 하고 싶은 것 같다

IF KANRAKU(TARGET)
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%과회화를 했다…
ELSE
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%과회화를 시도했다…
ENDIF


;-------------------------------------------------
;201,선물
;-------------------------------------------------
@MESSAGE_COM201
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%에선물를 졸라 왔다…

PRINTFORML 무엇을 줍니까?
PRINTFORML [0] 웃는 얼굴(공짜)
SIF MONEY >= 1000
	PRINTFORML [1] 음식(1000고슈금)
SIF MONEY >= 2500
	PRINTFORML [2] 일용품(2500고슈금)
SIF MONEY >= 10000
	PRINTFORML [3] 악세사리(10000고슈금)
SIF MONEY >= 25000
	PRINTFORML [4] 옷(25000고슈금)
SIF MONEY >= 150000
	PRINTFORML [5] 고가의 물건(150000고슈금)

;경장은 함락이 끝난 상태가 아니면 건네줄 수 없다 ;5000이던걸 게임과 같이 50000으로 수정. 옷보다 더 싸서...
SIF MONEY >= 5000 && KANRAKU(TARGET)
	PRINTFORML [10] 경장(50000고슈금)
	
;----------------------------------
;특별한선물(eraheta로부터 인용)
;----------------------------------
;extra 모드로 엔딩을 보거나 주인→노예의 집착을 지우지 않도록 하기 위한 조치입니다.

;모두 플래그를 주인 측에 기록하기 위해(때문에),복수의 노예에게 동시에 건네줄 수 없습니다.
;또,ED용 아이템에 한정해,원래의 주인,혹은 공범자로 밖에 건네줄 수가 없습니다.
;노예로서 조교해,조수가 된 캐릭터에서는 엔딩은 볼 수 없는,라는 것입니다.
;EXTRA 모드로 엔딩을 봐도,강해서 뉴 게임은 할 수 없습니다.

;FLAG:5 == 0 =스토리 모드미클리어 CFLAG:MASTER:1 == 3 =주인/공범자
;FLAG:40 == 0 =스토리 모드의 클리어 플래그 FLAG:5 == 9 =엔드리스 모드
;CFLAG:139 =엔딩용 아이템 CFLAG:140 =프로미스 링

;----------------------------------
;프로미스 링
;----------------------------------
;주인(공범자)이 아니면 안돼,벌써 누군가에게 건네주고 있으면 안돼,대상의 노예에게 집착을 안지 않았다고 안돼,10만고슈금 없으면 안돼
SIF CFLAG:MASTER:1 == 3 && !CFLAG:MASTER:140 && CFLAG:MASTER:41 == NO:TARGET && MONEY > 100000
	PRINTFORML [50] 프로미스 링

;----------------------------------
;엔딩용 아이템
;이하의 구문은,현재 게임상에서는 표시되지 않게 comment out 되고 있습니다.
;캐릭터의 엔딩을 만들어,이하의선물를 표시시키고 싶은 사람은,
;여기의 comment out를 제외하는 것은 아니고,캐릭터의 개별대사 플래그에 이동시켜 사용해 주세요.
;----------------------------------
;주인(공범자)이 아니면 안돼,벌써 누군가에게 건네주고 있으면 안돼,함락 끝난 노예가 없으면 안돼,
;엔드리스 모드가 아니면 안돼,벌써 클리어가 끝난 상태라면 안돼,100만고슈금 없으면 안돼
;IF CFLAG:MASTER:1 == 3 && !CFLAG:MASTER:139 && KANRAKU(TARGET) && FLAG:5 == 9 && FLAG:40 == 0 && MONEY > 1000000
;	;친애루트는 인게이지 링
;	IF TALENT:연모 || TALENT:친애 || TALENT:상사상애
;		PRINTFORML [90] 인게이지 링
;	;창부루트는 오더 메이드바이브
;	ELSEIF TALENT:음란 || TALENT:창부
;		PRINTFORML [91] 오더 메이드바이브
;	;예속루트는 오더 메이드 목걸이
;	ELSEIF TALENT:복종 || TALENT:예속
;		PRINTFORML [92] 오더 메이드 목걸이
;	ENDIF
;ENDIF
;역조교모드
IF TEQUIP:역조교
	PRINTFORML [99] 줄 수 있는 것은 없다
ELSE
	PRINTFORML [99] 역시 그만둔다
ENDIF

INPUT
;----------------------------------
IF RESULT == 0
	;웃는 얼굴을선물
	TFLAG:17 = 1
ELSEIF RESULT == 1 && MONEY > 1000
	;음식을선물
	TFLAG:17 = 2
	MONEY -= 1000
ELSEIF RESULT == 2 && MONEY > 2500
	;일용품을선물
	TFLAG:17 = 3
	MONEY -= 2500
ELSEIF RESULT == 3 && MONEY > 10000
	;악세사리를선물
	TFLAG:17 = 4
	MONEY -= 10000
ELSEIF RESULT == 4 && MONEY > 25000
	;옷을선물
	TFLAG:17 = 5
	MONEY -= 25000
ELSEIF RESULT == 5 && MONEY > 150000
	;고가의 물건을선물
	TFLAG:17 = 6
	MONEY -= 150000
	
ELSEIF RESULT == 10 && MONEY > 50000 && KANRAKU(TARGET)
	;경장을선물
	TFLAG:17 = 11
	MONEY -= 50000
	;경장입손플래그를 온
	CFLAG:614 = 1
;----------------------------------
;특별한 물건을선물
;----------------------------------
ELSEIF RESULT == 50 && CFLAG:MASTER:1 == 3 && !CFLAG:MASTER:140 && CFLAG:MASTER:41 == NO:TARGET && MONEY > 100000
	;프로미스 링
	TFLAG:17 = 50
;ELSEIF RESULT == 90
;	TFLAG:17 = 7
;	CFLAG:MASTER:139 = 1
;ELSEIF RESULT == 91
;	TFLAG:17 = 7
;	CFLAG:MASTER:139 = 1
;ELSEIF RESULT == 92
;	TFLAG:17 = 7
;	CFLAG:MASTER:139 = 1
ELSEIF RESULT == 99
	PRINTFORMW 선물를 건네주는 것을 멈추었습니다
	TFLAG:17 = 99
ELSE
	RESTART
ENDIF

IF TFLAG:17 == 99
ELSEIF TFLAG:17 == 50
	PRINTFORMW %CALLNAME:MASTER%는%CALLNAME%에 프로미스 링을 건네주었다
	PRINTFORML 
	;수 취해 받을 수 있다
	IF TALENT:상사상애 || TALENT:친애
		PRINTFORM %CALLNAME%는 기쁜듯이수 취해,
		IF TALENT:양팔결손
			PRINTW 반지를 응시하고 있다…
		ELSE
			PRINTW 그것을 약지에 끼워 보였다…
		ENDIF
		CFLAG:MASTER:140 = 1
		CFLAG:TARGET:140 = 1
		MONEY -= 100000
	;수 취해 받을 수 없다
	ELSE
		PRINTFORML …… 이지만,%CALLNAME%는 목을 옆에 흔들고 있다
		PRINTFORMW 수 집어 줄 생각은 없는 것 같다……
		TFLAG:17 = 99
	ENDIF
ELSE
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에
	IF TFLAG:17 == 1
		PRINT 상쾌한 웃는 얼굴
	ELSEIF TFLAG:17 == 2
		PRINT 맛있는 술
	ELSEIF TFLAG:17 == 3
		PRINT 가볍고 가까운 일용품
	ELSEIF TFLAG:17 == 4
		PRINT 예쁜악세사리
	ELSEIF TFLAG:17 == 5
		IF RAND:4 == 0
			PRINT 사랑스러운 옷
		ELSEIF RAND:3 == 0
			PRINT 눈부신 옷
		ELSEIF RAND:2 == 0
			PRINT 근사한 옷
		ELSE
			PRINT 착용감이 좋은 옷
		ENDIF
	ELSEIF TFLAG:17 == 6
		PRINT 기념년의 귀중한 술
	ELSEIF TFLAG:17 == 7
		IF CFLAG:5100 == 1
			PRINT 인게이지 링
		ELSEIF CFLAG:5101 == 1
			PRINT 오더 메이드바이브
		ELSEIF CFLAG:5102 == 1
			PRINT 오더 메이드 목걸이
		ENDIF
	ELSEIF TFLAG:17 == 11
		PRINT 경장
	ENDIF
	PRINTL 를선물 했다…
ENDIF


;-------------------------------------------------
;202,스킨십
;-------------------------------------------------
@MESSAGE_COM202
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%과스킨십 하고 싶은 것 같다
ELSE
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%과의스킨십를 꾀했다
ENDIF

IF TALENT:스킨십불편 && (TALENT:연모 || TALENT:친애)
	PRINTFORM %CALLNAME%는 어쩐지 근지러운 듯이 안절부절 하고 있다…
ELSEIF TALENT:스킨십불편
	PRINTFORM %CALLNAME%는 조금 지내기가 불편한 것 같게 하고 있다…
ELSE
	PRINTFORM %CALLNAME%는 뭔가 기쁜듯이 하고 있다…
ENDIF


;-------------------------------------------------
;203,느긋하게한다
;-------------------------------------------------
@MESSAGE_COM203
;역조교모드
SIF TEQUIP:역조교
	PRINTFORML %CALLNAME%빠져 있거나 하고 싶은 것 같다
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%과 함께 기다리거나 했다…


;-------------------------------------------------
;204,가사
;-------------------------------------------------
@MESSAGE_COM204
;역조교모드
SIF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%과가사를 하고 싶은 것 같다
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%과 함께가사를 끝마치기로 했다…


;-------------------------------------------------
;205,노래연습
;-------------------------------------------------
@MESSAGE_COM205
;역조교모드
SIF TEQUIP:역조교
	PRINTFORML %CALLNAME%는노래연습를 하고 싶은 것 같다
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에노래연습를 시켰다…

;-------------------------------------------------
;206,선물시킨다
;-------------------------------------------------
@MESSAGE_COM206
PRINTFORMW %CALLNAME:PLAYER%는%CALLNAME%에,선물를 주듯이 말해 보았다
IF TALENT:연모 || TALENT:음란 || TALENT:복종 || TALENT:친애 || TALENT:창부 || TALENT:예속
	;연모,친애
	IF TALENT:연모 || TALENT:친애
		PRINTFORML %CALLNAME%는 놀란 모습이다
		PRINTFORM 무려,%CALLNAME:PLAYER%를 위해서(때문에)
		IF RAND:3 == 0
			PRINTL 맛좋은 음식을 만들어 준 것 같다
		ELSEIF RAND:2 == 0
			PRINTL 새로운 옷을 준비해 주고 있던 것 같다
		ELSE
			PRINTL 자신을선물 하고 싶다고 생각하고 있던 것 같다
		ENDIF
		PRINTFORML 즐거운 시간을 보냈다…
	;음란,창부
	ELSEIF TALENT:음란 || TALENT:창부
		PRINTFORM %CALLNAME%는,
		;마조나새드소유로,채찍를 가지고 있다
		IF (TALENT:새드 || TALENT:마조) && ITEM:채찍 == 1 && NOITEM == 0
			PRINTL 새로운채찍를선물 해 주었다
		;A감각 3이상으로,남자나 A감각＞V감각으로,애널바이브를 가지고 있다
		ELSEIF ABL:A감각 >= 3 && (TALENT:남자 || ABL:A감각 > ABL:V감각) && ITEM:애널바이브 == 1
			PRINTL 새로운애널바이브를선물 해 주었다
		;V감각 3이상으로,처녀가 아니고,바이브를 가지고 있다
		ELSEIF ABL:V감각 >= 3 && !TALENT:처녀 && ITEM:바이브 == 1
			PRINTL 새로운바이브를선물 해 주었다
		;B감각 3이상으로,니플캡를 가지고 있다
		ELSEIF ABL:B감각 >= 3 && ITEM:니플캡 == 1
			PRINTL 새로운니플캡를선물 해 주었다
		;로터를 가지고 있다
		ELSEIF ITEM:로터 == 1
			PRINTL 새로운로터를선물 해 주었다
		ELSE
			PRINTL 선물는 자신이라고 말하며,기대고 걸려 왔다
		ENDIF
		PRINTFORML 무엇인가,%CALLNAME%편이 기뻐하고 있는 것 같은 생각이 든다…
	;복종,예속
	ELSEIF TALENT:복종 || TALENT:예속
		PRINTFORML %CALLNAME%자신에게선물 할 수 있는 것은 주인에게로의 봉사만이라고,당황한 모습으로 말했다
	ENDIF
;반발각인Lv3
ELSEIF MARK:반발각인 >= 3
	PRINTFORML %CALLNAME:PLAYER%는 사살할듯한 시선을 받았다…
;그 이외
ELSE
	PRINTFORML %CALLNAME%는,무슨 말을 하고 있다 이 녀석은,이라고 말하고 싶은 듯한 얼굴로 이쪽을 보고 있다…
ENDIF


;-------------------------------------------------
;207,데이트간다
;-------------------------------------------------
@MESSAGE_COM207
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 데이트를 하려고 권해 왔다
ELSE
	PRINTFORML %CALLNAME:PLAYER%과%CALLNAME%는 옷차림을 정돈해데이트간다 것으로 했다
ENDIF

;속옷을 입지 않다(가슴 있어라면 노브라에서도 가능)와노출데이트 선택
IF FLAG:2 & 128 && (!TEQUIP:속옷（아래） || (!BREAST(TARGET) && (TEQUIP:속옷（위） > 6 || !TEQUIP:속옷（위）)))
	PRINTFORML %CALLNAME%는 속옷을 입고 있지 않습니다만,이대로 나갑니까?
	PRINTL [ 0] 노출데이트간다
	PRINTL [99] 그만둔다
	$INPUT_LOOP_DATE
	INPUT
	IF RESULT == 99
		PRINTFORML 일단 돌아와,갈아입게 하기로 했다…
		TFLAG:17 = 99
	ELSEIF RESULT == 0
		PRINTFORMW %CALLNAME%의손를 당겨,그대로 밖에 데리고 나갔다
		PRINTFORML %CALLNAME%는 속옷을 입지 않은 것이 신경이 쓰이는지,
		PRINTFORMW 조마조마 근처를 둘러보면서 걷고 있다…
		TFLAG:17 = 2
	ELSE
		CLEARLINE 1
		GOTO INPUT_LOOP_DATE
	ENDIF
ELSE
	TFLAG:17 = 1
ENDIF


;===========================================================
;애무시킨다계
;===========================================================
;-------------------------------------------------
;220,애무시킨다
;-------------------------------------------------
@MESSAGE_COM220
;역조교모드가 아니다
SIF !TEQUIP:역조교
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%에 스스로의 신체를애무 시켰다

PRINTFORM %CALLNAME%는,
;순종 3 이상
SIF ABL:순종 >= 3
	PRINT 적극적으로
;기교
SIF ABL:기교 >= 3
PRINTFORM %CALLNAME:PLAYER%의
;작은체구
SIF TALENT:PLAYER:작은체구
	PRINT 미발달 말야
PRINTFORM 체를
;봉사정신 3 이상
IF ABL:봉사정신 >= 3
	PRINT 열심히
;기교제로
ELSEIF ABL:기교 == 0
	PRINT 변변치않고
ENDIF
PRINTFORML 애무 해 왔다…


;-------------------------------------------------
;221,가슴을주무르게한다
;-------------------------------------------------
@MESSAGE_COM221
;역조교모드가 아니다
SIF !TEQUIP:역조교
	PRINTFORML %CALLNAME:PLAYER%는,%CALLNAME%에 스스로의가슴를 비비듯이 말했다

PRINTFORM %CALLNAME%는,%CALLNAME:PLAYER%의
;남자나절벽
IF !BREAST(PLAYER)
	PRINTFORML \@ TALENT:PLAYER:큰체구? 가슴 관계 # 어디를 비비면 좋은 것인지 전혀 모르는가슴 \@를
;작은체구＋빈유
ELSEIF TALENT:PLAYER:작은체구 && TALENT:PLAYER:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의가슴를
;빈유
ELSEIF TALENT:PLAYER:빈유
	PRINTL 작은 흔들림의가슴를
;거유
ELSEIF TALENT:PLAYER:거유
	PRINTL 손로부터 흘러넘칠 것 같을 정도의가슴를
;폭유
ELSEIF TALENT:PLAYER:폭유
	PRINTL 손에 남을 만큼 큰가슴를
ELSE
	PRINTL 가슴를
ENDIF

;순종 3 이상
SIF ABL:순종 >= 3
	PRINT 적극적으로
;봉사정신 3 이상
IF ABL:봉사정신 >= 3
	PRINT 열심히
;기교제로
ELSEIF ABL:기교 == 0
	PRINT 변변치않고
ENDIF
PRINTFORML 애무 해 왔다…


;-------------------------------------------------
;222,커닐링구스강제
;-------------------------------------------------
@MESSAGE_COM222
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의 비순[秘唇]을 정중하게 빨고 풀어,
	PRINTFORM 클리토리스를 혀로 빨아 올려 왔다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME%에 %CALLNAME:PLAYER%의 음순을 빨게 했다…
ENDIF

;-------------------------------------------------
;223,손가락삽입시킨다
;-------------------------------------------------
@MESSAGE_COM223
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
	
	;처녀
	SIF TALENT:PLAYER:처녀
		PRINT 남을 모른다
	;작은체구의 유무로 분기
	IF TALENT:PLAYER:작은체구
		PRINT 어린 균열
	ELSE
		PRINT 비밀의 틈[秘裂]
	ENDIF
	
	;바이브의 유무로 분기
	IF TEQUIP:PLAYER:바이브
		PRINTFORM 중(안)에서,요염하게 꾸불꾸불하는바이브를 전후에 빼고 꽂기 해 왔다…
	ELSEIF TEQUIP:PLAYER:로터삽입
		PRINTFORM 로부터 처진,로터의 끈을 느슨하게 이끌어 왔다…
	ELSE
		PRINTL 에 천천히 손가락을 삽입해 왔다…
	ENDIF
;통상 모드
ELSE
	PRINTFORML %CALLNAME%에,%CALLNAME:PLAYER%의바기나를애무 시켰다…
ENDIF

;-------------------------------------------------
;224,애널핥기강제
;-------------------------------------------------
@MESSAGE_COM224
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는,
	PRINTFORM %CALLNAME:PLAYER%의
	;A경험 10 이하
	SIF EXP:PLAYER:A경험 <= 10
		PRINT 딱딱하게 닫혔다
	PRINTFORML 아누스를 정중하게 빨고 풀어 왔다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME:PLAYER%는,%CALLNAME%에애널를 빨게 했다…
ENDIF


;-------------------------------------------------
;225,애널애무시킨다
;-------------------------------------------------
@MESSAGE_COM225

;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는
	;애널바이브
	IF TEQUIP:PLAYER:애널바이브
		PRINTFORML 조금씩 떨리는애널바이브를 전후에 빼고 꽂기 해 왔다…
	;애널비즈
	ELSEIF TEQUIP:PLAYER:애널비즈
		PRINTFORML 비즈의 끈을 잡아당겨 가볍게 출납해 왔다…
	;로터A삽입
	ELSEIF TEQUIP:PLAYER:로터A삽입
		PRINTFORML 애널로부터 처진로터의 끈을,느슨하게 이끌어 왔다…
	;확장벌룬
	ELSEIF TEQUIP:PLAYER:확장벌룬
		PRINTFORML 애널를 한계까지 넓히고 있는 벌룬을 전후에 움직여 왔다…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%의애널를애무 해 왔다…
	ENDIF
;통상 모드
ELSE
	PRINTFORML %CALLNAME%에,%CALLNAME:PLAYER%의애널를애무 시켰다…
ENDIF


;-------------------------------------------------
;226,자위과시한다
;-------------------------------------------------
@MESSAGE_COM226

;역조교모드
SIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에자위를 해 보이도록 들었다

;수 넣는 경우만
PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%에자위를 과시했다…


;-------------------------------------------------
;227,자위과시한다
;-------------------------------------------------
@MESSAGE_COM227

;역조교모드
SIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에자위를 해 보이도록 들었다

;수 넣는 경우만
PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%에자위를 과시했다…


;-------------------------------------------------
;228,벗겨보여주기
;-------------------------------------------------
@MESSAGE_COM228
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에
;아이사이즈
IF TALENT:PLAYER:아이사이즈
	;소인체형가 아니다
	SIF TALENT:PLAYER:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:PLAYER:말자지
	;소인체형
	IF TALENT:PLAYER:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
SIF TALENT:PLAYER:포경
	PRINT 포경
PRINTL 페니스를 과시했다…


;-------------------------------------------------
;229,유두로터 시킨다
;-------------------------------------------------
@MESSAGE_COM229
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
;모유체질
SIF TALENT:PLAYER:모유체질
	PRINT 모유가 방울져 떨어진다
PRINTFORML 유두에 떨리는로터를 꽉 눌러 왔다…


;-------------------------------------------------
;230,니플캡 시킨다
;-------------------------------------------------
@MESSAGE_COM230
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:PLAYER:니플캡
	PRINTFORM 유두로부터니플캡를 떼어냈다
ELSE
	PRINTFORM 유두에니플캡를 끌어당겨 왔다
ENDIF

;-------------------------------------------------
;231,유두클립 시킨다
;-------------------------------------------------
@MESSAGE_COM231
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:PLAYER:유두클립
	PRINTFORM 유두로부터 클립을 제외해 주었다
ELSE
	PRINTFORM 가슴의 돌기에
	;고문중
	SIF TEQUIP:지하실
		PRINT ,톱니모양이 붙은 강력한
	PRINTFORM 클립을 물게 해 왔다…
ENDIF

;-------------------------------------------------
;232,유방전극 시킨다
;-------------------------------------------------
@MESSAGE_COM232
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:PLAYER:착유기
	PRINT 착유기에 전극를 연결해,
ELSEIF TEQUIP:PLAYER:유두클립
	PRINT 유두클립에 전극를 연결해,
ENDIF
PRINTFORM 스윗치를 넣었다…


;-------------------------------------------------
;233,모유 먹인다
;-------------------------------------------------
@MESSAGE_COM233
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의

;빈유＋작은체구
IF TALENT:PLAYER:작은체구 && TALENT:PLAYER:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:PLAYER:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:PLAYER:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:PLAYER:폭유
	PRINT 손에 남을 만큼 큰
ENDIF

PRINTFORM 가슴의 첨단에 달라붙어,모유를 마시고 있다…


;-------------------------------------------------
;234,로터 시킨다
;-------------------------------------------------
@MESSAGE_COM234
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
;페니스가 있다
IF PENIS(PLAYER)
	PRINT 페니스에
ELSE
	PRINT 비부[秘部]에
ENDIF
PRINTFORM 떨리는로터를 꽉 눌러 왔다…


;-------------------------------------------------
;235,진동마사지기 시킨다
;-------------------------------------------------
@MESSAGE_COM235
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
;페니스가 있다
IF PENIS(PLAYER)
	PRINT 페니스에
ELSE
	PRINT 비부[秘部]에
ENDIF
PRINTFORM 진동마사지기를 강압해 왔다…


;-------------------------------------------------
;236,클리캡 시킨다
;-------------------------------------------------
@MESSAGE_COM236
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
;해제시
IF TEQUIP:클리캡
	PRINTFORM 전동클리캡를 제외해 주었다
ELSE
	PRINTFORM 음핵에,전동클리캡를 달아 왔다
ENDIF


;-------------------------------------------------
;237,클리고문 시킨다
;-------------------------------------------------
@MESSAGE_COM237
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
;삼각목마승마중,새드 3 이상이나지하실에 있다고 꾸짖는 방법이 바뀐다
IF TEQUIP:PLAYER:삼각목마 && (ABL:새드끼 >= 3 || TALENT:새드 || TEQUIP:지하실)
	PRINTFORM %CALLNAME:PLAYER%를 실은삼각목마를,흔들흔들 전후에 흔들어 왔다…
ELSE
	PRINTFORM 음핵을,
	;고문중
	IF TEQUIP:지하실
		PRINTL 손톱을 세워 세게 긁어 돌려 왔다
	ELSE
		PRINTL 굴리듯이해 자극해 왔다…
	ENDIF
ENDIF


;-------------------------------------------------
;238,음핵클립 시킨다
;-------------------------------------------------
@MESSAGE_COM238
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:PLAYER:음핵클립
	PRINTFORM 음핵으로부터 클립을 제외해 주었다
ELSE
	PRINTFORM 음핵에,
	;고문중
	SIF TEQUIP:지하실
		PRINT 톱니모양이 붙은 강력한
	PRINTFORM 클립을 물게 해 왔다
ENDIF


;-------------------------------------------------
;239,음핵전극 시킨다
;-------------------------------------------------
@MESSAGE_COM239
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
PRINTFORM 음핵클립에 전극를 연결해,스윗치를 넣었다…



;===========================================================
;봉사하는 계
;===========================================================
;-------------------------------------------------
;240,파이즈리한다
;-------------------------------------------------
@MESSAGE_COM240
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%에파이즈리한다야 게 말해졌다

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%는,%CALLNAME%의페니스를
;빈유
IF TALENT:빈유
	PRINT 얇은가슴로
;거유
ELSEIF TALENT:거유
	PRINT 풍만한가슴로 감싸
;폭유
ELSEIF TALENT:폭유
	PRINT 너무 풍만할 정도의가슴로 숨기듯이해
ELSE
	PRINT 가슴에서는씨로
ENDIF
PRINTFORM 쓸어올렸다…


;-------------------------------------------------
;241,파이즈리펠라한다
;-------------------------------------------------
@MESSAGE_COM241
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%에파이즈리펠라한다야 게 말해졌다

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%는,%CALLNAME%의페니스를
;연모나친애
;빈유
IF TALENT:빈유
	PRINT 얇은가슴 뿐만이 아니라 혀도 사용해 자극하고 있었다…
;거유
ELSEIF TALENT:거유
	PRINT 풍만한가슴로 감싸,남은 귀두를 빨아 자극했다…
;폭유
ELSEIF TALENT:폭유
	PRINT 너무 풍만할 정도의가슴로 숨겨,때때로 혀를 넣어 빨아 주었다…
ELSE
	PRINT 가슴로 끼워,남은 부분을 혀로 자극했다…
ENDIF


;-------------------------------------------------
;242,풋잡한다
;-------------------------------------------------
@MESSAGE_COM242
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%에풋잡한다야 게 말해졌다

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%는

;착의(벗기지 않는다) 때
IF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
	IF TEQUIP:하의
		A = 4
	ELSEIF TEQUIP:속옷（아래）
		A = 2
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
;착의(비켜 놓는다) 때
ELSEIF TEQUIP:속옷（아래） || TEQUIP:하의
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 위로부터
	ENDIF
ENDIF

PRINTFORM %CALLNAME%의
;아이사이즈
IF TALENT:아이사이즈
	;소인체형가 아니다
	SIF TALENT:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:말자지
	;소인체형
	IF TALENT:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
PRINTFORM 페니스를,

SIF TEQUIP:지하실 || TEQUIP:야외플레이
	PRINT 신발를 신은 채로의
PRINTFORM 발로

IF TEQUIP:지하실 || ABL:PLAYER:새드끼 >= 3
	PRINTFORM 짓밟았다…
ELSE
	IF TEQUIP:야외플레이
		PRINTFORM 자극했다…
	ELSE
		PRINTFORM 훑어 주었다…
	ENDIF
ENDIF

;-------------------------------------------------
;243,가슴을비빈다
;-------------------------------------------------
@MESSAGE_COM243
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%는 가슴을 맞대 비비고 싶은 것 같다

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%과%CALLNAME%는 서로의가슴를 서로 비볐다…


;-------------------------------------------------
;244,조개비비기
;-------------------------------------------------
@MESSAGE_COM244
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%는조개비비기를 하고 싶은 것 같다

;수 넣는 경우만

;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 찢어,
	ENDIF
ENDIF

PRINTFORM %CALLNAME:PLAYER%과%CALLNAME%는 서로의 성기를 맞대고 비볐다…


;-------------------------------------------------
;245,장대비비기
;-------------------------------------------------
@MESSAGE_COM245
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%는장대비비기를 하고 싶은 것 같다

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%과 %CALLNAME:TARGET%는
IF KANRAKU2(TARGET)
	PRINT 령입끼리로 키스를
	IF RAND:2
		PRINT 시키면,
	ELSE
		PRINT 하면,
	ENDIF
	PRINT 페니스를 서로 관련되듯이
ELSE
	PRINT 서로의페니스를
ENDIF
PRINTL 맞대고 비볐다……

;-------------------------------------------------
;246,전기안마
;-------------------------------------------------
@MESSAGE_COM246
;역조교모드
SIF TEQUIP:역조교
	PRINTFORM %CALLNAME%에전기안마 하도록 들었다

PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의발를 잡아,

;착의(벗기지 않는다) 때
IF TEQUIP:속옷（아래） || BOTTOMS(TARGET) >= 4
	IF TEQUIP:하의
		A = 4
	ELSEIF TEQUIP:속옷（아래）
		A = 2
	ENDIF
	CALL CLOTHES_NAME(TARGET)
	PRINT 위로부터,
;착의(비켜 놓는다) 때
ELSEIF TEQUIP:속옷（아래） || TEQUIP:하의
	PRINTFORM %CALLNAME%의
	;본테이지
	IF BOTTOMS(TARGET) == 3
		PRINT 슈트의 지퍼를 열면,
	;스커트
	ELSEIF BOTTOMS(TARGET) == 1
		PRINT 스커트를 걷어 올리면,
	;그 이외의전신상의
	ELSEIF BOTTOMS(TARGET) == 2
		A = 4
		CALL CLOTHES_NAME(TARGET)
		PRINT 의 옷자락을 걷어 올리면,
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 위로부터
	ENDIF
ENDIF

PRINTFORM 사타구니를
SIF TEQUIP:지하실 || TEQUIP:야외플레이
	PRINT 신발를 신은 채로의
PRINTFORM 발로

IF TEQUIP:지하실 || ABL:PLAYER:새드끼 >= 3
	PRINTFORM 짓밟았다…
ELSE
	PRINTFORM 자극했다…
ENDIF


;-------------------------------------------------
;번호 감쌌기 때문에 comment out도 해 실장하는 경우는 커맨드 번호를 비켜 놓아 주세요
;246,전동오나홀 시킨다
;-------------------------------------------------
;@MESSAGE_COM246
;PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
;;해제시
;IF TEQUIP:PLAYER:전동오나홀
;	PRINTFORM 페니스로부터 오나홀을 제외했다…
;ELSE
;	;아이사이즈
;	IF TALENT:PLAYER:아이사이즈
;		;소인체형가 아니다
;		SIF TALENT:PLAYER:소인체형 == 0
;			PRINT 체에 어울리지 않고
;		PRINT 어리다
;	;말자지
;	ELSEIF TALENT:PLAYER:말자지
;		;소인체형
;		IF TALENT:PLAYER:소인체형
;			PRINT 신체가 숨어 버릴 만큼
;		ELSE
;			PRINT 눈이 휘둥그레 지는 만큼
;		ENDIF
;		PRINT 큰
;	ENDIF
;	SIF TALENT:PLAYER:포경
;		PRINT 포경
;	PRINT 페니스를 오나홀에
;	IF TALENT:PLAYER:말자지
;		PRINT 밀어넣었다
;	ELSE
;		PRINT 삽입했다
;	ENDIF
;ENDIF

;-------------------------------------------------
;247,귀두고문 시킨다
;-------------------------------------------------
@MESSAGE_COM247
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
IF TALENT:PLAYER:아이사이즈
	PRINT 어리다
ELSEIF TALENT:PLAYER:말자지
	PRINT 주저해 할 것 같을 정도큰
ENDIF
SIF TALENT:PLAYER:포경
	PRINT 포경
PRINTFORM 페니스의 귀두를손의 히등으로 쓸어올려 왔다…


;-------------------------------------------------
;248,카테터 시킨다
;-------------------------------------------------
@MESSAGE_COM248
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:PLAYER:카테터 == 1
	PRINTFORM 요도로부터카테터를 뽑아 냈다…
ELSE
	PRINTFORM 요도에
	;고문중
	IF TEQUIP:지하실
		PRINTFORML 통증를 주는 오돌오돌 부착의카테터를 삽입해 왔다
	ELSE
		PRINTFORML 요도 개발용의카테터를 삽입해 왔다
	ENDIF
ENDIF

;-------------------------------------------------
;249,요도벌룬 시킨다
;-------------------------------------------------
@MESSAGE_COM249
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:요도벌룬
	PRINTFORML 다 퍼진 요도로부터 벌룬카테터를 뽑아 주었다…
ELSE
	
	PRINTFORML 요도에 벌룬카테터를 삽입해,공기를 보내 왔다…
ENDIF

;-------------------------------------------------
;250,전극카테터 시킨다
;-------------------------------------------------
@MESSAGE_COM250
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
PRINTFORM 요도카테터에 전극를 연결해,스윗치를 넣었다…


;-------------------------------------------------
;251,벗겨 받는다
;-------------------------------------------------
@MESSAGE_COM251
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
PRINTFORM 페니스를 캐스트 오프 했다…


;-------------------------------------------------
;252,로터삽입 시킨다
;-------------------------------------------------
@MESSAGE_COM252
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의

;해제시
IF TEQUIP:PLAYER:로터삽입
	PRINTFORM 바기나로부터 떨리는로터를 천천히 뽑아 냈다…
;장착시
ELSE
	;바이브 넣고 있을 뿐
	SIF TEQUIP:PLAYER:바이브
		PRINT 완구를 문 채로의
	;작은체구
	IF TALENT:PLAYER:작은체구
		PRINT 어린 균열에
	ELSE
		PRINT 비밀의 틈[秘裂]에
	ENDIF
	
	PRINTFORML 떨리는로터를 밀어넣었다…
ENDIF

;-------------------------------------------------
;253,바이브삽입시킨다
;-------------------------------------------------
@MESSAGE_COM253
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의

;해제시
IF TEQUIP:PLAYER:바이브
	PRINTFORM 바기나로부터 꿈틀거리는바이브를 천천히 뽑아 냈다…
;장착시
ELSE
	PRINTFORM 바기나에
	;고문중
	SIF TEQUIP:지하실
		PRINT 흉악한 형태의
	PRINTFORM 바이브를 꽂아넣어 왔다…
ENDIF

;-------------------------------------------------
;254,바기나전극 시킨다
;-------------------------------------------------
@MESSAGE_COM254
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
PRINTFORM 바이브에 전극를 연결해,스윗치를 넣었다…


;-------------------------------------------------
;255,로터A삽입 시킨다
;-------------------------------------------------
@MESSAGE_COM255
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의

;해제시
IF TEQUIP:PLAYER:로터A삽입
	PRINTFORM 아누스로부터 떨리는로터를 뽑았다…
ELSE
	PRINTFORML 후 구멍에 떨리는로터를 밀어넣어 왔다…
ENDIF

;-------------------------------------------------
;256,애널비즈 시킨다
;-------------------------------------------------
@MESSAGE_COM256
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
IF TEQUIP:PLAYER:애널비즈
	PRINTFORM 아누스로부터애널비즈를 기세 좋게 뽑아 냈다…
ELSE
	PRINTFORM 후 구멍에애널비즈를 하나씩 삼키게 해 왔다…
ENDIF

;-------------------------------------------------
;257,애널바이브삽입시킨다
;-------------------------------------------------
@MESSAGE_COM257
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의

;해제시
IF TEQUIP:PLAYER:애널바이브
	PRINTFORM 아누스로부터애널바이브를 뽑았다…
ELSE
	PRINTFORM 후 구멍에
	
	;고문중
	IF TEQUIP:지하실
		PRINT 흉악한 형태의
	ELSEIF TALENT:PLAYER:동물귀
		IF TALENT:PLAYER:동물귀 == 11
			PRINT 매끈매끈한 고양이
		ELSEIF TALENT:PLAYER:동물귀 == 12
			PRINT 주렁주렁의 개
		ELSEIF TALENT:PLAYER:동물귀 == 13
			PRINT 둥실둥실 한 토끼
		ELSE
			PRINTFORM ,%CALLNAME:PLAYER%의 귀에 잘 어울리는 짐승
		ENDIF
		PRINT 의 꼬리를 본뜬
	ENDIF
	PRINTFORML 애널바이브를 꽂아넣어 왔다…
ENDIF

;-------------------------------------------------
;258,확장벌룬삽입시킨다
;-------------------------------------------------
@MESSAGE_COM258
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
;해제시
IF TEQUIP:PLAYER:확장벌룬
	PRINTFORM 다 퍼진 후 구멍으로부터 벌룬을 뽑아 주었다…
ELSE
	PRINTFORM 후 구멍에 에어벌룬을 삽입해,공기를 보내 왔다…
ENDIF

;-------------------------------------------------
;259,애널전극 시킨다
;-------------------------------------------------
@MESSAGE_COM259
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
PRINTFORM 애널바이브에 전극를 연결해,스윗치를 넣었다…


;===========================================================
;주인수섹스
;===========================================================
;-------------------------------------------------
;260,정상위시킨다
;-------------------------------------------------
@MESSAGE_COM260
;전회와 이번 조교자가 같아,전회도 주인수V 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 260 && PREVCOM <= 262
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTFORM %CALLNAME%는
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 260
			;노예에게 진짜의페니스가 있다
			IF PENIS(TARGET)
				PRINTFORM %CALLNAME:PLAYER%의질내의 감촉을 맛보면서,다시 허리를 움직여 왔다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에 유사페니스를 삽입한 채로,다시 허리를 움직여 왔다…
			ENDIF
		;전회의 커맨드가후배위시킨다,기승위한다
		ELSE
			PRINTFORM 질내로부터 앞지르는 일 없이 %CALLNAME:PLAYER%를 위로 향해 재워,다시 허리를 움직여 왔다…
		ENDIF
	ELSE
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME%의
		ELSE
			PRINT 유사
		ENDIF
		PRINTFORM 페니스가 빠지지 않게 조인 채로
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 260
			PRINT 껴안아,
		ELSE
			PRINT 위로 향해 되어,
		ENDIF
		PRINTL 새로운 밀어올림을 공갈했다…
	ENDIF
ELSE
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%를 깔아누르면,
	;통상 모드
	ELSE
		IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3)
			PRINTFORML %CALLNAME:PLAYER%가 엎드려 눕기다리를 열면,%CALLNAME%는 %CALLNAME:PLAYER%를 깔아눌러,
		ELSE
			PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의 전에 엎드려 누우면
			SIF ABL:PLAYER:욕망 > 3
				PRINT 비밀의 틈[秘裂]을 과시하듯이 크고
			PRINTFORML 가랑이를 열어,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;노예에게 진짜의페니스가 있다
	IF PENIS(TARGET)
		;아이사이즈
		IF TALENT:아이사이즈
			;소인체형가 아니다
			SIF !TALENT:소인체형
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:말자지
			;소인체형
			IF TALENT:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINT 로
	ELSE
		PRINT 를
	ENDIF
	
	;처녀
	SIF TALENT:PLAYER:처녀
		PRINT 남을 모른다
	;작은체구
	IF TALENT:PLAYER:작은체구 || TALENT:PLAYER:소인체형
		PRINT 어린 균열
	ELSE
		PRINT 바기나
	ENDIF
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTL 를 관철해 왔다…
	ELSE
		PRINTFORM 에수 넣었다…
	ENDIF
ENDIF


;-------------------------------------------------
;261,후배위시킨다
;-------------------------------------------------
@MESSAGE_COM261
;전회와 이번 조교자가 같아,전회도 주인수V 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 260 && PREVCOM <= 262
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTFORM %CALLNAME%는
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 261
			;노예에게 진짜의페니스가 있다
			IF PENIS(TARGET)
				PRINTFORM %CALLNAME:PLAYER%의질내의 감촉을 맛보면서,다시 허리를 움직여 왔다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에 유사페니스를 삽입한 채로,다시 허리를 움직여 왔다…
			ENDIF
		;전회의 커맨드가정상위시킨다
		ELSEIF PREVCOM == 260
			PRINTFORM 질내로부터 앞지르는 일 없이 %CALLNAME:PLAYER%의후로 돌아,다시 허리를 움직여 왔다…
		;전회의 커맨드가기승위한다
		ELSE
			PRINTFORM 삽입한 채로 %CALLNAME:PLAYER%를 재워 네발로 엎드리게 해,다시 허리를 움직여 왔다…
		ENDIF
	ELSE
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME%의
		ELSE
			PRINT 유사
		ENDIF
		PRINTFORM 페니스가 빠지지 않게 조인 채로
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 261
			PRINT 허리를 흔들어,
		ELSE
			PRINT 후를 향해,
		ENDIF
		PRINTL 새로운 밀어올림을 공갈했다…
	ENDIF
ELSE
	;역조교모드가 아니다
	SIF !TEQUIP:역조교
		PRINTFORML %CALLNAME:PLAYER%가 네발로 엎드림이 되어 삽입을 조르면,
	PRINTFORM %CALLNAME%는
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;노예에게 진짜의페니스가 있다
	IF PENIS(TARGET)
		;아이사이즈
		IF TALENT:아이사이즈
			;소인체형가 아니다
			SIF !TALENT:소인체형
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:말자지
			;소인체형
			IF TALENT:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINT 로
	ELSE
		PRINT 를
	ENDIF
	
	PRINTFORM %CALLNAME:PLAYER%의
	;처녀
	SIF TALENT:PLAYER:처녀
		PRINT 남을 모른다
	;작은체구
	IF TALENT:PLAYER:작은체구 || TALENT:PLAYER:소인체형
		PRINT 어린 균열
	ELSE
		PRINT 바기나
	ENDIF
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTL 를 관철해 왔다…
	ELSE
		PRINTL 에 천천히 삽입해 왔다…
	ENDIF
ENDIF


;-------------------------------------------------
;262,기승위한다
;-------------------------------------------------
@MESSAGE_COM262
;전회와 이번 조교자가 같아,전회도 주인수V 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 260 && PREVCOM <= 262
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORM %CALLNAME%는
		;노예에게 진짜의페니스가 있다
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME:PLAYER%의질내의 감촉을 맛보면서,다시 허리를 움직여 왔다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME:PLAYER%에 유사페니스를 삽입한 채로,다시 허리를 움직여 왔다…
		ENDIF
	;통상 모드
	ELSE
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME%의
		ELSE
			PRINT 유사
		ENDIF
		PRINTFORM 페니스가 빠지지 않게 조인 채로
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 262
			PRINTFORM ,집요하게 허리를 거절해 세웠다…
		ELSE
			PRINTFORM %CALLNAME%에 걸쳐,새로운 밀어올림을 공갈했다…
		ENDIF
	ENDIF
ELSE
	;역조교모드
	SIF TEQUIP:역조교
		PRINTFORMW %CALLNAME%에기승위한다야 게 말해졌다…
	
	;수 넣는 경우만
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%,
	ELSE
		PRINTFORM %CALLNAME%를 밀어 넘어뜨리면,
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;노예에게 진짜의페니스가 있다
	IF PENIS(TARGET)
		PRINTFORM %CALLNAME%의
		;아이사이즈
		IF TALENT:아이사이즈
			;소인체형가 아니다
			SIF !TALENT:소인체형
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:말자지
			;소인체형
			IF TALENT:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:포경
			PRINT 포경
		PRINT 페니스를
	ELSE
		PRINT 유사페니스를
	ENDIF
	
	SIF TALENT:PLAYER:처녀
		PRINT 아직 남자를수 넣은 일이 없다
	PRINTFORMW 스스로의바기나로 물었다…
ENDIF


;-------------------------------------------------
;263,정상위애널시킨다
;-------------------------------------------------
@MESSAGE_COM263
;전회와 이번 조교자가 같아,전회도 주인수A 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 263 && PREVCOM <= 265
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTFORM %CALLNAME%는
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 263
			;노예에게 진짜의페니스가 있다
			IF PENIS(TARGET)
				PRINTFORM %CALLNAME:PLAYER%의장내의 감촉을 맛보면서,다시 허리를 움직여 왔다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에 유사페니스를 삽입한 채로,다시 허리를 움직여 왔다…
			ENDIF
		;전회의 커맨드가후배위시킨다,기승위한다
		ELSE
			PRINTFORM 장내로부터 앞지르는 일 없이 %CALLNAME:PLAYER%를 위로 향해 재워,다시 허리를 움직여 왔다…
		ENDIF
	ELSE
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME%의
		ELSE
			PRINT 유사
		ENDIF
		PRINTFORM 페니스가 빠지지 않게 조인 채로
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 263
			PRINT 껴안아,
		ELSE
			PRINT 위로 향해 되어,
		ENDIF
		PRINTL 새로운 밀어올림을 공갈했다…
	ENDIF
ELSE
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%를 깔아누르면,
	;통상 모드
	ELSE
		IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3)
			PRINTFORML %CALLNAME:PLAYER%가 엎드려 눕기다리를 열면,%CALLNAME%는 %CALLNAME:PLAYER%를 깔아눌러,
		ELSE
			PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 전에 엎드려 누우면 가랑이를 열어,
		ENDIF
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;노예에게 진짜의페니스가 있다
	IF PENIS(TARGET)
		;아이사이즈
		IF TALENT:아이사이즈
			;소인체형가 아니다
			SIF !TALENT:소인체형
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:말자지
			;소인체형
			IF TALENT:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTL 로 아누스를 관철해 왔다…
	ELSE
		PRINTL 를 스스로의 아누스에수 넣었다…
	ENDIF
	
ENDIF

;-------------------------------------------------
;264,후배위애널시킨다
;-------------------------------------------------
@MESSAGE_COM264
;전회와 이번 조교자가 같아,전회도 주인수A 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 263 && PREVCOM <= 265
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTFORM %CALLNAME%는
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 264
			;노예에게 진짜의페니스가 있다
			IF PENIS(TARGET)
				PRINTFORM %CALLNAME:PLAYER%의장내의 감촉을 맛보면서,다시 허리를 움직여 왔다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에 유사페니스를 삽입한 채로,다시 허리를 움직여 왔다…
			ENDIF
		;전회의 커맨드가정상위시킨다
		ELSEIF PREVCOM == 263
			PRINTFORM 장내로부터 앞지르는 일 없이 %CALLNAME:PLAYER%의후로 돌아,다시 허리를 움직여 왔다…
		;전회의 커맨드가기승위한다
		ELSE
			PRINTFORM 삽입한 채로 %CALLNAME:PLAYER%를 재워 네발로 엎드리게 해,다시 허리를 움직여 왔다…
		ENDIF
	ELSE
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME%의
		ELSE
			PRINT 유사
		ENDIF
		PRINTFORM 페니스가 빠지지 않게 조인 채로
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 264
			PRINT 허리를 흔들어,
		ELSE
			PRINT 후를 향해,
		ENDIF
		PRINTL 새로운 밀어올림을 공갈했다…
	ENDIF
ELSE
	;역조교모드가 아니다
	SIF !TEQUIP:역조교
		PRINTFORML %CALLNAME:PLAYER%가 네발로 엎드림이 되어 삽입을 조르면,
	PRINTFORM %CALLNAME%는
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;노예에게 진짜의페니스가 있다
	IF PENIS(TARGET)
		;아이사이즈
		IF TALENT:아이사이즈
			;소인체형가 아니다
			SIF !TALENT:소인체형
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:말자지
			;소인체형
			IF TALENT:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:포경
			PRINT 포경
		PRINT 페니스
	ELSE
		PRINT 유사페니스
	ENDIF
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINT 로
	ELSE
		PRINT 를
	ENDIF
	
	PRINTFORM %CALLNAME:PLAYER%의
	
	IF TALENT:새드 || TALENT:공 || TALENT:PLAYER:수 || TALENT:PLAYER:오메가 || (PENIS(TARGET) && ABL:욕망 >= 3) || TEQUIP:역조교
		PRINTL 아누스를 관철해 왔다…
	ELSE
		PRINTL 아누스에 천천히 삽입해 왔다…
	ENDIF
ENDIF

;-------------------------------------------------
;265,기승위애널한다
;-------------------------------------------------
@MESSAGE_COM265
;전회와 이번 조교자가 같아,전회도 주인수A 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 263 && PREVCOM <= 265
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORM %CALLNAME%는
		;노예에게 진짜의페니스가 있다
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME:PLAYER%의질내의 감촉을 맛보면서,다시 허리를 움직여 왔다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORM %CALLNAME:PLAYER%에 유사페니스를 삽입한 채로,다시 허리를 움직여 왔다…
		ENDIF
	;통상 모드
	ELSE
		IF PENIS(TARGET)
			PRINTFORM %CALLNAME%의
		ELSE
			PRINT 유사
		ENDIF
		PRINTFORM 페니스가 빠지지 않게 조인 채로
		;전회의 커맨드가 이번과 같음
		IF PREVCOM == 265
			PRINTFORM ,집요하게 허리를 거절해 세웠다…
		ELSE
			PRINTFORM %CALLNAME%에 걸쳐,새로운 밀어올림을 공갈했다…
		ENDIF
	ENDIF
ELSE
	;역조교모드
	SIF TEQUIP:역조교
		PRINTFORMW %CALLNAME%에기승위한다야 게 말해졌다…
	
	;수 넣는 경우만
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%,
	ELSE
		PRINTFORM %CALLNAME%를 밀어 넘어뜨리면,
	ENDIF
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;노예에게 진짜의페니스가 있다
	IF PENIS(TARGET)
		PRINTFORM %CALLNAME%의
		;아이사이즈
		IF TALENT:아이사이즈
			;소인체형가 아니다
			SIF !TALENT:소인체형
				PRINT 체에 어울리지 않고
			PRINT 어리다
		;말자지
		ELSEIF TALENT:말자지
			;소인체형
			IF TALENT:소인체형
				PRINT 신체가 숨어 버릴 만큼
			ELSE
				PRINT 눈이 휘둥그레 지는 만큼
			ENDIF
			PRINT 큰
		ENDIF
		SIF TALENT:포경
			PRINT 포경
		PRINT 페니스를
	ELSE
		PRINT 유사페니스를
	ENDIF
	
	PRINTL 스스로의 아누스로 물었다…
ENDIF


;-------------------------------------------------
;267,피스트퍽 시킨다
;-------------------------------------------------
@MESSAGE_COM267
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
;작은체구
IF TALENT:작은체구
	PRINT 어린 균열에
ELSE
	PRINT 비밀의 틈[秘裂]에
ENDIF

;소인체형
SIF TALENT:소인체형
	PRINT 작은
PRINTFORML 주먹을 찔러 넣어 왔다…


;-------------------------------------------------
;268,애널피스트 시킨다
;-------------------------------------------------
@MESSAGE_COM268
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의 아누스에

;소인체형
SIF TALENT:소인체형
	PRINT 작은
PRINTFORML 주먹을 찔러 넣어 왔다…


;-------------------------------------------------
;269,양구멍피스트 시킨다
;-------------------------------------------------
@MESSAGE_COM269
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
;작은체구
IF TALENT:작은체구
	PRINT 어린 균열과
ELSE
	PRINT 비밀의 틈[秘裂]과
ENDIF
PRINT 아누스에

;소인체형
SIF TALENT:소인체형
	PRINT 작은
PRINTFORML 양의주먹을 찔러 넣어 왔다…


;-------------------------------------------------
;270,촉수에범하게시킨다
;-------------------------------------------------
@MESSAGE_COM270
;전회와 이번 조교자가 같아,전회도촉수에범하게시킨다
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM == 270
	PRINTFORML %CALLNAME%에 과시하면서,%CALLNAME:ASSI%과 계속 사귀었다…
ELSE
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORM 페니스를
	SIF TALENT:MASTER:처녀 || TALENT:MASTER:재생처녀
		PRINT 남을 모른다
	PRINTL 바기나로 삼켜 갔다…
ENDIF


;-------------------------------------------------
;271,촉수에A를범하게시킨다
;-------------------------------------------------
@MESSAGE_COM271
;뽑지 않는 플래그가 있어,전회와 이번 조교자가 같아,전회도촉수에A를범하게시킨다
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM == 271
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%과 계속 사귀었다…
ELSE
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORM 페니스를
	SIF TALENT:MASTER:백버진
		PRINT 남을 모른다
	PRINTL 애널로 삼켜 갔다…
ENDIF


;===========================================================
;주인마조계
;===========================================================
;-------------------------------------------------
;273,밧줄로 묶게 한다
;-------------------------------------------------
@MESSAGE_COM273
;해제시
IF TEQUIP:PLAYER:밧줄
	PRINTFORMW %CALLNAME%가 %CALLNAME:PLAYER%의밧줄를 풀어 주었다
ELSE
	PRINTFORML %CALLNAME%에밧줄로 속박되어 사지의 자유를 빼앗겼다…
ENDIF


;-------------------------------------------------
;274,아이마스크 한다
;-------------------------------------------------
@MESSAGE_COM274
;해제시
IF TEQUIP:PLAYER:아이마스크
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%로부터아이마스크를 제외해 주었다
ELSE
	PRINTFORML %CALLNAME%에아이마스크를 대게 되어졌다…
ENDIF

;-------------------------------------------------
;275,볼개그 한다
;-------------------------------------------------
@MESSAGE_COM275
;해제시
IF TEQUIP:PLAYER:볼개그
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%로부터볼개그를 집어 주었다
ELSE
	PRINTFORML %CALLNAME%에볼개그를 끼우게 되어졌다…
ENDIF

;-------------------------------------------------
;276,정조대 한다
;-------------------------------------------------
@MESSAGE_COM276
PRINTFORM %CALLNAME%는
IF TEQUIP:정조대
	PRINTFORML %CALLNAME:PLAYER%의 하반신을 경고하는정조대의 열쇠를 제외해,벗게 해 주었다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%에정조대를 입게 하면,제대로 자물쇠를 채워 왔다…
ENDIF

;-------------------------------------------------
;277,사정관리 시킨다
;-------------------------------------------------
@MESSAGE_COM277
PRINTFORM %CALLNAME%는

IF TEQUIP:사정관리
	PRINTFORML %CALLNAME:PLAYER%를 경고하는리본를 풀어 주었다…
ELSE
	PRINTFORM %CALLNAME:PLAYER%의페니스의 근원에,
	;고문중
	IF TEQUIP:지하실
		PRINTFORML 사정관리용의리본를 삐걱삐걱 묶어 왔다
	ELSE
		PRINTFORML 프릴이 붙은 사랑스러운리본를 힘들게 묶어 왔다
	ENDIF
	PRINTL 혈류가 모여 단단히 조일 수 있어 귀두가 검붉게 팽창하고 있다…
ENDIF


;-------------------------------------------------
;280,이라마치오시킨다
;-------------------------------------------------
@MESSAGE_COM280

IF TALENT:새드 || ABL:새드끼 >= 3 || TEQUIP:역조교
	PRINTFORM %CALLNAME:PLAYER%가입를 열면,%CALLNAME%는
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	;아이사이즈
	IF TALENT:아이사이즈
		;소인체형가 아니다
		SIF TALENT:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:말자지
		;소인체형
		IF TALENT:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:포경
		PRINT 포경
	
	PRINTFORM 페니스를,%CALLNAME:PLAYER%의입에 찔러 넣어 왔다…
ELSE
	PRINTFORM %CALLNAME:PLAYER%는
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINT 를 찢어 꺼냈다
	ENDIF
	
	PRINTFORM %CALLNAME%의
	;아이사이즈
	IF TALENT:아이사이즈
		;소인체형가 아니다
		SIF TALENT:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:말자지
		;소인체형
		IF TALENT:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:포경
		PRINT 포경
	
	PRINTFORML 페니스를 물어,그대로 허리를 사용하게 했다…
ENDIF

;-------------------------------------------------
;281,매도당한다
;-------------------------------------------------
@MESSAGE_COM281
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의 일을 매도해 왔다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에
	IF ABL:PLAYER:마조끼 >= 3 || TALENT:PLAYER:마조
		PRINTFORML 자신의 일을 매도해 주듯이 간원 했다
	ELSE
		PRINTFORML 매도의 말을 말할 수 있어 보았다
	ENDIF
	
	PRINTFORM %CALLNAME%
	;새드끼
	IF ABL:새드끼 == 0
		PRINTFORM 는,당황하면서 어떻게든 매도의 말을 쥐어짜내고 있다…
	ELSEIF ABL:새드끼 < 3
		PRINTFORM 의입로부터는,갖은 험담이 술술 흐르기 시작하고 있다…
	ELSEIF ABL:새드끼 < 5
		PRINTFORM 는,매도하는 일에 쾌감을 느끼고 있는 것 같다…
	ELSE
		PRINTFORML 는 얼굴을 홍조 시켜 콧김을 거칠게 하고 있다
		PRINTFORML 아무래도 매도하는 것에 완전히 쾌감을 느끼고 있는 것 같다…
		PRINTFORM %CALLNAME:PLAYER%에 말공를 하면서도 사타구니에
		SIF PENIS(TARGET)
			PRINT 앞질러 해
		SIF TALENT:후타나리 || TALENT:반음반양
			PRINT 와
		SIF VAGINA(TARGET)
			PRINT 애액
		PRINTL 를 배이게 하고 있다
		IF TALENT:PLAYER:마조
			PRINTFORML 그 말에 반응하는 %CALLNAME:PLAYER%를 봐,
			PRINTFORML %CALLNAME%는 새로운 욕설의 말을 뽑으려고 눈동자를 빛내고 있다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;282,발을핥는다
;-------------------------------------------------
@MESSAGE_COM282
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%에
	;신발를 신고 있다
	IF TEQUIP:신발
		PRINTFORM 신발
	ELSE
		PRINTFORM 발
	ENDIF
	PRINTFORML 를 빨듯이 말해졌다
ENDIF

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의
;신발를 신고 있다
IF TEQUIP:신발
	A = 9
	CALL CLOTHES_NAME(TARGET)
	PRINT 를 신었다
ELSEIF TEQUIP:특수2
	A = 1
	CALL CLOTHES_NAME(TARGET)
	PRINT 에 휩싸여졌다
ENDIF
PRINT 발를
;조교자의마조끼 3 이상
SIF ABL:PLAYER:마조끼 >=3
	PRINT 충분히 맛보면서,
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 열심히
PRINTL 빨았다…

;-------------------------------------------------
;283,스팽킹 시킨다
;-------------------------------------------------
@MESSAGE_COM283
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의 엉덩이를,
	;고문중
	IF TEQUIP:지하실
		PRINTFORML 스팽킹용의 패들로 고정시켜두어 왔다…
	ELSE
		PRINTFORML 평손로 강하게 두드려 왔다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 전에 엉덩이를 내밀어,두드리게 했다
ENDIF

;새드끼 3 이상
IF ABL:새드끼 >= 3
	PRINTFORML %CALLNAME%는 두드리는 일에 흥분했는지,숨을 거칠게 하고 있다
ENDIF


;-------------------------------------------------
;284,채찍 치게 한다
;-------------------------------------------------
@MESSAGE_COM284
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는채찍를 치켜들어,
	;성기
	IF TFLAG:56 == 1
		PRINTFORML %CALLNAME:PLAYER%의 성기를 노려 고정시켜두어 왔다
		;클립 붙어 있을 때,랜덤으로 튀어난다(치는 것손의기교가 필요)
		IF TEQUIP:PLAYER:음핵클립 && ABL:기교
			R = 50/ABL:기교
			IF !RAND:R
				PRINTFORML 그채찍는 정확하게 음핵을 붙잡아,물고 있던 클립이 튕겨날려졌다…
				TEQUIP:PLAYER:음핵클립 = 0
				SOURCE:PLAYER:쾌C += 800
			ENDIF
			R = 0
		ENDIF
	ELSEIF TFLAG:56 == 2
		PRINTFORML %CALLNAME:PLAYER%의 유두를 노려 고정시켜두어 왔다
		;클립 붙어 있을 때,랜덤으로 튀어난다(치는 것손의기교가 필요)
		IF TEQUIP:PLAYER:유두클립 && ABL:기교
			R = 50/ABL:기교
			IF !RAND:R
				PRINTFORML 그채찍는 정확하게 유두를 붙잡아,물고 있던 클립이 튕겨날려졌다…
				TEQUIP:PLAYER:유두클립 = 0
				SOURCE:PLAYER:쾌B += 800
			ENDIF
			R = 0
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:PLAYER%의 몸을 몇번이나 고정시켜두어 왔다
		;전회 커맨드가촛불라고,촛농이 난다
		SIF PREVCOM == 292
			PRINTFORML 채찍가 피부를 칠 때에,몸에 부착해 굳어지고 있던 촛농이 기분 좋게 튀어 흩어졌다…
	ENDIF
ELSE
	;새드끼 5 이상
	IF ABL:새드끼 >= 5
		PRINTFORML %CALLNAME%는 명령을 듣자마자채찍를 강탈하면,%CALLNAME:PLAYER%의 피부에 새빨간 줄기를 기본도 새겨 갔다
	ELSE
		PRINTFORML %CALLNAME%에채찍를 건네주어,이것으로 쳐 주듯이 말했다…
	ENDIF
ENDIF

;-------------------------------------------------
;285,음뇨한다
;-------------------------------------------------
@MESSAGE_COM285
;노예에게뇨의가 있다
IF BASE:뇨의 > 0
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%의 오줌을 마시듯이 명령되었다
	ENDIF
	
	;수 넣는 경우만
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%의
	IF TEQUIP:카테터
		PRINT 의 사타구니로부터 뚫고 나온카테터를 물어,
	ELSEIF PENIS(TARGET)
		PRINT 페니스를 물어,
	ELSE
		PRINT 사타구니에입를 대어,
	ENDIF
	PRINTL 마음껏 빨아 올리면,
	PRINTFORML 분출해 온 오줌을 목을 울려 삼켰다…
;그 자리에 오줌이 있다
ELSEIF TFLAG:66
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML 마루의 오줌을 마시듯이 명령되었다
	ENDIF
	
	;수 넣는 경우만
	PRINTFORM %CALLNAME:PLAYER%는
	IF TFLAG:66 & 1
		PRINTFORM %CALLNAME%
	ELSEIF TFLAG:66 & 2
		PRINTFORM %CALLNAME:PLAYER%
	ELSEIF TFLAG:66 & 4
		PRINTFORM %CALLNAME:ASSI%
	ENDIF
	PRINTFORML 가 낸 오줌을,과시하듯이 목을 울려 다 마셨다…
ENDIF


;-------------------------------------------------
;286,식분한다
;-------------------------------------------------
@MESSAGE_COM286
;애널자극로부터의 파생의 경우
IF TFLAG:56 == 1
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%의 아누스로부터,대량의 오물이 넘쳐 나왔다…
	ELSE
		PRINTFORML 변의를 견디려고 비지땀을 흘리는%CALLNAME%의 아누스에 혀끝을 들어갈 수 있어 휘저어,
		PRINTFORML 넘쳐 나온 오물을 베어 물었다…
	ENDIF
ELSE
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML 마루의 변을 먹어 정리하듯이 명령되었다
	ENDIF
	
	;수 넣는 경우만
	PRINTFORM %CALLNAME:PLAYER%는
	IF TFLAG:57 & 1
		PRINTFORM %CALLNAME%
	ELSEIF TFLAG:57 & 2
		PRINTFORM %CALLNAME:PLAYER%
	ELSEIF TFLAG:57 & 4
		PRINTFORM %CALLNAME:ASSI%
	ENDIF
	PRINTFORML 가 낸 변을,과시하듯이입에 포함해,삼켰다…
ENDIF


;-------------------------------------------------
;287,방뇨한다
;-------------------------------------------------
@MESSAGE_COM287
;벌써카테터가 들어가있는 경우는,무리하게 내게 할 수가 있다
IF TEQUIP:PLAYER:카테터
	PRINTFORM 요도에 꽂아진카테터를 안쪽에 밀어넣어지면,모여 있던 오줌이 분출했다…
ELSE
	;역조교모드만
	IF TEQUIP:역조교
		PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%
		SIF !TALENT:소인체형 && TALENT:PLAYER:소인체형 && !TEQUIP:야외플레이 && TEQUIP:PLAYER:머리장식!= 2
			PRINT 를 작은아이와 같이 양발를 안아
		;야외에서,견이 붙이고 있다
		SIF TEQUIP:야외플레이 && TEQUIP:PLAYER:머리장식 == 2 && !TEQUIP:PLAYER:밧줄
			PRINT 에,개와 같이 편발를 올려
		PRINTFORML 방뇨하듯이 명해 왔다…
	ENDIF

	PRINTFORM %CALLNAME:PLAYER%는 인내 할 수 없게 되어,
	SIF TEQUIP:PLAYER:카테터 && !TEQUIP:PLAYER:밧줄 && !TEQUIP:역조교
		PRINT 카테터를 빼내면
	SIF TEQUIP:밧줄
		PRINT 동작을 할 수 없는 채
	;야외에서,견이 붙이고 있다
	IF TEQUIP:야외플레이 && TEQUIP:PLAYER:머리장식 == 2 && !TEQUIP:PLAYER:밧줄
		PRINT 편발를 올려 방뇨했다…
	;아기 포즈
	ELSEIF !TALENT:소인체형 && TALENT:PLAYER:소인체형
		PRINT 오줌을 공중에 흩뿌렸다…
	;긴박중
	ELSEIF TEQUIP:PLAYER:밧줄
		PRINTL 흘림 했다…
	;그 이외
	ELSE
		;야외플레이중
		SIF TEQUIP:야외플레이
			PRINT 풀숲 중(안)에서
		;여자 아이는 주저앉는다
		SIF !PENIS(PLAYER) && VAGINA(PLAYER)
			PRINT 주저앉아
		PRINTL 방뇨했다…
	ENDIF
ENDIF

;-------------------------------------------------
;288,배변 한다
;-------------------------------------------------
@MESSAGE_COM288
;역조교모드
IF TEQUIP:역조교
	PRINTFORM %CALLNAME%는
	;아기 포즈
	SIF !TALENT:소인체형 && TALENT:PLAYER:소인체형
		PRINTFORM %CALLNAME:PLAYER%를 작은아이와 같이 양발를 안아
	PRINTFORM 탈분을 하듯이 명해 왔다…
ENDIF

SIF TEQUIP:PLAYER:밧줄
	PRINT 단단히 묶어졌다
PRINTFORM %CALLNAME:PLAYER%는 참지 못하고,
SIF TEQUIP:PLAYER:밧줄
	PRINT 동작을 할 수 없는 채
;촉수플레이중,혹은 아기 포즈
IF !TALENT:소인체형 && TALENT:PLAYER:소인체형
	PRINTL 배변 했다…
;긴박중
ELSEIF TEQUIP:PLAYER:밧줄
	PRINTL 흘림 했다…
;그 이외
ELSE
	;야외플레이중
	IF TEQUIP:야외플레이
		PRINT 풀숲 중(안)에서
	ELSE
		PRINT 그 자리에
	ENDIF
	PRINTL 주저앉아 배변 했다…
ENDIF

;-------------------------------------------------
;289,관장 시킨다
;-------------------------------------------------
@MESSAGE_COM289
PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의후공에,관장액을 흘려 넣어 왔다…

;-------------------------------------------------
;290,육변기 시킨다
;-------------------------------------------------
@MESSAGE_COM290
PRINTFORM %CALLNAME%는

;전회와 이번 조교자가 같아,전회도 주인수A 섹스
IF ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0)) && PREVCOM >= 263 && PREVCOM <= 265
	PRINTFORM %CALLNAME:PLAYER%에 삽입한 채로,
ELSE
	;애널바이브애널비즈
	IF TEQUIP:PLAYER:애널바이브 || TEQUIP:PLAYER:애널비즈
		IF TEQUIP:PLAYER:애널바이브 && TALENT:PLAYER:동물귀 == 11
			PRINT 매끄러운 고양이의 꼬리
		ELSEIF TEQUIP:PLAYER:애널바이브 && TALENT:PLAYER:동물귀 == 12
			PRINT 주렁주렁 한 개의 꼬리
		ELSEIF TEQUIP:PLAYER:애널바이브 && TALENT:PLAYER:동물귀 == 13
			PRINT 둥실둥실의 토끼의 꼬리
		ELSEIF TEQUIP:PLAYER:애널바이브
			PRINT 애널바이브
		ELSE
			PRINT 애널비즈
		ENDIF
		PRINT 를 문 채로의
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%의 아누스에페니스를 찔러 넣어,
ENDIF
PRINTFORML 장내에 방뇨하기 시작했다…


;-------------------------------------------------
;291,바늘 찌르게 한다
;-------------------------------------------------
@MESSAGE_COM291
;조
IF TFLAG:56 == 3 && TEQUIP:지하실
	PRINTFORM 손가락과 손톱의 사이에,굵은바늘을 쑤셔 넣어졌다…
;성기
ELSEIF TFLAG:56 == 1
	PRINTFORM %CALLNAME:PLAYER%의 성기에
	;조교자가바느질잘함나새드나지하실
	IF TALENT:바느질잘함 || TALENT:새드 || TEQUIP:지하실
		PRINT 가차 없이,바늘가 찔렸다…
	ELSE
		PRINT 바늘을 꽂을 수 있었다…
	ENDIF
;유두
ELSEIF TFLAG:56 == 2
	SIF TEQUIP:PLAYER:유두클립
		PRINT 클립에 끼여,납작 무너졌다
	PRINTFORM %CALLNAME:PLAYER%의 유두에
	;조교자가바느질잘함나새드나지하실
	IF TALENT:바느질잘함 || TALENT:새드 || TEQUIP:지하실
		PRINT 가차 없이,바늘가 찔렸다…
	ELSE
		PRINT 바늘을 꽂을 수 있었다…
	ENDIF
ELSE
	;팔이 없으면발에
	IF TALENT:양팔결손
		PRINTFORMW %CALLNAME:PLAYER%의발의,피부의 얇은 내퇴[內腿]를 노려바늘을 꽂을 수 있었다…
	;발가 없으면 팔에
	ELSEIF TALENT:양다리결손
		PRINTFORMW %CALLNAME:PLAYER%의 팔에,바늘을 꽂을 수 있었다…
	;양쪽 모두 있으면 랜덤으로 팔
	ELSEIF !RAND:1
		PRINTFORMW %CALLNAME:PLAYER%의 팔에,바늘을 꽂을 수 있었다…
	;양쪽 모두 있으면 랜덤으로발
	ELSE
		PRINTFORMW %CALLNAME:PLAYER%의발의,피부의 얇은 내퇴[內腿]를 노려바늘을 꽂을 수 있었다…
	ENDIF
ENDIF

;-------------------------------------------------
;292,촛불 시킨다
;-------------------------------------------------
@MESSAGE_COM292
;밧줄등으로 긴박중
SIF TEQUIP:PLAYER:밧줄
	PRINTFORM 속박되어 움직일 수 없다
;성기
IF TFLAG:56 == 1
	PRINTFORM %CALLNAME:PLAYER%의
	;페니스가 있으면 우선
	IF PENIS(PLAYER)
		IF TEQUIP:PLAYER:전동오나홀
			PRINT 사타구니에
		ELSE
			IF BASE:PLAYER:사정 >= 8000
				PRINT 딱딱 발기했다
			ELSEIF BASE:PLAYER:사정 >= 2000
				PRINT 느슨하게 일어섰다
			ELSE
				PRINT 쇠약해졌다
			ENDIF
			PRINTFORM 페니스에
		ENDIF
	ELSE
		IF TEQUIP:PLAYER:클리캡
			SIF TEQUIP:PLAYER:바이브
				PRINT 바이브를 물었다
			IF TALENT:PLAYER:작은체구 || TALENT:PLAYER:소인체형
				PRINT 어린 균열에
			ELSE
				PRINT 비부[秘部]에
			ENDIF
		ELSE
			PRINT 클리토리스에
		ENDIF
	ENDIF
;유두
ELSEIF TFLAG:56 == 2
	PRINTFORM %CALLNAME:PLAYER%의
	SIF ABL:PLAYER:B감각 >= 3
		PRINT 민감한
	PRINTFORM 유두에,
;설
ELSEIF TFLAG:56 == 3
	PRINTFORM %CALLNAME:PLAYER%의 혀에
;상반신,하반신
ELSE
	IF RAND:3 == 0
		PRINTFORM %CALLNAME:PLAYER%의 엉덩이나 등에,남김없이
	ELSEIF RAND:2 == 0
		PRINTFORM %CALLNAME:PLAYER%의 옆구리나 넓적다리등,피부의 얇은 부분을 중심으로
	ELSE
		PRINTFORM %CALLNAME:PLAYER%의 몸에,
	ENDIF
ENDIF
SIF TEQUIP:지하실
	PRINT 화상 입을 것 같을 정도로
PRINTFORMW 뜨거운 촛농을 떨궜다…

;-------------------------------------------------
;293,삼각목마 시킨다
;-------------------------------------------------
@MESSAGE_COM293
IF TEQUIP:PLAYER:삼각목마
	PRINTFORML 삼각목마 위로부터 구제해 받았다…
ELSE
	;밧줄등으로 긴박중
	SIF TEQUIP:PLAYER:밧줄
		PRINTFORM %CALLNAME%에 안아져
	SIF TEQUIP:지하실
		PRINTFORM 예리한 키의
	PRINTFORML 삼각목마에 실을 수 있었다…
ENDIF


;-------------------------------------------------
;294,가구에 시킨다
;-------------------------------------------------
@MESSAGE_COM294
IF RAND:4 == 0 && ITEM:저온초
	IF RAND:3 == 0 && EXP:PLAYER:V경험 >= 10 && !TALENT:PLAYER:처녀 && !TALENT:PLAYER:재생처녀
		PRINTFORML 굴곡위로 된바기나에촛불를 꽂아넣어져 그대로 촛대로서 보내듯이 명해졌다…
	ELSEIF RAND:2 == 0 && EXP:PLAYER:A경험 >= 10
		PRINTFORML 굴곡위로 한%CALLNAME%의 아누스에촛불를 꽂아넣어,촛대로서 보내게 했다…
	ELSE
		;손도발도 사용할 수 없으면 위에 둔다
		IF TALENT:PLAYER:달마
			PRINTL 엎드려 누운 몸 위에촛불를 실을 수 있어 그대로 촛대로서 보내듯이 명해졌다…
		;손를 사용할 수 없으면발에
		ELSEIF TALENT:PLAYER:양팔결손
			PRINTL 발에촛불를 끼여,그대로 촛대로서 보내듯이 명해졌다…
		;손를 사용할 수 있으면손에
		ELSE
			PRINTL 손에촛불를 갖게해 그대로 촛대로서 보내듯이 명해졌다…
		ENDIF
	ENDIF
	ITEM:저온초 -= 1
ELSEIF RAND:3 == 0
	IF EXP:PLAYER:V경험 && VAGINA(PLAYER)
		PRINTL 굴곡위로 된바기나에 꽃을 꽂을 수 있어 그대로 화병으로서 보내듯이 명해졌다…
	ELSEIF EXP:PLAYER:A경험
		PRINTL 굴곡위로 된애널에 꽃을 꽂을 수 있어 그대로 화병으로서 보내듯이 명해졌다…
	ELSEIF EXP:PLAYER:U경험 && PENIS(PLAYER)
		PRINTL 요도에 꽃을 꽂아 넣을 수 있어 그대로 화병으로서 보내듯이 명해졌다…
	ELSE
		PRINTL 비밀스런 장소에 꽃을 꽂을 수 있어 그대로 화병으로서 보내듯이 명해졌다…
	ENDIF
ELSEIF RAND:2 == 0
	PRINTFORML 네발로 엎드리게 몰려 위에 티 세트를 늘어놓여졌다
	PRINTFORML 그대로,당분간 테이블로서 보내듯이 명해졌다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%가 네발로 엎드림이 되면,%CALLNAME%가 위에 앉아 왔다
	PRINTFORML 그대로,당분간 의자로서 보내듯이 명해졌다…
ENDIF


;-------------------------------------------------
;295,발로 밟게 한다
;-------------------------------------------------
@MESSAGE_COM295
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%를 차 넘어뜨려,가차 없이 짓밟아 왔다…

;-------------------------------------------------
;296,배빵 시킨다
;-------------------------------------------------
@MESSAGE_COM296
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%를 질질 끌어 넘어뜨려,그 배를 후려갈겨 왔다…

;랜덤으로 구토
IF RAND:5 == 0
	PRINTL 
	PRINTFORML %CALLNAME:PLAYER%는 그 충격에 계속 참지 못하고,위안의 물건을 모두 되돌려 버렸다…
	
	STAIN:PLAYER:입 |= 2048
	STAIN:PLAYER:가슴 |= 2048
	STAIN:PLAYER:발 |= 2048
ENDIF

;-------------------------------------------------
;297,목조르기 시킨다
;-------------------------------------------------
@MESSAGE_COM297
PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의
PRINTFORML 목에손를 걸면,강하게 잡고 왔다…

;-------------------------------------------------
;298,물고문 시킨다
;-------------------------------------------------
@MESSAGE_COM298
PRINTFORM %CALLNAME%에 머리를 잡아져
IF TEQUIP:목욕탕
	PRINTL 두로부터 욕조에 가라앉힐 수 있었다…
ELSE
	PRINTL 수를 친 대야에 가라앉힐 수 있었다…
ENDIF

;-------------------------------------------------
;299,간질이게 한다
;-------------------------------------------------
@MESSAGE_COM299
PRINTFORM %CALLNAME%는
SIF TEQUIP:PLAYER:밧줄
	PRINT 속박되어 움직일 수 없다
PRINTFORML %CALLNAME:PLAYER%를 억눌러,그 체내를 간질여 왔다…


;===========================================================
;약품계
;===========================================================
;-------------------------------------------------
;300,로션
;-------------------------------------------------
@MESSAGE_COM300
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%에로션를 쳐발랐다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME%에로션를 쳐발랐다…
ENDIF


;-------------------------------------------------
;301,미약
;-------------------------------------------------
@MESSAGE_COM301
;촉수소환중
IF TEQUIP:촉수
	PRINTFORML 최음효과가 있는액체가촉수로부터 스며나왔다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에미약를 먹게 되었다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME%에
	;순종LV2 이하
	SIF ABL:순종 <= 2
		PRINT 억지로
	PRINTFORML 미약를 먹였다…
ENDIF


;-------------------------------------------------
;302,이뇨제
;-------------------------------------------------
@MESSAGE_COM302
;촉수소환중
IF TEQUIP:촉수
	PRINTFORML 극 가는촉수가,이뇨 작용이 있는액체를 요도로부터 직접 흘려 넣었다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에이뇨제를 먹게 되었다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME%에
	;순종LV2 이하
	SIF ABL:순종 <= 2
		PRINT 억지로
	PRINTFORML 이뇨제를 먹였다…
ENDIF


;-------------------------------------------------
;303,강정신약
;-------------------------------------------------
@MESSAGE_COM303
;촉수소환중
IF TEQUIP:촉수
	PRINTFORML 특수한 효과가 있는액체가촉수로부터 스며나왔다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에강정신약를 먹게 되었다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME:PLAYER%는%CALLNAME%에
	;순종LV2 이하
	SIF ABL:순종 <= 2
		PRINT 억지로
	PRINTFORM 강정신약를 먹였다…
ENDIF


;-------------------------------------------------
;304,구토제
;-------------------------------------------------
@MESSAGE_COM304
;촉수소환중
IF TEQUIP:촉수
	PRINTFORML 촉수로부터,배변을 재촉하는 작용이 있는액체가 스며나왔다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에 설사약을 먹게 되었다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME%에
	;순종LV2 이하
	SIF ABL:순종 <= 2
		PRINT 억지로
	PRINTFORML 설사약을 먹였다…
ENDIF


;-------------------------------------------------
;305,가려움증약
;-------------------------------------------------
@MESSAGE_COM305
IF TEQUIP:촉수
	PRINTFORML 촉수로부터,가려움를 가져오는 작용이 있는액체가 스며나왔다…
;역조교모드
ELSEIF TEQUIP:역조교
	;자신의 손가락
	IF TFLAG:56 == 99
		PRINTFORM %CALLNAME%는,튜브에 들어간 약을 손가락으로 충분히건져 올렸다…
	ELSE
		PRINTFORM %CALLNAME:PLAYER%의
		IF TFLAG:56 == 10
			PRINT 전신
		;가슴
		ELSEIF TFLAG:56 == 0
			PRINT 가슴
		;PorC
		ELSEIF TFLAG:56 == 1
			IF PENIS(TARGET)
				PRINT 페니스
			ELSE
				PRINT 클리토리스
			ENDIF
		;애널
		ELSEIF TFLAG:56 == 2
			PRINT 애널
		;바기나
		ELSEIF TFLAG:56 == 3
			PRINT 질내
		ENDIF
		PRINTFORM 에,가려워지는 약이 충분히
		IF TFLAG:56 == 10 || TFLAG:56 == 0 || TFLAG:56 == 1
			PRINTL 발라졌다…
		ELSE
			PRINTL 주입되었다…
		ENDIF
	ENDIF
;통상 모드
ELSE
	;자신의 손가락
	IF TFLAG:56 == 99
		PRINTL 튜브에 들어간 약을,손가락으로 충분히건져 올렸다…
	ELSE
		PRINTFORM %CALLNAME%의
		IF TFLAG:56 == 10
			PRINT 전신
		;가슴
		ELSEIF TFLAG:56 == 0
			PRINT 가슴
		;PorV
		ELSEIF TFLAG:56 == 1
			IF PENIS(TARGET)
				PRINT 페니스
			ELSE
				PRINT 클리토리스
			ENDIF
		;애널
		ELSEIF TFLAG:56 == 2
			PRINT 애널
		;바기나
		ELSEIF TFLAG:56 == 3
			PRINT 질내
		ENDIF
		PRINTFORML 에,가려워지는 약을 충분히
		IF TFLAG:56 == 10 || TFLAG:56 == 0 || TFLAG:56 == 1
			PRINTL 발랐다…
		ELSE
			PRINTL 주입했다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;306,꽐라플레이
;-------------------------------------------------
@MESSAGE_COM306
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%에 술을 먹게 되었다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME%에
	;순종LV2 이하
	SIF ABL:순종 <= 2
		PRINT 억지로
	PRINTFORML 술을 먹였다…
	
ENDIF

;===========================================================
;시추에이션별
;===========================================================
;-------------------------------------------------
;310,비디오촬영
;-------------------------------------------------
@MESSAGE_COM310
;해제시
IF TEQUIP:비디오카메라
	PRINTL 촬영을 종료했습니다
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORMW %CALLNAME%는비디오카메라로 %CALLNAME:PLAYER%의 촬영을 시작했다
	PRINTFORML 카메라에 향해,자기 소개를 하듯이 촉구받았지만…
;통상 모드
ELSE
	IF FLAG:22 == 1
		PRINTFORMW %CALLNAME:MASTER%는비디오카메라로%CALLNAME%의 촬영을 시작했다…
	ELSE
		PRINTFORMW %CALLNAME:ASSI%에비디오카메라로%CALLNAME%의 촬영을 시키기로 했다…
	ENDIF
	PRINTFORML %CALLNAME:MASTER%가%CALLNAME%에 자기 소개하도록 재촉하면,
	SELECTCASE KANRAKU(TARGET)
		;연모함락
		CASE 1
			PRINTL 카메라에 향해 자기 소개를 실시했다…
		;음란함락
		CASE 2
			;노출벽 6 이상
			IF ABL:노출벽 >= 6
				PRINTFORM %CALLNAME%는 자신의 본명이나 이것까지의 성체험에 대해
				SIF ABL:자위중독 >= 3
					PRINT ,더욱은 자위때로 하는 망상의 내용에 대해서까지
				PRINTFORML 희희낙락 해 말하기 시작했다…
				PRINTFORML 이 비디오가 거리에 흩뿌려진다고 하는 기대만으로 사타구니를\@ PENIS(TARGET) ? 부풀려 # 적시고\@있다…
			;노출벽 3 이상
			ELSEIF ABL:노출벽 >= 3
				PRINTFORML %CALLNAME%는카메라에 향해 추잡한 말을입 달리기 시작했다…
			;그것 이하
			ELSE
				PRINTFORML 자칭하는 것도 하는 둥 마는 둥,%CALLNAME:MASTER%를 도발 하기 시작했다…
			ENDIF
		;복종함락
		CASE 3
			;노출벽 4 이상
			SIF ABL:노출벽 >= 4
				PRINTFORM 치태를 남기지 않고 찍히는 것에의 기대에 사타구니를\@ PENIS(TARGET) ? 부풀리고 # 적셔\@면서,
			PRINTFORML 정중한 전대사을 말하고 있다…
		;그 외
		CASEELSE
			;굴복 3 한편,노출벽 3 이상 혹은순종 4 이상
			IF MARK:굴복각인 == 3 && (ABL:노출벽 >= 3 || ABL:순종 >= 4)
				PRINTFORML %CALLNAME%는카메라에 향해 자기 소개를 실시했다…
			ELSE
				PRINTFORML %CALLNAME%는 옆을 향잘라 아무것도 말하려고 하지 않는다…
			ENDIF
	ENDSELECT
ENDIF


;-------------------------------------------------
;311,비디오감상
;-------------------------------------------------
@MESSAGE_COM311
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%과 함께,비디오감상를 하는 일이 되었다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME%과 함께,비디오감상를 하기로 했다…
ENDIF


;-------------------------------------------------
;312,사진촬영
;-------------------------------------------------
@MESSAGE_COM312
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%에카메라를 향할 수 있어 치태가 촬영되었다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 치태를필름에 거두었다…
ENDIF


;-------------------------------------------------
;313,수치플레이
;-------------------------------------------------
@MESSAGE_COM313
;해제시
IF TEQUIP:수치플레이
	PRINTFORM 수치플레이용의대거울를 정리했다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORM %CALLNAME%는,수치플레이용의대거울를 꺼내 왔다…
;통상 모드
ELSE
	PRINTFORM %CALLNAME%의 전에,수치플레이용의대거울를 꺼내 왔다…
ENDIF


;-------------------------------------------------
;314,제모
;-------------------------------------------------
@MESSAGE_COM314
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%에,그늘털를 완전히 깎아 떨어뜨려졌다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 그늘털를 완전히 깎아 떨어뜨렸다
	PRINTFORM %CALLNAME%는
	IF ABL:노출벽 < 2
		PRINT 얼굴을 새빨갛게 해
		IF TEQUIP:밧줄
			PRINTFORML %CALLNAME:PLAYER%과 시선을 맞추지 않게 돌리고 있다…
		ELSE
			PRINTFORML 손로 반들반들이 된 성기를 숨기려고 하고 있다…
		ENDIF
	ELSEIF ABL:노출벽 == 2
		PRINTL 부끄러운 듯이 공공연하게 된 자신의 성기를 응시하고 있다…
	ELSEIF ABL:노출벽 == 3
		PRINTL 조금 얼굴을 숙이게 하고 있다…
	ELSEIF ABL:노출벽 == 4
		PRINTL 성기가 뻔히 보임 상태에 부끄러움을 느끼면서도,어딘지 모르게 즐기고 있는 것 같다…
	ELSE
		PRINTL 공기가 성기를 매만지는 감촉을 많이 즐기고 있는 것 같다…
	ENDIF
ENDIF


;-------------------------------------------------
;315,목욕탕플레이
;-------------------------------------------------
@MESSAGE_COM315
;해제시
IF TEQUIP:목욕탕
	PRINTFORML 목욕탕을 나와,방으로 돌아왔다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에 이끌려,목욕탕에 들어갔다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME%를 목욕탕에 데려 갔다…
ENDIF


;-------------------------------------------------
;316,샤워
;-------------------------------------------------
@MESSAGE_COM316
;역조교모드
IF TEQUIP:역조교
	IF TEQUIP:샤워
		PRINTFORMW %CALLNAME%가,%CALLNAME:PLAYER%에의샤워를 멈추었다…
	ELSE
		PRINTFORMW %CALLNAME%가 붙인샤워의 더운 물이 쏟아져 왔다…
	ENDIF
;통상 모드
ELSE
	IF TEQUIP:샤워
		PRINTFORMW %CALLNAME%에의샤워를 멈추었다…
	ELSE
		PRINTFORMW %CALLNAME%에샤워를 받게 했다…
	ENDIF
ENDIF


;-------------------------------------------------
;317,거품비비기
;-------------------------------------------------
@MESSAGE_COM317

PRINTFORM %CALLNAME%는 소프를 거품이 일게 해 자신에게 쳐발라,
;연모나친애
SIF (TALENT:연모 || TALENT:친애)
	PRINT 넋을 잃은 표정을 하면서
;작은체구
IF TALENT:작은체구
	PRINTFORM %CALLNAME:PLAYER%에 작은 신체를 힘껏 문질러
;거유나폭유＋작은체구가 아니다
ELSEIF TALENT:거유 || TALENT:폭유
	PRINTFORM %CALLNAME:PLAYER%에 풍만한가슴와 포근한 피부를 문질러
ELSE
	PRINTFORM %CALLNAME:PLAYER%에 전신을 문질러
ENDIF
PRINTFORML 애무 했다…

;-------------------------------------------------
;318,거품비비기한다
;-------------------------------------------------
@MESSAGE_COM318
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%에거품비비기한다말해졌다…
ENDIF

;수 넣는 경우만
PRINTFORM %CALLNAME:PLAYER%는 소프를 거품이 일게 해 자신에게 쳐발라,
;작은체구
IF TALENT:작은체구
	PRINTFORM %CALLNAME%에 작은 신체를 힘껏 문질러
;거유나폭유＋작은체구가 아니다
ELSEIF TALENT:거유 || TALENT:폭유
	PRINTFORM %CALLNAME%에 풍만한가슴와 포근한 피부를 문질러
ELSE
	PRINTFORM %CALLNAME%에 전신을 문질러
ENDIF
PRINTFORML 애무 했다…

;-------------------------------------------------
;319,신혼부부플레이
;-------------------------------------------------
@MESSAGE_COM319
;역조교모드
IF TEQUIP:역조교
	IF TEQUIP:에이프런
		PRINTFORM %CALLNAME%에
		IF KIMONO(MASTER)
			PRINT 앞치마
		ELSE
			PRINT 에이프런
		ENDIF
		PRINTL 를 벗겨졌다…
	ELSE
		PRINTFORM %CALLNAME%에
		IF KIMONO(MASTER)
			PRINT 앞치마
		ELSE
			PRINT 에이프런
		ENDIF
		PRINTL 를 붙이듯이 말해졌다…
	ENDIF
;통상 모드
ELSE
	IF TEQUIP:에이프런
		PRINTFORM %CALLNAME%의
		IF KIMONO(TARGET)
			PRINT 앞치마
		ELSE
			PRINT 에이프런
		ENDIF
		PRINTL 를 벗겼다…
	ELSE
		PRINTFORM %CALLNAME%에
		IF KIMONO(TARGET)
			PRINT 앞치마
		ELSE
			PRINT 에이프런
		ENDIF
		PRINTL 를 붙였다…
	ENDIF
ENDIF


;-------------------------------------------------
;320,야외플레이
;-------------------------------------------------
@MESSAGE_COM320
;역조교모드
IF TEQUIP:역조교
	;해제시
	IF TEQUIP:야외플레이
		PRINTFORML %CALLNAME%에 이끌려 방으로 돌아왔다…
	ELSE
		;가렵다
		IF ZUIKI(PLAYER)
			PRINT 신체의가려움도 무시되어
		;배설감
		ELSEIF BASE:PLAYER:뇨의 >= 60 || BASE:PLAYER:변의 >= 60
			PRINT 배설감도 무시되어
		;완구 들어가 있다
		ELSEIF VCHECK(PLAYER) || ACHECK(PLAYER)
			;VA양쪽 모두에 완구
			IF VCHECK(PLAYER) && ACHECK(PLAYER)
				PRINT 전후의 구멍에 완구를 넣은 채로
			;V만
			ELSEIF VCHECK(PLAYER)
				PRINT 바기나에
				IF TEQUIP:PLAYER:바이브
					PRINT 바이브
				ELSEIF TEQUIP:PLAYER:로터삽입
					PRINT 로터
				ENDIF
				PRINT 를 넣은 채로
			;A만
			ELSE
				;애널바이브
				IF TEQUIP:PLAYER:애널바이브
					PRINT 애널에바이브를 넣은 채로
				;애널바이브
				ELSEIF TEQUIP:PLAYER:애널비즈
					PRINT 애널에 비즈가 메워진 채로
				;확장벌룬
				ELSEIF TEQUIP:PLAYER:확장벌룬
					PRINT 벌룬으로애널를 펼친 채로
				;로터
				ELSEIF TEQUIP:PLAYER:로터A삽입
					PRINT 애널에로터를 넣은 채로
				ENDIF
			ENDIF
		ENDIF
		PRINTL 목걸이를 붙여져 밖에 데리고 나가졌다…
	ENDIF
;통상 모드
ELSE
	;해제시
	IF TEQUIP:야외플레이
		PRINTFORML %CALLNAME%를 따라,방으로 돌아왔다…
	ELSE
		;가렵다
		IF ZUIKI(TARGET)
			PRINT 신체의가려움에 몸부림친다
		;배설감
		ELSEIF BASE46(0)
			PRINT 배설감에 떨린다
		;완구 들어가 있다
		ELSEIF VCHECK(TARGET) || ACHECK(TARGET)
			;VA양쪽 모두에 완구
			IF VCHECK(TARGET) && ACHECK(TARGET)
				PRINT 전후의 구멍에 완구를 넣은 채로의
			;V만
			ELSEIF VCHECK(TARGET)
				PRINT 바기나에
				IF TEQUIP:바이브
					PRINT 바이브
				ELSEIF TEQUIP:로터삽입
					PRINT 로터
				ENDIF
				PRINT 를 넣은 채로의
			;A만
			ELSE
				;애널바이브
				IF TEQUIP:애널바이브
					IF TALENT:동물귀 == 11
						PRINT 고양이의 귀와 꼬리를 길렀다
					ELSEIF TALENT:동물귀 == 12
						PRINT 개의 귀와 꼬리를 길렀다
					ELSEIF TALENT:동물귀 == 13
						PRINT 토끼의 귀와 꼬리를 길렀다
					ELSE
						PRINT 애널에바이브를 넣은 채로의
					ENDIF
				;애널바이브
				ELSEIF TEQUIP:애널비즈
					PRINT 애널에 비즈가 메워진 채로의
				;확장벌룬
				ELSEIF TEQUIP:확장벌룬
					PRINT 벌룬으로애널를 펼칠 수 있던 채로의
				;로터
				ELSEIF TEQUIP:로터A삽입
					PRINT 애널에로터를 넣은 채로의
				ENDIF
			ENDIF
		ENDIF
		
		PRINTFORML %CALLNAME%에 목걸이를 입어,밖에 데리고 나갔다…
	ENDIF
ENDIF


;-------------------------------------------------
;321,사람을부른다
;-------------------------------------------------
@MESSAGE_COM321
;아무도 없다
IF TFLAG:151 == 0
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML 이 근처에 사람은 없는 것 같다
		PRINTFORML %CALLNAME%는,좀 더 사람이 많은 (분)편으로 %CALLNAME:PLAYER%를 데려 걷기 시작했다…
	;통상 모드
	ELSE
		PRINTFORML 둘러보았지만,이 근처에 사람은 없는 것 같다
		PRINTFORML %CALLNAME:PLAYER%는,좀 더 사람이 많은 (분)편으로%CALLNAME%를 데려 갔다…
	ENDIF
;기색을 느낀다. 진하다
ELSEIF TFLAG:151 == 1
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%는 마침 지나가는 사람의 기분을 끌려고,고의로에 큰 소리와 소리를 세우고 있다…
	;통상 모드
	ELSE
		PRINTFORML %CALLNAME:PLAYER%는 마침 지나가는 사람의 기분을 끌려고,고의로에 큰 소리와 소리를 세웠다…
	ENDIF
;시선을 느낀다. 여기로 와―
ELSEIF TFLAG:151 == 2
	PRINTFORML 시선을 느끼는…… 누군가는 모르지만,이쪽을 의식하고 있는 것이 있는 것 같다
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%는 주위의 기색으로 향해 추잡한 말로 불렀다
	;통상 모드
	ELSE
		PRINTFORML %CALLNAME:PLAYER%는 주위의 기색으로 향해 추잡한 말로 불렀다
	ENDIF
;시선을 느끼는→갤러리가 모습을 나타낸다(집단 시간[視姦] 모드에)
ELSEIF TFLAG:151 == 3
	;견
	IF TFLAG:500 == 1
		PRINTFORML 경계하고 있는지,견들은 좀처럼 가까워져 오지 않는다
		;역조교모드
		IF TEQUIP:역조교
			PRINTFORML 견들에게 좀 더 자주(잘) 보이도록,다리를 크게 틔워졌다…
		;통상 모드
		ELSE
			PRINTFORML 견들에게 좀 더 자주(잘) 보이도록,%CALLNAME%의 다리를 크게 벌어지게 했다…
		ENDIF
	ELSE
		PRINTFORML 시선의 주인은 아직도 모습을 나타내지 않는다…
		;역조교모드
		IF TEQUIP:역조교
			;순종 5 이상 or노출벽 3 이상
			IF ABL:순종 >= 5 || ABL:노출벽 >= 3
				PRINTFORML 자신의 단정치 못한 모습을 좀 더 근처에서 보면 좋다,라고 주위에 간원 당했다…
			ELSE
				PRINTFORML %CALLNAME%는,어차피 본다면 좀 더 근처에 오면 어때,라고 주위를 도발했다
			ENDIF
		;통상 모드
		ELSE
			PRINTFORM %CALLNAME:PLAYER%는
			;순종 5 이상 or노출벽 3 이상
			IF ABL:순종 >= 5 || ABL:노출벽 >= 3
				PRINTFORML %CALLNAME%에 명해,
				PRINTFORML 자신의 단정치 못한 모습을 좀 더 근처에서 보면 좋겠다고 주위에 간원 시켰다
			ELSE
				PRINTFORML 어차피 본다면 좀 더 근처에 오면 어때,라고 주위를 도발했다
			ENDIF
		ENDIF
	ENDIF
;집단 시간[視姦] 모드
ELSEIF TFLAG:151 == 4
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%가 부추기면,욕구에 번득인 눈으로 %CALLNAME:PLAYER%를 시간[視姦] 하고 있었다
		PRINTFORML %STR:81%의 시선이,한층 더 마음껏 %CALLNAME:PLAYER%에 따라졌다…
	;통상 모드
	ELSE
		PRINTFORML 욕구에 번득인 눈으로%CALLNAME%를 시간[視姦] 하고 있는 %STR:81%를 말로 부추겨,
		PRINTFORML %CALLNAME%의 치태를,한층 더 마음껏 과시해 주었다…
	ENDIF
;텐션 MAX(윤간실행가능)
ELSEIF TFLAG:151 >= 5
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML %CALLNAME%는 더욱 더 %CALLNAME:PLAYER%를 과시하듯이해 갤러리를 부추겼다…
		PRINTFORMW %STR:81%의 흥분은 더 이상 없을만큼 높아지고 있다
		PRINTFORML 
		PRINTFORML 후는%CALLNAME%조차 허락하면,%CALLNAME:PLAYER%는
		PRINTFORML %STR:81%가 부풀어 오른 성욕으로 가지고,유린해 다하여질 것이다…
	;통상 모드
	ELSE
		PRINTFORML %CALLNAME:PLAYER%는 더욱 더%CALLNAME%를 과시하듯이해 갤러리를 부추겼다…
		PRINTFORMW %STR:81%의 흥분은 더 이상 없을만큼 높아지고 있다
		PRINTFORML 
		PRINTFORML 후는 %CALLNAME:PLAYER%조차 허락하면,%STR:81%는
		PRINTFORML 그 부풀어 오른 성욕으로 가지고%CALLNAME%의 신체를 다 유린할 것이다…
	ENDIF
ENDIF


;-------------------------------------------------
;322,윤간
;-------------------------------------------------
@MESSAGE_COM322
;역조교모드
IF TEQUIP:역조교
	PRINTFORMW 흥분이 정점으로 달한 %STR:81%는,충혈된 눈으로 %CALLNAME:PLAYER%를 응시하고 있다
	
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
	;완구 들어가 있다
	IF VCHECK(PLAYER) || ACHECK(PLAYER)
		;VA양쪽 모두에 완구
		IF VCHECK(PLAYER) && ACHECK(PLAYER)
			PRINT 전후의 완구
		;V만
		ELSEIF VCHECK(PLAYER)
			PRINT 바기나의
			IF TEQUIP:PLAYER:바이브
				PRINT 바이브
			ELSEIF TEQUIP:PLAYER:로터삽입
				PRINT 로터
			ENDIF
		;A만
		ELSE
			PRINT 애널의
			;애널바이브
			IF TEQUIP:PLAYER:애널바이브
				IF TALENT:PLAYER:동물귀 == 11
					PRINT 고양이 꼬리
				ELSEIF TALENT:PLAYER:동물귀 == 12
					PRINT 개꼬리
				ELSEIF TALENT:PLAYER:동물귀 == 13
					PRINT 토끼 꼬리
				ENDIF
				PRINT 바이브
			;애널바이브
			ELSEIF TEQUIP:PLAYER:애널비즈
				PRINT 비즈
			;확장벌룬
			ELSEIF TEQUIP:PLAYER:확장벌룬
				PRINT 벌룬
			;로터
			ELSEIF TEQUIP:PLAYER:로터A삽입
				PRINT 로터
			ENDIF
		ENDIF
		PRINTL 를 과시하듯이 뽑아 내면,
	ENDIF
	
	;가렵다
	IF ZUIKI(PLAYER)
		PRINT 신체의가려움에 몸부림친다
	;배설감
	ELSEIF BASE:PLAYER:뇨의 >= 60 || BASE:PLAYER:변의 >= 60
		PRINT 배설감에 떨린다
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%를,
	
	PRINTFORMW 흥분한 %STR:81%안으로 밀어 냈다…
	
	PRINTL 
	PRINTFORMW 흥분한 %STR:81%의 시선이,한가닥의 실 감기지 않는 모습으로 넘어진 %CALLNAME:PLAYER%에 따라진다
	PRINTFORM 제지할 여유도 없고 %STR:81%에 모여져
	IF E && F
		PRINTW 구멍이라고 하는 구멍을 범해 다하여졌다…
	ELSE
		PRINTW 그 신체가 유린되었다…
	ENDIF
;통상 모드
ELSE
	PRINTFORMW 흥분이 정점으로 달한 %STR:81%는,충혈된 눈으로%CALLNAME%를 응시하고 있다
	
	PRINTFORM %CALLNAME:PLAYER%는
	;완구 들어가 있다
	IF VCHECK(TARGET) || ACHECK(TARGET)
		;VA양쪽 모두에 완구
		IF VCHECK(TARGET) && ACHECK(TARGET)
			PRINT 전후의 완구
		;V만
		ELSEIF VCHECK(TARGET)
			IF TEQUIP:바이브
				PRINT 꾸불꾸불하는바이브
			ELSEIF TEQUIP:로터삽입
				PRINT 떨리는로터
			ENDIF
		;A만
		ELSE
			;애널바이브
			IF TEQUIP:애널바이브
				PRINT 꾸불꾸불한다
				IF TALENT:동물귀 == 11
					PRINT 고양이 꼬리의
				ELSEIF TALENT:동물귀 == 12
					PRINT 개꼬리의
				ELSEIF TALENT:동물귀 == 13
					PRINT 토끼 꼬리의
				ENDIF
				PRINT 바이브
			;애널바이브
			ELSEIF TEQUIP:애널비즈
				PRINT 애널에 비즈
			;확장벌룬
			ELSEIF TEQUIP:확장벌룬
				PRINT 확장벌룬
			;로터
			ELSEIF TEQUIP:로터A삽입
				PRINT 애널의로터
			ENDIF
		ENDIF
		PRINTL 를 과시하듯이 뽑아 내면,
	ENDIF
	
	;가렵다
	IF ZUIKI(TARGET)
		PRINT 신체의가려움에 몸부림친다
	;배설감
	ELSEIF BASE46(0)
		PRINT 배설감에 떨린다
	ELSEIF ABL:노출벽 >= 6
		PRINT 정욕에 몸을 태운다
	ELSEIF ABL:노출벽 >= 4
		PRINT 치욕에 떨린다
	ELSE
		PRINT 공포에 떨린다
	ENDIF
	PRINTFORM %CALLNAME%를,
	
	PRINTFORMW 흥분한 %STR:81%안으로 밀어 냈다…
	
	PRINTL 
	PRINTFORMW 흥분한 %STR:81%의 시선이,한가닥의 실 감기지 않는 모습으로 넘어진%CALLNAME%에 따라진다
	PRINTFORM %STR:81%는%CALLNAME%에 쇄도해,
	IF E && F
		PRINTFORMW %CALLNAME%의 구멍이라고 하는 구멍을 다 범했다…
	ELSE
		PRINTFORMW 그 신체를 유린하기 시작했다…
	ENDIF
	
ENDIF


;-------------------------------------------------
;323,지하실
;-------------------------------------------------
@MESSAGE_COM323
IF TEQUIP:지하실
	PRINTFORML 지하실를 나와,조교 방으로 돌아왔다…
;역조교모드
ELSEIF TEQUIP:역조교
	PRINTFORML %CALLNAME%에 이끌려,고문 목적의지하실로 내렸다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME%를 따라,고문 목적의지하실로 내렸다…
	SIF TALENT:낙인 || TALENT:한팔결손 || TALENT:양팔결손 || TALENT:한다리결손 || TALENT:양다리결손 || TALENT:달마 || TALENT:모두발치
		PRINTFORML %CALLNAME%는 이전 이 장소에서 맛본공포를 생각해 내,몸을 진동시키고 있다
ENDIF

;===========================================================
;촉수계
;===========================================================
;-------------------------------------------------
;340,촉수소환
;-------------------------------------------------
@MESSAGE_COM340
;해제시
IF TEQUIP:촉수
	PRINTFORML 촉수를 보내 돌아가,%CALLNAME%의 몸을 개방했다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%가 호출한 이형의촉수가,%CALLNAME%의 몸을 얽어맸다…
ENDIF


;-------------------------------------------------
;341,촉수유두고문
;-------------------------------------------------
@MESSAGE_COM341
;해제시
IF TEQUIP:니플캡
	PRINTFORML 촉수는%CALLNAME%의 유두로부터 멀어졌다
ELSE
	IF TEQUIP:지하실
		PRINTFORML 촉수가%CALLNAME%의 유두에 매달려,물거나 이끌거나 해 꾸짖기 시작했다
	ELSE
		PRINTFORML 세세하게 꿈틀거리는촉수가%CALLNAME%의 유두를 만지작거리기 시작했다
	ENDIF
ENDIF


;-------------------------------------------------
;342,촉수클리고문
;-------------------------------------------------
@MESSAGE_COM342
;해제시
IF TEQUIP:클리캡
	PRINTFORML 촉수는%CALLNAME%의클리고문를 중단했다
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 점액으로 녹여,
		ENDIF
	ENDIF
	IF TEQUIP:지하실
		PRINTFORML 촉수가%CALLNAME%의 클리토리스에 매달려,물거나 이끌거나 해 꾸짖기 시작했다
	ELSE
		PRINTFORML 세세하게 꿈틀거리는촉수가%CALLNAME%의 클리토리스를 만지작거리기 시작했다
	ENDIF
ENDIF


;-------------------------------------------------
;343,촉수페니스고문
;-------------------------------------------------
@MESSAGE_COM343
;해제시
IF TEQUIP:전동오나홀
	PRINTFORML 촉수는%CALLNAME%의페니스를해방 했다
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 점액으로 녹여,
		ENDIF
	ENDIF
	PRINTFORML 추잡하고 고기 주름을 꾸불꾸불하게 한촉수가%CALLNAME%의페니스에 감겨 훑어 내기 시작했다
ENDIF


;-------------------------------------------------
;344,촉수삽입
;-------------------------------------------------
@MESSAGE_COM344
;해제시
IF TEQUIP:바이브
	PRINTFORML 촉수가%CALLNAME%의 성기로부터 뽑아 내졌다
ELSE
	IF TEQUIP:지하실
		PRINTFORML 흉악한 형태를 한촉수가,%CALLNAME%의 성기에 기어들어 갔다
	ELSE
		PRINTFORML 세세한 돌기에 덮인촉수가%CALLNAME%의 성기에 기어들어 갔다
	ENDIF
ENDIF


;-------------------------------------------------
;345,촉수애널
;-------------------------------------------------
@MESSAGE_COM345
;해제시
IF TEQUIP:애널바이브
	PRINTFORML 촉수는%CALLNAME%의 아누스로부터 뽑아 내졌다
ELSE
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		IF TFLAG:55
			PRINT 의 깨어진 부분을 펴넓혀,
		ELSE
			PRINT 를 점액으로 녹여,
		ENDIF
	ENDIF
	IF TEQUIP:지하실
		PRINTFORML 흉악한 형태를 한촉수가,%CALLNAME%의애널에 기어들어 갔다
	ELSE
		PRINTFORML 세세한 돌기에 덮인촉수가%CALLNAME%의애널에 기어들어 갔다
	ENDIF
ENDIF


;-------------------------------------------------
;350,촉수구욕
;-------------------------------------------------
@MESSAGE_COM350
;해제시
IF TEQUIP:촉수구욕
	PRINTFORML 촉수는%CALLNAME%의입로부터 뽑아 내졌다
ELSE
	PRINTFORML 젖고 빛나는촉수가%CALLNAME%의입에 침입했다
ENDIF


;-------------------------------------------------
;351,촉수구속
;-------------------------------------------------
@MESSAGE_COM351
;해제시
IF TEQUIP:밧줄
	PRINTFORML 촉수는%CALLNAME%의 구속을 풀었다
ELSE
	PRINTFORML 무수한촉수가%CALLNAME%의 몸을 묶었다
ENDIF


;-------------------------------------------------
;352,촉수관장
;-------------------------------------------------
@MESSAGE_COM352
;스타킹계
IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
	SIF TEQUIP:특수2 == 5
		PRINT 스타킹
	SIF TEQUIP:특수2 == 6
		PRINT 타이츠
	IF TFLAG:55
		PRINT 의 깨어진 부분을 펴넓혀,
	ELSE
		PRINT 를 점액으로 녹여,
	ENDIF
ENDIF
PRINTFORML 극태의촉수가%CALLNAME%의후 구멍을 막아,체액을 흘려 넣었다
;마조끼
IF ABL:마조끼 == 0
	PRINTFORM %CALLNAME%는 고민의 표정을 띄우고 있다
ELSEIF ABL:마조끼 == 1
	PRINTFORM %CALLNAME%는 비지땀을 띄워,배설감에 견디고 있다
ELSEIF ABL:마조끼 == 2
	PRINTFORM %CALLNAME%는 괴로운 듯한 표정을 하면서도,얼굴을 홍조 시키고 머뭇머뭇엉덩이를 흔들고 있다
ELSEIF ABL:마조끼 == 3
	PRINTFORM %CALLNAME%는,배설감에 시달리면서도,때때로 황홀로 한 표정을 띄우고 있다
ELSEIF ABL:마조끼 == 4
	PRINTFORM %CALLNAME%는,넋을 잃은 표정을 띄워,배설감과 복통을 즐기고 있다
ELSEIF ABL:마조끼 >= 5
	PRINTFORM %CALLNAME%는,배설감을 마음껏 맛보고 있다
	PRINTFORM %CALLNAME%의 표정은 쾌감에 야무지지 못하게 느슨해져,그입
	SIF VAGINA(TARGET)
		PRINT 와 질구
	PRINTFORML 로부터는 뚝뚝 군침을 방울져 떨어지게 하고 있다
ENDIF


;-------------------------------------------------
;360,촉수착유
;-------------------------------------------------
@MESSAGE_COM360
;해제시
IF TEQUIP:착유기
	PRINTFORML 촉수는%CALLNAME%의 유두를해방 했다
ELSE
	PRINTFORML 흡반장의촉수가%CALLNAME%의 유두를 들이마셔 올리기 시작했다
ENDIF


;-------------------------------------------------
;361,촉수유선고문
;-------------------------------------------------
@MESSAGE_COM361
IF TEQUIP:촉수유선삽입
	PRINTFORML 극세의촉수가%CALLNAME%의 유선으로부터 뽑아 내졌다
	SIF TALENT:모유체질
		PRINTFORML 해방 된%CALLNAME%의 유선으로부터 외곬의모유가 늘어지고 나왔다
ELSE
	PRINTFORM 극세의촉수가%CALLNAME%의
	IF TALENT:절벽
		PRINT 가까스로 포동포동하고 있다고 아는가슴에
	;남자 반음반양 컨트보이
	ELSEIF !BREAST(TARGET)
		PRINT 평평한가슴에
	ELSEIF TALENT:빈유
		PRINT 작은가슴에
	ELSEIF TALENT:거유
		PRINT 풍부한가슴에
	ELSEIF TALENT:폭유
		PRINT 너무 풍만한가슴에
	ELSE
		PRINT 형이 좋은가슴에
	ENDIF
	PRINTL 가까워지면,
	PRINTW 쌍구의 첨단으로부터 유선안으로 침입을 시도했다…
	PRINTL 
	IF TALENT:모유체질 == 0
		IF PALAM:욕정 > PALAMLV:5 && (((FLAG:2 & 256) == 0 && (BREAST(TARGET) || TALENT:절벽)) || (FLAG:2 & 256))
			PRINTFORMW 촉수로부터 분비되고 있는 호르몬의 작용인 것인가%CALLNAME%의 유선으로부터모유가 늘어지고 나오게 되었다
		ELSE
			PRINTFORMW 유방내의 이물감에%CALLNAME%의 이마에 비지땀이 떠올라 있다
		ENDIF
	ELSE
		PRINTFORMW 모유에 의해 침입이 용이하게 되어 있는지,%CALLNAME%의 유선은촉수를수 넣어 있는 것처럼 보인다…
	ENDIF
ENDIF


;-------------------------------------------------
;362,촉수G스팟고문
;-------------------------------------------------
@MESSAGE_COM362
IF TEQUIP:바이브 == 2
	PRINTFORMW 촉수는 계속해%CALLNAME%의질내의 가장 민감한 부분에 대해서 완급이 붙은 고문을 전개했다
ELSEIF TEQUIP:바이브 == 3
	PRINTFORML 일단%CALLNAME%의 자궁에 기어들고 있던촉수는 갑자기,자궁입를 후벼파듯이 역류 해
	PRINTFORMW 다음의 순간%CALLNAME%의 질의 천개를 돌기로 세게 긁듯이 더듬기 시작했다
ELSE
	PRINTFORMW %CALLNAME%의 성기에 기어들고 있던촉수는 갑자기,%CALLNAME%의질내의 천정을 몰아세우기 시작했다
ENDIF


;-------------------------------------------------
;363,촉수자궁고문
;-------------------------------------------------
@MESSAGE_COM363
IF TEQUIP:바이브 == 2
	PRINTFORML %CALLNAME%의질내가 껄끔거린 천정을 더듬고 있던촉수는
	PRINTFORML 갑자기 자궁입를 비틀어 열어 안쪽으로 돌진해,%CALLNAME%의 자궁내에 몸을 묻었다
	PRINTFORMW 촉수로 채워진%CALLNAME%의 하복부는 마치임신 하고 있는것 같이 부풀어 오르고 있다…
ELSEIF TEQUIP:바이브 == 3
	PRINTFORMW 자궁에 자리잡고 있는촉수는%CALLNAME%의 자궁의 벽을 훑는 것처럼 스스로의 돌기로 더듬었다
ELSE
	PRINTFORML %CALLNAME%의 성기에 기어들고 있던촉수는 한층 더 안쪽으로 몸을 기어들게 한,
	PRINTFORML 촉수는%CALLNAME%의 자궁입를 비틀어 열어,그대로 다 메울까와 같이 자궁내에 비집고 들어갔다
	PRINTFORMW 촉수로 다 메워진%CALLNAME%의 배는 마치 임산부와 같이 부풀어 올랐다…
ENDIF


;-------------------------------------------------
;364,촉수전립선자극
;-------------------------------------------------
@MESSAGE_COM364
IF TEQUIP:애널바이브 == 2
	PRINTFORMW 촉수는 계속해%CALLNAME%의 전립선에 대해서 완급이 붙은 고문을 전개했다
ELSE
	PRINTFORMW %CALLNAME%의애널에 기어들고 있던촉수는 갑자기,%CALLNAME%의 전립선을 몰아세우기 시작했다
ENDIF


;-------------------------------------------------
;365,촉수요도삽입
;-------------------------------------------------
@MESSAGE_COM365
IF TEQUIP:카테터 == 1
	PRINTFORML 극세의촉수가%CALLNAME%의 요도로부터 뽑아 내졌다
ELSE
	PRINTFORML 극세의촉수가%CALLNAME%의 요도안에 기어들었다
ENDIF


;-------------------------------------------------
;366,촉수바기나확장
;-------------------------------------------------
@MESSAGE_COM366
PRINTFORML %CALLNAME%의 질은 고야와 같은극태의촉수로 휘저어졌다
IF ABL:V감각 <= 1
	PRINTFORMW 촉수가 뽑아 내진후도%CALLNAME%의 질은 경련하고 있는것 같이 실룩거리고 있다
ELSEIF ABL:V감각 == 2
	PRINTFORMW 촉수가 뽑아진후도%CALLNAME%의 질은 뻐끔 입를 열고 있다
ELSEIF ABL:V감각 == 4
	PRINTFORMW 촉수가 뽑아진후도%CALLNAME%의 질은 펼쳐진 채로 좀처럼 돌아오지 않는다
ELSEIF ABL:V감각 == 6
	PRINTFORML 촉수가 뽑아진후의%CALLNAME%의 성기로부터는 고기 빌라가 넘쳐 나와,
	PRINTFORMW 뻐끔 연 질구의 고향은 자궁입가 보이고 있다
ELSE
	PRINTFORMW %CALLNAME%는 크게 펼쳐진 자신의 질구에 눈을 향한 채로,황홀로 한 표정을 띄우고 있다
ENDIF


;-------------------------------------------------
;367,촉수애널확장
;-------------------------------------------------
@MESSAGE_COM367
PRINTFORML %CALLNAME%의 항문은 고야와 같은극태의촉수로 휘저어졌다
PRINTL 
IF ABL:A감각 <= 1
	PRINTFORMW 촉수가 뽑아 내진후도%CALLNAME%의 항문은 경련하고 있는것 같이 실룩거리고 있다
ELSEIF ABL:A감각 == 2
	PRINTFORMW 촉수가 뽑아진후도%CALLNAME%의 항문은 뻐끔 입를 열고 있다
ELSEIF ABL:A감각 == 4
	PRINTFORMW 촉수가 뽑아진후도%CALLNAME%의 항문은 펼쳐진 채로 좀처럼 돌아오지 않는다
ELSEIF ABL:A감각 == 6
	PRINTFORML 촉수가 뽑아진후의%CALLNAME%의 항문으로부터는 고기 빌라가 넘쳐 나와,
	PRINTW 장안이 뻔히 보임이 되어 있다
ELSE
	PRINTFORMW %CALLNAME%는 크게 펼쳐진 자신의 항문에 눈을 향한 채로,황홀로 한 표정을 띄우고 있다
ENDIF


;-------------------------------------------------
;368,촉수양구멍확장
;-------------------------------------------------
@MESSAGE_COM368
PRINTFORML %CALLNAME%의 질과 항문은 고야와 같은극태의촉수로 휘저어졌다
PRINTL 
IF ABL:V감각 >= 7 && ABL:A감각 >= 7
	PRINTFORML %CALLNAME%는 양구멍이 확장된 여운을 맛보고 있다.
	PRINTFORML %CALLNAME%의 질구와 항문은 완전히 펼쳐져 내용을 노출하고 있어
	PRINTW 촉수가 빼내진후도 더 원래대로 돌아가는 기색을 보이지 않는다…
ELSEIF ABL:V감각 > 4 || ABL:A감각 > 4
	PRINTFORML %CALLNAME%는 양발를 넓히고 녹초가 되어 있다
	PRINTW 촉수에 의해 펼칠 수 있었던 질구와 항문으로부터는 점액이 질질 흘림이 되어 있다
ENDIF


;-------------------------------------------------
;369,촉수산란
;-------------------------------------------------
@MESSAGE_COM369
;전부 뽑았을 때
IF TFLAG:56 == 0
	CALL DOUBLE_EGGPLAY_OUT
;다른 한쪽만 뽑았을 때
ELSEIF TFLAG:56 == 1
	CALL EGGPLAY_OUT
;최초로 들어갈 수 있었을 때
ELSEIF TFLAG:56 == 2
	IF TEQUIP:바이브 == 2
		PRINTFORML %CALLNAME%의 질을 몰아세우고 있던촉수는 1개 몸부림 했다고 생각했는데,
		PRINTL 자궁입를 비틀어 열어,안쪽에 향해 알을 토해냈다
	ELSEIF TEQUIP:바이브 == 1
		PRINTFORML %CALLNAME%의 성기에 기어들고 있던촉수는
		PRINTL 갑자기 안쪽에 향해 돌진해,자궁입의 저쪽 편으로 내포 하고 있던 알을 던져 넣었다
	ELSE
		PRINTFORML 극태의촉수가%CALLNAME%의 비순[秘唇]에 기어들면,자궁의 안쪽에서 알을 낳았다
	ENDIF
;한층 더 추가했을 때
ELSE
	PRINTFORML 촉수는 알을 안고 있는%CALLNAME%의 자궁에 추가의 알을 낳았다
ENDIF

;-------------------------------------------------
;370,촉수애널산란
;-------------------------------------------------
@MESSAGE_COM370
;전부 뽑았을 때
IF TFLAG:56 == 0
	CALL DOUBLE_EGGPLAY_OUT
;다른 한쪽만 뽑았을 때
ELSEIF TFLAG:56 == 1
	CALL KANTYO_EGGPLAY_OUT
;최초로 들어갈 수 있었을 때
ELSEIF TFLAG:56 == 2
	IF TEQUIP:애널바이브
		PRINTFORML %CALLNAME%의 직장을 꾸짖고 있던촉수는 가볍게 몸부림 하면
		PRINTL 장내의 안쪽에 향하여 점액과 함께 알을 토해냈다
	ELSE
		PRINTFORML 극태의촉수가%CALLNAME%의 국혈에 기어들어,장내로 알을 낳았다
	ENDIF
;한층 더 추가했을 때
ELSE
	PRINTFORML 촉수는%CALLNAME%의장내에 새로운 알을 낳았다
ENDIF

;-------------------------------------------------
;371,촉수관통
;-------------------------------------------------
@MESSAGE_COM371
PRINTFORML 한 개의 긴촉수가%CALLNAME%의후 구멍으로부터 기어들어,체내를 더듬어 오르기 시작했다
PRINTFORML 이윽고장내로부터 위에,식도를 통해,%CALLNAME%의입로부터촉수의 첨단이 튀어 나왔다…

;-------------------------------------------------
;372,전기촉수
;-------------------------------------------------
@MESSAGE_COM372
PRINTFORML %CALLNAME%의 전신에 휘감긴촉수가,일제히 창백하게 스파크 했다…


;-------------------------------------------------
;373,촉수뇌간
;-------------------------------------------------
@MESSAGE_COM373
PRINTFORML 초극세의촉수가%CALLNAME%의 귀로부터 기어들어,그 뇌에까지 달하면,
PRINTFORML 쾌락 중추에 전류를 흘려,상궤를 벗어난 쾌락을 다이렉트에 주입했다…


;-------------------------------------------------
;374,최면촉수
;좋은 꿈 에로한 꿈 나쁜 꿈
;-------------------------------------------------
@MESSAGE_COM374
PRINTFORML %CALLNAME%에 착 달라붙는촉수가,최면 효과가 있는 가스를 토해냈다…
PRINTFORML 
PRINTFORM 아무래도,%CALLNAME%는
;에로한 꿈
IF TFLAG:56 == 1
	PRINTFORML 에로한 꿈을 꾸고 있는 것 같다
	PRINTFORML 단정치 못한 허덕이는 소리를 올려,때때로 몸을 경련시키고 있다…
;나쁜 꿈
ELSEIF TFLAG:56 == 2
	PRINTFORML 매우 무서운 꿈을 꾸고 있는 것 같다
	PRINTFORML 허공을 응시해 덜컹덜컹떨리면서 울며 아우성치고 있다…
;좋은 꿈
ELSEIF TFLAG:56 == 3
	PRINTFORML 매우 행복 꿈을 꾸고 있는 것 같다
	PRINTFORML 현실에서는촉수에 범해지면서,행복하게 허공을 응시하고 있다…
ENDIF


;===========================================================
;이종간
;===========================================================
;-------------------------------------------------
;380,버터견
;-------------------------------------------------
@MESSAGE_COM380
;역조교모드
IF TEQUIP:역조교
	PRINTFORML 사타구니에 버터를 쳐발라졌다
	PRINTFORML %CALLNAME%가우리로부터 개를 꺼내 오면,쏜살같이 날아 와 %CALLNAME:PLAYER%의 성기를 빨기 시작했다…
;통상 모드
ELSE
	PRINTFORML %CALLNAME:PLAYER%는 버터를%CALLNAME%의 성기에 쳐발랐다
	PRINTFORML 우리로부터 개를 꺼내 오면,쏜살같이 날아 와%CALLNAME%의 성기를 빨기 시작했다…
ENDIF


;-------------------------------------------------
;381,수간
;-------------------------------------------------
@MESSAGE_COM381
;역조교모드
IF TEQUIP:역조교
	PRINTFORML %CALLNAME%가우리를 열면,짐승은 %CALLNAME:PLAYER%에 덥쳐,
	
	;A와 V양쪽 모두 열려 있지 않다
	IF ((TEQUIP:PLAYER:정조대 || TEQUIP:PLAYER:바이브 || TALENT:PLAYER:남자) && (TEQUIP:PLAYER:애널바이브 || TEQUIP:PLAYER:애널비즈 || TEQUIP:PLAYER:확장벌룬))
		;윤활LV4 이상
		IF PALAM:PLAYER:윤활 >= PALAMLV:4
			IF TALENT:PLAYER:남자
				PRINT 체액
			ELSE
				PRINT 애액
			ENDIF
			PRINT 투성이가 되었다
		ENDIF
		PRINTL 가랑이에페니스를 칠해 왔다…
	;남자,또는 V에 도구 삽입
	ELSEIF TEQUIP:PLAYER:바이브 || TALENT:PLAYER:남자
		PRINTL 아누스를 격렬하게 몰아세워 왔다…
	ELSE
		PRINTL 성기를 격렬하게 몰아세워 왔다…
	ENDIF
;통상 모드
ELSE
	PRINTFORM 짐승은우리로부터 나오면,%CALLNAME%에 덥쳐,
	
	;스타킹계
	IF TEQUIP:특수2 == 5 || TEQUIP:특수2 == 6
		PRINT 힘껏
		SIF TEQUIP:특수2 == 5
			PRINT 스타킹
		SIF TEQUIP:특수2 == 6
			PRINT 타이츠
		PRINTL 를 찢어,
	ENDIF
	
	;A와 V양쪽 모두 열려 있지 않다
	IF (TEQUIP:정조대 || TEQUIP:바이브 && ABL:V감각 <= 5 || TALENT:남자) && (TEQUIP:애널바이브 && ABL:A감각 <= 5 || TEQUIP:애널비즈 || TEQUIP:확장벌룬)
		;윤활LV4 이상
		IF PALAM:윤활 >= PALAMLV:4
			IF TALENT:남자
				PRINT 체액
			ELSE
				PRINT 애액
			ENDIF
			PRINT 투성이가 되었다
		ENDIF
		PRINTL 가랑이에페니스를 칠했다…
	;남자,정조대외 첨부
	ELSEIF TEQUIP:정조대 || TEQUIP:바이브 && ABL:V감각 <= 5 || TALENT:남자
		PRINTL 아누스를 격렬하게 몰아세웠다…
	ELSEIF (!TEQUIP:정조대 || !TEQUIP:바이브) && TEQUIP:애널바이브 && ABL:A감각 >= 6
		PRINTL 성기를 격렬하게 몰아세웠다…
	;애널바이브삽입중＋A감각LV6 이상
	ELSEIF (TEQUIP:정조대 || TEQUIP:바이브) && TEQUIP:애널바이브 && ABL:A감각 >= 6
		PRINTL 바이브가 삽입된 채로의 아누스를 격렬하게 몰아세웠다…
	;V에 도구 삽입
	ELSEIF TEQUIP:바이브 && ABL:V감각 >= 6
		PRINTL 바이브가 삽입된 채로의 성기를 격렬하게 몰아세웠다…
	ELSE
		PRINTL 성기를 격렬하게 몰아세웠다…
	ENDIF
	
	;전나무 보고＋애널바이브
	SIF TALENT:동물귀 && TEQUIP:애널바이브
		PRINTFORML 2마리의 짐승의 서로 관련되는 모습은도착적다…
ENDIF

;-------------------------------------------------
;382,뱀장어탕
;-------------------------------------------------
@MESSAGE_COM382
;역조교모드
IF TEQUIP:역조교
	PRINTFORML 목욕탕통 중(안)에서는,만들이 미끌미끌 몸을 번뜩거릴 수 있어 서로 북적거리고 있다
	PRINTFORMW %CALLNAME%에 몸을 밀려 그 중에 떨어뜨려졌다…
	
	IF (!TALENT:남자 || !TEQUIP:바이브 || !TEQUIP:정조대) || (!TEQUIP:애널바이브 || !TEQUIP:애널비즈 || !TEQUIP:확장벌룬)
		PRINTL 
		PRINTFORML 한층 더 수필의 뱀장어는,%CALLNAME:PLAYER%의
	ENDIF
	;양쪽 모두에 침입할 수 있다
	IF (!TALENT:남자 || !TEQUIP:바이브 || !TEQUIP:정조대) && (!TEQUIP:애널바이브 || !TEQUIP:애널비즈 || !TEQUIP:확장벌룬)
		PRINT 애널와 질구가 비어 있는 것을 헤아리면,
		PRINTL 그 몸을 구불거리게 해,안에 기어들어 왔다…
	;A에 도구를 삽입하고 있지 않다
	ELSEIF (!TEQUIP:애널바이브 || !TEQUIP:애널비즈 || !TEQUIP:확장벌룬)
		PRINT 애널가 비어 있다고 헤아리면,
		PRINTL 그 몸을 구불거리게 해,안에 기어들어 왔다…
	ELSEIF (!TALENT:남자 || !TEQUIP:바이브 || !TEQUIP:정조대)
		PRINT 질구가 무방비이라고 눈치채면,
		PRINTL 그 몸을 구불거리게 해,안에 기어들어 왔다…
	ENDIF
	
	PRINT 
	PRINTL 목욕탕으로부터 끌어올려져 잠시 후도,몸에는 뱀장어의 감촉이 남아 있다…
;통상 모드
ELSE
	PRINTFORML 만들이 서로 북적거리고 있는 목욕탕통안에,%CALLNAME%를 밀어 떨어뜨렸다
	PRINTFORMW %CALLNAME%는 몸 위를 미끄러져 가는 이상한 감각이 참지 못하고 절규했다…
	
	;양쪽 모두에 침입할 수 있다
	IF (!TALENT:남자 || !TEQUIP:바이브 || !TEQUIP:정조대) && (!TEQUIP:애널바이브 || !TEQUIP:애널비즈 || !TEQUIP:확장벌룬)
		PRINTFORML 
		PRINTFORML 수필의 뱀장어는%CALLNAME%의애널와 질구가 비어 있는 것을 헤아리면,안에 기어들어 갔다
		IF TALENT:처녀
			PRINTFORML %CALLNAME%는 눈에 눈물을 머금어,반광란이 되면서 열심히 뱀장어를 잡아질내로부터 끌어내려고 하지만,
			PRINTFORMW 뱀장어들이 분비하는 점액에 의해 굉장한저항도 하지 못한 채처녀의 증거를 물어 찢어져 버렸다…
		ELSE
			PRINTFORMW %CALLNAME%도 필사적으로 전후의 구멍에 힘을 써 저항하려고 하고 있지만,쓸데없는저항다…
		ENDIF
	;A에 도구를 삽입하고 있지 않다
	ELSEIF (!TEQUIP:애널바이브 || !TEQUIP:애널비즈 || !TEQUIP:확장벌룬)
		PRINTFORML 
		PRINTFORML 수필의 뱀장어는%CALLNAME%의애널가 비어 있다고 헤아리면,안에 기어들어 갔다
		PRINTFORMW %CALLNAME%는 반사적으로 엉덩이에 힘을 써,이물의 침입에 저항하려고 하는 것도,결국은 쓸데없게 끝나 버린다…
	ELSEIF (!TALENT:남자 || !TEQUIP:바이브 || !TEQUIP:정조대)
		PRINT 
		PRINTFORML 수필의 뱀장어는%CALLNAME%의 질구가 무방비이라고 눈치채면,안에 기어들어 갔다
		;처녀
		IF TALENT:처녀
			PRINTFORML %CALLNAME%는 눈에 눈물을 머금어,반광란이 되면서 열심히 뱀장어를 잡아질내로부터 끌어내려고 하지만,
			PRINTFORMW 뱀장어들이 분비하는 점액에 의해 굉장한저항도 하지 못한 채처녀의 증거를 물어 찢어져 버렸다…
		ELSE
			PRINTFORMW %CALLNAME%는 뱀장어가 질벽이나 자궁입를 찔러 가는 감촉에 몹시 놀라고 있다…
		ENDIF
	ENDIF
	
	PRINT 
	PRINTFORM 목욕탕으로부터 끌어올려 잠시 후도,%CALLNAME%는 어안이 벙벙히 한 표정으로 몸부림 하고 있다…
ENDIF


;-------------------------------------------------
;383,곤충탕
;-------------------------------------------------
@MESSAGE_COM383
;역조교모드
IF TEQUIP:역조교
	PRINTFORML 목욕탕통 중(안)에서는,무수한 무서운 형태를 한 충들이 꿈틀거리고 있다
	PRINTFORMW %CALLNAME%에 몸을 밀려 그 중에 떨어뜨려졌다…
;통상 모드
ELSE
	PRINTFORML 무수한 충들이 꿈틀거리는 목욕탕통에,%CALLNAME%를 밀어 떨어뜨렸다…
	PRINTFORML 그 너무 이상한 감각에,%CALLNAME%는 소리가 되지 않는 비명을 올리고 있다……
ENDIF


;===========================================================
;광기계
;===========================================================
;-------------------------------------------------
;390,한쪽팔절단
;-------------------------------------------------
@MESSAGE_COM390
;역조교모드
IF TEQUIP:역조교
;통상 모드
ELSE
ENDIF
;친애·복종·예속
IF TALENT:친애 || TALENT:복종 || TALENT:예속
	PRINTFORML %CALLNAME%의 한쪽 팔을 절단 했다…
ELSE
	PRINTFORML %CALLNAME%를 구속해,한쪽 팔을 잘라냈다…
ENDIF


;-------------------------------------------------
;391,한쪽다리절단
;-------------------------------------------------
@MESSAGE_COM391
;역조교모드
IF TEQUIP:역조교
;통상 모드
ELSE
ENDIF
;친애·복종·예속
IF TALENT:친애 || TALENT:복종 || TALENT:예속
	PRINTFORML %CALLNAME%의 한쪽 다리를 절단 했다…
ELSE
	PRINTFORML %CALLNAME%를 구속해,한쪽 다리를 잘라냈다…
ENDIF


;-------------------------------------------------
;392,발치
;-------------------------------------------------
@MESSAGE_COM392
;역조교모드
IF TEQUIP:역조교
;통상 모드
ELSE
ENDIF
;친애·복종·예속
IF TALENT:친애 || TALENT:복종 || TALENT:예속
	PRINTFORML %CALLNAME%로부터 모든 이빨을 뽑았다…
ELSE
	PRINTFORML %CALLNAME%를 구속해,이빨을 모두 빼냈다…
ENDIF



;===========================================================
;조수명령계
;===========================================================
;-------------------------------------------------
;400,클리토리스애무명령
;-------------------------------------------------
@MESSAGE_COM400
PRINTFORM %CALLNAME:ASSI%에%CALLNAME%의
IF TALENT:아이사이즈
	PRINT 털도 나지 않은 사타구니의
ELSEIF TALENT:음모없음
	PRINT 아이와 같이 반들반들의
ENDIF
PRINTL 비밀스런 장소를애무 시켰다…


;-------------------------------------------------
;401,페니스애무명령
;-------------------------------------------------
@MESSAGE_COM401
PRINTFORM %CALLNAME:ASSI%에%CALLNAME%의
IF TALENT:말자지
	PRINT 그로테스크라고 말할 수 있을 만큼 큰
ELSEIF TALENT:거근
	PRINT 당당한
ELSEIF TALENT:단소
	PRINT 사랑스럽다
ELSEIF TALENT:아이사이즈
	PRINT 소년인 듯했다
ENDIF
SIF TALENT:포경
	PRINT 포경
PRINTL 페니스를애무 시켰다…


;-------------------------------------------------
;402,가슴애무명령
;-------------------------------------------------
@MESSAGE_COM402
PRINTFORM %CALLNAME:ASSI%에%CALLNAME%의
IF TALENT:폭유
	PRINT 터질 것 같게 풍부한가슴
ELSEIF TALENT:거유
	PRINT 풍만한 유방
ELSEIF TALENT:빈유
	PRINT 조심스러운가슴
ELSEIF !BREAST(TARGET)
	PRINT 유두
ELSE
	PRINT 가슴
ENDIF
PRINTL 를애무 시켰다…


;-------------------------------------------------
;403,손가락삽입명령
;-------------------------------------------------
@MESSAGE_COM403
PRINTFORML %CALLNAME:ASSI%에%CALLNAME%의바기나를애무 시켰다…


;-------------------------------------------------
;404,애널애무명령
;-------------------------------------------------
@MESSAGE_COM404
PRINTFORML %CALLNAME:ASSI%에%CALLNAME%의애널를애무 시켰다…


;-------------------------------------------------
;405,삽입명령
;-------------------------------------------------
@MESSAGE_COM405
PRINTFORML %CALLNAME:ASSI%에%CALLNAME%의바기나에 삽입시켰다…


;-------------------------------------------------
;406,애널삽입명령
;-------------------------------------------------
@MESSAGE_COM406
PRINTFORML %CALLNAME:ASSI%에%CALLNAME%의애널에 삽입시켰다…


;-------------------------------------------------
;407,구욕명령
;-------------------------------------------------
@MESSAGE_COM407
PRINTFORML %CALLNAME:ASSI%에%CALLNAME%의입를 범하게 했다…



;===========================================================
;정음계
;===========================================================
;-------------------------------------------------
;410,콘돔정음(노예)
;-------------------------------------------------
@MESSAGE_COM410
;역조교모드
IF TEQUIP:역조교
;통상 모드
ELSE
ENDIF
PRINTFORML %CALLNAME%의 뺨에,미지근한 사용이 끝난콘돔를 꽉 눌렀다
;정액중독 5 이상
IF ABL:정액중독 >= 5
	PRINTFORML %CALLNAME%는콘돔를 뒤집어,서운한 듯이 나머지국물까지도 빨고 있다…
;정액중독 3 이상
ELSEIF ABL:정액중독 >= 3
	PRINTFORML %CALLNAME%는 황홀로 한 얼굴로,입내에 흘려 넣어진 고무 냄새나는 정액을 맛보고 있다…
;정액중독 1 이상
ELSEIF ABL:정액중독 >= 1
	PRINTFORML %CALLNAME%는 희미하게 싫어하는 것 같은 기색을 하면서도,혀를 내밀어 정액을 감수 했다…
;정액중독없음
ELSE
	PRINTFORML 강하게 닫혀진%CALLNAME%의입를 비틀어 열어,고무취가 섞인 비릿한 정액을 흘려 넣었다…
ENDIF


;-------------------------------------------------
;411,콘돔정음(조수)
;-------------------------------------------------
@MESSAGE_COM411
PRINTFORML %CALLNAME:MASTER%는,%CALLNAME:ASSI%에 사용이 끝난콘돔를손 건네주었다
;조수가새드
IF TALENT:ASSI:새드
	PRINTFORM %CALLNAME:ASSI%는%CALLNAME%가 혀를 펴면 닿을 것 같은 정도의 거리로,
ELSE
	PRINTFORM %CALLNAME:ASSI%는%CALLNAME%에 과시하듯이
ENDIF
;조수의정액중독가 LV5 이상
IF ABL:ASSI:정액중독 >= 5
	PRINTFORML 황홀의 표정으로 정액을 맛보고 있다…
ELSE
	PRINTFORML 맛있게 정액을 맛보고 있다…
ENDIF
PRINTFORM %CALLNAME%는
IF ABL:정액중독 >= 5
	PRINTFORML 인내 할 수 없는 것인지,군침을 늘어뜨리면서 그 광경을 보고 있다…
ELSEIF ABL:정액중독 >= 3
	PRINTFORML 갖고 싶어하는 듯한 표정으로 가만히 그 광경을 보고 있다…
ELSEIF ABL:정액중독 >= 1
	PRINTFORML 필사적으로 눈을 떼려고 하고 있지만,그 눈은 눈앞의 광경에 못박음이 되어 있다…
ELSE
	PRINTFORML 그 광경에 기가 막힌 얼굴을 하고 있다…
ENDIF


;-------------------------------------------------
;412,정액키스
;-------------------------------------------------
@MESSAGE_COM412
PRINTFORML %CALLNAME:PLAYER%는입에 포함한 정액을 타액과 혼합해,키스로입 옮겨 했다…

;정액중독 5 이상
IF ABL:정액중독 >= 5
	PRINTFORML %CALLNAME%는 %CALLNAME:PLAYER%의 타액마다 탐욕에 흡인해,
	PRINTFORML 한층 더 %CALLNAME:PLAYER%의입내를 핥아대 잔향을 즐기고 있다…
;정액중독 3 이상
ELSEIF ABL:정액중독 >= 3
	PRINTFORM %CALLNAME%는 황홀로 하면서,
	;직전이 페라의 경우
	SIF PREVCOM == 31
		PRINT 당신의
	PRINTFORML 자식을 %CALLNAME:PLAYER%의 혀와 서로 걸고 있다…
;정액중독 1 이상
ELSEIF ABL:정액중독 >= 1
	PRINTFORM %CALLNAME%는
	SIF PREVCOM == 31
		PRINT 자신의
	PRINTFORML 정액을 먹게 되어 굴욕으로 가득 찬 표정을 하고 있다…
;정액중독없음
ELSE
	PRINTFORML %CALLNAME%는 눈물고인 눈으로 피하려고 한다…
	PRINTFORML 그러나 %CALLNAME:PLAYER%는 그것을 허락하지 않고,양손로 머리를 잡아,혀로 입술을 비틀어 열었다…
ENDIF


;===========================================================
;노예＋조수
;===========================================================
;-------------------------------------------------
;420,더블펠라
;-------------------------------------------------
@MESSAGE_COM420
PRINTFORM %CALLNAME%과 %CALLNAME:ASSI%는
;노예와 조수가연모나친애
SIF (TALENT:연모 || TALENT:친애) && (TALENT:ASSI:연모 || TALENT:ASSI:친애)
	PRINTFORM 넋을 잃은 눈을 치켜 뜨고 봄으로 %CALLNAME:MASTER%를 올려보면서,
PRINTFORM %CALLNAME:MASTER%의
;아이사이즈
IF TALENT:MASTER:아이사이즈
	;소인체형가 아니다
	SIF TALENT:MASTER:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:MASTER:말자지
	;소인체형
	IF TALENT:MASTER:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
SIF TALENT:PLAYER:포경
	PRINT 포경
PRINTFORML 페니스에 둘이서 혀를 기게 하고 있다…


;-------------------------------------------------
;421,더블파이즈리
;-------------------------------------------------
@MESSAGE_COM421
PRINTFORM %CALLNAME%과 %CALLNAME:ASSI%는
;노예와 조수가연모나친애
SIF (TALENT:연모 || TALENT:친애) && (TALENT:ASSI:연모 || TALENT:ASSI:친애)
	PRINTFORM 넋을 잃은 눈을 치켜 뜨고 봄으로 %CALLNAME:MASTER%를 올려보면서,
PRINTFORM 서로의
;노예와 조수가거유나폭유
IF (TALENT:절벽 || TALENT:거유) && (TALENT:ASSI:절벽 || TALENT:ASSI:거유)
	PRINTFORM 풍만한
;노예와 조수가빈유
ELSEIF TALENT:빈유 && TALENT:ASSI:빈유
	PRINTFORM 얇다
ENDIF
PRINTFORM 가슴를 거듭해 맞추어 %CALLNAME:MASTER%의
;아이사이즈
IF TALENT:MASTER:아이사이즈
	;소인체형가 아니다
	SIF TALENT:MASTER:소인체형 == 0
		PRINT 체에 어울리지 않고
	PRINT 어리다
;말자지
ELSEIF TALENT:MASTER:말자지
	;소인체형
	IF TALENT:MASTER:소인체형
		PRINT 신체가 숨어 버릴 만큼
	ELSE
		PRINT 눈이 휘둥그레 지는 만큼
	ENDIF
	PRINT 큰
ENDIF
SIF TALENT:PLAYER:포경
	PRINT 포경
PRINTFORML 페니스를 잡아당기고 있다…


;-------------------------------------------------
;422,더블스마타
;-------------------------------------------------
@MESSAGE_COM422
PRINTFORML %CALLNAME%과 %CALLNAME:ASSI%의 성기로페니스를 사이에 둬,두 명의 사이에 잡아당겼다…



;===========================================================
;노예×조수
;===========================================================
;-------------------------------------------------
;430,조수를범하게시킨다
;-------------------------------------------------
@MESSAGE_COM430
PRINTFORML %CALLNAME%에 %CALLNAME:ASSI%를 안게 하는 일로 했다…


;-------------------------------------------------
;431,조수의A를범하게시킨다
;-------------------------------------------------
@MESSAGE_COM431
PRINTFORML %CALLNAME%에 %CALLNAME:ASSI%의애널를 범하게 하는 일로 했다…



;===========================================================
;주인×조수
;===========================================================
;-------------------------------------------------
;440,조수를범한다
;-------------------------------------------------
@MESSAGE_COM440
;전회의 조교가조수를범한다이며,뽑지 않는 플래그가 있는 경우
IF PREVCOM == 440
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%를 계속 범했다…
ELSE
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%의
	SIF TALENT:ASSI:처녀
		PRINT 남을 모른다
	PRINTL 바기나를 관철해 갔다…
ENDIF

;-------------------------------------------------
;441,조수의A를범한다
;-------------------------------------------------
@MESSAGE_COM441
;전회의 조교가조수를범한다이며,뽑지 않는 플래그가 있는 경우
IF PREVCOM == 441
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%를 계속 범했다…
ELSE
	PRINTFORM %CALLNAME%에 과시하면서,%CALLNAME:ASSI%의
	SIF TALENT:ASSI:백버진
		PRINT 남을 모른다
	PRINTL 애널를 관철해 갔다…
ENDIF



;===========================================================
;파생 커맨드
;===========================================================
;-------------------------------------------------
;500,G스팟 자극
;-------------------------------------------------
@MESSAGE_COM500
;전회가 G스팟 자극
IF PREVCOM == 500
	;V경험가 120보다 많다
	IF EXP:V경험 > 120
		PRINTFORML %CALLNAME%는 군침을 흘려,스스로 허리를 흔들어 쾌락을 탐내고 있다…
	;V경험가 80보다 많다
	ELSEIF EXP:V경험 > 80
		PRINTFORML %CALLNAME%는 뱃속을 휘저어지는 감각에 뜨거운 한숨을 토해 몸부림치고 있다…
	;V경험가 50보다 많다
	ELSEIF EXP:V경험 > 50
		PRINTFORML %CALLNAME%는 손가락이 우글거리는 감촉에 넋을 잃고 있다…
	;V경험가 30보다 많다
	ELSEIF EXP:V경험 > 30
		PRINTFORML %CALLNAME%는 손가락의 감촉을 즐기고 있는 것 같다…
	;V경험가 0보다 많다
	ELSEIF EXP:V경험 > 0
		PRINTFORML %CALLNAME%는 손가락의 감촉에 한숨을 토해 참고 있다…
	ELSE
		PRINTFORML %CALLNAME%는질내의 이물감에 무서워하고 있다…
	ENDIF
;바이브삽입중＋욕정의 PALAMLV3 이상＋V감각LV3 이상
ELSEIF TEQUIP:바이브 && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 >= 3
	PRINTFORML %CALLNAME%의 질은,꿀에 젖은바이브에 끈적 휘감기고 있다…
;바이브삽입중＋욕정의 PALAMLV3 미만＋V감각LV3 이상
ELSEIF TEQUIP:바이브 && PALAM:욕정 < PALAMLV:3 && ABL:V감각 >= 3
	PRINTFORML %CALLNAME%는바이브의 자극에 몸부림치고 있다…
;바이브삽입중＋욕정의 PALAMLV3 이상＋V감각LV3 미만
ELSEIF TEQUIP:바이브 && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 < 3
	PRINTFORML %CALLNAME%는 뺨을 물들여 때때로 소리를 높이고 있다…
;바이브삽입중＋욕정의 PALAMLV3 미만＋V감각LV3 미만
ELSEIF TEQUIP:바이브 && PALAM:욕정 < PALAMLV:3 && ABL:V감각 < 3
	PRINTFORML %CALLNAME%는 조금 괴로운 듯한 표정을 하고 있다…
;바이브삽입중
ELSEIF TEQUIP:바이브
	PRINTFORML %CALLNAME:PLAYER%는 교묘하게바이브를 조종해,민감한 천정을 놀렸다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 질 중(안)에서 손가락을 굽혀,껄끔거린 천정을 자극했다…
ENDIF


;-------------------------------------------------
;501,전립선 자극
;-------------------------------------------------
@MESSAGE_COM501
;전회가 전립선 자극
IF PREVCOM == 501
	;A경험가 120보다 많다
	IF EXP:A경험 > 120
		PRINTFORML %CALLNAME%는 군침을 흘려,스스로 허리를 흔들어 쾌락을 탐내고 있다…
	;A경험가 80보다 많다
	ELSEIF EXP:A경험 > 80
		PRINTFORML %CALLNAME%는 뱃속을 휘저어지는 감각에 뜨거운 한숨을 토해 몸부림치고 있다…
	;A경험가 50보다 많다
	ELSEIF EXP:A경험 > 50
		PRINTFORML %CALLNAME%는 체내를 꿈틀거리는 감촉에 넋을 잃고 있다…
	;A경험가 30보다 많다
	ELSEIF EXP:A경험 > 30
		PRINTFORML %CALLNAME%는
		IF TEQUIP:촉수
			PRINT 촉수
		ELSEIF TEQUIP:로터A삽입
			PRINT 로터
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSEIF TEQUIP:확장벌룬
			PRINT 벌룬
		ELSE
			PRINT 손가락
		ENDIF
		PRINTL 의 감촉을 즐기고 있는 것 같다…
	;A경험가 0보다 많다
	ELSEIF EXP:A경험 > 0
		PRINTFORM %CALLNAME%는
		IF TEQUIP:촉수
			PRINT 촉수
		ELSEIF TEQUIP:로터A삽입
			PRINT 로터
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSEIF TEQUIP:확장벌룬
			PRINT 벌룬
		ELSE
			PRINT 손가락
		ENDIF
		PRINTL 의 감촉에 한숨을 토해 참고 있다…
	ELSE
		PRINTFORML %CALLNAME%는장내의 이물감에 무서워하고 있다…
	ENDIF
;애널바이브삽입중＋욕정의 PALAMLV3 이상＋A감각LV3 이상
ELSEIF (TEQUIP:애널바이브 || TEQUIP:확장벌룬 || TEQUIP:촉수) && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 >= 3
	PRINTFORM %CALLNAME%의장내는 꿈틀거려,
		IF TEQUIP:촉수
			PRINT 촉수
		ELSEIF TEQUIP:로터A삽입
			PRINT 로터
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSEIF TEQUIP:확장벌룬
			PRINT 벌룬
		ELSE
			PRINT 손가락
		ENDIF
		PRINTL 를 끈적 단단히 조이고 있다…
;애널바이브삽입중＋욕정의 PALAMLV3 미만＋A감각LV3 이상
ELSEIF (TEQUIP:애널바이브 || TEQUIP:확장벌룬 || TEQUIP:촉수) && PALAM:욕정 < PALAMLV:3 && ABL:V감각 >= 3
	PRINTFORM %CALLNAME%는
		IF TEQUIP:촉수
			PRINT 촉수
		ELSEIF TEQUIP:로터A삽입
			PRINT 로터
		ELSEIF TEQUIP:애널바이브
			PRINT 애널바이브
		ELSEIF TEQUIP:확장벌룬
			PRINT 벌룬
		ELSE
			PRINT 손가락
		ENDIF
		PRINTL 의 자극에 몸부림치고 있다…
;애널바이브삽입중＋욕정의 PALAMLV3 이상＋A감각LV3 미만
ELSEIF (TEQUIP:로터A삽입 || TEQUIP:애널바이브 || TEQUIP:확장벌룬 || TEQUIP:촉수) && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 < 3
	PRINTFORML %CALLNAME%는 뺨을 물들여 때때로 소리를 높이고 있다…
;애널바이브삽입중＋욕정의 PALAMLV3 미만＋A감각LV3 미만
ELSEIF (TEQUIP:로터A삽입 || TEQUIP:애널바이브 || TEQUIP:확장벌룬 || TEQUIP:촉수) && PALAM:욕정 < PALAMLV:3 && ABL:V감각 < 3
	PRINTFORML %CALLNAME%는 조금 괴로운 듯한 표정을 하고 있다…
;애널바이브삽입중
ELSEIF (TEQUIP:로터A삽입 || TEQUIP:애널바이브 || TEQUIP:확장벌룬 || TEQUIP:촉수)
	PRINTFORM %CALLNAME:PLAYER%는 교묘하게
	IF TEQUIP:촉수
		PRINT 촉수
	ELSEIF TEQUIP:애널바이브
		PRINT 애널바이브
	ELSEIF TEQUIP:로터A삽입
		PRINT 로터
	ELSEIF TEQUIP:확장벌룬
		PRINT 벌룬
	ELSE
		PRINT 손가락
	ENDIF
	PRINTL 를 조종해,민감한 천정을 놀렸다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%는%CALLNAME%의 장 중(안)에서 손가락을 굽혀,전립선을 자극했다…
ENDIF


;-------------------------------------------------
;502,착유
;-------------------------------------------------
@MESSAGE_COM502
PRINTFORM %CALLNAME%의
;욕정의 PALAMLV3 이상
SIF PALAM:욕정 >= PALAMLV:3
	PRINT 기대에 떨린다
;빈유＋작은체구
IF TALENT:작은체구 && TALENT:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:폭유
	PRINT 손에 남을 만큼 큰
ENDIF
;욕정의 PALAMLV3 이상＋연모나친애
IF PALAM:욕정 >= PALAMLV:3 && (TALENT:연모 || TALENT:친애)
	PRINTFORM 가슴를 강하게 짜 주면,
	PRINTFORM %CALLNAME%는 교성을 줘
	;빈유
	SIF TALENT:빈유
		PRINT 그 소극적인
	;거유
	SIF TALENT:거유
		PRINT 그 큰
	;폭유
	SIF TALENT:폭유
		PRINT 그 거대한
	;모유체질
	SIF TALENT:모유체질
		PRINT 모유가 방울져 떨어진다
	PRINTL 가슴를 강조하듯이 내밀어,
	PRINTL 보다 강한 자극을 요구했다…
;욕정의 PALAMLV3 이상
ELSEIF PALAM:욕정 > PALAMLV:3
	PRINTL 가슴를 강하게 짰다…
ELSE
	PRINTL 가슴를 짰다…
ENDIF


;-------------------------------------------------
;503,식스나인
;-------------------------------------------------
@MESSAGE_COM503
PRINTFORM %CALLNAME%과
;조수가 조교중
SIF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%는
PRINTFORML 서로의 성기를 서로 빨았다…

;-------------------------------------------------
;504,69파이즈리
;-------------------------------------------------
@MESSAGE_COM504
PRINTFORM %CALLNAME%과
;조수가 조교중
SIF ASSIPLAY
	PRINTFORM %CALLNAME:ASSI%는
PRINTFORML 서로의 성기를,서로의 유방으로 서로 자극했다…

;-------------------------------------------------
;505,이와시미즈
;-------------------------------------------------
@MESSAGE_COM505
;-------------------------------------------------
;506,혼란 모란
;-------------------------------------------------
@MESSAGE_COM506

;-------------------------------------------------
;507,삽입 G스포 고문
;-------------------------------------------------
@MESSAGE_COM507
PRINTFORML 허리를 떠,%CALLNAME%의질내의 천정을 조금씩 쓸어올렸다…

;-------------------------------------------------
;508,삽입 자궁입고문
;-------------------------------------------------
@MESSAGE_COM508
PRINTFORML 허리의 잘록함을 깊게 안아,%CALLNAME%의 자궁을 동글동글 자극했다…

;-------------------------------------------------
;509,기승위유방자위
;-------------------------------------------------
@MESSAGE_COM509
;전회와 이번 조교자가 같아,뽑지 않는 플래그가 있다
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	PRINTFORM %CALLNAME%는
	;전회 커맨드가기승위
	IF PREVCOM == 106
		PRINT 질내
	;전회 커맨드가기승위애널
	ELSEIF PREVCOM == 116
		PRINT 아누스
	ENDIF
	PRINTFORM 에 삽입된 채로의
	SIF !PENIS(PLAYER)
		PRINT 유사
	PRINTFORM 페니스를 단단히 조여,
ELSE
	PRINTFORM %CALLNAME%는 %CALLNAME:PLAYER%의
	SIF !PENIS(PLAYER)
		PRINT 유사
	PRINTFORM 페니스를
	;전회의 조교가기승위
	IF PREVCOM == 106
		PRINT 바기나로
	;전회의 조교가기승위애널
	ELSEIF PREVCOM == 116
		PRINT 아누스로
	ENDIF
	PRINTFORM 물면,
ENDIF
;빈유
SIF TALENT:빈유
	PRINT 그 소극적인
;거유
SIF TALENT:거유
	PRINT 그 큰
;폭유
SIF TALENT:폭유
	PRINT 그 거대한
;모유체질
SIF TALENT:모유체질
	PRINT 모유가 방울져 떨어진다
PRINTFORM 가슴를
;착유기장착중
IF TEQUIP:착유기
	PRINT 착유기로 짜면서
ELSE
	PRINT 스스로의 손가락으로 만지작거리면서
ENDIF
PRINTFORML 다시 허리를 움직이기 시작했다…

;-------------------------------------------------
;510,역에서 파는 도시락
;-------------------------------------------------
@MESSAGE_COM510
;전회와 이번 조교자가 같아,뽑지 않는 플래그가 있는 경우
IF TFLAG:60 && ((ASSIPLAY && TFLAG:50) || (ASSIPLAY == 0 && TFLAG:50 == 0))
	;전회의 조교가대면입위
	IF TFLAG:705 == 0
		PRINTFORM 질내에 삽입한 채로
	;전회의 조교가대면입위애널
	ELSE
		PRINTFORM 애널에 삽입한 채로
	ENDIF
	PRINTFORM %CALLNAME%의 신체를 안아 올려,
ELSE
	PRINTFORM %CALLNAME%의 신체를 안아 올려
	;전회의 조교가대면입위
	IF TFLAG:705 == 0
		PRINTFORM 질내에 삽입해,
	;전회의 조교가대면입위애널
	ELSE
		PRINTFORM 애널에 삽입해,
	ENDIF
ENDIF
PRINTFORML 흔들면서 걸어 다녔다…

;-------------------------------------------------
;511,스팽킹섹스
;-------------------------------------------------
@MESSAGE_COM511
PRINTFORML %CALLNAME%의바기나를 밀어올리면서,엉덩이가 새빨갛게 될 때까지 두드렸다…

;-------------------------------------------------
;512,스팽킹애널
;-------------------------------------------------
@MESSAGE_COM512
PRINTFORML %CALLNAME%의애널를 밀어올리면서,엉덩이가 새빨갛게 될 때까지 두드렸다…

;-------------------------------------------------
;513,목조르기섹스
;-------------------------------------------------
@MESSAGE_COM513
PRINTFORML %CALLNAME%에 삽입하면서 목을 조였다…

;-------------------------------------------------
;514,목조르기애널
;-------------------------------------------------
@MESSAGE_COM514
PRINTFORML %CALLNAME%의애널에 삽입하면서 목을 조였다…

;-------------------------------------------------
;515,샌드위치
;-------------------------------------------------
@MESSAGE_COM515
;먼저 조수의 행동을 본다
;V
IF TFLAG:41 == 1
	PRINTFORM %CALLNAME:ASSI%에 비순[秘唇]을 관철하게 하면서,
;A
ELSEIF TFLAG:41 == 2
	PRINTFORM %CALLNAME:ASSI%에애널에 찔러 넣게 하면서,
;입
ELSE
	PRINTFORM %CALLNAME:ASSI%에입내를 범하게 하면서,
ENDIF

;주인이 어디에 삽입하고 있을까?
;V
IF TFLAG:40 == 1
	PRINTFORML %CALLNAME%의 비순[秘唇]에 삽입했다…
;A
ELSEIF TFLAG:40 == 2
	PRINTFORML %CALLNAME%의애널에 묻었다…
;주인이 노예에게입봉사
ELSEIF TFLAG:40 == 3 || TFLAG:40 == 5 || TFLAG:40 == 4
	IF TFLAG:40 == 4
		PRINTFORML %CALLNAME%의 비순[秘唇]에 달라붙었다…
	ELSE
		PRINTFORML %CALLNAME%의페니스를 핥아댔다…
	ENDIF
;입
ELSE
	PRINTFORML %CALLNAME%에 봉사시켰다…
ENDIF

;-------------------------------------------------
;516,2 개 페라
;-------------------------------------------------
@MESSAGE_COM516
PRINTFORML %CALLNAME%에,%CALLNAME:MASTER%과 %CALLNAME:ASSI%의페니스를 동시에 봉사시켰다…

;-------------------------------------------------
;517,이륜 찔러
;-------------------------------------------------
@MESSAGE_COM517
;뽑지 않는 플래그가 있어,전회도 이륜 꽂아
IF TFLAG:60 && PREVCOM == 517
	PRINTFORML 어느 쪽의페니스도질내로부터 앞지르는 일 없이,다시 허리를 움직이기 시작했다…
;뽑지 않는 플래그가 있어,전회 커맨드가 주인의 섹스
ELSEIF TFLAG:60 && PREVCOM >= 100 && PREVCOM <= 106
	PRINTFORM %CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 비순[秘唇]을
	SIF EXP:V확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ELSE
	PRINTFORM %CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 비순[秘唇]을
	SIF EXP:V확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣었다…
ENDIF

;-------------------------------------------------
;518,이륜 찔러애널
;-------------------------------------------------
@MESSAGE_COM518
;뽑지 않는 플래그가 있어,전회도 이륜 꽂아애널
IF TFLAG:60 && PREVCOM == 518
	PRINTFORML 어느 쪽의페니스도질내로부터 앞지르는 일 없이,다시 허리를 움직이기 시작했다…
;뽑지 않는 플래그가 있어,전회 커맨드가 주인의 A섹스
ELSEIF TFLAG:60 && PREVCOM >= 110 && PREVCOM <= 116
	PRINTFORM %CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 아누스를
	SIF EXP:A확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ELSE
	PRINTFORM %CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 아누스를
	SIF EXP:A확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣었다…
ENDIF

;-------------------------------------------------
;519,연결
;-------------------------------------------------
@MESSAGE_COM519
;주인이 총공
;주인×((노예×조수) || (조수×노예))
IF (TFLAG:40 == 1 || TFLAG:40 == 2 || TFLAG:40 == 11 || TFLAG:40 == 12) && TFLAG:41!= 6 && TFLAG:41!= 7 && TFLAG:49!= 6 && TFLAG:49!= 7
	;주인×조수×노예
	IF TFLAG:41 == 1 || TFLAG:41 == 2
		;전회 커맨드가 조수삽입
		IF PREVCOM == 405 || PREVCOM == 406
			PRINTFORM %CALLNAME%를 범하는 %CALLNAME:ASSI%의 등 뒤로 돌아,그
			IF TFLAG:40 == 11
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 에 삽입했다…
		;주인이 먼저 행동하고 있으면 가정
		ELSE
			PRINTFORM %CALLNAME:ASSI%의
			IF TFLAG:40 == 11
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 에 삽입한 채로,%CALLNAME:ASSI%에%CALLNAME%를 범하게 했다…
		ENDIF
	;주인×노예×조수
	ELSE
		;전회 커맨드가조수를범하게시킨다
		IF PREVCOM == 430 || PREVCOM == 431
			PRINTFORM %CALLNAME:ASSI%를 범하는%CALLNAME%의 등 뒤로 돌아,그
			IF TFLAG:40 == 1
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 에 삽입했다…
		;주인이 먼저 행동하고 있으면 가정
		ELSE
			PRINTFORM %CALLNAME%의
			IF TFLAG:40 == 1
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 에 삽입한 채로,%CALLNAME%에 %CALLNAME:ASSI%를 범하게 했다…
		ENDIF
	ENDIF
;주인이수
;(조수×노예×주인) || (노예×조수×주인)
ELSEIF TFLAG:40!= 1 && TFLAG:40!= 2 && TFLAG:40!= 11 && TFLAG:40!= 12 && (TFLAG:41 == 1 || TFLAG:41 == 2 || TFLAG:41 == 6 || TFLAG:41 == 7)
	;조수×노예×주인
	IF TFLAG:41 == 1 || TFLAG:41 == 2
		;전회 커맨드가 조수삽입
		IF PREVCOM == 405 || PREVCOM == 406
			PRINTFORM %CALLNAME:ASSI%에 범해진 채로의%CALLNAME%에,자신의
			IF TFLAG:49 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 관철하게 했다…
		;주인이 먼저 행동하고 있으면 가정
		ELSE
			PRINTFORM %CALLNAME:MASTER%의
			IF TFLAG:49 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 관철하는%CALLNAME%를,%CALLNAME:ASSI%에 범하게 했다…
		ENDIF
	;노예×조수×주인
	ELSE
		;전회 커맨드가조수를범하게시킨다
		IF PREVCOM == 430 || PREVCOM == 431
			PRINTFORM %CALLNAME%에 범해진 채로의 %CALLNAME:ASSI%에,자신의
			IF TFLAG:41 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 관철하게 했다…
		;주인이 먼저 행동하고 있으면 가정
		ELSE
			PRINTFORM %CALLNAME:MASTER%의
			IF TFLAG:41 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 관철하는 %CALLNAME:ASSI%를,%CALLNAME%에 범하게 했다…
		ENDIF
	ENDIF
;주인이 리바
;(노예×주인×조수) || (조수×주인×노예)
ELSE
	;노예×주인×조수
	IF TFLAG:49 == 6 || TFLAG:49 == 7
		;전회 커맨드가 주인수섹스
		IF PREVCOM >= 260 && PREVCOM <= 265
			PRINTFORM %CALLNAME%에
			IF TFLAG:49 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORM 를 범하게 한 채로,%CALLNAME:ASSI%의
			IF TFLAG:40 == 11
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 관철했다…
		;주인이 먼저 행동하고 있으면 가정
		ELSE
			PRINTFORM %CALLNAME:ASSI%의
			IF TFLAG:40 == 11
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORM 에 삽입한 채로,%CALLNAME%에 자신의
			IF TFLAG:49 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 범하게 했다…
		ENDIF
	;조수×주인×노예
	ELSE
		;전회 커맨드가 주인수섹스
		IF PREVCOM == 270 || PREVCOM == 270
			PRINTFORM %CALLNAME:ASSI%에
			IF TFLAG:41 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORM 를 범하게 한 채로,%CALLNAME%의
			IF TFLAG:40 == 1
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 관철했다…
		;주인이 먼저 행동하고 있으면 가정
		ELSE
			PRINTFORM %CALLNAME%의
			IF TFLAG:40 == 1
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORM 에 삽입한 채로,%CALLNAME:ASSI%에 자신의
			IF TFLAG:41 == 6
				PRINT 바기나
			ELSE
				PRINT 아누스
			ENDIF
			PRINTFORML 를 범하게 했다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;520,샌드위치 시킨다
;-------------------------------------------------
@MESSAGE_COM520
;먼저 조수의 행동을 본다
;V
IF TFLAG:41 == 6
	PRINTFORM %CALLNAME:ASSI%에 비순[秘唇]을 관철하게 하면서,
;A
ELSE
	PRINTFORM %CALLNAME:ASSI%에애널에 찔러 넣게 하면서,
ENDIF

;노예의 행동을 본다
;V
IF TFLAG:49 == 6
	PRINTFORM %CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORML 페니스를바기나로 삼켰다…
;A
ELSEIF TFLAG:49 == 7
	PRINTFORM %CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORML 페니스를 아누스로 삼켰다…
;주인이 노예에게입봉사
ELSEIF TFLAG:40 == 3 || TFLAG:40 == 5 || TFLAG:40 == 4
	IF TFLAG:40 == 4
		PRINTFORML %CALLNAME%의 비순[秘唇]에 달라붙었다…
	ELSE
		PRINTFORML %CALLNAME%의페니스를 핥아댔다…
	ENDIF
;입
ELSE
	PRINTFORML %CALLNAME%에 봉사시켰다…
ENDIF

;-------------------------------------------------
;521,조수를 샌드위치
;-------------------------------------------------
@MESSAGE_COM521
;먼저 노예의 행동을 본다
;V
IF TFLAG:49 == 11
	PRINTFORM %CALLNAME%에 비순[秘唇]을 관철하게 하면서,
;A
ELSEIF TFLAG:49 == 12
	PRINTFORM %CALLNAME%에애널에 찔러 넣게 하면서,
;입
ELSE
	PRINTFORM %CALLNAME%에 봉사시키면서,
ENDIF

;주인의 행동
;V
IF TFLAG:40 == 1
	PRINTFORML %CALLNAME:ASSI%의 비순[秘唇]에 삽입했다…
;A
ELSE
	PRINTFORML %CALLNAME:ASSI%의애널에 묻었다…
ENDIF

;-------------------------------------------------
;522,이륜 꽂아 시킨다
;-------------------------------------------------
@MESSAGE_COM522
;전회도 이륜 꽂아 시킨다
IF PREVCOM == 522
	PRINTFORML 어느 쪽의페니스도질내로부터 앞지르는 일 없이,다시 허리를 움직이게 했다…
;전회 커맨드가 노예×주인
ELSEIF PREVCOM >= 260 && PREVCOM <= 262
	PRINTFORM %CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 비순[秘唇]을
	SIF EXP:MASTER:V확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼치게 해 %CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ELSE
	PRINTFORM %CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 비순[秘唇]을
	SIF EXP:MASTER:V확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼치게 해%CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ENDIF

;-------------------------------------------------
;523,이륜 꽂아애널 시킨다
;-------------------------------------------------
@MESSAGE_COM523
;전회도 이륜 꽂아 시킨다
IF PREVCOM == 523
	PRINTFORML 어느 쪽의페니스도장내로부터 앞지르는 일 없이,다시 허리를 움직이게 했다…
;전회 커맨드가 노예×주인
ELSEIF PREVCOM >= 263 && PREVCOM <= 265
	PRINTFORM %CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 아누스를
	SIF EXP:MASTER:A확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼치게 해 %CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ELSE
	PRINTFORM %CALLNAME:ASSI%의
	SIF !PENIS(ASSI)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 아누스를
	SIF EXP:MASTER:A확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼치게 해%CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ENDIF

;-------------------------------------------------
;524,조수를 이륜 꽂아
;-------------------------------------------------
@MESSAGE_COM524
;전회도 이륜 꽂아
IF PREVCOM == 524
	PRINTFORML 어느 쪽의페니스도질내로부터 앞지르는 일 없이,다시 허리를 움직이기 시작했다…
;전회 커맨드가 노예×조수
ELSEIF PREVCOM == 430
	PRINTFORM %CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 비순[秘唇]을
	SIF EXP:ASSI:V확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣었다…
ELSE
	PRINTFORM %CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 비순[秘唇]을
	SIF EXP:ASSI:V확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ENDIF

;-------------------------------------------------
;525,조수를 이륜 꽂아애널
;-------------------------------------------------
@MESSAGE_COM525
;전회도 이륜 꽂아
IF PREVCOM == 525
	PRINTFORML 어느 쪽의페니스도질내로부터 앞지르는 일 없이,다시 허리를 움직이기 시작했다…
;전회 커맨드가 노예×조수
ELSEIF PREVCOM == 431
	PRINTFORM %CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 아누스를
	SIF EXP:ASSI:A확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣었다…
ELSE
	PRINTFORM %CALLNAME:MASTER%의
	SIF !PENIS(MASTER)
		PRINT 유사
	PRINTFORM 페니스가 삽입된 채로의 아누스를
	SIF EXP:ASSI:A확장경험 < 30
		PRINT 무리하게
	PRINTFORM 펼쳐,%CALLNAME%의
	SIF !PENIS(TARGET)
		PRINT 유사
	PRINTFORML 페니스를 쑤셔 넣게 했다…
ENDIF

;-------------------------------------------------
;526,삽입 전립선 자극
;-------------------------------------------------
@MESSAGE_COM526
PRINTFORML 허리를 떠,%CALLNAME%의 전립선을 조금씩 쓸어올렸다…

;-------------------------------------------------
;527,셀프 연결 튜브
;-------------------------------------------------
@MESSAGE_COM527
IF TFLAG:56 == 1
	PRINTFORML %CALLNAME%의 요도와입내에 기어든촉수가,한 개의 튜브가 되어 연결되어 간다
	PRINTFORML 무리하게해방 당한뇨의가,촉수의 내부를 지나%CALLNAME%의입에 흘러들었다…
ELSEIF TFLAG:56 == 2
	PRINTFORML %CALLNAME%의애널와입내에 기어든촉수가,한 개의 튜브가 되어 연결되어 간다
	PRINTFORML 인내의 한계를 넘어 분출한 배설물이,촉수의 내부를 지나%CALLNAME%의입에 흘러들었다…
ENDIF

;-------------------------------------------------
;528,수유손코키
;-------------------------------------------------
@MESSAGE_COM528
PRINTFORM %CALLNAME%의
;빈유＋작은체구
IF TALENT:작은체구 && TALENT:빈유
	PRINT 조금 부푼 곳이 느껴지는 정도의
;빈유
ELSEIF TALENT:빈유
	PRINT 작은 흔들림의
;거유
ELSEIF TALENT:거유
	PRINT 손로부터 흘러넘칠 것 같을 정도의
;폭유
ELSEIF TALENT:폭유
	PRINT 손에 남을 만큼 큰
ENDIF
PRINT 가슴의 첨단으로부터 방울져 떨어지는모유를 마시면서,
PRINTFORML %CALLNAME:PLAYER%에손로 봉사시켰다
PRINTFORM %CALLNAME%는
PRINT 유두를 들이마셔지면서도
;연모나친애
SIF TALENT:연모 || TALENT:친애
	PRINTFORM 사랑하는 것 같은 표정으로
PRINTFORM ,%CALLNAME:PLAYER%의
;오나홀 장착중
IF TEQUIP:PLAYER:전동오나홀
	PRINTFORM 오나홀에 휩싸여진페니스를 힘들게 잡아,상하에 쓸어올려 왔다…
ELSE
	;아이사이즈
	IF TALENT:PLAYER:아이사이즈
		;소인체형가 아니다
		SIF TALENT:PLAYER:소인체형 == 0
			PRINT 체에 어울리지 않고
		PRINT 어리다
	;말자지
	ELSEIF TALENT:PLAYER:말자지
		;소인체형
		IF TALENT:PLAYER:소인체형
			PRINT 신체가 숨어 버릴 만큼
		ELSE
			PRINT 눈이 휘둥그레 지는 만큼
		ENDIF
		PRINT 큰
	ENDIF
	SIF TALENT:PLAYER:포경
		PRINT 포경
	;역조교모드
	IF TEQUIP:역조교
		PRINTFORML 페니스에 손가락을 걸어 쓸어올려 왔다…
	;통상 모드
	ELSE
		PRINTFORML 페니스에 손가락을 걸어 쓸어올리고 있다…
	ENDIF
ENDIF

